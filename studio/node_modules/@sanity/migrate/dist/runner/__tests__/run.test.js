function _array_like_to_array(arr, len) {
    if (len == null || len > arr.length) len = arr.length;
    for(var i = 0, arr2 = new Array(len); i < len; i++)arr2[i] = arr[i];
    return arr2;
}
function _array_with_holes(arr) {
    if (Array.isArray(arr)) return arr;
}
function _array_without_holes(arr) {
    if (Array.isArray(arr)) return _array_like_to_array(arr);
}
function asyncGeneratorStep(gen, resolve, reject, _next, _throw, key, arg) {
    try {
        var info = gen[key](arg);
        var value = info.value;
    } catch (error) {
        reject(error);
        return;
    }
    if (info.done) resolve(value);
    else Promise.resolve(value).then(_next, _throw);
}
function _async_to_generator(fn) {
    return function() {
        var self = this, args = arguments;
        return new Promise(function(resolve, reject) {
            var gen = fn.apply(self, args);
            function _next(value) {
                asyncGeneratorStep(gen, resolve, reject, _next, _throw, "next", value);
            }
            function _throw(err) {
                asyncGeneratorStep(gen, resolve, reject, _next, _throw, "throw", err);
            }
            _next(undefined);
        });
    };
}
function _define_property(obj, key, value) {
    if (key in obj) {
        Object.defineProperty(obj, key, {
            value: value,
            enumerable: true,
            configurable: true,
            writable: true
        });
    } else obj[key] = value;
    return obj;
}
function _instanceof(left, right) {
    "@swc/helpers - instanceof";
    if (right != null && typeof Symbol !== "undefined" && right[Symbol.hasInstance]) {
        return !!right[Symbol.hasInstance](left);
    } else return left instanceof right;
}
function _iterable_to_array(iter) {
    if (typeof Symbol !== "undefined" && iter[Symbol.iterator] != null || iter["@@iterator"] != null) {
        return Array.from(iter);
    }
}
function _iterable_to_array_limit(arr, i) {
    var _i = arr == null ? null : typeof Symbol !== "undefined" && arr[Symbol.iterator] || arr["@@iterator"];
    if (_i == null) return;
    var _arr = [];
    var _n = true;
    var _d = false;
    var _s, _e;
    try {
        for(_i = _i.call(arr); !(_n = (_s = _i.next()).done); _n = true){
            _arr.push(_s.value);
            if (i && _arr.length === i) break;
        }
    } catch (err) {
        _d = true;
        _e = err;
    } finally{
        try {
            if (!_n && _i["return"] != null) _i["return"]();
        } finally{
            if (_d) throw _e;
        }
    }
    return _arr;
}
function _non_iterable_rest() {
    throw new TypeError("Invalid attempt to destructure non-iterable instance.\nIn order to be iterable, non-array objects must have a [Symbol.iterator]() method.");
}
function _non_iterable_spread() {
    throw new TypeError("Invalid attempt to spread non-iterable instance.\nIn order to be iterable, non-array objects must have a [Symbol.iterator]() method.");
}
function _object_spread(target) {
    for(var i = 1; i < arguments.length; i++){
        var source = arguments[i] != null ? arguments[i] : {};
        var ownKeys = Object.keys(source);
        if (typeof Object.getOwnPropertySymbols === "function") {
            ownKeys = ownKeys.concat(Object.getOwnPropertySymbols(source).filter(function(sym) {
                return Object.getOwnPropertyDescriptor(source, sym).enumerable;
            }));
        }
        ownKeys.forEach(function(key) {
            _define_property(target, key, source[key]);
        });
    }
    return target;
}
function ownKeys(object, enumerableOnly) {
    var keys = Object.keys(object);
    if (Object.getOwnPropertySymbols) {
        var symbols = Object.getOwnPropertySymbols(object);
        if (enumerableOnly) {
            symbols = symbols.filter(function(sym) {
                return Object.getOwnPropertyDescriptor(object, sym).enumerable;
            });
        }
        keys.push.apply(keys, symbols);
    }
    return keys;
}
function _object_spread_props(target, source) {
    source = source != null ? source : {};
    if (Object.getOwnPropertyDescriptors) Object.defineProperties(target, Object.getOwnPropertyDescriptors(source));
    else {
        ownKeys(Object(source)).forEach(function(key) {
            Object.defineProperty(target, key, Object.getOwnPropertyDescriptor(source, key));
        });
    }
    return target;
}
function _sliced_to_array(arr, i) {
    return _array_with_holes(arr) || _iterable_to_array_limit(arr, i) || _unsupported_iterable_to_array(arr, i) || _non_iterable_rest();
}
function _to_consumable_array(arr) {
    return _array_without_holes(arr) || _iterable_to_array(arr) || _unsupported_iterable_to_array(arr) || _non_iterable_spread();
}
function _ts_generator(thisArg, body) {
    var f, y, t, _ = {
        label: 0,
        sent: function() {
            if (t[0] & 1) throw t[1];
            return t[1];
        },
        trys: [],
        ops: []
    }, g = Object.create((typeof Iterator === "function" ? Iterator : Object).prototype), d = Object.defineProperty;
    return d(g, "next", {
        value: verb(0)
    }), d(g, "throw", {
        value: verb(1)
    }), d(g, "return", {
        value: verb(2)
    }), typeof Symbol === "function" && d(g, Symbol.iterator, {
        value: function() {
            return this;
        }
    }), g;
    function verb(n) {
        return function(v) {
            return step([
                n,
                v
            ]);
        };
    }
    function step(op) {
        if (f) throw new TypeError("Generator is already executing.");
        while(g && (g = 0, op[0] && (_ = 0)), _)try {
            if (f = 1, y && (t = op[0] & 2 ? y["return"] : op[0] ? y["throw"] || ((t = y["return"]) && t.call(y), 0) : y.next) && !(t = t.call(y, op[1])).done) return t;
            if (y = 0, t) op = [
                op[0] & 2,
                t.value
            ];
            switch(op[0]){
                case 0:
                case 1:
                    t = op;
                    break;
                case 4:
                    _.label++;
                    return {
                        value: op[1],
                        done: false
                    };
                case 5:
                    _.label++;
                    y = op[1];
                    op = [
                        0
                    ];
                    continue;
                case 7:
                    op = _.ops.pop();
                    _.trys.pop();
                    continue;
                default:
                    if (!(t = _.trys, t = t.length > 0 && t[t.length - 1]) && (op[0] === 6 || op[0] === 2)) {
                        _ = 0;
                        continue;
                    }
                    if (op[0] === 3 && (!t || op[1] > t[0] && op[1] < t[3])) {
                        _.label = op[1];
                        break;
                    }
                    if (op[0] === 6 && _.label < t[1]) {
                        _.label = t[1];
                        t = op;
                        break;
                    }
                    if (t && _.label < t[2]) {
                        _.label = t[2];
                        _.ops.push(op);
                        break;
                    }
                    if (t[2]) _.ops.pop();
                    _.trys.pop();
                    continue;
            }
            op = body.call(thisArg, _);
        } catch (e) {
            op = [
                6,
                e
            ];
            y = 0;
        } finally{
            f = t = 0;
        }
        if (op[0] & 5) throw op[1];
        return {
            value: op[0] ? op[1] : void 0,
            done: true
        };
    }
}
function _type_of(obj) {
    "@swc/helpers - typeof";
    return obj && typeof Symbol !== "undefined" && obj.constructor === Symbol ? "symbol" : typeof obj;
}
function _unsupported_iterable_to_array(o, minLen) {
    if (!o) return;
    if (typeof o === "string") return _array_like_to_array(o, minLen);
    var n = Object.prototype.toString.call(o).slice(8, -1);
    if (n === "Object" && o.constructor) n = o.constructor.name;
    if (n === "Map" || n === "Set") return Array.from(n);
    if (n === "Arguments" || /^(?:Ui|I)nt(?:8|16|32)(?:Clamped)?Array$/.test(n)) return _array_like_to_array(o, minLen);
}
import { streamBody } from 'get-it/mock';
import { afterEach, describe, expect, it, vi } from 'vitest';
import { at, patch, set } from '../../mutations/index.js';
import { run } from '../run.js';
var _ref = await vi.hoisted(function() {
    return _async_to_generator(function() {
        var createMockFetch, mock;
        return _ts_generator(this, function(_state) {
            switch(_state.label){
                case 0:
                    return [
                        4,
                        import('get-it/mock')
                    ];
                case 1:
                    createMockFetch = _state.sent().createMockFetch;
                    mock = createMockFetch();
                    return [
                        2,
                        {
                            fetchMock: vi.fn(mock.fetch),
                            mock: mock
                        }
                    ];
            }
        });
    })();
}), fetchMock = _ref.fetchMock, mock = _ref.mock;
vi.mock('get-it/node', function() {
    return {
        createNodeFetch: function createNodeFetch() {
            return fetchMock;
        }
    };
});
var api = {
    apiVersion: 'v2024-01-01',
    dataset: 'test',
    projectId: 'abc123',
    token: 'token'
};
// Big enough that batchMutations (256KB max body size) puts 4 mutations in each batch
var FILLER = 'x'.repeat(64 * 1024);
function createDocuments(count) {
    return Array.from({
        length: count
    }, function(_unused, i) {
        return {
            _createdAt: '2024-02-16T14:13:59Z',
            _id: "doc-".concat(i),
            _rev: 'rev',
            _type: 'article',
            _updatedAt: '2024-02-16T14:13:59Z'
        };
    });
}
var migration = {
    migrate: {
        document: function document(doc) {
            return patch(doc._id, [
                at('filler', set(FILLER))
            ]);
        }
    },
    title: 'test migration'
};
function isMutateBody(value) {
    if ((typeof value === "undefined" ? "undefined" : _type_of(value)) !== 'object' || value === null || !('mutations' in value)) return false;
    if (!Array.isArray(value.mutations)) return false;
    return !('transactionId' in value) || typeof value.transactionId === 'string';
}
/** Asserts and narrows in one go, so tests can use the value without a non-null assertion */ function expectString(value) {
    expect(value).toEqual(expect.any(String));
    if (typeof value !== 'string') throw new TypeError('expected a string');
    return value;
}
/** Mimics an undici client-side timeout: `TypeError: fetch failed` wrapping a code-carrying cause */ function headersTimeoutError() {
    var cause = new Error('Headers Timeout Error');
    cause.name = 'HeadersTimeoutError';
    Object.assign(cause, {
        code: 'UND_ERR_HEADERS_TIMEOUT'
    });
    return new TypeError('fetch failed', {
        cause: cause
    });
}
function stubRequests(documents) {
    mock.on('GET', function(url) {
        return url.includes('/data/export/');
    }).respond({
        body: streamBody.apply(void 0, _to_consumable_array(documents.map(function(doc) {
            return "".concat(JSON.stringify(doc), "\n");
        })))
    });
    return mock.on('POST', function(url) {
        return url.includes('/data/mutate/');
    });
}
function mutateCalls() {
    return mock.getRequests().filter(function(req) {
        return req.url.includes('/data/mutate/');
    }).map(function(param) {
        var body = param.body;
        if (!isMutateBody(body)) throw new Error('Unexpected mutate request body');
        return body;
    });
}
function okResponse() {
    var transactionId = arguments.length > 0 && arguments[0] !== void 0 ? arguments[0] : 'server-txn';
    return {
        body: {
            results: [],
            transactionId: transactionId
        }
    };
}
describe('run', function() {
    afterEach(function() {
        fetchMock.mockReset();
        mock.assertAllConsumed();
        mock.clear();
    });
    it('reports transactions that committed before another request failed', function() {
        return _async_to_generator(function() {
            var _progress_at, documents, failFirst, timeout, progress, result, error;
            return _ts_generator(this, function(_state) {
                switch(_state.label){
                    case 0:
                        documents = createDocuments(8);
                        failFirst = Promise.withResolvers();
                        timeout = headersTimeoutError();
                        stubRequests(documents).respondWithError(timeout).respond(okResponse('committed-txn'));
                        fetchMock.mockImplementationOnce(mock.fetch) // Export request.
                        .mockImplementationOnce(function() {
                            for(var _len = arguments.length, args = new Array(_len), _key = 0; _key < _len; _key++){
                                args[_key] = arguments[_key];
                            }
                            return _async_to_generator(function() {
                                var _mock, error;
                                return _ts_generator(this, function(_state) {
                                    switch(_state.label){
                                        case 0:
                                            _state.trys.push([
                                                0,
                                                2,
                                                ,
                                                4
                                            ]);
                                            return [
                                                4,
                                                (_mock = mock).fetch.apply(_mock, _to_consumable_array(args))
                                            ];
                                        case 1:
                                            return [
                                                2,
                                                _state.sent()
                                            ];
                                        case 2:
                                            error = _state.sent();
                                            // Record the first mutation request, but hold its failure until the second commits.
                                            return [
                                                4,
                                                failFirst.promise
                                            ];
                                        case 3:
                                            _state.sent();
                                            throw error;
                                        case 4:
                                            return [
                                                2
                                            ];
                                    }
                                });
                            })();
                        });
                        progress = [];
                        result = run({
                            api: api,
                            concurrency: 2,
                            onProgress: function onProgress(event) {
                                progress.push(_object_spread_props(_object_spread({}, event), {
                                    completedTransactions: _to_consumable_array(event.completedTransactions)
                                }));
                            }
                        }, migration).catch(function(cause) {
                            return cause;
                        });
                        _state.label = 1;
                    case 1:
                        _state.trys.push([
                            1,
                            ,
                            3,
                            5
                        ]);
                        return [
                            4,
                            vi.waitFor(function() {
                                var _progress_at;
                                expect((_progress_at = progress.at(-1)) === null || _progress_at === void 0 ? void 0 : _progress_at.completedTransactions).toHaveLength(1);
                            })
                        ];
                    case 2:
                        _state.sent();
                        return [
                            3,
                            5
                        ];
                    case 3:
                        // Settle the stalled request even if the progress assertion fails.
                        failFirst.resolve();
                        return [
                            4,
                            result
                        ];
                    case 4:
                        _state.sent();
                        return [
                            7
                        ];
                    case 5:
                        return [
                            4,
                            result
                        ];
                    case 6:
                        error = _state.sent();
                        expect(error).toBeInstanceOf(Error);
                        expect(error).toMatchObject({
                            cause: timeout,
                            name: 'UnknownTransactionOutcomeError'
                        });
                        expect(mutateCalls()).toHaveLength(2);
                        expect((_progress_at = progress.at(-1)) === null || _progress_at === void 0 ? void 0 : _progress_at.completedTransactions).toEqual([
                            {
                                results: [],
                                transactionId: 'committed-txn'
                            }
                        ]);
                        return [
                            2
                        ];
                }
            });
        })();
    });
    it('assigns a transaction id to every submitted transaction', function() {
        return _async_to_generator(function() {
            var documents, calls, ids;
            return _ts_generator(this, function(_state) {
                switch(_state.label){
                    case 0:
                        documents = createDocuments(8);
                        stubRequests(documents).respondPersist(okResponse());
                        return [
                            4,
                            run({
                                api: api
                            }, migration)
                        ];
                    case 1:
                        _state.sent();
                        calls = mutateCalls();
                        expect(calls.length).toBeGreaterThan(1);
                        ids = calls.map(function(call) {
                            return expectString(call.transactionId);
                        });
                        expect(new Set(ids).size).toBe(calls.length);
                        return [
                            2
                        ];
                }
            });
        })();
    });
    it('reports the outcome of a timed-out transaction as unknown, naming its transaction id', function() {
        return _async_to_generator(function() {
            var _mutateCalls_at, documents, _ref, error;
            return _ts_generator(this, function(_state) {
                switch(_state.label){
                    case 0:
                        documents = createDocuments(1);
                        stubRequests(documents).respondWithError(headersTimeoutError());
                        return [
                            4,
                            run({
                                api: api
                            }, migration).then(function() {
                                return [
                                    undefined
                                ];
                            }, function(err) {
                                return [
                                    err
                                ];
                            })
                        ];
                    case 1:
                        _ref = _sliced_to_array.apply(void 0, [
                            _state.sent(),
                            1
                        ]), error = _ref[0];
                        expect(error).toBeInstanceOf(Error);
                        if (!_instanceof(error, Error)) throw new Error('expected an Error');
                        // Must not imply nothing was written, and must name the transaction so it can be looked up
                        expect(error.message).toMatch(/unknown/i);
                        expect(error.message).toContain(expectString((_mutateCalls_at = mutateCalls().at(0)) === null || _mutateCalls_at === void 0 ? void 0 : _mutateCalls_at.transactionId));
                        expect(error.message).toMatch(/history/i);
                        return [
                            2
                        ];
                }
            });
        })();
    });
    it('reports a rejected transaction as not applied', function() {
        return _async_to_generator(function() {
            var documents, _ref, error;
            return _ts_generator(this, function(_state) {
                switch(_state.label){
                    case 0:
                        documents = createDocuments(1);
                        stubRequests(documents).respond({
                            body: {
                                error: {
                                    description: 'Nope',
                                    type: 'mutationError'
                                }
                            },
                            status: 400
                        });
                        return [
                            4,
                            run({
                                api: api
                            }, migration).then(function() {
                                return [
                                    undefined
                                ];
                            }, function(err) {
                                return [
                                    err
                                ];
                            })
                        ];
                    case 1:
                        _ref = _sliced_to_array.apply(void 0, [
                            _state.sent(),
                            1
                        ]), error = _ref[0];
                        expect(error).toBeInstanceOf(Error);
                        if (!_instanceof(error, Error)) throw new Error('expected an Error');
                        expect(error.message).toContain('Nope');
                        expect(error.message).not.toMatch(/unknown/i);
                        return [
                            2
                        ];
                }
            });
        })();
    });
    it('reports server failures as unknown without retrying the mutation', function() {
        return _async_to_generator(function() {
            var error;
            return _ts_generator(this, function(_state) {
                switch(_state.label){
                    case 0:
                        stubRequests(createDocuments(1)).respond({
                            body: {
                                error: 'Service unavailable'
                            },
                            status: 503
                        });
                        return [
                            4,
                            run({
                                api: api
                            }, migration).catch(function(cause) {
                                return cause;
                            })
                        ];
                    case 1:
                        error = _state.sent();
                        expect(error).toBeInstanceOf(Error);
                        if (!_instanceof(error, Error)) throw new Error('expected an Error');
                        expect(error.name).toBe('UnknownTransactionOutcomeError');
                        expect(error.cause).toMatchObject({
                            name: 'HttpError',
                            status: 503
                        });
                        expect(mutateCalls()).toHaveLength(1);
                        return [
                            2
                        ];
                }
            });
        })();
    });
    it('decrements pending as requests settle', function() {
        return _async_to_generator(function() {
            var _progress_at, documents, progress;
            return _ts_generator(this, function(_state) {
                switch(_state.label){
                    case 0:
                        documents = createDocuments(8);
                        stubRequests(documents).respondPersist(_object_spread_props(_object_spread({}, okResponse()), {
                            delay: 5
                        }));
                        progress = [];
                        return [
                            4,
                            run({
                                api: api,
                                onProgress: function onProgress(event) {
                                    return progress.push(event);
                                }
                            }, migration)
                        ];
                    case 1:
                        _state.sent();
                        expect((_progress_at = progress.at(-1)) === null || _progress_at === void 0 ? void 0 : _progress_at.pending).toBe(0);
                        return [
                            2
                        ];
                }
            });
        })();
    });
});
