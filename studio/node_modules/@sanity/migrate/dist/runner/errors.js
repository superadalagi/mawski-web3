function _array_like_to_array(arr, len) {
    if (len == null || len > arr.length) len = arr.length;
    for(var i = 0, arr2 = new Array(len); i < len; i++)arr2[i] = arr[i];
    return arr2;
}
function _array_without_holes(arr) {
    if (Array.isArray(arr)) return _array_like_to_array(arr);
}
function _assert_this_initialized(self) {
    if (self === void 0) throw new ReferenceError("this hasn't been initialised - super() hasn't been called");
    return self;
}
function _call_super(_this, derived, args) {
    derived = _get_prototype_of(derived);
    return _possible_constructor_return(_this, _is_native_reflect_construct() ? Reflect.construct(derived, args || [], _get_prototype_of(_this).constructor) : derived.apply(_this, args));
}
function _class_call_check(instance, Constructor) {
    if (!(instance instanceof Constructor)) throw new TypeError("Cannot call a class as a function");
}
function _construct(Parent, args, Class) {
    if (_is_native_reflect_construct()) _construct = Reflect.construct;
    else {
        _construct = function construct(Parent, args, Class) {
            var a = [
                null
            ];
            a.push.apply(a, args);
            var Constructor = Function.bind.apply(Parent, a);
            var instance = new Constructor();
            if (Class) _set_prototype_of(instance, Class.prototype);
            return instance;
        };
    }
    return _construct.apply(null, arguments);
}
function _define_property(obj, key, value) {
    if (key in obj) {
        Object.defineProperty(obj, key, {
            value: value,
            enumerable: true,
            configurable: true,
            writable: true
        });
    } else obj[key] = value;
    return obj;
}
function _get_prototype_of(o) {
    _get_prototype_of = Object.setPrototypeOf ? Object.getPrototypeOf : function getPrototypeOf(o) {
        return o.__proto__ || Object.getPrototypeOf(o);
    };
    return _get_prototype_of(o);
}
function _inherits(subClass, superClass) {
    if (typeof superClass !== "function" && superClass !== null) {
        throw new TypeError("Super expression must either be null or a function");
    }
    subClass.prototype = Object.create(superClass && superClass.prototype, {
        constructor: {
            value: subClass,
            writable: true,
            configurable: true
        }
    });
    if (superClass) _set_prototype_of(subClass, superClass);
}
function _is_native_function(fn) {
    return Function.toString.call(fn).indexOf("[native code]") !== -1;
}
function _is_native_reflect_construct() {
    try {
        var result = !Boolean.prototype.valueOf.call(Reflect.construct(Boolean, [], function() {}));
    } catch (_) {}
    return (_is_native_reflect_construct = function() {
        return !!result;
    })();
}
function _iterable_to_array(iter) {
    if (typeof Symbol !== "undefined" && iter[Symbol.iterator] != null || iter["@@iterator"] != null) {
        return Array.from(iter);
    }
}
function _non_iterable_spread() {
    throw new TypeError("Invalid attempt to spread non-iterable instance.\nIn order to be iterable, non-array objects must have a [Symbol.iterator]() method.");
}
function _possible_constructor_return(self, call) {
    if (call && (_type_of(call) === "object" || typeof call === "function")) return call;
    return _assert_this_initialized(self);
}
function _set_prototype_of(o, p) {
    _set_prototype_of = Object.setPrototypeOf || function setPrototypeOf(o, p) {
        o.__proto__ = p;
        return o;
    };
    return _set_prototype_of(o, p);
}
function _to_consumable_array(arr) {
    return _array_without_holes(arr) || _iterable_to_array(arr) || _unsupported_iterable_to_array(arr) || _non_iterable_spread();
}
function _type_of(obj) {
    "@swc/helpers - typeof";
    return obj && typeof Symbol !== "undefined" && obj.constructor === Symbol ? "symbol" : typeof obj;
}
function _unsupported_iterable_to_array(o, minLen) {
    if (!o) return;
    if (typeof o === "string") return _array_like_to_array(o, minLen);
    var n = Object.prototype.toString.call(o).slice(8, -1);
    if (n === "Object" && o.constructor) n = o.constructor.name;
    if (n === "Map" || n === "Set") return Array.from(n);
    if (n === "Arguments" || /^(?:Ui|I)nt(?:8|16|32)(?:Clamped)?Array$/.test(n)) return _array_like_to_array(o, minLen);
}
function _wrap_native_super(Class) {
    var _cache = typeof Map === "function" ? new Map() : undefined;
    _wrap_native_super = function(Class) {
        if (Class === null || !_is_native_function(Class)) return Class;
        if (typeof Class !== "function") throw new TypeError("Super expression must either be null or a function");
        if (typeof _cache !== "undefined") {
            if (_cache.has(Class)) return _cache.get(Class);
            _cache.set(Class, Wrapper);
        }
        function Wrapper() {
            return _construct(Class, arguments, _get_prototype_of(this).constructor);
        }
        Wrapper.prototype = Object.create(Class.prototype, {
            constructor: {
                value: Wrapper,
                enumerable: false,
                writable: true,
                configurable: true
            }
        });
        return _set_prototype_of(Wrapper, Class);
    };
    return _wrap_native_super(Class);
}
var HISTORY_API_DOCS_URL = 'https://www.sanity.io/docs/history-api';
function transactionLogUrl(api, transactionId) {
    var _api_apiHost;
    var host = ((_api_apiHost = api.apiHost) !== null && _api_apiHost !== void 0 ? _api_apiHost : 'api.sanity.io').replace(/^https?:\/\//, '');
    var searchParams = new URLSearchParams({
        excludeContent: 'true',
        fromTransaction: transactionId,
        limit: '1'
    });
    return "https://".concat(api.projectId, ".").concat(host, "/").concat(api.apiVersion, "/data/history/").concat(api.dataset, "/transactions?").concat(searchParams);
}
function formatMessage(api, transactionIds) {
    var count = transactionIds.length;
    return [
        "Mutation request failed before the API confirmed the outcome, so the outcome of ".concat(count, " ").concat(count === 1 ? 'transaction' : 'transactions', " is unknown: ").concat(count === 1 ? 'it' : 'they', " may or may not have been committed."),
        '',
        "Check the transaction log to see what was written before re-running this migration. Re-running it will apply any non-idempotent mutations (append, create, inc, insert) a second time:"
    ].concat(_to_consumable_array(transactionIds.map(function(id) {
        return "  ".concat(id, "\n    ").concat(transactionLogUrl(api, id));
    })), [
        '',
        "See ".concat(HISTORY_API_DOCS_URL, " for how to read the transaction log.")
    ]).join('\n');
}
/**
 * Thrown when a mutation request fails without the API having confirmed the outcome, typically
 * because the request timed out client side or the connection was dropped. The transactions listed
 * in `transactionIds` may or may not have been committed, so the dataset is in an unknown state and
 * re-running the migration may apply non-idempotent mutations twice.
 *
 * @public
 */ export var UnknownTransactionOutcomeError = /*#__PURE__*/ function(Error1) {
    "use strict";
    _inherits(UnknownTransactionOutcomeError, Error1);
    function UnknownTransactionOutcomeError(transactionIds, options) {
        _class_call_check(this, UnknownTransactionOutcomeError);
        var _this;
        _this = _call_super(this, UnknownTransactionOutcomeError, [
            formatMessage(options.api, transactionIds),
            {
                cause: options.cause
            }
        ]), /**
   * IDs of the transactions whose outcome could not be determined. Look these up in the transaction
   * log to find out whether they were committed.
   */ _define_property(_this, "transactionIds", void 0);
        _this.name = 'UnknownTransactionOutcomeError';
        _this.transactionIds = transactionIds;
        return _this;
    }
    return UnknownTransactionOutcomeError;
}(_wrap_native_super(Error));
