function asyncGeneratorStep(gen, resolve, reject, _next, _throw, key, arg) {
    try {
        var info = gen[key](arg);
        var value = info.value;
    } catch (error) {
        reject(error);
        return;
    }
    if (info.done) resolve(value);
    else Promise.resolve(value).then(_next, _throw);
}
function _async_to_generator(fn) {
    return function() {
        var self = this, args = arguments;
        return new Promise(function(resolve, reject) {
            var gen = fn.apply(self, args);
            function _next(value) {
                asyncGeneratorStep(gen, resolve, reject, _next, _throw, "next", value);
            }
            function _throw(err) {
                asyncGeneratorStep(gen, resolve, reject, _next, _throw, "throw", err);
            }
            _next(undefined);
        });
    };
}
function _ts_generator(thisArg, body) {
    var f, y, t, _ = {
        label: 0,
        sent: function() {
            if (t[0] & 1) throw t[1];
            return t[1];
        },
        trys: [],
        ops: []
    }, g = Object.create((typeof Iterator === "function" ? Iterator : Object).prototype), d = Object.defineProperty;
    return d(g, "next", {
        value: verb(0)
    }), d(g, "throw", {
        value: verb(1)
    }), d(g, "return", {
        value: verb(2)
    }), typeof Symbol === "function" && d(g, Symbol.iterator, {
        value: function() {
            return this;
        }
    }), g;
    function verb(n) {
        return function(v) {
            return step([
                n,
                v
            ]);
        };
    }
    function step(op) {
        if (f) throw new TypeError("Generator is already executing.");
        while(g && (g = 0, op[0] && (_ = 0)), _)try {
            if (f = 1, y && (t = op[0] & 2 ? y["return"] : op[0] ? y["throw"] || ((t = y["return"]) && t.call(y), 0) : y.next) && !(t = t.call(y, op[1])).done) return t;
            if (y = 0, t) op = [
                op[0] & 2,
                t.value
            ];
            switch(op[0]){
                case 0:
                case 1:
                    t = op;
                    break;
                case 4:
                    _.label++;
                    return {
                        value: op[1],
                        done: false
                    };
                case 5:
                    _.label++;
                    y = op[1];
                    op = [
                        0
                    ];
                    continue;
                case 7:
                    op = _.ops.pop();
                    _.trys.pop();
                    continue;
                default:
                    if (!(t = _.trys, t = t.length > 0 && t[t.length - 1]) && (op[0] === 6 || op[0] === 2)) {
                        _ = 0;
                        continue;
                    }
                    if (op[0] === 3 && (!t || op[1] > t[0] && op[1] < t[3])) {
                        _.label = op[1];
                        break;
                    }
                    if (op[0] === 6 && _.label < t[1]) {
                        _.label = t[1];
                        t = op;
                        break;
                    }
                    if (t && _.label < t[2]) {
                        _.label = t[2];
                        _.ops.push(op);
                        break;
                    }
                    if (t[2]) _.ops.pop();
                    _.trys.pop();
                    continue;
            }
            op = body.call(thisArg, _);
        } catch (e) {
            op = [
                6,
                e
            ];
            y = 0;
        } finally{
            f = t = 0;
        }
        if (op[0] & 5) throw op[1];
        return {
            value: op[0] ? op[1] : void 0,
            done: true
        };
    }
}
import { mkdtemp, rm, writeFile } from 'node:fs/promises';
import { tmpdir } from 'node:os';
import { join } from 'node:path';
import { gzipSync } from 'node:zlib';
import { packTar } from 'modern-tar';
import { expect, test } from 'vitest';
import { decodeText, parse, toArray } from '../../it-utils/index.js';
import { fromExportArchive } from '../fromExportArchive.js';
test('untar movies dataset export, but not reading assets', function() {
    return _async_to_generator(function() {
        var docsFromExport, allDocs;
        return _ts_generator(this, function(_state) {
            switch(_state.label){
                case 0:
                    docsFromExport = fromExportArchive("".concat(import.meta.dirname, "/fixtures/example.tar.gz"));
                    return [
                        4,
                        toArray(parse(decodeText(docsFromExport)))
                    ];
                case 1:
                    allDocs = _state.sent();
                    expect(allDocs.map(function(doc) {
                        return doc._id;
                    })).toEqual([
                        'e749feed-4a9e-4175-b130-913f28436f62',
                        '12274d53-4d6d-4aa4-9ace-d15da7ba7c10'
                    ]);
                    return [
                        2
                    ];
            }
        });
    })();
});
test.each([
    'tar',
    'tar.gz'
])('read documents around skipped entries in %s archives', function(format) {
    return _async_to_generator(function() {
        var directory, docs, first, last, archive, path;
        return _ts_generator(this, function(_state) {
            switch(_state.label){
                case 0:
                    return [
                        4,
                        mkdtemp(join(tmpdir(), 'migrate-export-'))
                    ];
                case 1:
                    directory = _state.sent();
                    _state.label = 2;
                case 2:
                    _state.trys.push([
                        2,
                        ,
                        6,
                        8
                    ]);
                    docs = [
                        {
                            _id: 'first',
                            text: 'a'.repeat(40000)
                        },
                        {
                            _id: 'last'
                        }
                    ];
                    first = new TextEncoder().encode("".concat(JSON.stringify(docs[0]), "\n"));
                    last = new TextEncoder().encode("".concat(JSON.stringify(docs[1]), "\n"));
                    return [
                        4,
                        packTar([
                            {
                                header: {
                                    name: 'export/',
                                    size: 0,
                                    type: 'directory'
                                }
                            },
                            {
                                header: {
                                    name: 'export/empty.ndjson',
                                    size: 0
                                }
                            },
                            {
                                body: first,
                                header: {
                                    name: 'export/first.ndjson',
                                    size: first.length
                                }
                            },
                            {
                                body: new Uint8Array(50000),
                                header: {
                                    name: 'export/images/asset.jpg',
                                    size: 50000
                                }
                            },
                            {
                                header: {
                                    name: 'export/directory.ndjson',
                                    size: 0,
                                    type: 'directory'
                                }
                            },
                            {
                                body: last,
                                header: {
                                    name: 'export/last.ndjson',
                                    size: last.length
                                }
                            }
                        ])
                    ];
                case 3:
                    archive = _state.sent();
                    path = join(directory, "export.".concat(format));
                    return [
                        4,
                        writeFile(path, format === 'tar.gz' ? gzipSync(archive) : archive)
                    ];
                case 4:
                    _state.sent();
                    return [
                        4,
                        toArray(parse(decodeText(fromExportArchive(path))))
                    ];
                case 5:
                    expect.apply(void 0, [
                        _state.sent()
                    ]).toEqual(docs);
                    return [
                        3,
                        8
                    ];
                case 6:
                    return [
                        4,
                        rm(directory, {
                            force: true,
                            recursive: true
                        })
                    ];
                case 7:
                    _state.sent();
                    return [
                        7
                    ];
                case 8:
                    return [
                        2
                    ];
            }
        });
    })();
});
test.each([
    'empty.tar',
    'invalid.tar',
    'corrupted.tar'
])('reject %s archives', function(file) {
    return _async_to_generator(function() {
        return _ts_generator(this, function(_state) {
            switch(_state.label){
                case 0:
                    return [
                        4,
                        expect(toArray(fromExportArchive(join(import.meta.dirname, 'fixtures', file)))).rejects.toThrow()
                    ];
                case 1:
                    _state.sent();
                    return [
                        2
                    ];
            }
        });
    })();
});
