const ORGANIZATION_PREFIXES = [
    'organization',
    'organisations',
    'organisation',
    'org',
    'orgs'
];
export function organizationAliases(action) {
    return ORGANIZATION_PREFIXES.map((prefix)=>`${prefix}:${action}`);
}

//# sourceMappingURL=organizationAliases.js.map