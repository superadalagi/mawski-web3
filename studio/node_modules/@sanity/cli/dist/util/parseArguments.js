import minimist from 'minimist';
import { redactTelemetryArguments } from './telemetry/commandTelemetry.js';
function collectOptionArguments(argv, commandTelemetry) {
    return redactTelemetryArguments(argv, commandTelemetry).filter((argument)=>argument.startsWith('-'));
}
/**
 * Parse the arguments from the command line
 *
 * @param argv - The arguments from the command line
 * @param commandTelemetry - Resolved telemetry behavior for the command
 * @returns The parsed arguments
 */ export function parseArguments(argv = process.argv, commandTelemetry) {
    const args = argv.slice(2);
    const { '--': extraArguments, _, version, ...extOptions } = minimist(args, {
        '--': true,
        boolean: [
            'version'
        ],
        string: [
            '_'
        ]
    });
    const [groupOrCommand, ...argsWithoutOptions] = _;
    const finalExtraArguments = [
        ...extraArguments || [],
        ...collectOptionArguments(argv, commandTelemetry)
    ];
    // oclif allows to run `sanity help` or `sanity help <command>`
    // It does not fire the hooks on `--help` so this is okay to track
    const hasHelp = args.includes('help');
    // We only have global debug via env var
    const hasDebug = !!process.env.DEBUG;
    return {
        groupOrCommand,
        argsWithoutOptions,
        argv,
        extOptions,
        extraArguments: finalExtraArguments,
        coreOptions: {
            debug: hasDebug,
            help: hasHelp,
            version
        }
    };
}

//# sourceMappingURL=parseArguments.js.map