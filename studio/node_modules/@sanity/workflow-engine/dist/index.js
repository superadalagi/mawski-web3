import { rethrowWithContext, findOpenStageEntry, findCurrentActivityEntry, andConditions, deriveActivityKind, CALLER_BOUND_VARS, START_REQUIREMENT_VARS, CONDITION_VARS, isSubjectEntry, isUnevaluable, ContractViolationError, isStartableDefinition, conditionFieldReadNames, isGdr, errorMessage, conditionParameterNames, START_FILTER_VARS, WorkflowError, gdrFromResource, selfGdr, isGdrUri, MUTATION_GUARD_ID_SPACES, mutationGuardActionIdSpace, LAKE_MUTATION_GUARD_ACTIONS, isBareDocumentId, toBareId, normalizeAssignmentMembers, isSingleDocRefKind, actorMatchesAssignment, assignmentMembers, resolvedChildStatus, sameResource, resourceFromParsed, tryParseGdr, isTerminalActivityStatus, FieldValueShapeError, validateFieldValue, validateFieldAppendItem, isAssignmentFieldEntry, isAlwaysArrayFieldKind, evaluateCondition, isRecord, isSingleDocRefEntry, choiceValueIssues, scalarValidationIssues, StoredFieldOpSchema, formatIssues, conditionSyntaxIssues, checkWorkflowInvariants, formatIssuePath, isGuardReadExpr, GUARD_ACTIONS_REQUIRED_MESSAGE, conditionEffectReads, EFFECTS_READ, datasetResourceParts, mutationGuardRequiresSplitEmission, MUTATION_GUARD_ACTIONS, assignmentKindAcceptsRoles, evaluatePredicates, WORKFLOW_DEFINITION_TYPE, tagScopeFilter, liveSubworkflows, isCascadeFired, classifyPrincipalId, directoryBridgeId, parseGdr, gdrRef, isInputSourced, parseFieldValue, VersionSpecificDatasetGdrError, toPhysicalGdr, isBareSeedId, isParseableInstant, NonEmptyString, tolerantEntries, FIELD_VALUE_KINDS, tolerantObject, ActorShape, IsoTimestamp, ACTIVITY_STATUSES, GdrShape, DRIVER_KINDS, FIELD_SCOPES, fieldValueSchemas, parsePersistedDoc, ACTOR_KINDS, InstanceNotFoundError, evaluateConditionOutcome, runGroq as runGroq$1, FIELD_READ, resourceGdr, condemnSubworkflow, resourceFromGdrUri, refTypeIssues, rejectedRefTypes, DOCUMENT_VALUE_PERMISSIONS, lakePrincipalId, firstCarriedGlobalId, driverKind, actorFulfillsRole, condemnedSubworkflows, EffectNotFoundError, isFilterScopedOut, deriveExecutorClassification, validateTag, activeAssignmentMembers, extractDocumentId, UNIVERSAL_ROLE_ALIAS_KEY, parseStoredDefinition, validateResourceAliasName, labelFor, DefinitionNotFoundError, definitionDocId, SpawnContractsInvalidError, gdrResourcePrefix, RESOURCE_ALIAS_NAME_SOURCE, DefinitionInUseError, groupMembershipNames } from "./_chunks-es/invariants.js";

import { ACTION_SEMANTICS, ACTIVITY_KINDS, ANONYMOUS_IDENTITY, DECISION_SEMANTICS, DEFAULT_TRANSITION_WHEN, EXECUTOR_CLASSIFICATIONS, FILTER_SCOPE_VARS, GROUP_KINDS, GUARD_PREDICATE_VARS, PersistedDocShapeError, RESERVED_CONDITION_VARS, SIGNAL_SEMANTICS, SYSTEM_IDENTITY, assignmentMatch, assignmentState, assignmentStateCounts, clientConfigFromResource, gdrUri, identityMatchesAssignment, instanceAssignmentStateCounts, isNotesEntry, isTodoListEntry, isTodoListItem, openActivityAssignments, parseResourceGdr, readsRootDocument, refCanvas, refDashboard, refDataset, refKindAcceptsTypes, refMediaLibrary, releaseDocId, releaseRef, resourceAliasesToMap, schemaTreeShape, startKindOf } from "./_chunks-es/invariants.js";

import { atomNode, dedupeBy, analyzeCondition, atomReadsDataset, checklistLines as checklistLines$1, phrase, quoted, listAnd, describeAtom as describeAtom$1, describeCondition as describeCondition$1, guillemets, formatValue, humanize, listOr, scopeReadOf, describeRead, explainCondition, _exhaustiveOptions, whatIfCondition, runGroq, formatRead, dedupeReads, MAX_COUNTERFACTUAL_INDEX } from "@sanity/groq-condition-describe";

import { MAX_COUNTERFACTUAL_INDEX as MAX_COUNTERFACTUAL_INDEX2, NEUTRAL_MARK, OUTCOME_MARKS, analyzeCondition as analyzeCondition2, atomReadsDataset as atomReadsDataset2, explainCondition as explainCondition2, formatRead as formatRead2, guillemets as guillemets2, humanize as humanize2, isComparisonOp, quoted as quoted2, sentenceCase, whatIfCondition as whatIfCondition2, withAssignment } from "@sanity/groq-condition-describe";

import { evaluate, parse } from "groq-js";

import { getPublishedId, getDraftId } from "@sanity/id-utils";

import * as v from "valibot";

const WORKFLOW_INSTANCE_TYPE = "sanity.workflow.instance";

function terminalState(instance) {
    return instance.abortedAt !== void 0 ? "aborted" : instance.completedAt !== void 0 ? "completed" : "in-flight";
}

function isUnprimed(instance) {
    return instance.stages.length === 0 && terminalState(instance) === "in-flight";
}

function parseDefinitionSnapshotValue(instance) {
    try {
        return normalizeLegacyActivityRequirements(JSON.parse(instance.definitionSnapshot));
    } catch (err) {
        rethrowWithContext(err, `Failed to parse definitionSnapshot on instance "${instance._id}"`);
    }
}

function normalizeLegacyActivityRequirements(value) {
    for (const stage of arrayMember(value, "stages")) for (const activity of arrayMember(stage, "activities")) normalizeLegacyRequirementMap(activity);
    return value;
}

function arrayMember(value, key) {
    if (typeof value != "object" || value === null) return [];
    const member = value[key];
    return Array.isArray(member) ? member : [];
}

function normalizeLegacyRequirementMap(value) {
    if (typeof value != "object" || value === null) return;
    const activity = value, requirements = activity.requirements;
    typeof requirements != "object" || requirements === null || Array.isArray(requirements) || (activity.requirements = Object.entries(requirements).map(([name, query]) => ({
        type: "groq",
        name: name,
        query: query
    })));
}

function parseDefinitionSnapshot(instance) {
    return parseDefinitionSnapshotValue(instance);
}

function parentRef(instance) {
    return instance.ancestors.at(-1);
}

function effectiveEditable(baseline, override) {
    if (baseline === void 0) return;
    if (override === void 0) return baseline;
    if (baseline === !0 && override === !0) return !0;
    const parts = [ baseline, override ].map(e => e === !0 ? void 0 : e);
    return andConditions(parts) ?? !0;
}

const DEFAULT_EDIT_MODE = "set";

function fieldSites(definition, stage) {
    const sites = [];
    for (const entry of definition.fields ?? []) sites.push({
        scope: "workflow",
        entry: entry
    });
    for (const entry of stage.fields ?? []) sites.push({
        scope: "stage",
        entry: entry
    });
    for (const activity of stage.activities ?? []) for (const entry of activity.fields ?? []) sites.push({
        scope: "activity",
        activity: activity.name,
        entry: entry
    });
    return sites;
}

function resolveFieldSite(site, stage) {
    return {
        scope: site.scope,
        ...site.activity !== void 0 ? {
            activity: site.activity
        } : {},
        name: site.entry.name,
        type: site.entry.type,
        ...site.entry.title !== void 0 ? {
            title: site.entry.title
        } : {},
        ...site.entry.validation !== void 0 ? {
            validation: site.entry.validation
        } : {},
        ...site.entry.roles !== void 0 ? {
            roles: site.entry.roles
        } : {},
        ref: {
            scope: site.scope,
            field: site.entry.name
        },
        effective: effectiveEditable(site.entry.editable, stage.editable?.[site.entry.name])
    };
}

function editableFieldsInStage(definition, stage) {
    return fieldSites(definition, stage).filter(site => site.entry.editable !== void 0).map(site => resolveFieldSite(site, stage));
}

function editTargetMatches(site, target) {
    return site.name !== target.field ? !1 : target.activity !== void 0 ? site.scope === "activity" && site.activity === target.activity : target.scope !== void 0 ? site.scope === target.scope : !0;
}

const SCOPE_PRECEDENCE = {
    activity: 0,
    stage: 1,
    workflow: 2
};

function resolveEditTarget(args) {
    const {definition: definition, stage: stage, target: target} = args, found = fieldSites(definition, stage).filter(site => editTargetMatches({
        scope: site.scope,
        name: site.entry.name,
        ...site.activity !== void 0 ? {
            activity: site.activity
        } : {}
    }, target)).sort((a, b) => SCOPE_PRECEDENCE[a.scope] - SCOPE_PRECEDENCE[b.scope])[0];
    return found ? resolveFieldSite(found, stage) : void 0;
}

function fieldWindowOpen(instance, site) {
    const terminal = terminalState(instance);
    if (terminal !== "in-flight") return {
        open: !1,
        detail: `instance ${terminal}`
    };
    if (site.scope !== "activity") return {
        open: !0
    };
    const status = findCurrentActivityEntry(instance, site.activity)?.status;
    return status === "active" ? {
        open: !0
    } : {
        open: !1,
        detail: `activity "${site.activity}" is ${status ?? "not active"}`
    };
}

function readFieldValue(instance, site) {
    return resolveFieldEntry$1(instance, site)?.value;
}

function resolveFieldEntry$1(instance, site) {
    return fieldEntryHost(instance, site)?.find(e => e.name === site.name);
}

function fieldEntryHost(instance, site) {
    if (site.scope === "workflow") return instance.fields;
    const stageEntry = findOpenStageEntry(instance);
    return site.scope === "stage" ? stageEntry?.fields : stageEntry?.activities.find(t => t.name === site.activity)?.fields;
}

function fieldProvenance(instance, ref) {
    let latest;
    for (const entry of instance.history) entry._type === "opApplied" && (entry.target?.scope !== ref.scope || entry.target.field !== ref.field || (latest = {
        at: entry.at,
        ...entry.actor !== void 0 ? {
            actor: entry.actor
        } : {}
    }));
    return latest === void 0 ? {} : {
        ...latest.actor !== void 0 ? {
            setBy: latest.actor
        } : {},
        setAt: latest.at
    };
}

function editDisabledReason(args) {
    const {effective: effective, instance: instance, window: window, guardDenial: guardDenial, predicateSatisfied: predicateSatisfied} = args;
    if (effective === void 0) return {
        kind: "not-editable"
    };
    if (!window.open) {
        const terminalReason = instanceTerminalReason(instance);
        return terminalReason !== void 0 ? terminalReason : {
            kind: "edit-window-closed",
            detail: window.detail ?? "closed"
        };
    }
    if (guardDenial !== void 0) return guardDenial;
    if (!predicateSatisfied) return {
        kind: "editor-not-permitted",
        predicate: effective === !0 ? "true" : effective
    };
}

function instanceTerminalReason(instance) {
    const terminal = terminalState(instance);
    if (terminal === "aborted" && instance.abortedAt !== void 0) return {
        kind: "instance-aborted",
        abortedAt: instance.abortedAt
    };
    if (terminal === "completed" && instance.completedAt !== void 0) return {
        kind: "instance-completed",
        completedAt: instance.completedAt
    };
}

function defineWorkflowEvent(options) {
    return {
        type: "log",
        ...options
    };
}

const noopTelemetry = {
    log: () => {}
};

function resolveTelemetry(telemetry) {
    return telemetry === void 0 ? noopTelemetry : {
        log: (event, data) => {
            try {
                telemetry.log(event, data);
            } catch (error) {
                console.warn(`[workflow-engine] telemetry logger threw while logging "${event.name}" — event dropped`, error);
            }
        }
    };
}

const HIGH_FREQUENCY_SAMPLE_MS = 6e4, WorkflowDefinitionDeployed = defineWorkflowEvent({
    name: "Workflows Definition Deployed",
    version: 1,
    description: "A definition went through a deploy — status distinguishes a newly minted version from an unchanged redeploy"
}), WorkflowInstanceStarted = defineWorkflowEvent({
    name: "Workflows Instance Started",
    version: 1,
    description: "A workflow instance was started from a deployed definition"
}), WorkflowStageTransitioned = defineWorkflowEvent({
    name: "Workflows Stage Transitioned",
    version: 1,
    description: "A committed stage move — one event per hop, cascades included, whichever verb drove it. Unsampled: funnel and dwell reads need complete counts, and volume is bounded by actual movement"
}), WorkflowActionFired = defineWorkflowEvent({
    name: "Workflows Action Fired",
    version: 1,
    description: "An action was fired against an activity"
}), WorkflowFieldEdited = defineWorkflowEvent({
    name: "Workflows Field Edited",
    version: 1,
    description: "A declared-editable field was edited through the generic edit seam",
    maxSampleRate: HIGH_FREQUENCY_SAMPLE_MS
}), WorkflowInstanceTicked = defineWorkflowEvent({
    name: "Workflows Instance Ticked",
    version: 1,
    description: "An instance was re-evaluated for auto-transitions and due waits",
    maxSampleRate: HIGH_FREQUENCY_SAMPLE_MS
}), WorkflowEffectsDrained = defineWorkflowEvent({
    name: "Workflows Effects Drained",
    version: 1,
    description: "A drain pass ran — drainedCount/drainedEffects report the successful dispatches. Unsampled when non-empty (outcome counts must be complete; volume is bounded by real effect work); empty polls are machine-cadence noise, throttled engine-side to at most one per minute"
}), WorkflowEffectStateReported = defineWorkflowEvent({
    name: "Workflows Effect State Reported",
    version: 1,
    description: "A running effect handler committed mid-dispatch field state through commitEffectOps. Sampled (high-frequency by design — a dispatch may report many times): an adoption and usage signal, not an exact per-run report count",
    maxSampleRate: HIGH_FREQUENCY_SAMPLE_MS
}), WorkflowEffectCompleted = defineWorkflowEvent({
    name: "Workflows Effect Completed",
    version: 1,
    description: "An effect outcome was recorded — reported directly through completeEffect, or cancelled by the abort verb (external runtimes; engine-drained effects surface via Workflows Effects Drained instead — each outcome counts once)"
}), WorkflowInstanceAborted = defineWorkflowEvent({
    name: "Workflows Instance Aborted",
    version: 1,
    description: "The abort admin override was invoked (changed: false = instance already terminal)"
}), WorkflowStageSet = defineWorkflowEvent({
    name: "Workflows Stage Set",
    version: 1,
    description: "The set-stage admin override was invoked (changed: false = already at the target stage)"
}), WorkflowActivityReset = defineWorkflowEvent({
    name: "Workflows Activity Reset",
    version: 1,
    description: "The reset-activity admin override was invoked (changed: false = already at the target status, or the instance was terminal)"
}), WorkflowDefinitionDeleted = defineWorkflowEvent({
    name: "Workflows Definition Deleted",
    version: 1,
    description: "A deployed definition (or one version of it) was removed via the admin verb"
});

function definitionDeployedData(definition, outcome) {
    const stages = definition.stages, activities = stages.flatMap(stage => stage.activities ?? []), actions = activities.flatMap(activity => activity.actions ?? []), transitions = stages.flatMap(stage => stage.transitions ?? []), fieldEntries2 = [ ...definition.fields ?? [], ...stages.flatMap(stage => stage.fields ?? []), ...activities.flatMap(activity => activity.fields ?? []) ];
    return {
        contentHash: outcome.contentHash,
        status: outcome.status,
        deployId: outcome.deployId,
        stageCount: stages.length,
        activityCount: activities.length,
        actionCount: actions.length,
        transitionCount: transitions.length,
        fieldCount: fieldEntries2.length,
        activityKinds: [ ...new Set(activities.map(deriveActivityKind)) ].sort(),
        fieldKinds: [ ...new Set(fieldEntries2.map(entry => entry.type)) ].sort(),
        guardCount: stages.reduce((n, stage) => n + (stage.guards ?? []).length, 0),
        effectCount: actions.reduce((n, action) => n + (action.effects ?? []).length, 0),
        subworkflowCount: actions.filter(action => action.spawn !== void 0).length,
        lifecycle: definition.lifecycle ?? "standalone"
    };
}

function definitionHashFragment(contentHash) {
    return contentHash !== void 0 ? {
        definitionContentHash: contentHash
    } : {};
}

function instanceStartedData(args) {
    return {
        ...definitionHashFragment(args.definition.contentHash),
        instanceId: args.instanceId,
        initialFieldCount: args.initialFieldCount,
        viaSpawn: args.viaSpawn,
        lifecycle: args.definition.lifecycle ?? "standalone"
    };
}

function instanceStartedDataFor(args) {
    return instanceStartedData({
        definition: {
            ...parseDefinitionSnapshot(args.instance),
            ...args.instance.pinnedContentHash !== void 0 ? {
                contentHash: args.instance.pinnedContentHash
            } : {}
        },
        instanceId: args.instance._id,
        initialFieldCount: args.initialFieldCount,
        viaSpawn: args.viaSpawn
    });
}

function stageTransitionedData(args) {
    const stageIndex = name => args.definition.stages.findIndex(stage => stage.name === name), openEntry = findOpenStageEntry(args.instance), dwellMs = openEntry !== void 0 ? Date.parse(args.at) - Date.parse(openEntry.enteredAt) : Number.NaN;
    return {
        ...definitionHashFragment(args.instance.pinnedContentHash),
        instanceId: args.instance._id,
        fromStageIndex: stageIndex(args.instance.currentStage),
        toStageIndex: stageIndex(args.toStage),
        toIsTerminal: args.toIsTerminal,
        isRevisit: args.instance.stages.some(entry => entry.name === args.toStage),
        ...Number.isFinite(dwellMs) ? {
            dwellMs: dwellMs
        } : {},
        via: args.via
    };
}

function openStage$1(instance) {
    const definition = parseDefinitionSnapshot(instance);
    return {
        definition: definition,
        stage: definition.stages.find(s => s.name === instance.currentStage)
    };
}

function firedActivityKind(instance, activity) {
    const node = openStage$1(instance).stage?.activities?.find(a => a.name === activity);
    return node === void 0 ? void 0 : deriveActivityKind(node);
}

function editedFieldKind(instance, target) {
    const {definition: definition, stage: stage} = openStage$1(instance);
    return stage === void 0 ? void 0 : resolveEditTarget({
        definition: definition,
        stage: stage,
        target: target
    })?.type;
}

function actionFiredData(args) {
    const kind = firedActivityKind(args.instance, args.activity);
    return {
        ...definitionHashFragment(args.instance.pinnedContentHash),
        instanceId: args.instance._id,
        ...kind !== void 0 ? {
            activityKind: kind
        } : {},
        hasParams: args.params !== void 0,
        cascaded: args.cascaded
    };
}

function fieldEditedData(args) {
    const kind = editedFieldKind(args.instance, args.target);
    return {
        ...definitionHashFragment(args.instance.pinnedContentHash),
        instanceId: args.instance._id,
        ...kind !== void 0 ? {
            fieldKind: kind
        } : {},
        mode: args.mode ?? DEFAULT_EDIT_MODE
    };
}

function effectSites(def) {
    const sites = [];
    for (const stage of def.stages ?? []) for (const activity of stage.activities ?? []) for (const action of activity.actions ?? []) for (const effect of action.effects ?? []) sites.push({
        effect: effect,
        location: `stage[${stage.name}].activity[${activity.name}].action[${action.name}].effects`
    });
    return sites;
}

function findEffect(def, name) {
    return effectSites(def).find(site => site.effect.name === name)?.effect;
}

function rolesGateOf(atom) {
    const node = atomNode(atom);
    if (atom.negated || node.type !== "OpCall" || node.op !== ">" || node.right.type !== "Value" || node.right.value !== 0) return;
    const count = node.left;
    if (!(count.type !== "FuncCall" || count.name !== "count" || count.args.length !== 1)) return rolesFilterList(count.args[0]);
}

function isAssignedGate(atom) {
    const node = atomNode(atom);
    return !atom.negated && node.type === "Parameter" && node.name === "assigned";
}

function rolesFilterList(node) {
    if (node.type !== "Filter") return;
    const base = node.base;
    if (!(base.type === "AccessAttribute" && base.name === "roles" && base.base?.type === "Parameter" && base.base.name === "actor")) return;
    const expr = node.expr;
    if (expr.type !== "OpCall" || expr.op !== "in" || expr.left.type !== "This" || expr.right.type !== "Array") return;
    const roles = expr.right.elements.flatMap(element => !element.isSplat && element.value.type === "Value" && typeof element.value.value == "string" ? [ element.value.value ] : []);
    if (roles.length === expr.right.elements.length) return roles;
}

function deriveWorkflowAutonomy(definition, options = {}) {
    return workflowAutonomy({
        definition: definition,
        children: options.children ?? /* @__PURE__ */ new Map,
        lineage: /* @__PURE__ */ new Set
    });
}

function stageAutonomyOf(autonomy, stageName) {
    const stage = autonomy.stages.find(candidate => candidate.stage === stageName);
    if (stage === void 0) throw new Error(`Autonomy rollup is missing stage "${stageName}"`);
    return stage;
}

function activityAutonomyOf(stageAutonomy2, activityName) {
    const answer = stageAutonomy2.activities[activityName];
    if (answer === void 0) throw new Error(`Autonomy rollup is missing activity "${activityName}" in stage "${stageAutonomy2.stage}"`);
    return answer;
}

const AUTONOMOUS = {
    completesWithoutCaller: "yes",
    waitsOn: []
}, UNDECIDABLE = {
    completesWithoutCaller: "conditional",
    waitsOn: []
}, CALLER = {
    completesWithoutCaller: "no",
    waitsOn: []
}, VERDICT_RANK = {
    yes: 0,
    conditional: 1,
    no: 2
};

function allOf(deps) {
    return {
        completesWithoutCaller: deps.map(dep => dep.completesWithoutCaller).reduce((worst, verdict) => VERDICT_RANK[verdict] > VERDICT_RANK[worst] ? verdict : worst, "yes"),
        waitsOn: mergeWaits(deps)
    };
}

function anyOf(deps) {
    const [first, ...rest] = deps;
    if (first === void 0) throw new Error("anyOf needs at least one route — callers guard empties");
    return {
        completesWithoutCaller: rest.every(dep => dep.completesWithoutCaller === first.completesWithoutCaller) ? first.completesWithoutCaller : "conditional",
        waitsOn: mergeWaits(deps)
    };
}

function mergeWaits(deps) {
    return dedupeBy(deps.flatMap(dep => dep.waitsOn), wait => JSON.stringify(wait));
}

function analysisCtx(args) {
    const memo = /* @__PURE__ */ new Map, visiting = /* @__PURE__ */ new Set;
    return {
        ...args,
        memoized: (key, compute) => {
            const hit = memo.get(key);
            if (hit !== void 0) return hit;
            if (visiting.has(key)) return UNDECIDABLE;
            visiting.add(key);
            try {
                const answer = compute();
                return memo.set(key, answer), answer;
            } finally {
                visiting.delete(key);
            }
        }
    };
}

function workflowAutonomy(args) {
    const ctx = analysisCtx({
        definition: args.definition,
        children: args.children,
        lineage: /* @__PURE__ */ new Set([ ...args.lineage, args.definition.name ])
    }), stages = ctx.definition.stages.map(stage => stageAutonomy(stage, ctx));
    return {
        ...allOf(stages),
        stages: stages
    };
}

function stageAutonomy(stage, ctx) {
    const activities = Object.fromEntries((stage.activities ?? []).map(activity => [ activity.name, activityCompletion({
        stage: stage,
        activity: activity
    }, ctx) ]));
    return {
        ...stageProgress(stage, ctx),
        stage: stage.name,
        activities: activities
    };
}

function stageProgress(stage, ctx) {
    const transitions = stage.transitions ?? [];
    return transitions.length === 0 ? AUTONOMOUS : anyOf(transitions.map(transition => conditionAutonomy(transition.when, {
        ...ctx,
        site: {
            stage: stage
        }
    })));
}

function activityCompletion(ref, ctx) {
    return ctx.memoized(`activity:${ref.stage.name}/${ref.activity.name}`, () => {
        const routes = completionActions(ref.stage, ref.activity.name).map(site => actionFire(site, ctx));
        return routes.length === 0 ? CALLER : anyOf(routes);
    });
}

function completionActions(stage, activityName) {
    return stageActionSites(stage).filter(site => (site.action.ops ?? []).some(op => op.type === "status.set" && op.activity === activityName && (op.status === "done" || op.status === "skipped")));
}

function stageActionSites(stage) {
    return (stage.activities ?? []).flatMap(activity => (activity.actions ?? []).map(action => ({
        stage: stage,
        activity: activity,
        action: action
    })));
}

function actionSitesOf(definition) {
    return definition.stages.flatMap(stageActionSites);
}

function actionFire(site, ctx) {
    const {stage: stage, activity: activity, action: action} = site;
    return ctx.memoized(`action:${stage.name}/${activity.name}/${action.name}`, () => {
        const trigger = action.when;
        if (trigger === void 0) return {
            completesWithoutCaller: "no",
            waitsOn: [ callerActionWait(site) ]
        };
        const scope = {
            ...ctx,
            site: {
                stage: stage,
                activity: activity
            }
        }, gates = [ conditionAutonomy(trigger, scope), ...executePinDependency(site) ];
        return action.filter !== void 0 && gates.push(conditionAutonomy(action.filter, scope)), 
        allOf(gates);
    });
}

function executePinDependency({stage: stage, activity: activity, action: action}) {
    const roles = action.roles ?? [];
    return roles.length === 0 ? [] : [ {
        completesWithoutCaller: "conditional",
        waitsOn: [ {
            kind: "execute-pin",
            stage: stage.name,
            activity: activity.name,
            action: action.name,
            roles: roles
        } ]
    } ];
}

function callerActionWait({stage: stage, activity: activity, action: action}) {
    const roles = rolesPinnedInFilter(action.filter);
    return {
        kind: "caller-action",
        stage: stage.name,
        activity: activity.name,
        action: action.name,
        ...roles !== void 0 && roles.length > 0 ? {
            roles: roles
        } : {}
    };
}

function rolesPinnedInFilter(filter) {
    if (filter === void 0) return;
    const root = analyzeCondition(filter).root, conjuncts = root.kind === "all" ? root.clauses : [ root ];
    for (const clause of conjuncts) {
        if (clause.kind !== "atom") continue;
        const roles = rolesGateOf(clause);
        if (roles !== void 0) return roles;
    }
}

function conditionAutonomy(condition, scope) {
    return clauseAutonomy(analyzeCondition(condition).root, scope);
}

function clauseAutonomy(clause, scope) {
    return clause.kind === "all" ? allOf(clause.clauses.map(leg => clauseAutonomy(leg, scope))) : clause.kind === "any" ? anyOf(clause.clauses.map(leg => clauseAutonomy(leg, scope))) : atomAutonomy(clause, scope);
}

function atomAutonomy(atom, scope) {
    const deps = atom.reads.map(read => readAutonomy(read, scope));
    return atomReadsDataset(atom) && deps.push({
        completesWithoutCaller: "no",
        waitsOn: [ {
            kind: "editor-content",
            condition: atom.groq
        } ]
    }), allOf(deps);
}

const ENGINE_STATE_VARS = /* @__PURE__ */ new Set([ "now", "self", "stage", "parent", "ancestors" ]), CALLER_VARS = new Set(CALLER_BOUND_VARS);

function readAutonomy(read, scope) {
    const body = predicateBodyOf(scope.definition, read.variable);
    return body !== void 0 ? predicateAutonomy({
        name: read.variable,
        body: body
    }, scope) : ENGINE_STATE_VARS.has(read.variable) ? AUTONOMOUS : CALLER_VARS.has(read.variable) ? CALLER : namespaceAutonomy(read, scope);
}

function predicateBodyOf(definition, name) {
    const predicates = definition.predicates ?? {};
    return Object.hasOwn(predicates, name) ? predicates[name] : void 0;
}

function namespaceAutonomy(read, scope) {
    switch (read.variable) {
      case "fields":
        return fieldAutonomy(read, scope);

      case "effects":
      case "effectStatus":
        return effectAutonomy(read, scope);

      case "context":
        return contextAutonomy(read);

      case "subworkflows":
        return subworkflowAutonomy(scope);

      case "activities":
      case "allActivitiesDone":
        return siblingsAutonomy(scope);

      default:
        return UNDECIDABLE;
    }
}

function predicateAutonomy(predicate, scope) {
    const stage = scope.site.stage;
    return scope.memoized(`predicate:${predicate.name}@${stage?.name ?? ""}`, () => conditionAutonomy(predicate.body, {
        ...scope,
        site: {
            ...stage !== void 0 ? {
                stage: stage
            } : {}
        }
    }));
}

function contextAutonomy(read) {
    const [head] = read.path;
    return {
        completesWithoutCaller: "conditional",
        waitsOn: [ {
            kind: "context",
            ...typeof head == "string" ? {
                key: head
            } : {}
        } ]
    };
}

function siblingsAutonomy(scope) {
    const stage = scope.site.stage;
    if (stage === void 0) return UNDECIDABLE;
    const siblings = (stage.activities ?? []).filter(activity => activity !== scope.site.activity);
    return siblings.length === 0 ? AUTONOMOUS : allOf(siblings.map(activity => activityCompletion({
        stage: stage,
        activity: activity
    }, scope)));
}

function fieldAutonomy(read, scope) {
    const [head] = read.path;
    if (typeof head != "string") return UNDECIDABLE;
    const resolved = resolveFieldEntry(head, scope);
    return resolved === void 0 ? UNDECIDABLE : scope.memoized(fieldKeyOf(resolved), () => fieldProducers(resolved, scope));
}

function resolveFieldEntry(name, scope) {
    const {stage: stage, activity: activity} = scope.site, activityEntry = (activity?.fields ?? []).find(entry => entry.name === name);
    if (activityEntry !== void 0 && activity !== void 0) return {
        entry: activityEntry,
        scope: "activity",
        stage: stage,
        activity: activity
    };
    const stageEntry = (stage?.fields ?? []).find(entry => entry.name === name);
    if (stageEntry !== void 0 && stage !== void 0) return {
        entry: stageEntry,
        scope: "stage",
        stage: stage
    };
    const workflowEntry = (scope.definition.fields ?? []).find(entry => entry.name === name);
    return workflowEntry !== void 0 ? {
        entry: workflowEntry,
        scope: "workflow"
    } : void 0;
}

function fieldKeyOf(resolved) {
    const owner = `${resolved.stage?.name ?? ""}/${resolved.activity?.name ?? ""}`;
    return `field:${resolved.scope}/${owner}/${resolved.entry.name}`;
}

function fieldProducers(resolved, ctx) {
    const routes = [ ...writerActions(resolved, ctx.definition).map(site => actionFire(site, ctx)), ...seedProducer(resolved.entry), ...editSeamProducer(resolved.entry) ];
    return routes.length === 0 ? unwrittenField(resolved, ctx) : anyOf(routes);
}

function seedProducer(entry) {
    const seed = entry.initialValue;
    return seed === void 0 ? [] : seed.type === "input" ? [ {
        completesWithoutCaller: "conditional",
        waitsOn: [ {
            kind: "start-input",
            field: entry.name
        } ]
    } ] : [ AUTONOMOUS ];
}

function editSeamProducer(entry) {
    return entry.editable === void 0 ? [] : [ {
        completesWithoutCaller: "no",
        waitsOn: [ {
            kind: "editor-field",
            field: entry.name
        } ]
    } ];
}

function unwrittenField(resolved, ctx) {
    const handlerPossible = resolved.scope !== "activity" && effectSites(ctx.definition).length > 0;
    return {
        completesWithoutCaller: handlerPossible ? "conditional" : "no",
        waitsOn: [ {
            kind: "unwritten-field",
            field: resolved.entry.name,
            handlerPossible: handlerPossible
        } ]
    };
}

function writerActions(resolved, definition) {
    return actionSitesOf(definition).filter(site => ownsEntryScope(site, resolved) && writesEntry(site.action, resolved));
}

function ownsEntryScope(site, resolved) {
    return resolved.scope === "workflow" ? !0 : resolved.scope === "stage" ? site.stage === resolved.stage : site.activity === resolved.activity;
}

function writesEntry(action, resolved) {
    return (action.ops ?? []).some(op => op.type !== "status.set" && op.target.scope === resolved.scope && op.target.field === resolved.entry.name);
}

function effectAutonomy(read, ctx) {
    const [head] = read.path;
    if (typeof head != "string") return UNDECIDABLE;
    const carriers = actionSitesOf(ctx.definition).filter(site => (site.action.effects ?? []).some(effect => effect.name === head));
    if (carriers.length === 0) return UNDECIDABLE;
    const settle = {
        completesWithoutCaller: "yes",
        waitsOn: [ {
            kind: "effect",
            effect: head
        } ]
    };
    return anyOf(carriers.map(site => allOf([ actionFire(site, ctx), settle ])));
}

function subworkflowAutonomy(ctx) {
    const spawns = actionSitesOf(ctx.definition).flatMap(site => site.action.spawn !== void 0 ? [ {
        site: site,
        child: site.action.spawn.definition.name
    } ] : []);
    return spawns.length === 0 ? AUTONOMOUS : anyOf(spawns.map(({site: site, child: child}) => allOf([ actionFire(site, ctx), childAutonomy(child, ctx) ])));
}

function childAutonomy(name, ctx) {
    return ctx.memoized(`child:${name}`, () => {
        const child = ctx.children.get(name);
        return child === void 0 || ctx.lineage.has(name) ? {
            completesWithoutCaller: "conditional",
            waitsOn: [ {
                kind: "subworkflow",
                definition: name,
                resolved: !1
            } ]
        } : {
            completesWithoutCaller: workflowAutonomy({
                definition: child,
                children: ctx.children,
                lineage: ctx.lineage
            }).completesWithoutCaller,
            waitsOn: [ {
                kind: "subworkflow",
                definition: name,
                resolved: !0
            } ]
        };
    });
}

function describeCondition(insight, ctx) {
    return describeCondition$1(insight, toGroqDescribeContext(ctx));
}

function describeAtom(atom, ctx) {
    return describeAtom$1(atom, toGroqDescribeContext(ctx));
}

const checklistLines = checklistLines$1;

function toGroqDescribeContext(ctx) {
    return {
        renderRead: read => renderWorkflowRead(read, ctx),
        renderRequirement: requirement => workflowRequirement(requirement, ctx),
        atomPatterns: [ rolesGateText, claimGateText ],
        nowParam: "now"
    };
}

const VAR_LABELS = new Map([ ...START_REQUIREMENT_VARS, ...CONDITION_VARS ].map(entry => [ entry.name, entry.label ]));

function renderWorkflowRead(read, ctx) {
    if (read.variable === "fields" && typeof read.path[0] == "string") {
        const title = fieldTitleOf(read, ctx);
        return phrase("read.field", {
            params: {
                title: title,
                path: read.path
            },
            text: guillemets(title)
        });
    }
    const keyed = keyedCallerRead(read);
    if (keyed !== void 0) return keyed;
    if (read.variable === "actor" && read.path.length === 1 && read.path[0] === "id") return phrase("read.actor-id", {
        params: {},
        text: "your id"
    });
    if (read.path.length === 0 && predicateBodyOf(ctx.definition, read.variable) !== void 0) return phrase("read.predicate", {
        params: {
            name: read.variable
        },
        text: humanize(read.variable)
    });
    const label = VAR_LABELS.get(read.variable);
    if (label === void 0) return;
    const text = read.path.length === 0 ? label : `${label} (${read.path.join(".")})`;
    return phrase(`read.var.${read.variable}`, {
        params: {
            path: read.path
        },
        text: text
    });
}

function keyedCallerRead(read) {
    const key = read.path[0];
    if (typeof key == "string") {
        if (read.variable === "can") return phrase("read.permission", {
            params: {
                permission: key
            },
            text: `your ${key} permission`
        });
        if (read.variable === "attributes") return phrase("read.attribute", {
            params: {
                attribute: key
            },
            text: `your ${guillemets(key)} attribute`
        });
    }
}

function workflowRequirement(requirement, ctx) {
    const {target: target} = requirement, clause = clauseVarRequirement(requirement);
    if (clause !== void 0) return clause;
    if (target.variable === "assigned" && target.path.length === 0) {
        if (requirement.kind === "truthy") return phrase("requirement.assigned", {
            params: {},
            text: "you must be assigned to this activity"
        });
        if (requirement.kind === "falsy") return phrase("requirement.not-assigned", {
            params: {},
            text: "you must not be assigned to this activity"
        });
    }
    return effectStatusPhrase(requirement, ctx);
}

function clauseVarRequirement(requirement) {
    const {target: target, kind: kind} = requirement;
    if (!(target.path.length > 0 || kind !== "truthy" && kind !== "falsy")) {
        if (target.variable === "allActivitiesDone") return kind === "truthy" ? phrase("requirement.all-activities-done", {
            params: {},
            text: "all activities in this stage must be finished"
        }) : phrase("requirement.not-all-activities-done", {
            params: {},
            text: "some activity in this stage must still be unfinished"
        });
        if (target.variable === "anyActivityFailed") return kind === "truthy" ? phrase("requirement.any-activity-failed", {
            params: {},
            text: "an activity in this stage must have failed"
        }) : phrase("requirement.no-activity-failed", {
            params: {},
            text: "no activity in this stage may have failed"
        });
    }
}

function effectStatusPhrase(requirement, ctx) {
    const {target: target} = requirement;
    if (target.variable !== "effectStatus" || typeof target.path[0] != "string") return;
    const effect = target.path[0], title = effectTitleOf(effect, ctx), name = `the ${quoted(title)} automation`;
    if (requirement.kind === "equals" && requirement.value === "done") return phrase("requirement.effect-done", {
        params: {
            effect: effect,
            title: title
        },
        text: `${name} must have succeeded`
    });
    if (requirement.kind === "equals" && requirement.value === "failed") return phrase("requirement.effect-failed", {
        params: {
            effect: effect,
            title: title
        },
        text: `${name} must have failed`
    });
    if (requirement.kind === "defined") return phrase("requirement.effect-settled", {
        params: {
            effect: effect,
            title: title
        },
        text: `${name} must have finished`
    });
}

function rolesGateText(atom) {
    const roles = rolesGateOf(atom);
    if (roles !== void 0) return phrase("atom.roles", {
        params: {
            roles: roles
        },
        text: `you must have the ${listOr(roles)} role`
    });
}

function claimGateText(atom, ctx) {
    const node = atomNode(atom);
    if (atom.negated || node.type !== "OpCall" || node.op !== "==") return;
    const sides = [ scopeReadOf(node.left), scopeReadOf(node.right) ], actor = sides.find(s => s?.variable === "actor" && s.path.length === 1 && s.path[0] === "id"), field = sides.find(s => s?.variable === "fields" && s.path.at(-1) === "id");
    if (actor === void 0 || field === void 0) return;
    const holder = describeRead({
        variable: "fields",
        path: field.path.slice(0, -1)
    }, ctx).text;
    return phrase("atom.claim", {
        params: {
            holder: holder
        },
        text: `${holder} must be you`
    });
}

function describeSiteHeading(address, ctx) {
    const id = `site-heading.${address.kind}`;
    switch (address.kind) {
      case "predicate":
        {
            const predicate = humanize(address.predicate);
            return phrase(id, {
                params: {
                    predicate: predicate
                },
                text: `predicate ${quoted(predicate)} holds when:`
            });
        }

      case "transition":
        {
            const title = transitionTitle(address.transition, ctx);
            return phrase(id, {
                params: {
                    title: title
                },
                text: `transition ${quoted(title)} fires when:`
            });
        }

      case "action":
        {
            const title = actionTitle(address, ctx), activity = activityTitle(address.activity, ctx);
            return phrase(id, {
                params: {
                    title: title,
                    activity: activity
                },
                text: `action ${quoted(title)} on ${quoted(activity)} is available when:`
            });
        }

      case "requirement":
        {
            const requirement = humanize(address.requirement), activity = activityTitle(address.activity, ctx);
            return phrase(id, {
                params: {
                    requirement: requirement,
                    activity: activity
                },
                text: `requirement ${quoted(requirement)} of ${quoted(activity)} is met when:`
            });
        }

      case "activity-filter":
        {
            const activity = activityTitle(address.activity, ctx);
            return phrase(id, {
                params: {
                    activity: activity
                },
                text: `${quoted(activity)} is shown when:`
            });
        }

      case "action-when":
        {
            const title = actionTitle(address, ctx), activity = activityTitle(address.activity, ctx);
            return phrase(id, {
                params: {
                    title: title,
                    activity: activity
                },
                text: `action ${quoted(title)} on ${quoted(activity)} fires automatically when:`
            });
        }

      case "editable-field":
        {
            const field = fieldTitleOf({
                path: [ address.name ]
            }, ctx);
            return phrase(id, {
                params: {
                    field: field
                },
                text: `${guillemets(field)} can be edited when:`
            });
        }

      case "editable-override":
        {
            const field = fieldTitleOf({
                path: [ address.name ]
            }, ctx);
            return phrase(id, {
                params: {
                    field: field
                },
                text: `in this stage, ${guillemets(field)} can only be edited when:`
            });
        }
    }
}

function describeSite(site, ctx) {
    const id = `site.${site.kind}`;
    switch (site.kind) {
      case "transition":
        {
            const title = transitionTitle(site.transition, ctx);
            return phrase(id, {
                params: {
                    title: title
                },
                text: `the ${quoted(title)} transition`
            });
        }

      case "action":
        {
            const title = actionTitle(site, ctx), activity = activityTitle(site.activity, ctx);
            return phrase(id, {
                params: {
                    title: title,
                    activity: activity
                },
                text: `the ${quoted(title)} action on ${quoted(activity)}`
            });
        }

      case "requirement":
        {
            const requirement = humanize(site.requirement), activity = activityTitle(site.activity, ctx);
            return phrase(id, {
                params: {
                    requirement: requirement,
                    activity: activity
                },
                text: `requirement ${quoted(requirement)} of ${quoted(activity)}`
            });
        }

      case "action-when":
        {
            const title = actionTitle(site, ctx), activity = activityTitle(site.activity, ctx);
            return phrase(id, {
                params: {
                    title: title,
                    activity: activity
                },
                text: `the ${quoted(title)} trigger on ${quoted(activity)}`
            });
        }

      case "activity-filter":
        {
            const activity = activityTitle(site.activity, ctx);
            return phrase(id, {
                params: {
                    activity: activity
                },
                text: `existence of ${quoted(activity)}`
            });
        }

      case "editable-field":
        {
            const field = fieldTitleOf({
                path: [ site.name ]
            }, ctx);
            return phrase(id, {
                params: {
                    field: field
                },
                text: `editing ${guillemets(field)}`
            });
        }
    }
}

function describeFieldInsight(field, ctx) {
    const title = fieldTitleOf({
        path: [ field.field ]
    }, ctx), sites = field.involvedIn.map(site => describeSite(site, ctx).text);
    return {
        involvement: phrase("field.involvement", {
            params: {
                title: title,
                sites: sites
            },
            text: `${guillemets(title)} affects ${listAnd(sites)}`
        }),
        proposals: field.proposals.map(proposal => {
            const effects = proposal.consequences.map(c => `${c.after === "satisfied" ? "satisfies" : "changes"} ${describeSite(c.site, ctx).text}`);
            return phrase("field.proposal", {
                params: {
                    title: title,
                    value: proposal.assign.value,
                    effects: effects
                },
                text: `Setting ${guillemets(title)} to ${formatValue(proposal.assign.value)} ${listAnd(effects)}`
            });
        })
    };
}

function unsatisfiedTransitionSummaries(evaluation) {
    const ctx = {
        definition: evaluation.definition
    };
    return evaluation.currentStage.transitions.filter(t => !t.whenSatisfied).flatMap(t => {
        const summary = describeCondition(t.insight, ctx).summary;
        return summary !== void 0 ? [ {
            transition: t.transition.name,
            summary: summary
        } ] : [];
    });
}

const WAIT_TEXTS = {
    "caller-action": (wait, ctx) => {
        const title = actionTitle(wait, ctx), roles = wait.roles === void 0 ? "" : ` (roles: ${listOr(wait.roles)})`;
        return phrase("autonomy-wait.caller-action", {
            params: {
                stage: wait.stage,
                activity: wait.activity,
                action: wait.action,
                roles: wait.roles
            },
            text: `waits on caller action ${quoted(title)}${roles}`
        });
    },
    "execute-pin": (wait, ctx) => {
        const title = actionTitle(wait, ctx);
        return phrase("autonomy-wait.execute-pin", {
            params: {
                stage: wait.stage,
                activity: wait.activity,
                action: wait.action,
                roles: wait.roles
            },
            text: `waits on a cascading token with the ${listOr(wait.roles)} role to run ${quoted(title)}`
        });
    },
    "editor-field": (wait, ctx) => {
        const title = fieldTitleOf({
            path: [ wait.field ]
        }, ctx);
        return phrase("autonomy-wait.editor-field", {
            params: {
                field: wait.field
            },
            text: `waits on an editor writing ${guillemets(title)}`
        });
    },
    "editor-content": wait => phrase("autonomy-wait.editor-content", {
        params: {
            condition: wait.condition
        },
        text: `waits on editor content matching ${quoted(wait.condition)}`
    }),
    effect: (wait, ctx) => {
        const title = effectTitleOf(wait.effect, ctx);
        return phrase("autonomy-wait.effect", {
            params: {
                effect: wait.effect,
                title: title
            },
            text: `waits on the ${quoted(title)} automation`
        });
    },
    "start-input": (wait, ctx) => {
        const title = fieldTitleOf({
            path: [ wait.field ]
        }, ctx);
        return phrase("autonomy-wait.start-input", {
            params: {
                field: wait.field
            },
            text: `depends on the ${guillemets(title)} start input`
        });
    },
    context: wait => phrase("autonomy-wait.context", {
        params: {
            key: wait.key
        },
        text: `depends on start-time context${wait.key === void 0 ? "" : ` ${quoted(wait.key)}`}`
    }),
    subworkflow: wait => phrase("autonomy-wait.subworkflow", {
        params: {
            definition: wait.definition,
            resolved: wait.resolved
        },
        text: `waits on each ${quoted(wait.definition)} child workflow`
    }),
    "unwritten-field": (wait, ctx) => {
        const title = fieldTitleOf({
            path: [ wait.field ]
        }, ctx);
        return phrase("autonomy-wait.unwritten-field", {
            params: {
                field: wait.field,
                handlerPossible: wait.handlerPossible
            },
            text: wait.handlerPossible ? `waits on an automation handler writing ${guillemets(title)}` : `${guillemets(title)} is never written`
        });
    }
};

function describeAutonomyWait(wait, ctx) {
    const render = WAIT_TEXTS[wait.kind];
    return render(wait, ctx);
}

function narrateAutonomyWaits(waits, ctx) {
    return dedupeBy(waits.map(wait => describeAutonomyWait(wait, ctx).text), text => text);
}

function autonomySummary(autonomy, ctx) {
    const held = autonomy.stages.filter(stage => stage.completesWithoutCaller !== "yes");
    if (held.length === 0) return phrase("autonomy.self-running", {
        params: {},
        text: "this workflow runs itself"
    });
    const names = held.map(stage => quoted(stageTitle(stage.stage, ctx)));
    return phrase("autonomy.held-at", {
        params: {
            stages: held.map(stage => stage.stage)
        },
        text: `this workflow runs itself except at ${listAnd(names)}`
    });
}

function describeNode(node, ctx) {
    return dispatchDescribe(node, ctx);
}

function dispatchDescribe(node, ctx) {
    if ("involvedIn" in node) return describeFieldInsight(node, ctx);
    if ("actions" in node) return describeActivity(node, ctx);
    if ("transition" in node) return describeCondition(node.insight, ctx);
    if ("analysis" in node) return describeCondition(node, ctx);
    const gate = "whenInsight" in node ? node.whenInsight ?? node.insight : node.insight;
    return gate !== void 0 ? describeCondition(gate, ctx) : void 0;
}

function describeActivity(node, ctx) {
    const requirements = Object.entries(node.requirementInsights ?? {}).map(([name, insight]) => [ name, describeCondition(insight, ctx) ]);
    return {
        ...requirements.length > 0 ? {
            requirements: Object.fromEntries(requirements)
        } : {},
        ...node.filterInsight !== void 0 ? {
            filter: describeCondition(node.filterInsight, ctx)
        } : {}
    };
}

function fieldTitleOf(read, ctx) {
    const [head, ...rest] = read.path, title = fieldEntries$1(ctx.definition).find(field => field.name === head)?.title ?? humanize(String(head));
    return rest.length > 0 ? `${title} (${rest.join(".")})` : title;
}

function fieldEntries$1(definition) {
    return [ ...definition.stages.flatMap(stage => [ ...(stage.activities ?? []).flatMap(activity => activity.fields ?? []), ...stage.fields ?? [] ]), ...definition.fields ?? [] ];
}

function stageTitle(name, ctx) {
    return ctx.definition.stages.find(candidate => candidate.name === name)?.title ?? humanize(name);
}

function transitionTitle(name, ctx) {
    return ctx.definition.stages.flatMap(stage => stage.transitions ?? []).find(candidate => candidate.name === name)?.title ?? humanize(name);
}

function activityTitle(name, ctx) {
    return ctx.definition.stages.flatMap(stage => stage.activities ?? []).find(candidate => candidate.name === name)?.title ?? humanize(name);
}

function actionTitle(site, ctx) {
    return ctx.definition.stages.flatMap(stage => stage.activities ?? []).filter(activity => activity.name === site.activity).flatMap(activity => activity.actions ?? []).find(candidate => candidate.name === site.action)?.title ?? humanize(site.action);
}

function effectTitleOf(name, ctx) {
    return findEffect(ctx.definition, name)?.title ?? name;
}

function conditionSitesOf(definition) {
    const sites = [];
    for (const [predicate, condition] of Object.entries(definition.predicates ?? {})) sites.push({
        address: {
            kind: "predicate",
            predicate: predicate
        },
        condition: condition
    });
    for (const stage of definition.stages) sites.push(...stageSites(stage, definition));
    return sites;
}

function stageSites(stage, definition) {
    const sites = [];
    for (const transition of stage.transitions ?? []) sites.push({
        stage: stage.name,
        address: {
            kind: "transition",
            transition: transition.name
        },
        condition: transition.when
    });
    for (const activity of stage.activities ?? []) sites.push(...activitySites(activity, stage.name));
    for (const site of editableFieldsInStage(definition, stage)) typeof site.effective == "string" && sites.push({
        stage: stage.name,
        address: {
            kind: "editable-field",
            scope: site.scope,
            name: site.name,
            ...site.activity !== void 0 ? {
                activity: site.activity
            } : {}
        },
        condition: site.effective
    });
    for (const [name, override] of Object.entries(stage.editable ?? {})) typeof override == "string" && sites.push({
        stage: stage.name,
        address: {
            kind: "editable-override",
            name: name
        },
        condition: override
    });
    return sites;
}

function activitySites(activity, stage) {
    return [ ...activity.filter !== void 0 ? [ {
        stage: stage,
        address: {
            kind: "activity-filter",
            activity: activity.name
        },
        condition: activity.filter
    } ] : [], ...(activity.requirements ?? []).map(requirement => ({
        stage: stage,
        address: {
            kind: "requirement",
            activity: activity.name,
            requirement: requirement.name
        },
        condition: requirement.query
    })), ...(activity.actions ?? []).flatMap(action => [ ...action.filter !== void 0 ? [ {
        stage: stage,
        address: {
            kind: "action",
            activity: activity.name,
            action: action.name
        },
        condition: action.filter
    } ] : [], ...action.when !== void 0 ? [ {
        stage: stage,
        address: {
            kind: "action-when",
            activity: activity.name,
            action: action.name
        },
        condition: action.when
    } ] : [] ]) ];
}

async function describeDefinition(definition) {
    const ctx = {
        definition: definition
    }, described = [];
    for (const site of conditionSitesOf(definition)) {
        const insight = await explainCondition({
            condition: site.condition,
            dataset: [],
            params: {}
        });
        described.push({
            ...site,
            description: describeCondition(insight, ctx)
        });
    }
    return described;
}

function acceptsDocumentType(definition, documentType) {
    return (definition.fields ?? []).some(entry => isSubjectEntry(entry) && (entry.types === void 0 || entry.types.includes(documentType)));
}

async function isDefinitionApplicable(args) {
    const {definition: definition, document: document, scope: scope} = args;
    return assertCandidateDocument(document), !isStartableDefinition(definition) || !acceptsDocumentType(definition, document._type) ? !1 : evaluateDeclaredStartFilter({
        definition: definition,
        document: document,
        scope: scope
    });
}

async function applicableDefinitions(args) {
    const {definitions: definitions, document: document, scope: scope} = args;
    assertCandidateDocument(document);
    const verdicts = await Promise.all(definitions.map(definition => isDefinitionApplicable({
        definition: definition,
        document: document,
        ...scope !== void 0 ? {
            scope: scope
        } : {}
    })));
    return definitions.filter((_, i) => verdicts[i]);
}

async function evaluateStartFilter(args) {
    const {filter: filter, definition: definition, document: document, scope: scope} = args, parsed = parseStartFilter({
        filter: filter,
        definition: definition
    }), dataset = await filterDataset(parsed.needsDataset, scope);
    if (dataset === void 0) return !1;
    try {
        const result = await (await evaluate(parsed.tree, {
            ...document !== void 0 ? {
                root: document
            } : {},
            dataset: dataset,
            params: startContextParams({
                definition: definition,
                scope: scope
            })
        })).get();
        return isUnevaluable(result) ? !1 : !!result;
    } catch (err) {
        rethrowNamingDefinition({
            err: err,
            definition: definition,
            key: "filter"
        });
    }
}

function parseStartFilter(args) {
    const {filter: filter, definition: definition} = args;
    try {
        return {
            tree: parse(filter),
            needsDataset: analyzeCondition(filter).readsDataset
        };
    } catch (err) {
        rethrowNamingDefinition({
            err: err,
            definition: definition,
            key: "filter"
        });
    }
}

async function filterDataset(needsDataset, scope) {
    if (!needsDataset) return [];
    if (scope?.fetchDataset !== void 0) return scope.fetchDataset();
}

async function explainStartRequirement(args) {
    const {query: query, definition: definition, fields: fields, scope: scope} = args;
    let needsDataset;
    try {
        needsDataset = analyzeCondition(query).readsDataset;
    } catch (err) {
        rethrowNamingDefinition({
            err: err,
            definition: definition,
            key: "requirement"
        });
    }
    let dataset = [];
    if (needsDataset) {
        if (scope?.fetchDataset === void 0) throw new ContractViolationError(`start requirement on definition "${definition.name ?? "<unnamed>"}" reads the dataset (\`*[...]\`), but the caller supplied no \`scope.fetchDataset\` — evaluating over an empty slice would let a count()-style rule pass vacuously. Supply the instance slice.`);
        dataset = await scope.fetchDataset();
    }
    try {
        return await explainCondition({
            condition: query,
            dataset: dataset,
            params: startContextParams({
                definition: definition,
                scope: scope,
                fields: fields
            })
        });
    } catch (err) {
        rethrowNamingDefinition({
            err: err,
            definition: definition,
            key: "requirement"
        });
    }
}

function unboundRequirementReads(query, fields) {
    return [ ...conditionFieldReadNames(query) ].filter(name => !Object.hasOwn(fields, name));
}

function hasSingleSubjectRequirement(definition) {
    return definition.start?.requirements?.some(requirement => requirement.type === "singleSubject") ?? !1;
}

async function singleSubjectRequirementRefused(args) {
    const {definition: definition, fields: fields, scope: scope} = args, name = singleSubjectDefinitionName(definition), subject = (fields !== void 0 ? startSubject(definition, fields) : void 0) ?? scope?.subject;
    if (subject === void 0) return !1;
    if (scope?.fetchDataset === void 0) throw new ContractViolationError(`singleSubject requirement on definition "${name}" scans the tag's instance slice, but the caller supplied no \`scope.fetchDataset\` — evaluating over an empty slice would let the rule pass vacuously. Supply the projected start slice.`);
    const dataset = await scope.fetchDataset();
    return sliceHasInFlightSubject({
        dataset: dataset,
        subject: subject,
        definition: name
    });
}

function singleSubjectDefinitionName(definition) {
    const label = definition.name ?? "<unnamed>";
    if (definition.start?.requirements?.find(requirement => requirement.type === "singleSubject") === void 0) throw new ContractViolationError(`singleSubjectRequirementRefused called for definition "${label}", which declares no \`singleSubject\` requirement — evaluate only what the definition declares.`);
    if (definition.name === void 0) throw new ContractViolationError("singleSubjectRequirementRefused needs the definition `name` — the rule scopes its in-flight scan to instances of the definition, which a nameless source cannot identify.");
    return definition.name;
}

function rethrowNamingDefinition({err: err, definition: definition, key: key}) {
    rethrowWithContext(err, `${key === "filter" ? "start.filter" : "start requirement"} on definition "${definition.name ?? "<unnamed>"}" failed to evaluate (check the predicate and its evaluation inputs)`);
}

function startContextParams({definition: definition, scope: scope, fields: fields}) {
    const params = baseStartParams(definition, scope);
    return fields !== void 0 && (params.fields = fields), params;
}

function baseStartParams(definition, scope) {
    return {
        ...definition.name !== void 0 ? {
            definition: definition.name
        } : {},
        ...scope?.tag !== void 0 ? {
            tag: scope.tag
        } : {},
        ...scope?.now !== void 0 ? {
            now: scope.now
        } : {}
    };
}

function startSubject(definition, fields) {
    const subjectEntry = (definition.fields ?? []).find(isSubjectEntry);
    if (subjectEntry === void 0) return;
    const value = fields[subjectEntry.name];
    return isGdr(value) ? value.id : void 0;
}

function sliceHasInFlightSubject(args) {
    const {dataset: dataset, subject: subject, definition: definition} = args;
    return dataset.some(row => {
        if (typeof row != "object" || row === null) return !1;
        const instance = row;
        return instance.completedAt != null || instance.definition !== definition ? !1 : instance.subject === subject;
    });
}

async function evaluateDeclaredStartFilter(args) {
    const {definition: definition, document: document, scope: scope} = args, filter = definition.start?.filter;
    return filter === void 0 ? !0 : evaluateStartFilter({
        filter: filter,
        definition: definition,
        document: document,
        ...scope !== void 0 ? {
            scope: scope
        } : {}
    });
}

function assertCandidateDocument(document) {
    const docType = document._type;
    if (typeof docType != "string" || docType.length === 0) throw new ContractViolationError("Applicability: the candidate document must carry a non-empty `_type` — pass the loaded document, not a reference or bare id.");
}

function startFilterSyntaxIssues(groq) {
    return startContextSyntaxIssues({
        groq: groq,
        vars: START_FILTER_VARS,
        context: "start-filter",
        skip: "fields"
    });
}

function startRequirementSyntaxIssues(groq) {
    return startContextSyntaxIssues({
        groq: groq,
        vars: START_REQUIREMENT_VARS,
        context: "start-requirement"
    });
}

function startContextSyntaxIssues({groq: groq, vars: vars, context: context, skip: skip}) {
    try {
        parse(groq);
    } catch (err) {
        return [ errorMessage(err) ];
    }
    const bound = new Set(vars.map(v2 => v2.name));
    return [ ...conditionParameterNames(groq) ].filter(name => !bound.has(name) && name !== skip).map(name => `reads $${name}, which the ${context} context does not bind (an unbound variable is GROQ null, so the predicate silently misevaluates — failing closed or passing vacuously by shape). Bound here: ` + vars.map(v2 => `$${v2.name}`).join(", "));
}

const GUARD_DOC_TYPE = "temp.system.guard";

class MutationGuardDeniedError extends WorkflowError {
    denied;
    documentId;
    action;
    constructor(args) {
        const ids = args.denied.map(d => d.guardId).join(", ");
        super("mutation-guard-denied", `Mutation on "${args.documentId}" (${args.action}) denied by guard(s) [${ids}]`), 
        this.name = "MutationGuardDeniedError", this.denied = args.denied, this.documentId = args.documentId, 
        this.action = args.action;
    }
    static fromGuards(args) {
        return new MutationGuardDeniedError({
            documentId: args.documentId,
            action: args.action,
            denied: deniedGuardRefs(args.guards)
        });
    }
}

function deniedGuardRefs(guards) {
    return guards.map(g => ({
        guardId: g._id,
        ...g.name !== void 0 ? {
            name: g.name
        } : {}
    }));
}

function deniedGuardLabels(denied) {
    return denied.map(d => d.name ?? d.guardId);
}

function mapJsonStrings(value, transform) {
    const walk = (val, key) => {
        if (typeof val == "string") return transform(val, key);
        if (Array.isArray(val)) return val.map(item => walk(item, key));
        if (val !== null && typeof val == "object") {
            const out = {};
            for (const [k, v2] of Object.entries(val)) out[k] = walk(v2, k);
            return out;
        }
        return val;
    };
    return walk(value, void 0);
}

function buildSnapshot(args) {
    const docs = [], knownIds = /* @__PURE__ */ new Set;
    for (const {doc: doc, resource: resource} of args.docs) {
        const uri = gdrFromResource(resource, doc._id), restamped = restampForSnapshot({
            doc: doc,
            gdrUri: uri,
            resource: resource
        });
        docs.push(restamped), knownIds.add(uri);
    }
    return {
        docs: docs,
        knownIds: knownIds
    };
}

function emptySnapshot() {
    return {
        docs: [],
        knownIds: /* @__PURE__ */ new Set
    };
}

function findSnapshotDoc(snapshot, gdrUri2) {
    return snapshot.docs.find(doc => doc._id === gdrUri2);
}

function restampForSnapshot({doc: doc, gdrUri: gdrUri2, resource: resource}) {
    return {
        ...rewriteRefsRecursive(doc, resource),
        _id: gdrUri2
    };
}

function rewriteRefsRecursive(value, resource) {
    return mapJsonStrings(value, (str, key) => key === "_ref" && !str.includes(":") ? gdrFromResource(resource, str) : str);
}

function overlayInstanceInSnapshot(snapshot, instance) {
    const uri = selfGdr(instance), restamped = {
        ...instance,
        _id: uri
    };
    return snapshot.knownIds.has(uri) ? {
        docs: snapshot.docs.map(doc => doc._id === uri ? restamped : doc),
        knownIds: snapshot.knownIds
    } : {
        docs: [ ...snapshot.docs, restamped ],
        knownIds: /* @__PURE__ */ new Set([ ...snapshot.knownIds, uri ])
    };
}

function lakeGuardId(args) {
    return `${GUARD_DOC_TYPE}.${args.instanceDocId}.${args.guardName}`;
}

function lakeActionsFor(action) {
    return action === "publish" ? [ "create", "update" ] : action === "unpublish" ? [ "delete" ] : [ action ];
}

function lakeGuardActions(actions) {
    const requested = new Set(actions.flatMap(lakeActionsFor));
    return LAKE_MUTATION_GUARD_ACTIONS.filter(action => requested.has(action));
}

function publishedId(id) {
    return getPublishedId(id);
}

function draftId(id) {
    return getDraftId(id);
}

function versionPatternTranslation(pattern) {
    const documentSeparator = pattern.indexOf(".", 9);
    if (documentSeparator === -1) return {
        issue: `id pattern "${pattern}" does not identify a versioned document`
    };
    const documentPattern = pattern.slice(documentSeparator + 1);
    return /^\*+$/.test(documentPattern) ? {
        issue: `id pattern "${pattern}" cannot be translated from a release version to a draft or published document without broadening it to every document`
    } : {
        documentPattern: documentPattern
    };
}

function publishedPattern(pattern) {
    if (pattern.startsWith("drafts.")) return pattern.slice(7);
    if (!pattern.startsWith("versions.")) return pattern;
    const translated = versionPatternTranslation(pattern);
    if (translated.issue !== void 0) throw new Error(translated.issue);
    return translated.documentPattern;
}

function draftPattern(pattern) {
    return `drafts.${publishedPattern(pattern)}`;
}

function idsInSpace(args) {
    const {ids: ids, space: space, sourceInstanceId: sourceInstanceId} = args;
    return space === "authored" ? ids : space === "published" ? [ ...new Set(ids.map(publishedId)) ] : [ ...new Set(ids.map(id => publishedId(id) === sourceInstanceId ? publishedId(id) : draftId(id))) ];
}

function patternsInSpace(patterns, space) {
    if (space === "authored") return patterns;
    const convert = space === "edit" ? draftPattern : publishedPattern;
    return [ ...new Set(patterns.map(convert)) ];
}

function matchInSpace(args) {
    const {match: match, actions: actions, space: space, sourceInstanceId: sourceInstanceId} = args;
    return {
        ...match,
        ...match.idRefs !== void 0 ? {
            idRefs: idsInSpace({
                ids: match.idRefs,
                space: space,
                sourceInstanceId: sourceInstanceId
            })
        } : {},
        ...match.idPatterns !== void 0 ? {
            idPatterns: patternsInSpace(match.idPatterns, space)
        } : {},
        actions: lakeGuardActions(actions)
    };
}

function guardPartitions(id, actions) {
    return MUTATION_GUARD_ID_SPACES.flatMap(space => {
        const matchingActions = actions.filter(action => mutationGuardActionIdSpace(action) === space);
        return matchingActions.length > 0 ? [ {
            space: space,
            actions: matchingActions
        } ] : [];
    }).map((partition, index) => ({
        ...partition,
        id: index === 0 ? id : `${id}.${partition.space}`
    }));
}

function compiledGuardIds(args) {
    return guardPartitions(args.id, args.actions).map(partition => partition.id);
}

function lakeGuardDocument(args) {
    return {
        _id: args.id,
        _type: GUARD_DOC_TYPE,
        resourceType: args.resourceType,
        resourceId: args.resourceId,
        owner: args.owner,
        sourceInstanceId: args.sourceInstanceId,
        sourceDefinition: args.sourceDefinition,
        sourceStage: args.sourceStage,
        ...args.name !== void 0 ? {
            name: args.name
        } : {},
        ...args.description !== void 0 ? {
            description: args.description
        } : {},
        match: args.match,
        predicate: args.predicate,
        metadata: args.metadata
    };
}

function compileGuards(args) {
    const [first, ...remaining] = guardPartitions(args.id, args.match.actions);
    if (first === void 0) throw new Error("cannot compile a guard without an action");
    const compilePartition = ({space: space, actions: actions, id: id}) => lakeGuardDocument({
        ...args,
        id: id,
        match: matchInSpace({
            match: args.match,
            actions: actions,
            space: space,
            sourceInstanceId: args.sourceInstanceId
        })
    });
    return [ compilePartition(first), ...remaining.map(compilePartition) ];
}

function globMatch(pattern, value) {
    if (!pattern.includes("*")) return pattern === value;
    const escaped = pattern.replace(/[.+?^${}()|[\]\\]/g, "\\$&").replace(/\*/g, ".*");
    return new RegExp(`^${escaped}$`).test(value);
}

function guardIdPatternIssue(pattern, actions) {
    const representativeId = pattern.replaceAll("*", "x");
    if (!isBareDocumentId(representativeId)) return `id pattern "${pattern}" is not a valid resource-local document-id glob; use document-id characters (letters, digits, \`_\`, \`-\`, \`.\`) and \`*\` wildcards`;
    if (actions?.some(action => mutationGuardActionIdSpace(action) !== "authored") && pattern.startsWith("versions.")) return versionPatternTranslation(pattern).issue;
}

function parseGuardPredicate(predicate) {
    return parse(predicate, {
        mode: "delta"
    });
}

function guardPredicateSyntaxIssue(predicate) {
    try {
        parseGuardPredicate(predicate);
        return;
    } catch (error) {
        return errorMessage(error);
    }
}

function lakeDocumentIdForAction(id, action) {
    return mutationGuardActionIdSpace(action) === "published" ? publishedId(id) : id;
}

function legacyLifecycleActionFor(action, documentId) {
    if (publishedId(documentId) === documentId) {
        if (action === "create" || action === "update") return "publish";
        if (action === "delete") return "unpublish";
    }
}

function guardMatches({guard: guard, doc: doc, action: action}) {
    const m = guard.match, documentId = lakeDocumentIdForAction(doc.id, action), legacyLifecycleAction = legacyLifecycleActionFor(action, documentId);
    if (![ action, ...lakeActionsFor(action), legacyLifecycleAction ].some(candidate => candidate !== void 0 && m.actions.includes(candidate)) || m.types && m.types.length > 0 && !(doc.type !== void 0 && m.types.includes(doc.type))) return !1;
    if (m.idRefs && m.idRefs.length > 0 || m.idPatterns && m.idPatterns.length > 0) {
        const byRef = m.idRefs?.includes(documentId) ?? !1, byPattern = m.idPatterns?.some(p => globMatch(p, documentId)) ?? !1;
        if (!byRef && !byPattern) return !1;
    }
    return !0;
}

function guardPredicateRoot(args) {
    return {
        document: {
            before: args.context.before,
            after: args.context.after
        },
        guard: args.guard,
        mutation: {
            action: args.context.action
        }
    };
}

function lakeShapedSnapshotDocument(doc) {
    return mapJsonStrings(doc, (value, key) => key === "_id" || key === "_ref" ? toBareId(value) : value);
}

function snapshotGuardDereference(args) {
    return ({_ref: _ref}) => {
        const lookupId = isGdrUri(_ref) ? _ref : gdrFromResource(args.resource, _ref), found = findSnapshotDoc(args.snapshot, lookupId);
        return Promise.resolve(found === void 0 ? null : lakeShapedSnapshotDocument(found));
    };
}

async function evaluateMutationGuard(args) {
    const {guard: guard, context: context, dereference: dereference} = args;
    if (guard.predicate === "") return !1;
    const root = guardPredicateRoot({
        guard: guard,
        context: context
    });
    let dereferenceFailed = !1;
    const guardedDereference = dereference === void 0 ? void 0 : async ref => {
        try {
            return await dereference(ref);
        } catch (error) {
            throw dereferenceFailed = !0, error;
        }
    };
    try {
        const tree = parseGuardPredicate(guard.predicate);
        return await (await evaluate(tree, {
            root: root,
            before: context.before,
            after: context.after,
            ...guardedDereference !== void 0 ? {
                dataset: [],
                dereference: guardedDereference
            } : {},
            ...context.identity !== void 0 ? {
                identity: context.identity
            } : {}
        })).get() === !0;
    } catch (error) {
        if (dereferenceFailed) throw error;
        return !1;
    }
}

async function denyingGuards(args) {
    const {guards: guards, doc: doc, context: context, dereference: dereference} = args, denied = [];
    for (const guard of guards) guardMatches({
        guard: guard,
        doc: doc,
        action: context.action
    }) && (await evaluateMutationGuard({
        guard: guard,
        context: context,
        ...dereference !== void 0 ? {
            dereference: dereference
        } : {}
    }) || denied.push(guard));
    return denied;
}

async function documentActionDenials(args) {
    const {mutation: mutation, resource: resource, guards: guards, identity: identity, dereference: dereference} = args, document = mutation.after ?? mutation.before;
    if (document === null) throw new Error("a concrete Lake mutation requires a before or after image");
    const enforceable = guards.filter(g => g.resourceType === resource.type && g.resourceId === resource.id);
    return enforceable.length === 0 ? [] : denyingGuards({
        guards: enforceable,
        doc: {
            id: document._id,
            type: document._type
        },
        context: {
            ...mutation,
            ...identity !== void 0 ? {
                identity: identity
            } : {}
        },
        ...dereference !== void 0 ? {
            dereference: dereference
        } : {}
    });
}

async function instanceWriteDenials(args) {
    const {instance: instance, guards: guards, identity: identity, dereference: dereference} = args;
    return documentActionDenials({
        mutation: {
            before: instance,
            after: instance,
            action: "update"
        },
        resource: instance.workflowResource,
        guards: guards,
        dereference: dereference,
        ...identity !== void 0 ? {
            identity: identity
        } : {}
    });
}

async function assertInstanceWriteAllowed(args) {
    const denied = await instanceWriteDenials(args);
    if (denied.length !== 0) throw MutationGuardDeniedError.fromGuards({
        documentId: args.instance._id,
        action: "update",
        guards: denied
    });
}

const ASSIGNMENT_LIST_MODEL = 9;

function isAssignmentListModel(modelVersion) {
    return modelVersion >= ASSIGNMENT_LIST_MODEL;
}

function definitionAssignmentModel(instance) {
    if (typeof instance.definitionSnapshot != "string") return 0;
    const modelVersion = parseDefinitionSnapshotValue({
        _id: instance._id,
        definitionSnapshot: instance.definitionSnapshot
    }).modelVersion;
    return typeof modelVersion == "number" ? modelVersion : 0;
}

function writesSingularAssignmentLists(instance) {
    return isAssignmentListModel(definitionAssignmentModel(instance));
}

function legacyAssigneeValue(entry) {
    return legacyAssignmentValue(entry.value, entry.name);
}

function legacyAssignmentValue(value, path) {
    if (!Array.isArray(value)) return value;
    if (value.length === 0) return null;
    if (value.length === 1) return value[0];
    throw new ContractViolationError(`legacy assignee field "${path}" cannot persist ${value.length} members; redeploy the definition at the assignment-list reader model before writing a role pool`);
}

function mapShapeValues(args) {
    const {value: value, shapes: shapes, mapAssignee: mapAssignee, parentPath: parentPath} = args;
    if (typeof value != "object" || value === null || Array.isArray(value)) return value;
    const source = value, next = {
        ...source
    };
    for (const shape of shapes) {
        if (!Object.hasOwn(source, shape.name)) continue;
        const path = `${parentPath}.${shape.name}`, member = source[shape.name], mapped = mappedShapeValue({
            shape: shape,
            member: member,
            mapAssignee: mapAssignee,
            path: path
        });
        mapped.found && defineOwnValue(next, {
            name: shape.name,
            value: mapped.value
        });
    }
    return next;
}

function mappedShapeValue(args) {
    const {shape: shape, member: member, mapAssignee: mapAssignee, path: path} = args;
    return shape.type === "assignee" ? {
        found: !0,
        value: mapAssignee(member, path)
    } : shape.type === "object" ? {
        found: !0,
        value: mapShapeValues({
            value: member,
            shapes: shape.fields ?? [],
            mapAssignee: mapAssignee,
            parentPath: path
        })
    } : shape.type !== "array" || !Array.isArray(member) ? {
        found: !1
    } : {
        found: !0,
        value: member.map((row, index) => mapShapeValues({
            value: row,
            shapes: shape.of ?? [],
            mapAssignee: mapAssignee,
            parentPath: `${path}[${index}]`
        }))
    };
}

function defineOwnValue(target, property) {
    Object.defineProperty(target, property.name, {
        configurable: !0,
        enumerable: !0,
        value: property.value,
        writable: !0
    });
}

function mapNestedAssignmentValues(entry, mapAssignee) {
    return entry._type === "object" ? mapShapeValues({
        value: entry.value,
        shapes: entry.fields,
        mapAssignee: mapAssignee,
        parentPath: entry.name
    }) : entry.value.map((row, index) => mapShapeValues({
        value: row,
        shapes: entry.of,
        mapAssignee: mapAssignee,
        parentPath: `${entry.name}[${index}]`
    }));
}

function normalizeNestedAssignmentValues(entry) {
    return entry._type !== "object" && entry._type !== "array" ? entry : {
        ...entry,
        value: mapNestedAssignmentValues(entry, normalizeAssignmentMembers)
    };
}

function assignmentFieldsForWrite(fields, listModel) {
    return listModel ? fields : fields.map(entry => entry._type === "assignee" ? {
        ...entry,
        value: legacyAssigneeValue(entry)
    } : entry._type === "object" || entry._type === "array" ? {
        ...entry,
        value: mapNestedAssignmentValues(entry, legacyAssignmentValue)
    } : entry);
}

function activityForWrite(activity, listModel) {
    return {
        ...activity,
        ...activity.fields === void 0 ? {} : {
            fields: assignmentFieldsForWrite(activity.fields, listModel)
        }
    };
}

function assignmentStagesForWrite(stages, listModel) {
    return stages.map(stage => ({
        ...stage,
        fields: assignmentFieldsForWrite(stage.fields ?? [], listModel),
        activities: stage.activities.map(activity => activityForWrite(activity, listModel))
    }));
}

function getPath(value, path) {
    let current = value;
    for (const part of path.split(".")) {
        if (current == null || typeof current != "object") return;
        current = current[part];
    }
    return current;
}

function isReferenceObject(value) {
    return typeof value._ref == "string";
}

function walkDocPath(value, path) {
    let current = value;
    for (const part of path.split(".")) {
        if (current == null || typeof current != "object") return;
        if (isReferenceObject(current) && !Object.hasOwn(current, part)) throw new Error(`cannot read "${part}" through the nested reference "${current._ref}" — the workflow only loads references it declares. Declare it as a doc.ref field entry to read through it.`);
        current = current[part];
    }
    return current;
}

function resolveFieldRead(args) {
    const {kind: kind, value: value, path: path, snapshot: snapshot, targetKind: targetKind} = args;
    if (isSingleDocRefKind(kind)) {
        if (!isGdr(value)) return null;
        if (path === void 0 && targetKind !== void 0 && isSingleDocRefKind(targetKind)) return value;
        const base = derefBase(value, snapshot);
        return path === void 0 ? base : walkDocPath(base, path);
    }
    return path !== void 0 ? getPath(value, path) : value;
}

function derefBase(ref, snapshot) {
    return (snapshot !== void 0 ? findSnapshotDoc(snapshot, ref.id) : void 0) ?? {
        _id: ref.id,
        _type: ref.type
    };
}

function buildParams(args) {
    const {instance: instance, now: now, snapshot: snapshot, extra: extra} = args, currentActivities2 = findOpenStageEntry(instance)?.activities ?? [];
    return {
        self: selfGdr(instance),
        fields: renderedFields(instance.fields ?? [], snapshot),
        parent: parentRef(instance)?.id ?? null,
        ancestors: instance.ancestors.map(a => a.id),
        stage: instance.currentStage,
        now: now,
        context: contextMap(instance),
        effects: effectOutputsMap(instance),
        effectStatus: effectStatusMap(instance),
        activities: currentActivities2,
        subworkflows: subworkflowVarRows(instance, snapshot),
        ...activityGateParams(currentActivities2),
        ...extra
    };
}

function subworkflowVarRows(instance, snapshot) {
    const openKey = findOpenStageEntry(instance)?._key;
    return (instance.subworkflows ?? []).map(row => ({
        _id: toBareId(row.ref.id),
        rowKey: row.rowKey,
        activity: row.activity,
        action: row.action,
        definition: row.definition,
        current: row.stageEntry === openKey,
        ...childState(row, snapshot),
        spawnedAt: row.spawnedAt
    }));
}

function childState(row, snapshot) {
    if (row.resolved !== void 0) return {
        stage: row.resolved.stage ?? null,
        status: row.resolved.aborted === !0 ? "aborted" : "done"
    };
    const doc = snapshot === void 0 ? void 0 : findSnapshotDoc(snapshot, row.ref.id);
    return doc === void 0 ? {
        stage: null,
        status: "active"
    } : {
        stage: doc.currentStage ?? null,
        status: resolvedChildStatus(doc) ?? "active"
    };
}

function activityGateParams(activities) {
    return {
        allActivitiesDone: activities.every(activity => activity.status === "done" || activity.status === "skipped"),
        anyActivityFailed: activities.some(activity => activity.status === "failed")
    };
}

function renderedFields(entries, snapshot) {
    const out = {};
    for (const entry of entries) out[entry.name] = renderedValue(entry, snapshot);
    return out;
}

function renderedValue(entry, snapshot) {
    return resolveFieldRead({
        kind: entry._type,
        value: entry.value,
        path: void 0,
        snapshot: snapshot
    });
}

function scopedFieldOverlay({instance: instance, snapshot: snapshot, activityName: activityName}) {
    const stageEntry = findOpenStageEntry(instance);
    if (stageEntry === void 0) return {};
    const stageFields = renderedFields(stageEntry.fields ?? [], snapshot), activity = activityName ? stageEntry.activities.find(t => t.name === activityName) : void 0;
    return {
        ...stageFields,
        ...renderedFields(activity?.fields ?? [], snapshot)
    };
}

function assignedFor({instance: instance, activityName: activityName, actor: actor, roleAliases: roleAliases}) {
    if (actor === void 0) return !1;
    const activity = findCurrentActivityEntry(instance, activityName);
    return actorMatchesAssignment({
        actor: actor,
        members: assignmentMembers(activity?.fields ?? []),
        roleAliases: roleAliases
    });
}

function effectStatusMap(instance) {
    const entry = findOpenStageEntry(instance);
    if (entry === void 0) return {};
    const out = {};
    for (const run of instance.effectHistory) run.stageEntryKey === entry._key && (out[run.name] = run.status);
    return out;
}

function contextMap(instance) {
    const out = {};
    for (const entry of instance.context) out[entry.name] = entry._type === "context.json" ? parseJsonContextEntry(entry) : entry.value;
    return out;
}

function effectOutputsMap(instance) {
    const out = {};
    for (const run of instance.effectHistory) run.outputs !== void 0 && (out[run.name] = run.outputs);
    return out;
}

function parseJsonContextEntry(entry) {
    try {
        return JSON.parse(entry.value);
    } catch (err) {
        rethrowWithContext(err, `context entry "${entry.name}" holds unparseable JSON`);
    }
}

function paramsForLake(params) {
    return {
        ...params,
        self: typeof params.self == "string" ? toBareId(params.self) : params.self,
        parent: typeof params.parent == "string" ? toBareId(params.parent) : params.parent,
        ancestors: Array.isArray(params.ancestors) ? params.ancestors.map(a => typeof a == "string" ? toBareId(a) : a) : params.ancestors,
        fields: stripFieldsForLake(params.fields)
    };
}

function stripFieldsForLake(value) {
    return mapJsonStrings(value, s => toBareId(s));
}

function resolveStaticValueExpr(src, ctx) {
    switch (src.type) {
      case "literal":
        return src.value;

      case "param":
        return ctx.params?.[src.param];

      case "actor":
        return ctx.actor;

      case "now":
        return ctx.now;
    }
}

function randomKey(length = 12) {
    const bytes = new Uint8Array(length);
    return globalThis.crypto.getRandomValues(bytes), [ ...bytes ].map(b => b.toString(16).padStart(2, "0")).join("").slice(0, length);
}

function instanceDocId(tag) {
    return `${tag}.wf-instance.${randomKey()}`;
}

function mapFieldRefValues(args) {
    const {entryType: entryType, value: value, fields: fields, of: of, mapRef: mapRef} = args;
    return value == null ? value : isSingleDocRefKind(entryType) || entryType === "release.ref" ? mapRef(value) : entryType === "doc.refs" ? mapItems(value, mapRef) : entryType === "object" ? mapRowRefValues({
        row: value,
        shapes: fields ?? [],
        mapRef: mapRef
    }) : entryType === "array" ? mapItems(value, row => mapRowRefValues({
        row: row,
        shapes: of ?? [],
        mapRef: mapRef
    })) : value;
}

function mapItems(value, mapItem) {
    return Array.isArray(value) ? value.map(mapItem) : value;
}

function mapRowRefValues(args) {
    const {row: row, shapes: shapes, mapRef: mapRef} = args;
    if (typeof row != "object" || row === null || Array.isArray(row)) return row;
    const record = row, out = {
        ...record
    };
    for (const shape of shapes) shape.name in record && (out[shape.name] = mapFieldRefValues({
        entryType: shape.type,
        value: record[shape.name],
        fields: shape.fields,
        of: shape.of,
        mapRef: mapRef
    }));
    return out;
}

function buildResourceSurface(args) {
    const {workflowResource: workflowResource, resourceClients: resourceClients} = args;
    return {
        allows: parsed => sameResource(resourceFromParsed(parsed), workflowResource) ? !0 : resourceClients?.(parsed) !== void 0,
        description: describeSurface(args)
    };
}

function describeSurface(args) {
    const parts = [ `the workflow resource (${describeResource(args.workflowResource)})` ];
    return args.resourceClients !== void 0 && parts.push("any resource the resourceClients resolver serves"), 
    parts.join("; ");
}

function describeResource(resource) {
    return `${resource.type} "${resource.id}"`;
}

class RefResourceUndeclaredError extends WorkflowError {
    entryType;
    entryName;
    issues;
    constructor(args) {
        super("ref-resource-undeclared", `Field entry "${args.entryName}" (${args.entryType}) rejected: ${args.issues.join("; ")}`), 
        this.name = "RefResourceUndeclaredError", this.entryType = args.entryType, this.entryName = args.entryName, 
        this.issues = args.issues;
    }
}

function refSurfaceIssues(args) {
    const {surface: surface, ...walk} = args, issues = [];
    return mapFieldRefValues({
        ...walk,
        mapRef: ref => {
            if (!isGdr(ref)) return ref;
            const parsed = tryParseGdr(ref.id);
            return parsed !== void 0 && !surface.allows(parsed) && issues.push(`ref "${ref.id}" targets ${describeResource(resourceFromParsed(parsed))}, which this deployment does not declare — declared surface: ${surface.description}. Add a resourceClients entry for the target resource, or fix the ref.`), 
            ref;
        }
    }), issues.length > 0 ? issues : void 0;
}

function assertRefsWithinSurface(args) {
    const issues = refSurfaceIssues(args);
    if (issues !== void 0) throw new RefResourceUndeclaredError({
        entryName: args.entryName,
        entryType: args.entryType,
        issues: issues
    });
}

function fieldQueryDiscardedEntry(args) {
    return {
        _key: randomKey(),
        _type: "fieldQueryDiscarded",
        at: args.at,
        scope: args.scope,
        field: args.field,
        detail: args.detail
    };
}

function recordFieldDiscards({target: target, scope: scope, at: at}) {
    return discard => target.push(fieldQueryDiscardedEntry({
        scope: scope,
        field: discard.field,
        detail: discard.detail,
        at: at
    }));
}

function applyActivityStatusChange(args) {
    const {entry: entry, history: history, stage: stage, to: to, at: at, actor: actor} = args, from = entry.status;
    entry.status = to, isTerminalActivityStatus(to) ? (entry.completedAt = at, actor !== void 0 && (entry.completedBy = actor.id)) : (delete entry.completedAt, 
    delete entry.completedBy), history.push({
        _key: randomKey(),
        _type: "activityStatusChanged",
        at: at,
        stage: stage,
        activity: entry.name,
        from: from,
        to: to,
        ...actor !== void 0 ? {
            actor: actor
        } : {}
    });
}

function stageTransitionHistory(args) {
    const {fromStage: fromStage, toStage: toStage, at: at, transition: transition, actor: actor, via: via, reason: reason} = args, shared = {
        at: at,
        ...transition !== void 0 ? {
            transition: transition
        } : {},
        ...actor !== void 0 ? {
            actor: actor
        } : {},
        ...via !== void 0 ? {
            via: via
        } : {},
        ...reason !== void 0 ? {
            reason: reason
        } : {}
    };
    return [ {
        _key: randomKey(),
        _type: "stageExited",
        stage: fromStage,
        toStage: toStage,
        ...shared
    }, {
        _key: randomKey(),
        _type: "transitionFired",
        fromStage: fromStage,
        toStage: toStage,
        ...shared
    }, {
        _key: randomKey(),
        _type: "stageEntered",
        stage: toStage,
        fromStage: fromStage,
        ...shared
    } ];
}

class ActionParamsInvalidError extends WorkflowError {
    action;
    activity;
    issues;
    constructor(args) {
        const lines = args.issues.map(i => `  - ${i.param}: ${i.reason}`).join(`\n`);
        super("action-params-invalid", `Action "${args.action}" on activity "${args.activity}" rejected: invalid params\n${lines}`), 
        this.name = "ActionParamsInvalidError", this.action = args.action, this.activity = args.activity, 
        this.issues = args.issues;
    }
}

function effectIssuesMessage(args) {
    const lines = args.issues.map(i => `  - ${i}`).join(`\n`);
    return `Effect "${args.effect}" returned invalid ${args.label}:\n${lines}`;
}

class EffectOpsInvalidError extends WorkflowError {
    effect;
    issues;
    constructor(args) {
        super("effect-ops-invalid", effectIssuesMessage({
            ...args,
            label: "completion ops"
        })), this.name = "EffectOpsInvalidError", this.effect = args.effect, this.issues = args.issues;
    }
}

class EffectOutputsInvalidError extends WorkflowError {
    effect;
    issues;
    constructor(args) {
        super("effect-outputs-invalid", effectIssuesMessage({
            ...args,
            label: "outputs"
        })), this.name = "EffectOutputsInvalidError", this.effect = args.effect, this.issues = args.issues;
    }
}

class RequiredFieldNotProvidedError extends WorkflowError {
    definition;
    missing;
    constructor(args) {
        const lines = args.missing.map(m => `  - ${m.name} (${m.type})`).join(`\n`), where = args.definition !== void 0 ? ` for workflow "${args.definition}"` : "";
        super("required-field-not-provided", `Required input fields${where} were not provided:\n${lines}\nProvide each via \`initialFields\` when starting standalone, or via the parent's \`spawn.with\` when spawned.`), 
        this.name = "RequiredFieldNotProvidedError", args.definition !== void 0 && (this.definition = args.definition), 
        this.missing = args.missing;
    }
}

class InitialFieldsInvalidError extends WorkflowError {
    definition;
    issues;
    constructor(args) {
        const where = args.definition === void 0 ? "" : ` for workflow "${args.definition}"`, lines = args.issues.map(formatInitialFieldIssue).join(`\n`);
        super("initial-fields-invalid", `Initial fields${where} contain rows the engine cannot consume:\n${lines}\nRemove or correct every listed row before retrying.`), 
        this.name = "InitialFieldsInvalidError", args.definition !== void 0 && (this.definition = args.definition), 
        this.issues = args.issues;
    }
}

function formatInitialFieldIssue(issue) {
    const row = `  - row ${issue.index}: ${issue.name} (${issue.type})`;
    if (issue.reason === "duplicate") return `${row} duplicates rows ${issue.duplicateIndexes.join(", ")}`;
    if (issue.reason === "undeclared") return `${row} is not declared by the workflow`;
    if (issue.reason === "wrong-kind") return `${row} has the wrong kind; expected ${issue.expectedTypes.join(" or ")}`;
    if (issue.reason === "not-input") return `${row} is ${issue.source}-sourced, not caller-input-sourced`;
    const scope = issue.scope === "activity" ? `activity "${issue.activity}" in stage "${issue.stage}"` : `stage "${issue.stage}"`;
    return `${row} targets ${scope}, not workflow scope (declared as ${issue.declaredType})`;
}

class WorkflowStateDivergedError extends WorkflowError {
    instanceId;
    guardError;
    rollbackError;
    constructor(args) {
        super("workflow-state-diverged", `Workflow "${args.instanceId}" state committed but its guards could not be reconciled: ${args.reason}.`, {
            cause: args.guardError
        }), this.name = "WorkflowStateDivergedError", this.instanceId = args.instanceId, 
        this.guardError = args.guardError, args.rollbackError !== void 0 && (this.rollbackError = args.rollbackError);
    }
}

class PartialGuardDeployError extends WorkflowError {
    stageName;
    deployed;
    rollbackError;
    constructor(args) {
        super("partial-guard-deploy", `Partial guard deploy on stage "${args.stageName}": ${args.deployed} guard(s) deployed before a later one failed.`, {
            cause: args.cause
        }), this.name = "PartialGuardDeployError", this.stageName = args.stageName, this.deployed = args.deployed, 
        args.rollbackError !== void 0 && (this.rollbackError = args.rollbackError);
    }
}

class StartNotPrimedError extends WorkflowError {
    instanceId;
    constructor(args) {
        super("start-not-primed", `Workflow instance "${args.instanceId}" was created but its start failed before priming: ${errorMessage(args.cause)}. Retrying the start with the same instanceId resumes it; aborting the instance discards the unfinished start.`, {
            cause: args.cause
        }), this.name = "StartNotPrimedError", this.instanceId = args.instanceId;
    }
}

class StartNotSettledError extends WorkflowError {
    instanceId;
    instance;
    constructor(args) {
        super("start-not-settled", `Workflow instance "${args.instanceId}" started (created and primed) but its first cascade failed: ${errorMessage(args.cause)}. A tick — or retrying the start with the same instanceId — resumes it.`, {
            cause: args.cause
        }), this.name = "StartNotSettledError", this.instanceId = args.instanceId, args.instance !== void 0 && (this.instance = args.instance);
    }
}

class StaleEffectClaimError extends WorkflowError {
    instanceId;
    effectKey;
    reason;
    constructor(args) {
        super("stale-effect-claim", `Mid-dispatch report for effect entry "${args.effectKey}" on ${args.instanceId} rejected: ${staleClaimDetail(args.reason)}. Nothing was written. Stop reporting from this dispatch — a newer dispatch (or none) owns the entry now.`), 
        this.name = "StaleEffectClaimError", this.instanceId = args.instanceId, this.effectKey = args.effectKey, 
        this.reason = args.reason;
    }
}

function staleClaimDetail(reason) {
    return reason === "unclaimed" ? "the entry holds no claim" : reason === "claim-superseded" ? "the claim was taken over by another dispatch" : "the claim's lease has expired";
}

const CONCURRENT_COMMIT_MAX_ATTEMPTS = 3;

function lostRaceMessage(args) {
    return `${args.what} (instance ${args.instanceId}) lost the optimistic-locking race ${args.attempts} attempts running — a concurrent writer kept committing first. ${args.recovery ?? "Re-read and retry, or investigate a write storm on this instance."}`;
}

function isRetryableInstanceCommitConflict(error) {
    return !isCreateIdCollision(error) && isRevisionConflict(error);
}

class InstanceCommitRevisionConflict extends Error {
    constructor(cause) {
        super("Instance commit lost its revision fence", {
            cause: cause
        }), this.name = "InstanceCommitRevisionConflict";
    }
}

function isInstanceCommitRevisionConflict(error) {
    return error instanceof InstanceCommitRevisionConflict;
}

class ConcurrentFireActionError extends WorkflowError {
    instanceId;
    activity;
    action;
    attempts;
    constructor(args) {
        super("concurrent-fire-action", lostRaceMessage({
            what: `Action "${args.action}" on activity "${args.activity}"`,
            instanceId: args.instanceId,
            attempts: args.attempts
        })), this.name = "ConcurrentFireActionError", this.instanceId = args.instanceId, 
        this.activity = args.activity, this.action = args.action, this.attempts = args.attempts;
    }
}

class ConcurrentCascadeError extends WorkflowError {
    instanceId;
    attempts;
    constructor(args) {
        super("concurrent-cascade", lostRaceMessage({
            what: "Automatic cascade",
            instanceId: args.instanceId,
            attempts: args.attempts,
            recovery: "An earlier verb commit may already have landed. Re-read the instance and invoke tick after contention subsides."
        })), this.name = "ConcurrentCascadeError", this.instanceId = args.instanceId, this.attempts = args.attempts;
    }
}

function isRevisionConflict(error) {
    if (typeof error != "object" || error === null) return !1;
    const {statusCode: statusCode, message: message} = error;
    return statusCode === 409 ? !0 : typeof message == "string" && message.includes("ifRevisionId check failed");
}

function isCreateIdCollision(error) {
    if (typeof error != "object" || error === null) return !1;
    const {message: message, responseBody: responseBody} = error;
    return typeof message == "string" && message.includes("already exists") && (message.includes("Document by ID") || message.includes("create() failed")) ? !0 : typeof responseBody == "string" && responseBody.includes("documentAlreadyExistsError");
}

class ConcurrentEditFieldError extends WorkflowError {
    instanceId;
    target;
    attempts;
    constructor(args) {
        const where = args.target.activity !== void 0 ? `${args.target.activity}.${args.target.field}` : args.target.field;
        super("concurrent-edit-field", lostRaceMessage({
            what: `Edit of "${args.target.scope}:${where}"`,
            instanceId: args.instanceId,
            attempts: args.attempts
        })), this.name = "ConcurrentEditFieldError", this.instanceId = args.instanceId, 
        this.target = args.target, this.attempts = args.attempts;
    }
}

class ConcurrentCompleteEffectError extends WorkflowError {
    instanceId;
    effectKey;
    attempts;
    constructor(args) {
        super("concurrent-complete-effect", lostRaceMessage({
            what: `Completion of effect entry "${args.effectKey}"`,
            instanceId: args.instanceId,
            attempts: args.attempts
        })), this.name = "ConcurrentCompleteEffectError", this.instanceId = args.instanceId, 
        this.effectKey = args.effectKey, this.attempts = args.attempts;
    }
}

class ConcurrentCommitEffectOpsError extends WorkflowError {
    instanceId;
    effectKey;
    attempts;
    constructor(args) {
        super("concurrent-commit-effect-ops", lostRaceMessage({
            what: `Mid-dispatch report for effect entry "${args.effectKey}"`,
            instanceId: args.instanceId,
            attempts: args.attempts
        })), this.name = "ConcurrentCommitEffectOpsError", this.instanceId = args.instanceId, 
        this.effectKey = args.effectKey, this.attempts = args.attempts;
    }
}

class CascadeLimitError extends WorkflowError {
    instanceId;
    limit;
    constructor(args) {
        super("cascade-limit", `Cascade did not stabilise after ${args.limit} hops on ${args.instanceId} — likely a runaway loop (transition \`when\`s that stay mutually satisfied). Check that each transition's \`when\` eventually goes false.`), 
        this.name = "CascadeLimitError", this.instanceId = args.instanceId, this.limit = args.limit;
    }
}

const FIELD_OP_TYPES = [ "field.set", "field.setIfMissing", "field.unset", "field.append", "field.inc", "field.dec", "field.updateWhere", "field.removeWhere" ];

function isFieldOp(summary) {
    return FIELD_OP_TYPES.includes(summary.opType);
}

function validateActionParams({action: action, activityName: activityName, callerParams: callerParams}) {
    const declared = action.params ?? [], params = callerParams ?? {}, issues = [];
    for (const decl of declared) issues.push(...declaredParamIssues(decl, params));
    if (issues.length > 0) throw new ActionParamsInvalidError({
        action: action.name,
        activity: activityName,
        issues: issues
    });
    return params;
}

function declaredParamIssues(decl, params) {
    const value = params[decl.name];
    if (!(decl.name in params && value !== void 0 && value !== null)) return decl.required === !0 ? [ {
        param: decl.name,
        reason: "required but missing"
    } ] : [];
    if (!checkParamType(value, decl)) return [ {
        param: decl.name,
        reason: `expected type=${decl.type}, got ${typeof value}`
    } ];
    const choiceIssues = choiceValueIssues(decl.options, value), validationIssues = scalarValidationIssues({
        entryType: decl.type,
        validation: decl.validation,
        value: value
    });
    return [ ...choiceIssues === void 0 ? [] : [ {
        param: decl.name,
        reason: choiceIssues.join("; ")
    } ], ...validationIssues === void 0 ? [] : [ {
        param: decl.name,
        reason: validationIssues.join("; ")
    } ] ];
}

function hasStringField(value, field) {
    return typeof value == "object" && value !== null && field in value && typeof value[field] == "string";
}

function checkParamType(value, decl) {
    switch (decl.type) {
      case "string":
      case "url":
      case "dateTime":
        return typeof value == "string";

      case "number":
        return typeof value == "number" && Number.isFinite(value);

      case "boolean":
        return typeof value == "boolean";

      case "actor":
        return hasStringField(value, "kind");

      case "doc.ref":
        return hasStringField(value, "id");

      case "doc.refs":
        return Array.isArray(value) && value.every(ref => checkParamType(ref, {
            type: "doc.ref"
        }));

      case "json":
        return !0;

      default:
        return !1;
    }
}

async function runOps(args) {
    const {ops: ops, mutation: mutation, stage: stage, origin: origin, params: params, actor: actor, self: self, now: now, snapshot: snapshot, refSurface: refSurface} = args;
    if (ops === void 0 || ops.length === 0) return [];
    const opsFromDefinition = origin.effect === void 0 && origin.edit !== !0, summaries = [];
    for (const op of ops) {
        const summary = await applyOp(op, {
            mutation: mutation,
            stage: stage,
            activityName: origin.activity,
            params: params,
            actor: actor,
            self: self,
            now: now,
            snapshot: snapshot,
            refSurface: refSurface,
            opsFromDefinition: opsFromDefinition,
            memberRoles: args.memberRoles,
            roleAliases: args.roleAliases
        });
        summaries.push(summary), shouldRecordOpApplied(summary) && mutation.history.push(opAppliedEntry({
            origin: origin,
            summary: summary,
            stage: stage,
            actor: actor,
            now: now
        }));
    }
    return summaries;
}

function shouldRecordOpApplied(summary) {
    return summary.opType !== "field.setIfMissing" || summary.resolved !== void 0;
}

function opAppliedEntry(args) {
    const {origin: origin, summary: summary, stage: stage, actor: actor, now: now} = args;
    return {
        _key: randomKey(),
        _type: "opApplied",
        at: now,
        stage: stage,
        ...origin.activity !== void 0 ? {
            activity: origin.activity
        } : {},
        ...origin.action !== void 0 ? {
            action: origin.action
        } : {},
        ...origin.edit === !0 ? {
            edit: !0
        } : {},
        ...origin.effect !== void 0 ? {
            effect: origin.effect
        } : {},
        opType: summary.opType,
        ...summary.target !== void 0 ? {
            target: summary.target
        } : {},
        ...summary.resolved !== void 0 ? {
            resolved: summary.resolved
        } : {},
        ...actor !== void 0 ? {
            actor: actor
        } : {}
    };
}

function validateEffectOps(ops, effectName) {
    const result = v.safeParse(v.array(StoredFieldOpSchema), ops);
    if (!result.success) throw new EffectOpsInvalidError({
        effect: effectName,
        issues: formatIssues(result.issues, issue => {
            const path = issue.path;
            return issue.input === void 0 && path?.at(-2)?.key === "target" && path.at(-1)?.key === "scope" ? "Effect completion field ops require target.scope: 'workflow' | 'stage'." : issue.message;
        })
    });
    const activityScoped = result.output.find(op => op.target.scope === "activity");
    if (activityScoped !== void 0) throw new EffectOpsInvalidError({
        effect: effectName,
        issues: [ `${activityScoped.type} targets an activity-scope field — an effect's completion ops write workflow- or stage-scope fields` ]
    });
    const whereIssues = result.output.flatMap(op => op.type === "field.updateWhere" || op.type === "field.removeWhere" ? conditionSyntaxIssues(op.where, WHERE_SCOPE_PARAM_NAMES).map(issue => `${op.type} target "${op.target.field}" where: ${issue}`) : []);
    if (whereIssues.length > 0) throw new EffectOpsInvalidError({
        effect: effectName,
        issues: whereIssues
    });
    return result.output;
}

function isDeployVettedValueExpr(src) {
    return src.type === "object" ? Object.values(src.fields).every(isDeployVettedValueExpr) : src.type !== "param";
}

function assertRuntimeRefsWithinSurface(args) {
    const {ctx: ctx, src: src, ...walk} = args;
    ctx.opsFromDefinition && isDeployVettedValueExpr(src) || assertRefsWithinSurface({
        ...walk,
        surface: ctx.refSurface
    });
}

async function applyOp(op, ctx) {
    switch (op.type) {
      case "field.set":
        return applyFieldSet(op, ctx);

      case "field.setIfMissing":
        return applyFieldSetIfMissing(op, ctx);

      case "field.unset":
        return applyFieldUnset(op, ctx);

      case "field.append":
        return applyFieldAppend(op, ctx);

      case "field.inc":
      case "field.dec":
        return applyFieldArithmetic(op, ctx);

      case "field.updateWhere":
        return applyFieldUpdateWhere(op, ctx);

      case "field.removeWhere":
        return applyFieldRemoveWhere(op, ctx);

      case "status.set":
        return applyStatusSet(op, ctx);
    }
}

function applyFieldSet(op, ctx) {
    const entry = locateEntry(ctx, op.target), value = resolveOpValue({
        src: op.value,
        ctx: ctx,
        target: entrySlot(entry)
    }), validated = validateFieldValue({
        entryType: entry._type,
        entryName: entry.name,
        value: value,
        ...entryShape(entry),
        memberRoles: ctx.memberRoles,
        roleAliases: ctx.roleAliases,
        ...entry._type === "assignee" || entry._type === "assignees" || entry._type === "object" || entry._type === "array" ? {
            previousValue: entry.value
        } : {}
    });
    return assertLegacySingularValue({
        entry: entry,
        value: validated,
        ctx: ctx
    }), assertRuntimeRefsWithinSurface({
        entryType: entry._type,
        entryName: entry.name,
        value: validated,
        ...entryShape(entry),
        ctx: ctx,
        src: op.value
    }), setEntryValue(entry, validated), {
        opType: op.type,
        target: op.target,
        resolved: {
            value: validated
        }
    };
}

function applyFieldSetIfMissing(op, ctx) {
    const entry = locateEntry(ctx, op.target), legacySingular = isLegacySingularAssignment(entry, ctx);
    return isAlwaysArrayFieldKind(entry._type) && !legacySingular && rejectFieldOpTarget({
        entry: entry,
        op: op,
        issue: "is always array-valued — setIfMissing applies to nullable entries only; an empty array entry already holds []"
    }), entry.value !== null && entry.value !== void 0 && !(legacySingular && Array.isArray(entry.value) && entry.value.length === 0) ? {
        opType: op.type,
        target: op.target
    } : {
        ...applyFieldSet({
            ...op,
            type: "field.set"
        }, ctx),
        opType: op.type
    };
}

function applyFieldArithmetic(op, ctx) {
    const entry = locateEntry(ctx, op.target);
    entry._type !== "number" && rejectFieldOpTarget({
        entry: entry,
        op: op,
        issue: `is a ${entry._type} entry — arithmetic ops target \`number\` entries only`
    }), typeof entry.value != "number" && rejectFieldOpTarget({
        entry: entry,
        op: op,
        issue: "has no numeric value — initialize it before applying arithmetic"
    });
    const resolvedDelta = op.value === void 0 ? 1 : resolveOpValue({
        src: op.value,
        ctx: ctx,
        target: {
            kind: "number"
        }
    });
    (typeof resolvedDelta != "number" || !Number.isFinite(resolvedDelta)) && rejectFieldOpTarget({
        entry: entry,
        op: op,
        issue: "value must resolve to a finite number"
    });
    const nextValue = entry.value + (op.type === "field.inc" ? resolvedDelta : -resolvedDelta);
    Number.isFinite(nextValue) || rejectFieldOpTarget({
        entry: entry,
        op: op,
        issue: "result must be a finite number"
    });
    const value = validateFieldValue({
        entryType: entry._type,
        entryName: entry.name,
        value: nextValue,
        ...entryShape(entry)
    });
    return setEntryValue(entry, value), {
        opType: op.type,
        target: op.target,
        resolved: {
            value: value
        }
    };
}

function rejectFieldOpTarget(args) {
    const {entry: entry, op: op, issue: issue} = args;
    throw new FieldValueShapeError({
        entryType: entry._type,
        entryName: entry.name,
        mode: "value",
        issues: [ `${op.type} target ${op.target.scope}:"${op.target.field}" ${issue}` ]
    });
}

function entryShape(entry) {
    return entry._type === "object" ? {
        fields: entry.fields
    } : entry._type === "array" ? {
        of: entry.of
    } : isSingleDocRefEntry(entry) || entry._type === "doc.refs" ? entry.types !== void 0 ? {
        types: entry.types
    } : {} : isAssignmentFieldEntry(entry) ? assignmentEntryShape(entry) : scalarEntryShape(entry);
}

function assignmentEntryShape(entry) {
    return entry.roles !== void 0 ? {
        roles: entry.roles
    } : {};
}

function scalarEntryShape(entry) {
    return {
        ...entry.options !== void 0 ? {
            options: entry.options
        } : {},
        ...entry.validation !== void 0 ? {
            validation: entry.validation
        } : {}
    };
}

function entrySlot(entry) {
    return entry._type === "object" ? {
        kind: entry._type,
        fields: entry.fields
    } : {
        kind: entry._type
    };
}

function appendItemSlot(entry) {
    if (entry._type === "doc.refs") return {
        kind: "doc.ref"
    };
    if (entry._type === "array") return {
        fields: entry.of
    };
}

function applyFieldUnset(op, ctx) {
    const entry = locateEntry(ctx, op.target);
    return setEntryValue(entry, isAlwaysArrayFieldKind(entry._type) ? [] : null), {
        opType: op.type,
        target: op.target
    };
}

function applyFieldAppend(op, ctx) {
    const entry = locateEntry(ctx, op.target);
    if (isLegacySingularAssignment(entry, ctx)) throw new FieldValueShapeError({
        entryType: entry._type,
        entryName: entry.name,
        mode: "item",
        issues: [ "legacy singular assignee entries do not support append; redeploy at model 9" ]
    });
    const slot = appendItemSlot(entry), item = resolveOpValue({
        src: op.value,
        ctx: ctx,
        ...slot !== void 0 ? {
            target: slot
        } : {}
    }), validated = validateFieldAppendItem({
        entryType: entry._type,
        entryName: entry.name,
        item: item,
        types: entry._type === "doc.refs" ? entry.types : void 0,
        of: entry._type === "array" ? entry.of : void 0,
        roles: isAssignmentFieldEntry(entry) ? entry.roles : void 0,
        memberRoles: ctx.memberRoles,
        roleAliases: ctx.roleAliases
    });
    assertRuntimeRefsWithinSurface({
        entryType: entry._type === "doc.refs" ? "doc.ref" : "object",
        entryName: entry.name,
        value: validated,
        fields: entry._type === "array" ? entry.of : void 0,
        ctx: ctx,
        src: op.value
    });
    const next = [ ...Array.isArray(entry.value) ? entry.value : [], withRowKey(validated) ], value = isAssignmentFieldEntry(entry) ? validateFieldValue({
        entryType: entry._type,
        entryName: entry.name,
        value: next,
        ...assignmentEntryShape(entry),
        previousValue: entry.value,
        memberRoles: ctx.memberRoles,
        roleAliases: ctx.roleAliases
    }) : next;
    return setEntryValue(entry, value), {
        opType: op.type,
        target: op.target,
        resolved: {
            item: validated
        }
    };
}

function withRowKey(item) {
    return isRecord(item) && !("_key" in item) ? {
        _key: randomKey(),
        ...item
    } : item;
}

async function applyFieldUpdateWhere(op, ctx) {
    const entry = locateEntry(ctx, op.target);
    requireMergeableRows(entry, op);
    const rows = requireArrayValue(entry, op), merge = resolveOpValue({
        src: op.value,
        ctx: ctx,
        target: {
            fields: entry.of
        }
    });
    if (merge === null || typeof merge != "object" || Array.isArray(merge)) throw new FieldValueShapeError({
        entryType: entry._type,
        entryName: entry.name,
        mode: "value",
        issues: [ `field.updateWhere value must resolve to an object of fields to merge (target "${op.target.field}")` ]
    });
    const mergeRecord = merge;
    requireNoReservedMergeKeys({
        merge: mergeRecord,
        entry: entry,
        op: op
    }), validateFieldValue({
        entryType: "array",
        entryName: entry.name,
        value: [ mergeRecord ],
        of: entry.of,
        memberRoles: ctx.memberRoles,
        roleAliases: ctx.roleAliases
    });
    const matches = await rowMatches({
        where: op.where,
        rows: rows,
        ctx: ctx
    }), next = rows.map((row, index) => matches[index] ? {
        ...row,
        ...mergeRecord
    } : row), validated = validateFieldValue({
        entryType: entry._type,
        entryName: entry.name,
        value: next,
        of: entry.of
    });
    assertRuntimeRefsWithinSurface({
        entryType: "object",
        entryName: entry.name,
        value: merge,
        fields: entry.of,
        ctx: ctx,
        src: op.value
    }), setEntryValue(entry, validated);
    const firstMatch = matches.indexOf(!0), persistedRows = validated, persistedMerge = firstMatch === -1 ? merge : Object.fromEntries(Object.keys(mergeRecord).map(key => [ key, persistedRows[firstMatch]?.[key] ]));
    return {
        opType: op.type,
        target: op.target,
        resolved: {
            merge: persistedMerge
        }
    };
}

async function applyFieldRemoveWhere(op, ctx) {
    const entry = locateEntry(ctx, op.target);
    if (isLegacySingularAssignment(entry, ctx)) throw new FieldValueShapeError({
        entryType: entry._type,
        entryName: entry.name,
        mode: "value",
        issues: [ "legacy singular assignee entries do not support removeWhere; redeploy at model 9" ]
    });
    const rows = requireArrayValue(entry, op), matches = await rowMatches({
        where: op.where,
        rows: rows,
        ctx: ctx
    });
    return setEntryValue(entry, rows.filter((_, index) => !matches[index])), {
        opType: op.type,
        target: op.target
    };
}

function assertLegacySingularValue(args) {
    const {entry: entry, value: value, ctx: ctx} = args;
    if (!(!isLegacySingularAssignment(entry, ctx) || !Array.isArray(value) || value.length <= 1)) throw new FieldValueShapeError({
        entryType: entry._type,
        entryName: entry.name,
        mode: "value",
        issues: [ "legacy singular assignee entries accept at most one total member; redeploy at model 9" ]
    });
}

function isLegacySingularAssignment(entry, ctx) {
    return entry._type === "assignee" && !isAssignmentListModel(ctx.mutation.definitionModelVersion);
}

function requireMergeableRows(entry, op) {
    if (entry._type !== "array") throw new Error(`${op.type} target ${op.target.scope}:"${op.target.field}" is a ${entry._type} entry — updateWhere merges declared row sub-fields, so it targets \`array\` entries only`);
}

function requireNoReservedMergeKeys(args) {
    const {merge: merge, entry: entry, op: op} = args, reserved = [ "_key", "_type" ].filter(key => key in merge);
    if (reserved.length !== 0) throw new FieldValueShapeError({
        entryType: entry._type,
        entryName: entry.name,
        mode: "value",
        issues: [ `field.updateWhere merge writes the reserved row key${reserved.length === 1 ? "" : "s"} ${reserved.map(k => `"${k}"`).join(", ")} (target "${op.target.field}") — row identity and bookkeeping are engine-stamped, never merged` ]
    });
}

function requireArrayValue(entry, op) {
    if (!Array.isArray(entry.value)) throw new Error(`${op.type} target ${op.target.scope}:"${op.target.field}" (${entry._type}) holds a non-array value — where-ops operate on array entries`);
    return entry.value;
}

function applyStatusSet(op, ctx) {
    const entry = findCurrentActivityEntry(ctx.mutation, op.activity);
    if (entry === void 0) throw new Error(`status.set targets activity "${op.activity}" which has no entry in the open stage`);
    return applyActivityStatusChange({
        entry: entry,
        history: ctx.mutation.history,
        stage: ctx.stage,
        to: op.status,
        at: ctx.now,
        ...ctx.actor !== void 0 ? {
            actor: ctx.actor
        } : {}
    }), {
        opType: op.type,
        resolved: {
            activity: op.activity,
            status: op.status
        }
    };
}

function setEntryValue(entry, value) {
    entry.value = value;
}

function locateEntry(ctx, target) {
    const entry = fieldHost(ctx, target.scope)?.find(s => s.name === target.field);
    if (entry === void 0) throw new Error(`Op target ${target.scope}:"${target.field}" has no resolved field entry on this instance — the deployed definition and the instance state have diverged`);
    return entry;
}

function fieldHost(ctx, scope) {
    if (scope === "workflow") return ctx.mutation.fields;
    const stageEntry = findOpenStageEntry(ctx.mutation);
    if (scope === "stage") return stageEntry?.fields;
    const activity = stageEntry?.activities.find(t => t.name === ctx.activityName);
    return activity !== void 0 && activity.fields === void 0 && (activity.fields = []), 
    activity?.fields;
}

function subSlot(target, field) {
    const shape = target?.fields?.find(f => f.name === field);
    if (shape !== void 0) return {
        kind: shape.type,
        ...shape.fields !== void 0 ? {
            fields: shape.fields
        } : {}
    };
}

function resolveOpValue(args) {
    const {src: src, ctx: ctx, target: target} = args;
    switch (src.type) {
      case "fieldRead":
        {
            const entry = readEntryFromMutation(ctx, src);
            return entry === void 0 ? void 0 : resolveFieldRead({
                kind: entry._type,
                value: entry.value,
                path: src.path,
                snapshot: ctx.snapshot,
                ...target?.kind !== void 0 ? {
                    targetKind: target.kind
                } : {}
            });
        }

      case "object":
        {
            const out = {};
            for (const [field, fieldSrc] of Object.entries(src.fields)) {
                const slot = subSlot(target, field);
                out[field] = resolveOpValue({
                    src: fieldSrc,
                    ctx: ctx,
                    ...slot !== void 0 ? {
                        target: slot
                    } : {}
                });
            }
            return out;
        }

      default:
        return resolveAtomicOpValue(src, ctx);
    }
}

function resolveAtomicOpValue(src, ctx) {
    switch (src.type) {
      case "literal":
      case "param":
      case "actor":
      case "now":
        return resolveStaticValueExpr(src, ctx);

      case "self":
        return ctx.self;

      case "stage":
        return ctx.stage;
    }
}

function readEntryFromMutation(ctx, src) {
    const scopes = src.scope !== void 0 ? [ src.scope ] : [ "activity", "stage", "workflow" ], stageEntry = findOpenStageEntry(ctx.mutation);
    for (const scope of scopes) {
        let host;
        scope === "workflow" ? host = ctx.mutation.fields : scope === "stage" ? host = stageEntry?.fields : host = findCurrentActivityEntry(ctx.mutation, ctx.activityName)?.fields;
        const entry = host?.find(s => s.name === src.field);
        if (entry !== void 0) return entry;
    }
}

const EMPTY_SNAPSHOT = emptySnapshot();

async function rowMatches({where: where, rows: rows, ctx: ctx}) {
    const base = whereParams(ctx);
    return Promise.all(rows.map(row => evaluateCondition({
        condition: where,
        snapshot: EMPTY_SNAPSHOT,
        params: {
            ...base,
            row: row
        }
    })));
}

const WHERE_SCOPE_PARAM_NAMES = [ "row", "params", "actor", "now", "self", "stage", "fields", "context", "effects", "effectStatus", "activities", "allActivitiesDone", "anyActivityFailed" ];

function whereParams(ctx) {
    const activities = findOpenStageEntry(ctx.mutation)?.activities ?? [];
    return {
        params: ctx.params,
        actor: ctx.actor ?? null,
        now: ctx.now,
        self: ctx.self,
        stage: ctx.stage,
        fields: {
            ...renderedFields(ctx.mutation.fields, void 0),
            ...scopedFieldOverlay({
                instance: ctx.mutation,
                snapshot: void 0,
                activityName: ctx.activityName
            })
        },
        context: contextMap(ctx.mutation),
        effects: effectOutputsMap(ctx.mutation),
        effectStatus: effectStatusMap(ctx.mutation),
        activities: activities,
        ...activityGateParams(activities)
    };
}

function validateDefinition(definition) {
    const v2 = createDefinitionValidator();
    validateConditionSites(v2, definition), validateStart(v2, definition);
    for (const entry of definition.fields ?? []) v2.checkEntry(entry, `workflow.fields "${entry.name}"`);
    for (const stage of definition.stages) validateStage(v2, stage);
    for (const issue of checkWorkflowInvariants(definition)) v2.errors.push(`  · ${formatIssuePath(issue.path)}: ${issue.message}`);
    if (v2.errors.length > 0) throw new Error(`validateDefinition("${definition.name}"): ${v2.errors.length} deploy-time validation error${v2.errors.length === 1 ? "" : "s"}:\n` + v2.errors.join(`\n`));
}

function validateConditionSites(v2, definition) {
    for (const site of conditionSitesOf(definition)) {
        const {address: address} = site;
        address.kind !== "editable-field" && v2.checkCondition(site.condition, conditionSiteLabel({
            stage: site.stage,
            address: address
        }));
    }
}

function validateStart(v2, definition) {
    definition.start?.filter !== void 0 && v2.report("start.filter", startFilterSyntaxIssues(definition.start.filter));
    for (const [index, requirement] of (definition.start?.requirements ?? []).entries()) requirement.type === "groq" && v2.report(`start.requirements[${index}].query`, startRequirementSyntaxIssues(requirement.query));
}

function createDefinitionValidator() {
    const errors = [], tryParse = (groq, where) => {
        try {
            parse(groq);
        } catch (err) {
            errors.push(`  · ${where}: ${errorMessage(err)}`);
        }
    }, report = (where, issues) => {
        for (const issue of issues) errors.push(`  · ${where}: ${issue}`);
    }, checkCondition = (groq, where) => report(where, conditionSyntaxIssues(groq));
    return {
        errors: errors,
        report: report,
        tryParse: tryParse,
        checkCondition: checkCondition,
        checkWhere: (groq, where) => report(where, conditionSyntaxIssues(groq, WHERE_SCOPE_PARAM_NAMES)),
        checkEntry: (entry, label) => {
            entry.initialValue?.type === "query" && tryParse(entry.initialValue.query, `${label}.query`), 
            typeof entry.editable == "string" && checkCondition(entry.editable, `${label}.editable`);
        },
        checkGuardRead: (expr, where) => {
            isGuardReadExpr(expr) || errors.push(`  · ${where}: guard read "${expr}" is not a supported deploy-time value — use "$self", "$now", "$fields.<name>[.path]", or "$effects['<name>'][.path]" (guards store resolved values; the lake cannot see $fields or $effects)`);
        }
    };
}

function conditionSiteLabel({stage: stage, address: address}) {
    const at = `stage "${stage}"`;
    return address.kind === "predicate" ? `predicate "${address.predicate}"` : address.kind === "transition" ? `${at} transition "${address.transition}" when` : address.kind === "activity-filter" ? `${at} activity "${address.activity}".filter` : address.kind === "requirement" ? `${at} activity "${address.activity}".requirements "${address.requirement}"` : address.kind === "action" ? `${at} activity "${address.activity}" action "${address.action}".filter` : address.kind === "action-when" ? `${at} activity "${address.activity}" action "${address.action}".when` : `${at}.editable "${address.name}"`;
}

function validateStage(v2, stage) {
    for (const entry of stage.fields ?? []) v2.checkEntry(entry, `stage "${stage.name}".fields "${entry.name}"`);
    for (const guard of stage.guards ?? []) validateGuard({
        v: v2,
        stageName: stage.name,
        guard: guard
    });
    for (const activity of stage.activities ?? []) validateActivity({
        v: v2,
        stageName: stage.name,
        activity: activity
    });
}

function validateGuard({v: v2, stageName: stageName, guard: guard}) {
    const where = `stage "${stageName}" guard "${guard.name}"`;
    if (validateGuardMatch({
        v: v2,
        where: where,
        guard: guard
    }), guard.predicate !== void 0 && guard.predicate !== "") {
        const issue = guardPredicateSyntaxIssue(guard.predicate);
        issue !== void 0 && v2.report(`${where} predicate`, [ issue ]);
    }
    for (const [key, expr] of Object.entries(guard.metadata ?? {})) v2.checkGuardRead(expr, `${where} metadata "${key}"`);
}

function validateGuardMatch({v: v2, where: where, guard: guard}) {
    guard.match.actions.length === 0 && v2.report(`${where} match.actions`, [ GUARD_ACTIONS_REQUIRED_MESSAGE ]);
    for (const pattern of guard.match.idPatterns ?? []) {
        const issue = guardIdPatternIssue(pattern, guard.match.actions);
        issue !== void 0 && v2.report(`${where} match.idPatterns`, [ issue ]);
    }
    for (const expr of guard.match.idRefs ?? []) v2.checkGuardRead(expr, `${where} match.idRefs`);
}

function validateActivity({v: v2, stageName: stageName, activity: activity}) {
    const where = `stage "${stageName}" activity "${activity.name}"`;
    for (const entry of activity.fields ?? []) v2.checkEntry(entry, `${where}.fields "${entry.name}"`);
    validateActivityActions({
        v: v2,
        where: where,
        activity: activity
    });
}

function validateActivityActions({v: v2, where: where, activity: activity}) {
    for (const a of activity.actions ?? []) {
        const label = `${where} action "${a.name}"`;
        checkOpWheres({
            v: v2,
            ops: a.ops,
            label: label
        });
        for (const [key, groq] of effectBindings(a.effects)) v2.tryParse(groq, `${label} effect binding "${key}"`);
        a.spawn !== void 0 && validateSpawn({
            v: v2,
            where: label,
            sub: a.spawn
        });
    }
}

function checkOpWheres({v: v2, ops: ops, label: label}) {
    for (const op of ops ?? []) (op.type === "field.updateWhere" || op.type === "field.removeWhere") && v2.checkWhere(op.where, `${label} ${op.type} where`);
}

function validateSpawn({v: v2, where: where, sub: sub}) {
    v2.tryParse(sub.forEach, `${where}.spawn.forEach`);
    for (const [key, groq] of Object.entries(sub.with ?? {})) v2.tryParse(groq, `${where}.spawn.with "${key}"`);
    for (const [key, groq] of Object.entries(sub.context ?? {})) v2.tryParse(groq, `${where}.spawn.context "${key}"`);
}

function effectBindings(effects) {
    return (effects ?? []).flatMap(e => Object.entries(e.bindings ?? {}).map(([k, groq]) => [ `${e.name}.${k}`, groq ]));
}

function lintEffectOutputs(definition) {
    const declared = /* @__PURE__ */ new Map;
    for (const {effect: effect} of effectSites(definition)) declared.set(effect.name, effect.outputs?.map(shape => shape.name) ?? []);
    const warnings = /* @__PURE__ */ new Set, check = ({effect: effect, key: key}) => {
        const outputs = declared.get(effect);
        if (outputs === void 0) {
            warnings.add(`\`$effects['${effect}'].${key}\` reads an effect this definition never declares — $effects carries completed effects' outputs only (a start seed or spawn handoff is read via $context)`);
            return;
        }
        outputs.includes(key) || warnings.add(`effect "${effect}" is read as \`$effects['${effect}'].${key}\` but declares outputs [${outputs.join(", ")}] — no declared producer for "${key}"`);
    };
    for (const groq of effectReadGroq(definition)) conditionEffectReads(groq).forEach(check);
    return guardEffectReads(definition).forEach(check), [ ...warnings ];
}

function effectReadGroq(def) {
    const effects = effectSites(def).map(site => site.effect);
    return [ ...conditionSitesOf(def).map(site => site.condition), ...effectBindings(effects).map(([, groq]) => groq) ];
}

function guardEffectReads(def) {
    const reads = [];
    for (const stage of def.stages) for (const guard of stage.guards ?? []) {
        const exprs = [ ...guard.match.idRefs ?? [], ...Object.values(guard.metadata ?? {}) ];
        for (const expr of exprs) {
            const match = EFFECTS_READ.exec(expr);
            match?.[2] !== void 0 && reads.push({
                effect: match[1],
                key: match[2].split(".")[0]
            });
        }
    }
    return reads;
}

function actionVerdict(activity, action) {
    return {
        activity: activity.activity.name,
        activityStatus: activity.status,
        action: action.action.name,
        title: action.action.title,
        allowed: action.allowed,
        disabledReason: action.disabledReason,
        params: action.action.params ?? []
    };
}

function availableActions(activities) {
    return activities.filter(t => !t.scopedOut).flatMap(t => t.actions.map(a => actionVerdict(t, a)));
}

function buildClientForGdr(args) {
    const {client: client, workflowResource: workflowResource, resourceClients: resourceClients} = args, siblings = /* @__PURE__ */ new Map;
    return parsed => {
        const resolved = resourceClients?.(parsed);
        if (resolved !== void 0) return resolved;
        const target = resourceFromParsed(parsed);
        return sameResource(target, workflowResource) ? client : siblingFor({
            client: client,
            workflowResource: workflowResource,
            target: target,
            parsed: parsed,
            siblings: siblings
        });
    };
}

function siblingFor(args) {
    const {client: client, workflowResource: workflowResource, target: target, parsed: parsed, siblings: siblings} = args, key = `${target.type}:${target.id}`, cached = siblings.get(key);
    if (cached !== void 0) return cached;
    if (client.withConfig === void 0) throw new ContractViolationError(`Cannot route "${gdrFromResource(target, parsed.documentId)}": the GDR targets ${target.type} "${target.id}", not the workflow resource (${workflowResource.type} "${workflowResource.id}"), and the workflow client cannot derive a sibling client for it (no withConfig). Pass resourceClients to route foreign resources, or use a client with withConfig (e.g. @sanity/client).`);
    const sibling = client.withConfig(siblingConfig(target));
    return siblings.set(key, sibling), sibling;
}

function siblingConfig(target) {
    if (target.type === "dataset") {
        const {projectId: projectId, dataset: dataset} = datasetResourceParts(target.id);
        return {
            resource: target,
            projectId: projectId,
            dataset: dataset
        };
    }
    return {
        resource: target
    };
}

const wallClock = () => /* @__PURE__ */ (new Date).toISOString();

function addMs(instant, ms) {
    return new Date(Date.parse(instant) + ms).toISOString();
}

function hasPassed(instant, now) {
    return Date.parse(instant) <= Date.parse(now);
}

const DATA_MODEL_VERSION = 9, DATA_MODEL_MIN_READER = 4, DATA_MODEL_MAX_READER = 9, READER_MODEL_ROLLOUT_URL = "https://www.sanity.io/docs/workflows/prerelease";

class ReaderModelAcknowledgementError extends WorkflowError {
    code="WORKFLOW_READER_MODEL_ACKNOWLEDGEMENT_MISMATCH";
    expectedMinReaderModel;
    requiredMinReaderModel;
    engineMinReaderModel=DATA_MODEL_MIN_READER;
    engineMaxReaderModel=DATA_MODEL_MAX_READER;
    engineModelVersion=DATA_MODEL_VERSION;
    documentationUrl=READER_MODEL_ROLLOUT_URL;
    constructor(expectedMinReaderModel, options = {}) {
        const {context: context = "Deployment", requiredMinReaderModel: requiredMinReaderModel = DATA_MODEL_MIN_READER} = options, expected = expectedMinReaderModel === void 0 ? "missing" : String(expectedMinReaderModel);
        super("reader-model-acknowledgement", `${context} acknowledges reader model ${expected}; the submitted definitions require at least ${requiredMinReaderModel}, and this writer supports acknowledgements through ${DATA_MODEL_MAX_READER}.\nUse a reviewed numeric literal in that range. Before raising it, ensure every runtime sharing the workflow resource can read the required model.\nRollout guide: ${READER_MODEL_ROLLOUT_URL}`), 
        this.name = "ReaderModelAcknowledgementError", this.expectedMinReaderModel = expectedMinReaderModel, 
        this.requiredMinReaderModel = requiredMinReaderModel;
    }
}

function assertReaderModelAcknowledgement(expectedMinReaderModel, options) {
    const {context: context, requiredMinReaderModel: requiredMinReaderModel} = options;
    if (typeof expectedMinReaderModel != "number" || !Number.isFinite(expectedMinReaderModel) || !Number.isInteger(expectedMinReaderModel) || expectedMinReaderModel < 0 || expectedMinReaderModel < requiredMinReaderModel || expectedMinReaderModel > DATA_MODEL_MAX_READER) throw new ReaderModelAcknowledgementError(expectedMinReaderModel, {
        requiredMinReaderModel: requiredMinReaderModel,
        ...context !== void 0 ? {
            context: context
        } : {}
    });
}

const DATA_MODEL_CHANGES = Object.freeze([ Object.freeze({
    id: "governed-model-stamps",
    introducedInModel: 1,
    minReaderModel: 0,
    documentTypes: Object.freeze([ "definition", "instance" ]),
    compatibility: "additive",
    applicability: "unconditional",
    summary: "Definition and instance documents carry model provenance and reader-floor stamps."
}), Object.freeze({
    id: "subject-field-kind",
    introducedInModel: 2,
    minReaderModel: 0,
    documentTypes: Object.freeze([ "definition", "instance" ]),
    compatibility: "additive",
    applicability: "detectable",
    summary: "A workflow-level subject field identifies the document a workflow is about."
}), Object.freeze({
    id: "typed-scalar-choice-lists",
    introducedInModel: 2,
    minReaderModel: 2,
    documentTypes: Object.freeze([ "definition", "instance" ]),
    compatibility: "reader-floor",
    applicability: "detectable",
    summary: "Scalar fields may constrain writes to a persisted typed choice list."
}), Object.freeze({
    id: "action-semantics",
    introducedInModel: 2,
    minReaderModel: 0,
    documentTypes: Object.freeze([ "definition" ]),
    compatibility: "additive",
    applicability: "detectable",
    summary: "Ordinary actions may carry advisory workflow semantics."
}), Object.freeze({
    id: "inclusive-scalar-bounds",
    introducedInModel: 2,
    minReaderModel: 2,
    documentTypes: Object.freeze([ "definition", "instance" ]),
    compatibility: "reader-floor",
    applicability: "detectable",
    summary: "String, text, and number values may carry persisted inclusive bounds."
}), Object.freeze({
    id: "progress-field-kind",
    introducedInModel: 3,
    minReaderModel: 0,
    documentTypes: Object.freeze([ "definition", "instance" ]),
    compatibility: "additive",
    applicability: "detectable",
    summary: "A progress field kind carries application-defined 0–100 completion."
}), Object.freeze({
    id: "effect-claim-tokens",
    introducedInModel: 3,
    minReaderModel: 0,
    documentTypes: Object.freeze([ "instance" ]),
    compatibility: "additive",
    applicability: "detectable",
    summary: "Pending-effect claims carry an exact-claim token gating mid-dispatch state reports."
}), Object.freeze({
    id: "classified-principal-ids",
    introducedInModel: 4,
    minReaderModel: 4,
    documentTypes: Object.freeze([ "instance" ]),
    compatibility: "reader-floor",
    applicability: "unconditional",
    summary: "Principal ids are namespace-classified: actor and assignee writes carry the account-global user id only, and readers resolve legacy project-scoped ids through the prefix classifier at the instance read funnel."
}), Object.freeze({
    id: "readiness-requirements",
    introducedInModel: 4,
    minReaderModel: 4,
    documentTypes: Object.freeze([ "definition" ]),
    compatibility: "reader-floor",
    applicability: "detectable",
    summary: "Start and activity readiness use named polymorphic requirement arrays."
}), Object.freeze({
    id: "due-date-field-kinds",
    introducedInModel: 5,
    minReaderModel: 0,
    documentTypes: Object.freeze([ "definition", "instance" ]),
    compatibility: "additive",
    applicability: "detectable",
    summary: "Due-date field kinds (dueDate, dueDatetime) mark a level deadline, elevated aliases of date/datetime carrying the same stored value."
}), Object.freeze({
    id: "node-semantics",
    introducedInModel: 6,
    minReaderModel: 0,
    documentTypes: Object.freeze([ "definition" ]),
    compatibility: "additive",
    applicability: "detectable",
    summary: "Workflow, stage, activity, and action nodes may carry signal or custom advisory semantics."
}), Object.freeze({
    id: "field-patch-ops",
    introducedInModel: 6,
    minReaderModel: 0,
    documentTypes: Object.freeze([ "definition", "instance" ]),
    compatibility: "additive",
    applicability: "detectable",
    summary: "Field ops may increment, decrement, or initialize a missing field value."
}), Object.freeze({
    id: "attributes-condition-var",
    introducedInModel: 7,
    minReaderModel: 0,
    documentTypes: Object.freeze([ "definition" ]),
    compatibility: "additive",
    applicability: "unconditional",
    summary: "Caller-bound $attributes condition variable binds the acting token's org-level User Attributes (advisory; absent when unavailable)."
}), Object.freeze({
    id: "role-constrained-assignment-fields",
    introducedInModel: 8,
    minReaderModel: 8,
    documentTypes: Object.freeze([ "definition", "instance" ]),
    compatibility: "reader-floor",
    applicability: "detectable",
    summary: "Assignee fields may restrict newly assigned users and collective roles by role."
}), Object.freeze({
    id: "split-guard-id-spaces",
    introducedInModel: 9,
    minReaderModel: 9,
    documentTypes: Object.freeze([ "definition", "instance" ]),
    compatibility: "reader-floor",
    applicability: "detectable",
    summary: "Guards spanning edit and lifecycle id spaces emit independently retractable documents."
}), Object.freeze({
    id: "singular-assignee-lists",
    introducedInModel: 9,
    minReaderModel: 9,
    documentTypes: Object.freeze([ "definition", "instance" ]),
    compatibility: "reader-floor",
    applicability: "detectable",
    summary: "Singular assignee fields use member lists with at most one user and any number of roles."
}) ]);

function recordOf(value) {
    return value !== null && typeof value == "object" && !Array.isArray(value) ? value : void 0;
}

function recordsAt(record, key) {
    const value = record[key];
    return Array.isArray(value) ? value.map(recordOf).filter(item => item !== void 0) : [];
}

function nestedFieldEntries(entries) {
    return entries.flatMap(entry => [ entry, ...nestedFieldEntries(recordsAt(entry, "fields")), ...nestedFieldEntries(recordsAt(entry, "of")) ]);
}

function parsedDefinitionSnapshot(root) {
    if (typeof root.definitionSnapshot == "string") return recordOf(parseDefinitionSnapshotValue({
        _id: typeof root._id == "string" ? root._id : "<unknown instance>",
        definitionSnapshot: root.definitionSnapshot
    }));
}

function persistedDefinitionTree(document) {
    const root = recordOf(document);
    if (root === void 0) return;
    const snapshot = parsedDefinitionSnapshot(root), roots = snapshot === void 0 ? [ root ] : [ root, snapshot ], {stages: stages, activities: activities, actions: actions} = definitionDescendants(roots);
    return {
        roots: roots,
        stages: stages,
        activities: activities,
        actions: actions
    };
}

function persistedFieldEntries(document) {
    const tree = persistedDefinitionTree(document);
    if (tree === void 0) return [];
    const {roots: roots, stages: stages, activities: activities, actions: actions} = tree, effects = actions.flatMap(action => recordsAt(action, "effects"));
    return nestedFieldEntries([ ...roots.flatMap(candidate => recordsAt(candidate, "fields")), ...stages.flatMap(stage => recordsAt(stage, "fields")), ...activities.flatMap(activity => recordsAt(activity, "fields")), ...actions.flatMap(action => recordsAt(action, "params")), ...effects.flatMap(effect => recordsAt(effect, "outputs")) ]);
}

function definitionDescendants(roots) {
    const stages = roots.flatMap(root => recordsAt(root, "stages")), activities = stages.flatMap(stage => recordsAt(stage, "activities")), actions = activities.flatMap(activity => recordsAt(activity, "actions"));
    return {
        stages: stages,
        activities: activities,
        actions: actions
    };
}

function hasChoiceList(document) {
    return persistedFieldEntries(document).some(entry => {
        const options = recordOf(entry.options);
        return options !== void 0 && Array.isArray(options.list);
    });
}

function entryHasKind(entry, kind) {
    return entryKindMatches(entry, candidate => candidate === kind);
}

function entryKindMatches(entry, accepts) {
    return [ entry.type, entry._type ].some(kind => typeof kind == "string" && accepts(kind));
}

function hasFieldKind(document, kind) {
    return persistedFieldEntries(document).some(entry => entryHasKind(entry, kind));
}

function definitionNodes(document) {
    const root = recordOf(document);
    if (root !== void 0) return {
        root: root,
        ...definitionDescendants([ root ])
    };
}

function hasActionSemantics(document) {
    return definitionNodes(document)?.actions.some(hasSemantics) ?? !1;
}

function hasNodeSemantics(document) {
    const nodes = definitionNodes(document);
    return nodes === void 0 ? !1 : [ nodes.root, ...nodes.stages, ...nodes.activities ].some(hasSemantics) ? !0 : nodes.actions.some(action => hasSemantics(action) && action.semantics.some(isNodeSemanticValue));
}

function isNodeSemanticValue(semantic) {
    return typeof semantic == "string" && (semantic.startsWith("signal.") || semantic.startsWith("custom."));
}

function hasSemantics(node) {
    return Array.isArray(node.semantics);
}

function hasPersistedOpType(document, types) {
    const tree = persistedDefinitionTree(document);
    return tree === void 0 ? !1 : tree.actions.flatMap(action => recordsAt(action, "ops")).some(op => typeof op.type == "string" && types.includes(op.type)) ? !0 : tree.roots.some(root => recordsAt(root, "history").some(entry => entry._type === "opApplied" && typeof entry.opType == "string" && types.includes(entry.opType)));
}

function hasScalarValidation(document) {
    return persistedFieldEntries(document).some(entry => {
        const validation = recordOf(entry.validation);
        return typeof validation?.min == "number" || typeof validation?.max == "number";
    });
}

function hasClaimTokens(document) {
    const root = recordOf(document);
    return root === void 0 ? !1 : recordsAt(root, "pendingEffects").some(entry => {
        const claim = recordOf(entry.claim);
        return claim !== void 0 && typeof claim.claimToken == "string";
    });
}

function hasReadinessRequirements(document) {
    const root = recordOf(document);
    return root === void 0 ? !1 : Array.isArray(recordOf(root.start)?.requirements) ? !0 : recordsAt(root, "stages").some(stage => recordsAt(stage, "activities").some(activity => Array.isArray(activity.requirements)));
}

function hasRoleConstrainedAssignments(document) {
    return persistedFieldEntries(document).some(entry => entryKindMatches(entry, assignmentKindAcceptsRoles) && Array.isArray(entry.roles) && entry.roles.length > 0);
}

function hasSingularAssigneeLists(document) {
    const root = recordOf(document);
    if (root === void 0) return !1;
    if (typeof root.definitionSnapshot != "string") return persistedFieldEntries(document).some(entry => entryHasKind(entry, "assignee"));
    const snapshot = parsedDefinitionSnapshot(root);
    return typeof snapshot?.modelVersion == "number" && isAssignmentListModel(snapshot.modelVersion) && persistedFieldEntries(document).some(entry => entryHasKind(entry, "assignee"));
}

function isMutationGuardAction(action) {
    return typeof action == "string" && MUTATION_GUARD_ACTIONS.some(candidate => candidate === action);
}

function hasSplitGuardIdSpaces(document) {
    return persistedDefinitionTree(document)?.stages.some(stage => recordsAt(stage, "guards").some(guard => {
        const actions = recordOf(guard.match)?.actions;
        return Array.isArray(actions) && mutationGuardRequiresSplitEmission(actions.filter(isMutationGuardAction));
    })) ?? !1;
}

const featureDetectors = {
    "governed-model-stamps": () => !0,
    "subject-field-kind": document => hasFieldKind(document, "subject"),
    "typed-scalar-choice-lists": hasChoiceList,
    "action-semantics": hasActionSemantics,
    "inclusive-scalar-bounds": hasScalarValidation,
    "progress-field-kind": document => hasFieldKind(document, "progress"),
    "effect-claim-tokens": hasClaimTokens,
    "classified-principal-ids": () => !0,
    "readiness-requirements": hasReadinessRequirements,
    "due-date-field-kinds": document => hasFieldKind(document, "dueDate") || hasFieldKind(document, "dueDatetime"),
    "role-constrained-assignment-fields": hasRoleConstrainedAssignments,
    "singular-assignee-lists": hasSingularAssigneeLists,
    "split-guard-id-spaces": hasSplitGuardIdSpaces,
    "node-semantics": hasNodeSemantics,
    "field-patch-ops": document => hasPersistedOpType(document, [ "field.inc", "field.dec", "field.setIfMissing" ]),
    "attributes-condition-var": () => !0
};

function requiredModelFeatures(documentType, document) {
    return DATA_MODEL_CHANGES.filter(change => change.documentTypes.some(candidate => candidate === documentType) && featureDetectors[change.id](document));
}

function requiredReaderModel(documentType, document) {
    return Math.max(0, ...requiredModelFeatures(documentType, document).map(change => change.minReaderModel));
}

function requiredDefinitionReaderModel(definitions) {
    return Math.max(DATA_MODEL_MIN_READER, ...definitions.map(definition => requiredReaderModel("definition", definition)));
}

function modelStampFor(args) {
    return {
        modelVersion: DATA_MODEL_VERSION,
        minReaderModel: Math.max(DATA_MODEL_MIN_READER, args.storedMinReaderModel ?? 0, requiredReaderModel(args.documentType, args.document))
    };
}

function fieldTreeShape(value) {
    if (Array.isArray(value)) return value.map(fieldTreeShape);
    if (value === null) return "null";
    if (typeof value == "object") {
        const record = value;
        return Object.fromEntries(Object.keys(record).toSorted().map(key => [ key, fieldTreeShape(record[key]) ]));
    }
    return typeof value;
}

function modelVersionOf(doc) {
    const stamp = doc.modelVersion;
    return typeof stamp == "number" ? stamp : 0;
}

function minReaderModelOf(doc) {
    const floor = doc.minReaderModel;
    return typeof floor == "number" ? floor : modelVersionOf(doc);
}

class ModelVersionAheadError extends WorkflowError {
    documentId;
    documentModelVersion;
    requiredReaderModel;
    engineModelVersion;
    constructor(args) {
        super("model-version-ahead", `Document "${args.documentId}" was written by engine data model ${args.documentModelVersion} and requires a reader at model ${args.requiredReaderModel} or newer; this engine reads up to model ${DATA_MODEL_VERSION}. Upgrade @sanity/workflow-engine to a version that understands it.`), 
        this.name = "ModelVersionAheadError", this.documentId = args.documentId, this.documentModelVersion = args.documentModelVersion, 
        this.requiredReaderModel = args.requiredReaderModel, this.engineModelVersion = DATA_MODEL_VERSION;
    }
}

function assertReadableModel(doc) {
    const documentReaderModel = minReaderModelOf(doc);
    if (documentReaderModel > DATA_MODEL_VERSION) throw new ModelVersionAheadError({
        documentId: doc._id,
        documentModelVersion: modelVersionOf(doc),
        requiredReaderModel: documentReaderModel
    });
    return doc;
}

async function renderConditionScope(source, opts) {
    const {instance: instance, definition: definition, snapshot: snapshot, now: now} = source, base = buildParams({
        instance: instance,
        now: now,
        snapshot: snapshot
    }), fields = {
        ...base.fields,
        ...scopedFieldOverlay({
            instance: instance,
            snapshot: snapshot,
            activityName: opts?.activityName
        })
    }, params = {
        ...base,
        fields: fields,
        actor: opts?.actor,
        assigned: opts?.activityName !== void 0 ? assignedFor({
            instance: instance,
            activityName: opts.activityName,
            actor: opts?.actor,
            roleAliases: definition.roleAliases
        }) : !1,
        ...opts?.vars
    };
    return {
        ...await evaluatePredicates({
            predicates: definition.predicates,
            snapshot: snapshot,
            params: params
        }),
        ...params
    };
}

function definitionLookupGroq(explicit) {
    const scoped = `_type == "${WORKFLOW_DEFINITION_TYPE}" && name == $definition && ${tagScopeFilter()}`;
    return explicit ? `*[${scoped} && version == $version][0]` : `*[${scoped}] | order(version desc)[0]`;
}

function definitionsListGroq(versionOrder) {
    return `*[_type == "${WORKFLOW_DEFINITION_TYPE}" && ${tagScopeFilter()}] | order(name asc, version ${versionOrder})`;
}

function latestDefinitionsGroq() {
    const scoped = `_type == "${WORKFLOW_DEFINITION_TYPE}" && ${tagScopeFilter()}`;
    return `*[${scoped} && version == math::max(*[${scoped} && name == ^.name].version)] | order(name asc)`;
}

function deployedTagsGroq() {
    return `array::unique(*[_type == "${WORKFLOW_DEFINITION_TYPE}"].tag) | order(@ asc)`;
}

function definitionTagsGroq() {
    return `array::unique(*[_type == "${WORKFLOW_DEFINITION_TYPE}" && name == $definition].tag) | order(@ asc)`;
}

function latestDeployedDefinitions(rows) {
    const byName = /* @__PURE__ */ new Map;
    for (const row of rows) {
        const held = byName.get(row.name);
        (held === void 0 || row.version > held.version) && byName.set(row.name, row);
    }
    return [ ...byName.values() ];
}

function findStageNode(args) {
    return args.definition?.stages.find(entry => entry.name === args.stageName);
}

function findActivityNode(args) {
    return findStageNode(args)?.activities?.find(entry => entry.name === args.activityName);
}

function requirementDescriptor(requirement) {
    return {
        name: requirement.name,
        ...requirement.title !== void 0 ? {
            title: requirement.title
        } : {},
        ...requirement.description !== void 0 ? {
            description: requirement.description
        } : {}
    };
}

function subjectDenialLabels(denied) {
    return denied.map(d => `${d.permission} on ${d.subject} (${d.resource})`);
}

class ActionDisabledError extends WorkflowError {
    reason;
    activity;
    action;
    constructor(args) {
        super("action-disabled", formatDisabledReason({
            activity: args.activity,
            action: args.action,
            reason: args.reason
        })), this.name = "ActionDisabledError", this.reason = args.reason, this.activity = args.activity, 
        this.action = args.action;
    }
}

class StartNotAllowedError extends WorkflowError {
    definition;
    unmetRequirements;
    constructor(args) {
        super("start-not-allowed", `startInstance refused definition "${args.definition}": ${args.unmetRequirements.map(requirement => requirement.title ?? requirement.name).join(", ")}. Pre-flight the verdict with evaluateStart.`), 
        this.name = "StartNotAllowedError", this.definition = args.definition, this.unmetRequirements = args.unmetRequirements;
    }
}

function actionRendering(action) {
    const kind = action.disabledReason?.kind;
    return kind === "filter-failed" ? "absent" : action.triggered === !0 || kind === "cascade-fired" ? "automation" : "button";
}

const disabledReasonDetail = {
    "filter-failed": r => `action filter returned false${r.detail ? ` (${r.detail})` : ""}`,
    "cascade-fired": r => `the action is cascade-fired (when: ${JSON.stringify(r.when)}) — the engine fires it on truth; it cannot be invoked via fireAction`,
    "activity-not-active": r => `activity status is "${r.status}"`,
    "stage-terminal": r => `stage "${r.stage}" is terminal`,
    "instance-completed": r => `instance completed at ${r.completedAt}`,
    "instance-aborted": r => `instance aborted at ${r.abortedAt}`,
    "requirements-unmet": r => `unmet requirement(s): ${r.unmetRequirements.map(requirement => requirement.name).join(", ")}`,
    "subject-permission-denied": r => `missing subject permission(s): ${subjectDenialLabels(r.denied).join(", ")}`
};

function actionDisabledDetail(reason) {
    return disabledReasonDetail[reason.kind](reason);
}

function formatDisabledReason({activity: activity, action: action, reason: reason}) {
    return `Action "${activity}:${action}" is not allowed: ${actionDisabledDetail(reason)}`;
}

class EditFieldDeniedError extends WorkflowError {
    reason;
    target;
    constructor(args) {
        super("edit-field-denied", formatEditDisabledReason(args.target, args.reason)), 
        this.name = "EditFieldDeniedError", this.reason = args.reason, this.target = args.target;
    }
}

const editDisabledReasonDetail = {
    "not-editable": () => "field is not declared editable",
    "instance-completed": r => `instance completed at ${r.completedAt}`,
    "instance-aborted": r => `instance aborted at ${r.abortedAt}`,
    "edit-window-closed": r => `edit window closed (${r.detail})`,
    "editor-not-permitted": r => `editor not permitted (${r.predicate})`
};

function formatEditDisabledReason(target, reason) {
    const detail = editDisabledReasonDetail[reason.kind](reason), where = target.activity !== void 0 ? `${target.activity}.${target.field}` : target.field;
    return `Field "${target.scope}:${where}" is not editable: ${detail}`;
}

function liveChildrenField(instance) {
    const live = liveSubworkflows(instance).length;
    return live > 0 ? {
        liveChildren: live
    } : {};
}

function abortReason(instance) {
    return instance.history.find(h => h._type === "aborted")?.reason;
}

function diagnoseInputFromEvaluation(evaluation) {
    const stage = openStage(evaluation.instance);
    return {
        instance: evaluation.instance,
        activities: evaluation.currentStage.activities,
        transitions: evaluation.currentStage.transitions,
        assignees: Object.fromEntries(evaluation.currentStage.activities.map(t => [ t.activity.name, assigneesOf(stage, t.activity.name) ]))
    };
}

function openStage(instance) {
    return findOpenStageEntry(instance);
}

function assigneesOf(stage, activityName) {
    const entry = stage?.activities.find(t => t.name === activityName);
    return [ ...assignmentMembers(entry?.fields ?? []) ];
}

function isResolved(status) {
    return status === "done" || status === "skipped";
}

function failedEffectCause(input) {
    const stalledEffectNames = new Set(input.activities.filter(t => !isResolved(t.status)).flatMap(t => (t.activity.actions ?? []).flatMap(action => action.effects ?? [])).map(effect2 => effect2.name)), effect = [ ...input.instance.effectHistory ].reverse().find(e => e.status === "failed" && stalledEffectNames.has(e.name));
    return effect ? {
        kind: "failed-effect",
        effect: effect
    } : void 0;
}

function hungEffectCause(input) {
    const effect = input.instance.pendingEffects.find(e => e.claim !== void 0);
    return effect ? {
        kind: "hung-effect",
        effect: effect
    } : void 0;
}

function failedActivityCause(input) {
    const failed = input.activities.find(t => t.status === "failed");
    return failed ? {
        kind: "failed-activity",
        activity: failed.activity.name
    } : void 0;
}

function waitingState(input) {
    const active = input.activities.find(t => t.status === "active" && (t.activity.actions ?? []).length > 0 && (t.unmetRequirements ?? []).length === 0);
    if (active === void 0) return;
    const actions = active.actions.filter(action => actionRendering(action) === "button" && action.allowed).map(action => action.action.name), hasManualAction = active.actions.some(action => !isCascadeFired(action.action));
    let waitingFor = "automation";
    return actions.length > 0 ? waitingFor = "caller" : hasManualAction && (waitingFor = "manual-action"), 
    {
        state: "waiting",
        activity: active.activity.name,
        assignees: input.assignees[active.activity.name] ?? [],
        actions: actions,
        waitingFor: waitingFor
    };
}

function blockedState(input) {
    const blocked = input.activities.find(t => t.status === "active" && (t.activity.actions ?? []).length > 0 && (t.unmetRequirements ?? []).length > 0);
    if (blocked !== void 0) return {
        state: "blocked",
        activity: blocked.activity.name,
        requirements: (blocked.unmetRequirements ?? []).map(requirement => requirement.name),
        assignees: input.assignees[blocked.activity.name] ?? []
    };
}

function noTransitionFiresCause(input) {
    if (input.transitions.length === 0 || input.transitions.some(t => t.whenSatisfied) || !input.activities.every(t => isResolved(t.status))) return;
    const undecidable = input.transitions.filter(t => t.unevaluable);
    return undecidable.length > 0 ? {
        kind: "transition-unevaluable",
        transitions: undecidable.map(t => t.transition.name)
    } : {
        kind: "no-transition-fires"
    };
}

function stuckFromDocumentState(input) {
    return failedEffectCause(input) ?? failedActivityCause(input) ?? hungEffectCause(input);
}

function documentStuckCause(args) {
    const stage = findOpenStageEntry(args.instance);
    if (stage !== void 0) return stuckFromDocumentState({
        instance: args.instance,
        activities: stage.activities.map(entry => ({
            status: entry.status,
            activity: findActivityNode({
                activityName: entry.name,
                definition: args.definition,
                stageName: stage.name
            }) ?? {
                name: entry.name
            }
        }))
    });
}

function diagnoseInstance(input) {
    const {instance: instance} = input, terminal = terminalState(instance);
    if (terminal === "aborted" && instance.abortedAt !== void 0) {
        const reason = abortReason(instance);
        return {
            state: "aborted",
            at: instance.abortedAt,
            ...reason !== void 0 ? {
                reason: reason
            } : {},
            ...liveChildrenField(instance)
        };
    }
    if (terminal === "completed" && instance.completedAt !== void 0) return {
        state: "completed",
        at: instance.completedAt,
        ...liveChildrenField(instance)
    };
    const cause = stuckFromDocumentState(input) ?? noTransitionFiresCause(input);
    return cause !== void 0 ? {
        state: "stuck",
        cause: cause
    } : waitingState(input) ?? blockedState(input) ?? {
        state: "progressing"
    };
}

const RUNNABLE_VERBS = /* @__PURE__ */ new Set([ "set-stage", "abort", "drain-effects", "reset-activity" ]);

function remediationsForCause(cause) {
    switch (cause.kind) {
      case "failed-effect":
        return [ {
            verb: "retry-effect",
            rationale: "Re-run the failed effect once the upstream system is healthy."
        }, {
            verb: "abort",
            rationale: "Abort the instance if it can no longer recover."
        } ];

      case "hung-effect":
        return [ {
            verb: "drain-effects",
            rationale: "Re-run the effect drainer — the runtime that registered the effect handlers — to re-pick the claimed effect."
        }, {
            verb: "abort",
            rationale: "Abort the instance if the effect never drains."
        } ];

      case "failed-activity":
        return [ {
            verb: "reset-activity",
            rationale: "Reset or skip the failed activity."
        }, {
            verb: "set-stage",
            rationale: "Move the instance past the failed activity to the intended next stage."
        } ];

      case "no-transition-fires":
        return [ {
            verb: "set-stage",
            rationale: "Move the instance to the intended next stage to force it forward."
        } ];

      case "transition-unevaluable":
        return [];
    }
}

function remediationsFor(diagnosis) {
    return diagnosis.state !== "stuck" ? [] : remediationsForCause(diagnosis.cause).map(seed => ({
        ...seed,
        available: RUNNABLE_VERBS.has(seed.verb)
    }));
}

function isClaimExpired(claim, now) {
    return claim.leaseExpiresAt === void 0 || hasPassed(claim.leaseExpiresAt, now);
}

const EFFECT_RUN_STATUSES = _exhaustiveOptions()([ "done", "failed", "cancelled" ]);

function userLoginProvider(user) {
    return user?.provider ?? user?.loginProvider;
}

function optionalString(value) {
    return value === void 0 || typeof value == "string";
}

function isClientProjectUser(value) {
    return isRecord(value) && typeof value.id == "string" && optionalString(value.sanityUserId) && optionalString(value.displayName) && optionalString(value.email) && (value.imageUrl === null || optionalString(value.imageUrl)) && optionalString(value.provider) && optionalString(value.loginProvider);
}

function objectProperty(value, property) {
    if (!isRecord(value)) return;
    const propertyValue = value[property];
    return typeof propertyValue == "object" && propertyValue !== null ? propertyValue : void 0;
}

function stringProperty(value, property) {
    const propertyValue = Reflect.get(value, property);
    return typeof propertyValue == "string" ? propertyValue : void 0;
}

function apiErrorType(error) {
    const response = objectProperty(error, "response"), body = objectProperty(response, "body");
    if (!body) return;
    const nestedError = objectProperty(body, "error");
    return (nestedError ? stringProperty(nestedError, "type") : void 0) ?? stringProperty(body, "type");
}

function isProjectUserNotFoundError(error) {
    return apiErrorType(error) === "projectUserNotFoundError";
}

const memberJoinCache = /* @__PURE__ */ new WeakMap;

function joinCacheFor(client) {
    let cache = memberJoinCache.get(client);
    return cache === void 0 && (cache = /* @__PURE__ */ new Map, memberJoinCache.set(client, cache)), 
    cache;
}

function isClientProjectMember(value) {
    return isRecord(value) && typeof value.id == "string";
}

async function requestProjectMembers(request, projectId) {
    const project = await request({
        uri: `/projects/${encodeURIComponent(projectId)}`
    }), members = isRecord(project) ? project.members : void 0;
    if (!Array.isArray(members) || !members.every(isClientProjectMember)) throw new Error(`Project member directory response is invalid for project "${projectId}"`);
    return members;
}

async function requestProjectUsers(args) {
    const {request: request, projectId: projectId, memberIds: memberIds} = args, response = await request({
        uri: `/projects/${encodeURIComponent(projectId)}/users/${memberIds.map(encodeURIComponent).join(",")}`
    }), rows = Array.isArray(response) ? response : [ response ];
    if (!rows.every(isClientProjectUser)) throw new Error(`Project user directory response is invalid for project "${projectId}"`);
    const requested = new Set(memberIds);
    return rows.filter(row => requested.has(row.id));
}

async function requestProjectMemberDirectory(args) {
    const {request: request, projectId: projectId} = args, members = await requestProjectMembers(request, projectId), memberIds = members.map(member => member.id).filter(id => classifyPrincipalId(id).namespace === "project");
    if (memberIds.length === 0) return {
        members: members,
        globalUsers: []
    };
    const users = await requestProjectUsers({
        request: request,
        projectId: projectId,
        memberIds: memberIds
    });
    return {
        members: members,
        globalUsers: users.flatMap(user => {
            const globalId = directoryBridgeId(user.sanityUserId);
            return globalId === void 0 ? [] : [ {
                memberId: user.id,
                globalId: globalId,
                user: user
            } ];
        })
    };
}

async function resolveGlobalProjectUser(args) {
    const {client: client, request: request, projectId: projectId, id: id} = args, cache = joinCacheFor(client), key = `${projectId}:${id}`, hit = cache.get(key);
    if (hit !== void 0) return hit === null ? {
        status: "missing"
    } : {
        status: "resolved",
        user: hit
    };
    try {
        await ingestMemberJoin({
            request: request,
            projectId: projectId,
            cache: cache
        });
        const found = cache.get(key);
        return found != null ? {
            status: "resolved",
            user: found
        } : (cache.set(key, null), {
            status: "missing"
        });
    } catch (cause) {
        return {
            status: "inaccessible",
            cause: cause
        };
    }
}

async function ingestMemberJoin(args) {
    const {request: request, projectId: projectId, cache: cache} = args, snapshot = await requestProjectMemberDirectory({
        request: request,
        projectId: projectId
    });
    for (const {globalId: globalId, user: user} of snapshot.globalUsers) cache.set(`${projectId}:${globalId}`, user);
}

function clientProjectUserDirectory(client, projectId) {
    return {
        findById: async id => {
            if (!client.request) return {
                status: "inaccessible",
                cause: new Error("Project-user resolution requires WorkflowClient.request")
            };
            if (classifyPrincipalId(id).namespace === "global") return resolveGlobalProjectUser({
                client: client,
                request: client.request,
                projectId: projectId,
                id: id
            });
            try {
                const response = await client.request({
                    uri: `/projects/${encodeURIComponent(projectId)}/users/${encodeURIComponent(id)}`
                }), candidate = Array.isArray(response) ? response[0] : response;
                return candidate == null ? {
                    status: "missing"
                } : isClientProjectUser(candidate) ? candidate.id !== id ? {
                    status: "inaccessible",
                    cause: new Error(`Project-user response answered id "${candidate.id}", not the requested "${id}"`)
                } : {
                    status: "resolved",
                    user: candidate
                } : {
                    status: "inaccessible",
                    cause: new Error("Project-user response had an invalid shape")
                };
            } catch (cause) {
                return isProjectUserNotFoundError(cause) ? {
                    status: "missing"
                } : {
                    status: "inaccessible",
                    cause: cause
                };
            }
        }
    };
}

function resolveClientActor(client, args) {
    return resolveActor(clientProjectUserDirectory(client, args.projectId), args.actor);
}

async function resolveActor(directory, actor) {
    if (actor.kind !== "person") return {
        status: "not-person",
        actor: actor
    };
    const personActor = {
        ...actor,
        kind: "person"
    }, lookup = await directory.findById(personActor.id);
    return lookup.status === "resolved" ? {
        status: "resolved",
        actor: personActor,
        user: lookup.user
    } : lookup.status === "inaccessible" ? {
        status: "inaccessible",
        actor: personActor,
        ...lookup.cause === void 0 ? {} : {
            cause: lookup.cause
        }
    } : {
        status: "missing",
        actor: personActor
    };
}

function assertRoleConstrainedAssignmentResource(args) {
    if (hasRoleConstrainedAssignments(args.definition) && args.workflowResource.type !== "dataset") throw new Error(`${args.context}: definition "${args.definition.name}" uses role-constrained assignment fields, which require a dataset workflow resource; received resource type "${args.workflowResource.type}"`);
}

const EXECUTION_KINDS = {
    interactive: "interactive",
    server: "server",
    cli: "cli",
    mcp: "mcp",
    drainer: "drainer",
    script: "script",
    test: "test",
    studio: "studio",
    sdkApp: "sdk-app"
};

function detectRuntime() {
    const g = globalThis;
    if (g.Deno !== void 0) return "deno";
    if (g.Bun !== void 0) return "bun";
    if (g.EdgeRuntime !== void 0) return "edge";
    if (g.window !== void 0 && g.document !== void 0) return "browser";
    if (g.WorkerGlobalScope !== void 0 && g.self !== void 0) return "worker";
    const proc = g.process;
    return typeof proc == "object" && proc !== null && typeof proc.versions?.node == "string" ? "node" : "unknown";
}

let inferredRuntime;

function resolveExecutionContext(declared) {
    return inferredRuntime ??= detectRuntime(), {
        runtime: inferredRuntime,
        ...declared?.kind !== void 0 ? {
            kind: declared.kind
        } : {},
        ...declared?.id !== void 0 ? {
            id: declared.id
        } : {}
    };
}

function stampHistoryEntries(entries, stamp) {
    return entries.map(entry => ({
        ...entry,
        executionContext: stamp
    }));
}

function collectWatchRefs(instance) {
    return [ gdrRef({
        res: instance.workflowResource,
        documentId: instance._id,
        type: instance._type
    }), ...instance.ancestors, ...liveSubworkflowRefs(instance), ...collectSubjectRefs(instance) ];
}

function collectSubjectRefs(instance) {
    const stage = findOpenStageEntry(instance), activityFields = stage?.activities.map(activity => activity.fields) ?? [];
    return [ ...entryDocRefs(instance.fields), ...entryDocRefs(stage?.fields), ...activityFields.flatMap(entryDocRefs), ...entryReleaseRefs(instance.fields), ...entryReleaseRefs(stage?.fields), ...activityFields.flatMap(entryReleaseRefs) ];
}

function foreignSubjectRefs(instance) {
    return collectSubjectRefs(instance).flatMap(ref => {
        const parsed = tryParseGdr(ref.id);
        if (parsed === void 0) return [];
        const resource = resourceFromParsed(parsed);
        return sameResource(resource, instance.workflowResource) ? [] : [ {
            ref: ref,
            parsed: parsed,
            resource: resource
        } ];
    });
}

function liveSubworkflowRefs(instance) {
    return liveSubworkflows(instance).map(row => row.ref);
}

function readsRaw(ref) {
    return ref.type === WORKFLOW_INSTANCE_TYPE || ref.type === "system.release";
}

const DEFAULT_CONTENT_PERSPECTIVE = "drafts";

function contentReleaseName(args) {
    const {ref: ref, perspective: perspective} = args;
    if (!readsRaw(ref) && Array.isArray(perspective)) return perspective.find(entry => entry !== "drafts" && entry !== "published" && entry !== "raw");
}

function contentDraftFallback(args) {
    if (readsRaw(args.ref)) return !0;
    const effective = args.perspective ?? DEFAULT_CONTENT_PERSPECTIVE;
    return Array.isArray(effective) ? effective.includes("drafts") : effective === "drafts";
}

function contentDocQuery(documentId) {
    return {
        query: "*[_id == $id][0]",
        params: {
            id: documentId
        }
    };
}

function watchRefRepresentations(args) {
    const {ref: ref} = args;
    if (readsRaw(ref)) return [ ref.documentId ];
    const release = contentReleaseName(args);
    return [ ref.documentId, ...contentDraftFallback(args) ? [ `drafts.${ref.documentId}` ] : [], ...release !== void 0 ? [ `versions.${release}.${ref.documentId}` ] : [] ];
}

function projectToWatchRef(args) {
    const {doc: doc, ref: ref} = args;
    if (doc._id === ref.documentId) return doc;
    const accepted = watchRefRepresentations(args);
    if (!accepted.includes(doc._id)) throw new Error(`projectToWatchRef: the store resolved "${doc._id}" for watch ref "${ref.globalDocumentId}" — not a representation of "${ref.documentId}" under the instance's perspective (accepted: ${accepted.map(id => `"${id}"`).join(", ")}). The consumer is feeding the wrong document.`);
    return {
        ...doc,
        _id: ref.documentId,
        _originalId: doc._id
    };
}

function subscriptionDocument(ref) {
    return {
        ...parseGdr(ref.id),
        globalDocumentId: ref.id,
        type: ref.type
    };
}

function subscriptionDocumentsForInstance(instance) {
    const seen = /* @__PURE__ */ new Set, documents = [];
    for (const ref of collectWatchRefs(instance)) !isGdrUri(ref.id) || seen.has(ref.id) || (seen.add(ref.id), 
    documents.push(subscriptionDocument(ref)));
    return {
        documents: documents,
        ...instance.perspective !== void 0 ? {
            perspective: instance.perspective
        } : {}
    };
}

function instanceWatchesDocument(instance, document) {
    return collectWatchRefs(instance).some(ref => ref.id === document);
}

function entryDocRefs(entries) {
    return fieldEntries(entries).flatMap(entry => typeof entry._type == "string" && isSingleDocRefKind(entry._type) ? isGdr(entry.value) ? [ entry.value ] : [] : entry._type === "doc.refs" ? Array.isArray(entry.value) ? entry.value.filter(isGdr) : [] : []);
}

function entryReleaseRefs(entries) {
    return fieldEntries(entries).flatMap(entry => entry._type === "release.ref" && isGdr(entry.value) ? [ entry.value ] : []);
}

function entrySubjectRefs(entries) {
    return fieldEntries(entries).flatMap(entry => (typeof entry._type == "string" && isSingleDocRefKind(entry._type) || entry._type === "release.ref") && isGdr(entry.value) ? [ entry.value ] : []);
}

function fieldEntries(entries) {
    return Array.isArray(entries) ? entries.filter(entry => !!entry && typeof entry == "object") : [];
}

function derivePerspectiveFromFields(entries) {
    for (const entry of entries ?? []) {
        if (entry._type !== "release.ref") continue;
        const perspective = perspectiveFromReleaseValue(entry.value);
        if (perspective !== void 0) return perspective;
    }
}

function perspectiveFromReleaseValue(value) {
    if (typeof value != "object" || value === null) return;
    const releaseName = value.releaseName;
    return typeof releaseName == "string" && releaseName.length > 0 ? [ releaseName ] : void 0;
}

async function resolveDeclaredFields(args) {
    const {entryDefs: entryDefs, initialFields: initialFields, ctx: ctx, randomKey: randomKey2} = args;
    if (assertInitialFieldsConsumable({
        workflowFields: entryDefs ?? [],
        initialFields: initialFields,
        ...ctx.definitionName !== void 0 ? {
            definitionName: ctx.definitionName
        } : {}
    }), entryDefs === void 0 || entryDefs.length === 0) return [];
    assertRequiredInputProvided({
        entryDefs: entryDefs,
        initialFields: initialFields,
        definitionName: ctx.definitionName
    });
    const out = [];
    for (const entry of entryDefs) {
        const scopedCtx = {
            ...ctx,
            resolvedFields: out
        };
        out.push(await resolveOneEntry({
            entry: entry,
            initialFields: initialFields,
            ctx: scopedCtx,
            randomKey: randomKey2
        }));
    }
    return out;
}

function initialFieldIssues(args) {
    const duplicateIndexes = /* @__PURE__ */ new Map;
    return args.initialFields.forEach((field, index) => {
        const key = `${field.name}\0${field.type}`, indexes = duplicateIndexes.get(key) ?? [];
        indexes.push(index), duplicateIndexes.set(key, indexes);
    }), args.initialFields.flatMap((field, index) => {
        const issues = [], indexes = duplicateIndexes.get(`${field.name}\0${field.type}`) ?? [];
        indexes.length > 1 && issues.push({
            index: index,
            name: field.name,
            type: field.type,
            reason: "duplicate",
            duplicateIndexes: indexes.filter(candidate => candidate !== index)
        });
        const workflowMatches = args.workflowFields.filter(entry => entry.name === field.name);
        if (workflowMatches.length > 0) {
            const exact = workflowMatches.find(entry => entry.type === field.type);
            return exact === void 0 ? issues.push({
                index: index,
                name: field.name,
                type: field.type,
                reason: "wrong-kind",
                expectedTypes: [ ...new Set(workflowMatches.map(entry => entry.type)) ]
            }) : exact.initialValue?.type !== "input" && issues.push({
                index: index,
                name: field.name,
                type: field.type,
                reason: "not-input",
                source: exact.initialValue?.type ?? "working-memory"
            }), issues;
        }
        const scoped = findNonWorkflowDeclaration(args.stages ?? [], field.name);
        return scoped !== void 0 ? issues.push({
            index: index,
            name: field.name,
            type: field.type,
            reason: "invalid-scope",
            ...scoped
        }) : issues.push({
            index: index,
            name: field.name,
            type: field.type,
            reason: "undeclared"
        }), issues;
    });
}

function findNonWorkflowDeclaration(stages, name) {
    for (const stage of stages) {
        const stageField = stage.fields?.find(entry => entry.name === name);
        if (stageField !== void 0) return {
            scope: "stage",
            stage: stage.name,
            declaredType: stageField.type
        };
        for (const activity of stage.activities ?? []) {
            const activityField = activity.fields?.find(entry => entry.name === name);
            if (activityField !== void 0) return {
                scope: "activity",
                stage: stage.name,
                activity: activity.name,
                declaredType: activityField.type
            };
        }
    }
}

function assertInitialFieldsConsumable(args) {
    const issues = initialFieldIssues(args);
    if (issues.length !== 0) throw new InitialFieldsInvalidError({
        ...args.definitionName !== void 0 ? {
            definition: args.definitionName
        } : {},
        issues: issues
    });
}

function missingRequiredInputs(args) {
    return args.entryDefs.filter(entry => entry.required === !0 && isInputSourced(entry)).filter(entry => {
        const match = suppliedFieldFor(entry, args.initialFields);
        return match === void 0 || match.value === void 0 || match.value === null;
    }).map(entry => ({
        name: entry.name,
        type: entry.type
    }));
}

function suppliedFieldFor(entry, initialFields) {
    return initialFields.find(field => field.name === entry.name && field.type === entry.type);
}

function startFieldsParam(args) {
    const out = {};
    for (const entry of args.entryDefs) {
        if (!isInputSourced(entry)) continue;
        const match = suppliedFieldFor(entry, args.initialFields);
        match !== void 0 && match.value !== void 0 && match.value !== null && (out[entry.name] = match.value);
    }
    return out;
}

function assertRequiredInputProvided({entryDefs: entryDefs, initialFields: initialFields, definitionName: definitionName}) {
    const missing = missingRequiredInputs({
        entryDefs: entryDefs,
        initialFields: initialFields
    });
    if (missing.length !== 0) throw new RequiredFieldNotProvidedError({
        ...definitionName !== void 0 ? {
            definition: definitionName
        } : {},
        missing: missing
    });
}

function defaultEntryValue(entryType) {
    return isAlwaysArrayFieldKind(entryType) ? [] : null;
}

function resolveInputValue({entry: entry, initialFields: initialFields, defaultValue: defaultValue}) {
    const initMatch = suppliedFieldFor(entry, initialFields);
    return initMatch === void 0 ? defaultValue : (assertInputValueShape(entry, initMatch.value), 
    initMatch.value);
}

function resolveFieldReadValue({entry: entry, source: source, ctx: ctx, defaultValue: defaultValue}) {
    const sourceEntry = (source.scope === "workflow" ? ctx.workflowFields ?? [] : ctx.resolvedFields ?? []).find(s => s.name === source.field);
    return sourceEntry === void 0 ? defaultValue : resolveFieldRead({
        kind: sourceEntry._type,
        value: sourceEntry.value,
        path: source.path,
        snapshot: void 0,
        targetKind: entry.type
    }) ?? defaultValue;
}

async function resolveQueryValue({entry: entry, source: source, ctx: ctx, client: client, defaultValue: defaultValue}) {
    const params = paramsForLake({
        self: ctx.selfId ?? null,
        fields: fieldMapFromResolved(ctx.resolvedFields ?? []),
        stage: ctx.stageName ?? null,
        activity: ctx.activityName ?? null,
        now: ctx.now,
        tag: ctx.tag
    });
    try {
        const fetchOptions = {
            perspective: ctx.perspective ?? DEFAULT_CONTENT_PERSPECTIVE
        }, result = await client.fetch(source.query, params, fetchOptions);
        return normalizeQueryResult({
            entryType: entry.type,
            raw: result,
            workflowResource: ctx.workflowResource
        }) ?? defaultValue;
    } catch (err) {
        rethrowWithContext(err, `Failed to resolve query entry "${entry.name}" (${entry.type})`);
    }
}

async function resolveEntryValue({entry: entry, initialFields: initialFields, ctx: ctx, defaultValue: defaultValue}) {
    const source = entry.initialValue;
    return source === void 0 ? defaultValue : source.type === "input" ? resolveInputValue({
        entry: entry,
        initialFields: initialFields,
        defaultValue: defaultValue
    }) : source.type === "literal" ? resolveLiteralValue({
        entry: entry,
        source: source,
        ctx: ctx,
        defaultValue: defaultValue
    }) : source.type === "fieldRead" ? resolveFieldReadValue({
        entry: entry,
        source: source,
        ctx: ctx,
        defaultValue: defaultValue
    }) : source.type === "query" && ctx.client !== void 0 ? resolveQueryValue({
        entry: entry,
        source: source,
        ctx: ctx,
        client: ctx.client,
        defaultValue: defaultValue
    }) : defaultValue;
}

function resolveLiteralValue({entry: entry, source: source, ctx: ctx, defaultValue: defaultValue}) {
    const value = source.value ?? defaultValue, home = ctx.workflowResource;
    return home === void 0 ? value : mapFieldRefValues({
        entryType: entry.type,
        value: value,
        fields: entry.fields,
        of: entry.of,
        mapRef: ref => rootRefObject(ref, home)
    });
}

function rootRefObject(value, home) {
    if (typeof value != "object" || value === null || !("id" in value)) return value;
    const id = value.id;
    return typeof id != "string" || !isBareSeedId(id) ? value : {
        ...value,
        id: toPhysicalGdr(id, home)
    };
}

function buildResolvedEntry({entry: entry, value: value, _key: _key, now: now}) {
    return {
        _key: _key,
        _type: entry.type,
        name: entry.name,
        ...entry.title !== void 0 ? {
            title: entry.title
        } : {},
        ...entry.description !== void 0 ? {
            description: entry.description
        } : {},
        ...entry.options !== void 0 ? {
            options: entry.options
        } : {},
        ...entry.validation !== void 0 ? {
            validation: entry.validation
        } : {},
        ...entry.roles !== void 0 ? {
            roles: entry.roles
        } : {},
        value: value,
        ...entry.initialValue?.type === "query" ? {
            resolvedAt: now
        } : {},
        ...entry.type === "object" ? {
            fields: entry.fields ?? []
        } : {},
        ...entry.type === "array" ? {
            of: entry.of ?? []
        } : {},
        ...entry.types !== void 0 ? {
            types: entry.types
        } : {}
    };
}

async function resolveOneEntry({entry: entry, initialFields: initialFields, ctx: ctx, randomKey: randomKey2}) {
    const defaultValue = defaultEntryValue(entry.type), value = await resolveEntryValue({
        entry: entry,
        initialFields: initialFields,
        ctx: ctx,
        defaultValue: defaultValue
    });
    if (entry.initialValue?.type === "query") {
        const check = parseFieldValue({
            entryType: entry.type,
            value: value,
            types: entry.types,
            fields: entry.fields,
            of: entry.of,
            options: entry.options,
            validation: entry.validation,
            roles: entry.roles,
            memberRoles: ctx.memberRoles,
            roleAliases: ctx.roleAliases
        });
        return "issues" in check ? (ctx.recordDiscard?.({
            field: entry.name,
            detail: check.issues.join("; ")
        }), buildResolvedEntry({
            entry: entry,
            value: defaultValue,
            _key: randomKey2(),
            now: ctx.now
        })) : buildResolvedEntry({
            entry: entry,
            value: check.output,
            _key: randomKey2(),
            now: ctx.now
        });
    }
    const validated = validateFieldValue({
        entryType: entry.type,
        entryName: entry.name,
        value: value,
        types: entry.types,
        fields: entry.fields,
        of: entry.of,
        options: entry.options,
        validation: entry.validation,
        roles: entry.roles,
        memberRoles: ctx.memberRoles,
        roleAliases: ctx.roleAliases
    });
    return entry.initialValue?.type === "input" && ctx.inputProvenance !== "definition" && assertRefsWithinSurface({
        entryType: entry.type,
        entryName: entry.name,
        value: validated,
        fields: entry.fields,
        of: entry.of,
        surface: ctx.refSurface
    }), buildResolvedEntry({
        entry: entry,
        value: validated,
        _key: randomKey2(),
        now: ctx.now
    });
}

function fieldMapFromResolved(entries) {
    const out = {};
    for (const s of entries) out[s.name] = s.value;
    return out;
}

function assertInputValueShape(entry, value) {
    if (isSingleDocRefKind(entry.type)) {
        assertGdrShape(value, `field entry "${entry.name}" (${entry.type})`);
        return;
    }
    if (entry.type === "release.ref") {
        assertGdrShape(value, `field entry "${entry.name}" (release.ref)`);
        const v2 = value;
        if (typeof v2.releaseName != "string" || v2.releaseName.length === 0) throw new ContractViolationError(`Invalid input value for field entry "${entry.name}" (release.ref): \`releaseName\` must be a non-empty string. Got ${JSON.stringify(v2.releaseName)}.`);
        return;
    }
    if (entry.type === "doc.refs") {
        if (!Array.isArray(value)) throw new ContractViolationError(`Invalid input value for field entry "${entry.name}" (doc.refs): expected an array of GDRs, got ${typeof value}.`);
        for (const [i, item] of value.entries()) assertGdrShape(item, `field entry "${entry.name}" (doc.refs) item [${i}]`);
    }
}

function assertGdrShape(value, context) {
    if (typeof value != "object" || value === null) throw new ContractViolationError(`Invalid GDR for ${context}: expected { id: "<scheme>:...", type: "<schema>" }, got ${typeof value}.`);
    const v2 = value;
    if (typeof v2.id != "string") throw invalidGdrUriError(context, v2.id);
    try {
        parseGdr(v2.id);
    } catch (error) {
        throw error instanceof VersionSpecificDatasetGdrError ? new ContractViolationError(`Invalid GDR for ${context}: ${error.message}`) : invalidGdrUriError(context, v2.id);
    }
    if (typeof v2.type != "string" || v2.type.length === 0) throw new ContractViolationError(`Invalid GDR for ${context}: \`type\` (schema name) must be a non-empty string. Got ${JSON.stringify(v2.type)}.`);
}

function invalidGdrUriError(context, id) {
    return new ContractViolationError(`Invalid GDR for ${context}: \`id\` must be a GDR URI ("<scheme>:<...id-parts>" with scheme dataset|canvas|media-library|dashboard). Got ${JSON.stringify(id)}. Construct via \`gdrFromResource\` / \`refDataset\` / \`refCanvas\` etc. — bare document ids are not accepted.`);
}

function normalizeQueryResult({entryType: entryType, raw: raw, workflowResource: workflowResource}) {
    if (raw == null) return raw;
    try {
        return isSingleDocRefKind(entryType) ? coerceToGdr(raw, workflowResource) : entryType === "doc.refs" ? Array.isArray(raw) ? raw.map(item => coerceToGdr(item, workflowResource)).filter(v2 => v2 !== null) : [] : raw;
    } catch (error) {
        if (error instanceof VersionSpecificDatasetGdrError) return raw;
        throw error;
    }
}

function coerceGdrShape(raw, workflowResource) {
    if (!("id" in raw) || !("type" in raw)) return null;
    const r = raw;
    return typeof r.id != "string" || typeof r.type != "string" ? null : isGdrUri(r.id) ? {
        id: r.id,
        type: r.type
    } : workflowResource ? {
        id: toPhysicalGdr(r.id, workflowResource),
        type: r.type
    } : null;
}

function coerceRefEnvelope(raw, workflowResource) {
    if (!("_ref" in raw)) return null;
    const r = raw;
    return typeof r._ref != "string" || !workflowResource ? null : {
        id: toPhysicalGdr(r._ref, workflowResource),
        type: "document"
    };
}

function coerceToGdr(raw, workflowResource) {
    return raw == null ? null : typeof raw == "object" ? coerceGdrShape(raw, workflowResource) ?? coerceRefEnvelope(raw, workflowResource) : typeof raw == "string" && workflowResource ? {
        id: toPhysicalGdr(raw, workflowResource),
        type: "document"
    } : null;
}

const NOW = {
    variable: "now",
    path: []
}, LAST_REPRESENTABLE_MS = 864e13;

function readsNowIn(reads) {
    return reads.some(read => read.variable === "now");
}

function readsNow(condition) {
    return readsNowIn(analyzeCondition(condition).reads);
}

async function stageTimeSites(args) {
    const {definition: definition, stage: stage, scopeFor: scopeFor} = args, sites = [];
    for (const site of conditionSitesOf(definition)) {
        if (site.stage !== stage || site.address.kind === "editable-override" || !readsNow(site.condition)) continue;
        const activity = "activity" in site.address ? site.address.activity : void 0;
        sites.push({
            condition: site.condition,
            params: await scopeFor(activity)
        });
    }
    return sites;
}

async function nextEvaluationInstant(args) {
    const {sites: sites, dataset: dataset, now: now} = args, boundaries = [];
    for (const site of sites) {
        const boundary = await siteBoundary({
            ...site,
            dataset: dataset,
            now: now
        });
        boundary !== void 0 && boundaries.push(boundary);
    }
    return [ ...boundaries ].sort(byInstant)[0];
}

function byInstant(a, b) {
    return Date.parse(a) - Date.parse(b);
}

async function siteBoundary(args) {
    const {condition: condition, params: params, dataset: dataset, now: now} = args, insight = await explainCondition({
        condition: condition,
        dataset: dataset,
        params: params
    });
    for (const candidate of await candidateInstants({
        insight: insight,
        params: params,
        dataset: dataset,
        now: now
    })) if ((await whatIfCondition({
        condition: condition,
        dataset: dataset,
        params: params,
        assign: {
            target: NOW,
            value: candidate
        }
    })).changed) return candidate;
}

async function candidateInstants(args) {
    const {insight: insight, params: params, dataset: dataset, now: now} = args, comparands = [];
    for (const atom of insight.atoms) readsNowIn(atom.atom.reads) && comparands.push(...await comparandsOf({
        atom: atom,
        params: params,
        dataset: dataset
    }));
    const instants = comparands.filter(isParseableInstant).map(normalizeInstant).filter(instant => Date.parse(instant) < LAST_REPRESENTABLE_MS).flatMap(instant => [ instant, addMs(instant, 1) ]).filter(instant => !hasPassed(instant, now));
    return [ ...new Set(instants) ].sort(byInstant);
}

function normalizeInstant(value) {
    return new Date(Date.parse(value)).toISOString();
}

async function comparandsOf(args) {
    const {atom: atom, params: params, dataset: dataset} = args, requirement = atom.requirement, values = requirement !== void 0 && requirement.target.variable === "now" && "value" in requirement ? [ requirement.value ] : [];
    for (const read of atom.atom.reads) read.variable !== "now" && values.push(await runGroq({
        groq: formatRead(read),
        params: params,
        dataset: dataset
    }));
    return values;
}

const NonEmpty = NonEmptyString, PersistedAssignmentRolesSchema = v.pipe(v.array(NonEmptyString), v.minLength(1, "assignment roles must not be empty")), UnknownRecord = v.record(v.string(), v.unknown()), PersistedChoiceOptionsSchema = v.looseObject({
    list: v.array(v.looseObject({
        title: v.string(),
        value: v.union([ v.string(), v.number() ])
    }))
}), PersistedScalarValidationSchema = v.looseObject({
    min: v.optional(v.number()),
    max: v.optional(v.number())
}), PersistedFieldShapeSchema = v.lazy(() => v.looseObject(tolerantEntries()({
    type: v.picklist(FIELD_VALUE_KINDS),
    name: NonEmpty,
    title: v.optional(v.string()),
    description: v.optional(v.string()),
    options: v.optional(PersistedChoiceOptionsSchema),
    validation: v.optional(PersistedScalarValidationSchema),
    roles: v.optional(PersistedAssignmentRolesSchema),
    fields: v.optional(v.array(PersistedFieldShapeSchema)),
    of: v.optional(v.array(PersistedFieldShapeSchema))
}))), ExecutionContextSchema = tolerantObject()({
    runtime: v.string(),
    kind: v.exactOptional(v.string()),
    id: v.exactOptional(v.string())
}), OptionalActor = v.exactOptional(ActorShape), WorkflowResourceSchema = tolerantObject()({
    type: v.picklist([ "dataset", "canvas", "media-library", "dashboard" ]),
    id: NonEmpty
}), WorkflowPerspectiveSchema = v.union([ v.picklist([ "raw", "published", "drafts" ]), v.array(v.string()) ]);

function contextArm(type, value) {
    return {
        _key: NonEmpty,
        _type: v.literal(type),
        name: NonEmpty,
        value: value
    };
}

const ContextEntrySchema = v.variant("_type", [ v.looseObject(tolerantEntries()(contextArm("context.string", v.string()))), v.looseObject(tolerantEntries()(contextArm("context.number", v.number()))), v.looseObject(tolerantEntries()(contextArm("context.boolean", v.boolean()))), v.looseObject(tolerantEntries()(contextArm("context.ref", GdrShape))), v.looseObject(tolerantEntries()(contextArm("context.json", v.string()))) ]);

function fieldArm(kind, value) {
    return {
        _key: NonEmpty,
        _type: v.literal(kind),
        name: NonEmpty,
        title: v.exactOptional(v.string()),
        description: v.exactOptional(v.string()),
        options: v.exactOptional(PersistedChoiceOptionsSchema),
        validation: v.exactOptional(PersistedScalarValidationSchema),
        value: value,
        resolvedAt: v.exactOptional(IsoTimestamp)
    };
}

const OptionalRefTypes = v.exactOptional(v.array(v.string())), RawResolvedFieldEntrySchema = v.variant("_type", [ v.looseObject(tolerantEntries()({
    ...fieldArm("doc.ref", fieldValueSchemas["doc.ref"]),
    types: OptionalRefTypes
})), v.looseObject(tolerantEntries()({
    ...fieldArm("doc.refs", fieldValueSchemas["doc.refs"]),
    types: OptionalRefTypes
})), v.looseObject(tolerantEntries()({
    ...fieldArm("subject", fieldValueSchemas.subject),
    types: OptionalRefTypes
})), v.looseObject(tolerantEntries()(fieldArm("release.ref", fieldValueSchemas["release.ref"]))), v.looseObject(tolerantEntries()(fieldArm("string", fieldValueSchemas.string))), v.looseObject(tolerantEntries()(fieldArm("text", fieldValueSchemas.text))), v.looseObject(tolerantEntries()(fieldArm("number", fieldValueSchemas.number))), v.looseObject(tolerantEntries()(fieldArm("progress", fieldValueSchemas.progress))), v.looseObject(tolerantEntries()(fieldArm("boolean", fieldValueSchemas.boolean))), v.looseObject(tolerantEntries()(fieldArm("date", fieldValueSchemas.date))), v.looseObject(tolerantEntries()(fieldArm("dueDate", fieldValueSchemas.dueDate))), v.looseObject(tolerantEntries()(fieldArm("datetime", fieldValueSchemas.datetime))), v.looseObject(tolerantEntries()(fieldArm("dueDatetime", fieldValueSchemas.dueDatetime))), v.looseObject(tolerantEntries()(fieldArm("url", fieldValueSchemas.url))), v.looseObject(tolerantEntries()(fieldArm("actor", fieldValueSchemas.actor))), v.looseObject(tolerantEntries()({
    ...fieldArm("assignee", fieldValueSchemas.assignee),
    roles: v.exactOptional(PersistedAssignmentRolesSchema)
})), v.looseObject(tolerantEntries()({
    ...fieldArm("assignees", fieldValueSchemas.assignees),
    roles: v.exactOptional(PersistedAssignmentRolesSchema)
})), v.looseObject(tolerantEntries()({
    ...fieldArm("object", v.union([ v.null(), UnknownRecord ])),
    fields: v.array(PersistedFieldShapeSchema)
})), v.looseObject(tolerantEntries()({
    ...fieldArm("array", v.array(UnknownRecord)),
    of: v.array(PersistedFieldShapeSchema)
})) ]), ResolvedFieldEntrySchema = v.pipe(RawResolvedFieldEntrySchema, v.transform(normalizeNestedAssignmentValues)), EffectOriginSchema = tolerantObject()({
    kind: v.literal("action"),
    name: NonEmpty
}), PendingEffectClaimSchema = tolerantObject()({
    _type: v.literal("pendingEffect.claim"),
    claimedAt: IsoTimestamp,
    claimedBy: ActorShape,
    leaseExpiresAt: v.exactOptional(IsoTimestamp),
    claimToken: v.exactOptional(NonEmpty)
}), PendingEffectSchema = tolerantObject()({
    _key: NonEmpty,
    _type: v.exactOptional(v.literal("pendingEffect")),
    name: NonEmpty,
    title: v.exactOptional(v.string()),
    description: v.exactOptional(v.string()),
    bindings: v.exactOptional(v.record(v.string(), v.string())),
    params: UnknownRecord,
    origin: EffectOriginSchema,
    actor: OptionalActor,
    queuedAt: IsoTimestamp,
    stageEntryKey: v.exactOptional(NonEmpty),
    claim: v.exactOptional(PendingEffectClaimSchema)
}), EffectRunStatusSchema = v.picklist(EFFECT_RUN_STATUSES), EffectHistoryEntrySchema = tolerantObject()({
    _key: NonEmpty,
    name: NonEmpty,
    title: v.exactOptional(v.string()),
    description: v.exactOptional(v.string()),
    params: UnknownRecord,
    origin: EffectOriginSchema,
    actor: OptionalActor,
    stageEntryKey: v.exactOptional(NonEmpty),
    ranAt: IsoTimestamp,
    durationMs: v.exactOptional(v.number()),
    status: EffectRunStatusSchema,
    detail: v.exactOptional(v.string()),
    error: v.exactOptional(tolerantObject()({
        message: v.string(),
        stack: v.exactOptional(v.string())
    })),
    outputs: v.exactOptional(UnknownRecord)
}), ActivityEntrySchema = tolerantObject()({
    _key: NonEmpty,
    name: NonEmpty,
    status: v.picklist(ACTIVITY_STATUSES),
    startedAt: v.exactOptional(IsoTimestamp),
    completedAt: v.exactOptional(IsoTimestamp),
    completedBy: v.exactOptional(v.string()),
    firedActions: v.exactOptional(v.array(v.string())),
    filterEvaluation: v.exactOptional(tolerantObject()({
        at: IsoTimestamp,
        truthy: v.boolean(),
        unevaluable: v.exactOptional(v.boolean()),
        detail: v.exactOptional(v.string())
    })),
    fields: v.exactOptional(v.array(ResolvedFieldEntrySchema))
}), StageEntrySchema = tolerantObject()({
    _key: NonEmpty,
    name: NonEmpty,
    enteredAt: IsoTimestamp,
    exitedAt: v.exactOptional(IsoTimestamp),
    fields: v.array(ResolvedFieldEntrySchema),
    activities: v.array(ActivityEntrySchema)
}), SubworkflowEntrySchema = tolerantObject()({
    _key: NonEmpty,
    activity: NonEmpty,
    action: NonEmpty,
    definition: NonEmpty,
    stageEntry: NonEmpty,
    cohortStage: NonEmpty,
    rowKey: v.string(),
    ref: GdrShape,
    spawnedAt: IsoTimestamp,
    abortPending: v.exactOptional(tolerantObject()({
        at: IsoTimestamp,
        reason: v.string()
    })),
    resolved: v.exactOptional(tolerantObject()({
        at: IsoTimestamp,
        stage: v.exactOptional(v.string()),
        aborted: v.exactOptional(v.literal(!0))
    }))
}), ProcessedRequestSchema = tolerantObject()({
    _key: NonEmpty,
    _type: v.literal("processedRequest"),
    key: NonEmpty,
    op: NonEmpty,
    processedAt: IsoTimestamp,
    expiresAt: IsoTimestamp
});

function historyArm(type) {
    return {
        _key: NonEmpty,
        _type: v.literal(type),
        at: IsoTimestamp,
        executionContext: v.exactOptional(ExecutionContextSchema)
    };
}

const TransitionVia = v.exactOptional(v.picklist([ "transition", "setStage" ])), HISTORY_ARMS = [ v.looseObject(tolerantEntries()({
    ...historyArm("stageEntered"),
    stage: NonEmpty,
    fromStage: v.exactOptional(v.string()),
    transition: v.exactOptional(v.string()),
    via: TransitionVia,
    reason: v.exactOptional(v.string()),
    actor: OptionalActor
})), v.looseObject(tolerantEntries()({
    ...historyArm("stageExited"),
    stage: NonEmpty,
    toStage: NonEmpty,
    transition: v.exactOptional(v.string()),
    via: TransitionVia,
    reason: v.exactOptional(v.string()),
    actor: OptionalActor
})), v.looseObject(tolerantEntries()({
    ...historyArm("activityStatusChanged"),
    stage: NonEmpty,
    activity: NonEmpty,
    from: v.picklist(ACTIVITY_STATUSES),
    to: v.picklist(ACTIVITY_STATUSES),
    actor: OptionalActor
})), v.looseObject(tolerantEntries()({
    ...historyArm("actionFired"),
    stage: NonEmpty,
    activity: NonEmpty,
    action: NonEmpty,
    actor: OptionalActor,
    driverKind: v.exactOptional(v.picklist(DRIVER_KINDS)),
    triggered: v.exactOptional(v.literal(!0))
})), v.looseObject(tolerantEntries()({
    ...historyArm("transitionFired"),
    fromStage: NonEmpty,
    toStage: NonEmpty,
    transition: v.exactOptional(v.string()),
    via: TransitionVia,
    reason: v.exactOptional(v.string()),
    actor: OptionalActor
})), v.looseObject(tolerantEntries()({
    ...historyArm("effectQueued"),
    effectKey: NonEmpty,
    effect: NonEmpty,
    origin: EffectOriginSchema
})), v.looseObject(tolerantEntries()({
    ...historyArm("effectCompleted"),
    effectKey: NonEmpty,
    effect: NonEmpty,
    status: EffectRunStatusSchema,
    outputs: v.exactOptional(UnknownRecord),
    detail: v.exactOptional(v.string()),
    actor: OptionalActor
})), v.looseObject(tolerantEntries()({
    ...historyArm("effectClaimReleased"),
    effectKey: NonEmpty,
    effect: NonEmpty,
    claim: PendingEffectClaimSchema,
    via: v.picklist([ "drain-takeover", "sweep" ]),
    actor: OptionalActor
})), v.looseObject(tolerantEntries()({
    ...historyArm("spawned"),
    activity: NonEmpty,
    instanceRef: GdrShape,
    rowKey: v.exactOptional(v.string()),
    actor: OptionalActor
})), v.looseObject(tolerantEntries()({
    ...historyArm("subworkflowAdopted"),
    stage: NonEmpty,
    activity: NonEmpty,
    instanceRef: GdrShape,
    rowKey: v.string()
})), v.looseObject(tolerantEntries()({
    ...historyArm("subworkflowResolved"),
    activity: NonEmpty,
    instanceRef: GdrShape,
    status: v.picklist([ "done", "aborted" ])
})), v.looseObject(tolerantEntries()({
    ...historyArm("subworkflowOrphaned"),
    instanceRef: GdrShape,
    detail: v.string()
})), v.looseObject(tolerantEntries()({
    ...historyArm("aborted"),
    stage: NonEmpty,
    reason: v.exactOptional(v.string()),
    actor: OptionalActor
})), v.looseObject(tolerantEntries()({
    ...historyArm("opApplied"),
    stage: NonEmpty,
    activity: v.exactOptional(v.string()),
    action: v.exactOptional(v.string()),
    edit: v.exactOptional(v.literal(!0)),
    effect: v.exactOptional(v.string()),
    opType: NonEmpty,
    target: v.exactOptional(tolerantObject()({
        scope: v.picklist(FIELD_SCOPES),
        field: v.string()
    })),
    resolved: v.exactOptional(UnknownRecord),
    actor: OptionalActor
})), v.looseObject(tolerantEntries()({
    ...historyArm("fieldQueryDiscarded"),
    scope: v.picklist(FIELD_SCOPES),
    field: NonEmpty,
    detail: v.string()
})) ], knownHistoryTypes = new Set(HISTORY_ARMS.map(arm => arm.entries._type.literal)), UnknownHistoryEntry = v.looseObject({
    _key: NonEmpty,
    _type: v.pipe(v.string(), v.check(t => !knownHistoryTypes.has(t), "must not shadow a known history variant"))
}), HistoryEntrySchema = v.union([ v.variant("_type", HISTORY_ARMS), UnknownHistoryEntry ]), WorkflowInstanceSchema = v.looseObject(tolerantEntries()({
    _id: NonEmpty,
    _type: v.literal(WORKFLOW_INSTANCE_TYPE),
    _rev: v.string(),
    _createdAt: IsoTimestamp,
    _updatedAt: IsoTimestamp,
    modelVersion: v.exactOptional(v.number()),
    minReaderModel: v.exactOptional(v.number()),
    tag: NonEmpty,
    workflowResource: WorkflowResourceSchema,
    definition: NonEmpty,
    pinnedVersion: v.number(),
    pinnedContentHash: v.exactOptional(v.string()),
    definitionSnapshot: v.string(),
    fields: v.array(ResolvedFieldEntrySchema),
    context: v.array(ContextEntrySchema),
    ancestors: v.array(GdrShape),
    perspective: v.exactOptional(WorkflowPerspectiveSchema),
    currentStage: NonEmpty,
    stages: v.array(StageEntrySchema),
    subworkflows: v.exactOptional(v.array(SubworkflowEntrySchema)),
    pendingEffects: v.array(PendingEffectSchema),
    effectHistory: v.array(EffectHistoryEntrySchema),
    history: v.array(HistoryEntrySchema),
    processedRequests: v.exactOptional(v.array(ProcessedRequestSchema)),
    startedAt: IsoTimestamp,
    lastChangedAt: IsoTimestamp,
    completedAt: v.exactOptional(IsoTimestamp),
    abortedAt: v.exactOptional(IsoTimestamp)
}));

function parseInstanceDocument(doc) {
    return parsePersistedDoc({
        schema: WorkflowInstanceSchema,
        doc: doc,
        docType: WORKFLOW_INSTANCE_TYPE
    });
}

const WorkflowInstancePreviewEffectSchema = tolerantObject()({
    name: NonEmpty,
    title: v.exactOptional(v.string())
}), WorkflowInstancePreviewSchema = v.looseObject(tolerantEntries()({
    _id: NonEmpty,
    _type: v.literal(WORKFLOW_INSTANCE_TYPE),
    modelVersion: v.exactOptional(v.number()),
    minReaderModel: v.exactOptional(v.number()),
    workflowResource: WorkflowResourceSchema,
    definition: NonEmpty,
    pinnedVersion: v.number(),
    fields: v.array(ResolvedFieldEntrySchema),
    ancestors: v.array(GdrShape),
    perspective: v.exactOptional(WorkflowPerspectiveSchema),
    currentStage: NonEmpty,
    stages: v.array(StageEntrySchema),
    claimedEffects: v.array(WorkflowInstancePreviewEffectSchema),
    failedEffects: v.array(WorkflowInstancePreviewEffectSchema),
    startedAt: IsoTimestamp,
    completedAt: v.exactOptional(IsoTimestamp),
    abortedAt: v.exactOptional(IsoTimestamp)
}));

function parseInstancePreviewDocument(doc) {
    return parsePersistedDoc({
        schema: WorkflowInstancePreviewSchema,
        doc: doc,
        docType: WORKFLOW_INSTANCE_TYPE
    });
}

const SYNC_COMMIT = {
    visibility: "sync"
}, ENGINE_API_VERSION = "2026-04-29", REQUEST_TAG = {
    engine: "workflow",
    deploy: "workflow.deploy",
    deleteDefinition: "workflow.delete-definition",
    start: "workflow.start",
    fireAction: "workflow.fire-action",
    editField: "workflow.edit-field",
    completeEffect: "workflow.complete-effect",
    commitEffectOps: "workflow.commit-effect-ops",
    tick: "workflow.tick",
    evaluate: "workflow.evaluate",
    evaluateStart: "workflow.evaluate-start",
    diagnose: "workflow.diagnose",
    availableActions: "workflow.available-actions",
    setStage: "workflow.set-stage",
    abort: "workflow.abort",
    resetActivity: "workflow.reset-activity",
    getInstance: "workflow.get-instance",
    children: "workflow.children",
    instancesForDocument: "workflow.instances-for-document",
    definitionsForDocument: "workflow.definitions-for-document",
    query: "workflow.query",
    discover: "workflow.discover",
    guardQuery: "workflow.guard.query",
    guardDeploy: "workflow.guard.deploy",
    guardRefresh: "workflow.guard.refresh",
    guardRetract: "workflow.guard.retract",
    effectList: "workflow.effect.list",
    effectFind: "workflow.effect.find",
    drain: "workflow.drain",
    effect: "workflow.effect",
    verifyDefinitions: "workflow.verify-definitions",
    accessResolveActor: "workflow.access.resolve-actor",
    accessGrants: "workflow.access.grants",
    accessResolveOrg: "workflow.access.resolve-org",
    accessAttributes: "workflow.access.attributes"
}, RAW_PATCH = /* @__PURE__ */ Symbol("workflow-engine.raw-patch");

function unwrapPatch(patch) {
    return typeof patch == "object" && patch !== null && RAW_PATCH in patch ? patch[RAW_PATCH] : patch;
}

const wrapperCache = /* @__PURE__ */ new WeakMap, effectHandlerClientCache = /* @__PURE__ */ new WeakMap, RAW_CLIENT = /* @__PURE__ */ Symbol("workflow-engine.raw-client");

function unwrapRequestTag(client) {
    let current = client;
    for (;RAW_CLIENT in current; ) current = current[RAW_CLIENT];
    return current;
}

function withRequestTag(client, fallback) {
    let byTag = wrapperCache.get(client);
    byTag === void 0 && (byTag = /* @__PURE__ */ new Map, wrapperCache.set(client, byTag));
    const cached = byTag.get(fallback);
    if (cached !== void 0) return cached;
    const wrapped = buildTaggedClient(client, fallback);
    return byTag.set(fallback, wrapped), wrapped;
}

function effectHandlerClient(client) {
    const cached = effectHandlerClientCache.get(client);
    if (cached !== void 0) return cached;
    let tagged;
    if (client.withConfig === void 0) tagged = withRequestTag(client, REQUEST_TAG.effect); else {
        const prefix = clientRequestTagPrefix(client), effectTag = composeRequestTag(prefix, REQUEST_TAG.effect);
        tagged = client.withConfig({
            requestTagPrefix: effectTag
        });
    }
    return effectHandlerClientCache.set(client, tagged), tagged;
}

function effectHandlerResolver(resolver) {
    return mapResourceClientResolver(resolver, effectHandlerClient);
}

function mapResourceClientResolver(resolver, mapClient) {
    if (resolver !== void 0) return parsed => {
        const client = resolver(parsed);
        return client === void 0 ? void 0 : mapClient(client);
    };
}

function isWorkflowsFamilyPrefix(prefix) {
    return prefix === void 0 ? !1 : prefix === "sanity.workflows" || prefix.startsWith("sanity.workflows.") || prefix === "sanity.workflows-mcp";
}

function clientRequestTagPrefix(client) {
    const probe = client;
    return typeof probe.config == "function" ? probe.config().requestTagPrefix : void 0;
}

function composeRequestTag(prefix, tag) {
    if (prefix === void 0) return tag;
    const relative = isWorkflowsFamilyPrefix(prefix) ? stripWorkflowRoot(tag) : tag;
    return `${prefix}.${relative}`;
}

function stripWorkflowRoot(tag) {
    return tag.startsWith("workflow.") ? tag.slice(9) : tag;
}

function buildTaggedClient(client, fallback) {
    const strip = isWorkflowsFamilyPrefix(clientRequestTagPrefix(unwrapRequestTag(client))), normalize = tag => strip ? stripWorkflowRoot(tag) : tag, stamp = options => options === void 0 ? {
        tag: normalize(fallback)
    } : options.tag === void 0 ? {
        ...options,
        tag: normalize(fallback)
    } : typeof options.tag == "string" && options.tag !== normalize(options.tag) ? {
        ...options,
        tag: normalize(options.tag)
    } : options, wrapPatch = patch => {
        let target = patch;
        const wrapped = {
            get [RAW_PATCH]() {
                return target;
            },
            set: props => (target = target.set(props), wrapped),
            setIfMissing: props => (target = target.setIfMissing(props), wrapped),
            unset: paths => (target = target.unset(paths), wrapped),
            ifRevisionId: rev => (target = target.ifRevisionId(rev), wrapped),
            commit: options => target.commit(stamp(options))
        };
        return wrapped;
    }, wrapTransaction = tx => {
        let target = tx;
        const wrapped = {
            create: doc => (target = target.create(doc), wrapped),
            patch: patch => (target = target.patch(unwrapPatch(patch)), wrapped),
            delete: id => (target = target.delete(id), wrapped),
            commit: options => target.commit(stamp(options))
        };
        return wrapped;
    }, overrides = {
        [RAW_CLIENT]: unwrapRequestTag(client),
        fetch: (query, params, options) => client.fetch(query, params, stamp(options)),
        getDocument: (id, options) => client.getDocument(id, stamp(options)),
        patch: documentId => wrapPatch(client.patch(documentId)),
        create: (doc, options) => client.create(doc, stamp(options)),
        transaction: () => wrapTransaction(client.transaction()),
        ...client.withConfig !== void 0 ? {
            withConfig: config => withRequestTag(client.withConfig(config), fallback)
        } : {},
        ...client.action !== void 0 ? {
            action: (action, options) => client.action(action, stamp(options))
        } : {},
        ...client.request !== void 0 ? {
            request: opts => client.request(stamp(opts))
        } : {}
    };
    return new Proxy(client, {
        has: (target, property) => Object.hasOwn(overrides, property) || property in target,
        get: (target, property) => {
            if (Object.hasOwn(overrides, property)) return Reflect.get(overrides, property, overrides);
            const value = Reflect.get(target, property, target);
            return typeof value == "function" ? value.bind(target) : value;
        }
    });
}

const pinCache = /* @__PURE__ */ new WeakMap, enginePinned = /* @__PURE__ */ new WeakSet;

function pinApiVersion(client) {
    const raw = unwrapRequestTag(client);
    if (enginePinned.has(raw) || raw.withConfig === void 0) return client;
    let sibling = pinCache.get(raw);
    return sibling === void 0 && (sibling = raw.withConfig({
        apiVersion: ENGINE_API_VERSION
    }), pinCache.set(raw, sibling), enginePinned.add(unwrapRequestTag(sibling))), sibling;
}

function taggedResolver(resolver, fallback) {
    return mapResourceClientResolver(resolver, client => withRequestTag(pinApiVersion(client), fallback));
}

function taggedScope(args, tag) {
    const resourceClients = taggedResolver(args.resourceClients, tag);
    return {
        ...args,
        client: withRequestTag(pinApiVersion(args.client), tag),
        ...resourceClients !== void 0 ? {
            resourceClients: resourceClients
        } : {}
    };
}

const ACTOR_KIND_SET = new Set(ACTOR_KINDS), resolutionCache = /* @__PURE__ */ new WeakMap, warnedInstances = /* @__PURE__ */ new WeakMap;

async function normalizeInstanceIdentities(args) {
    const {client: client, instance: instance} = args, pending = collectLegacyIds(instance);
    if (pending.size === 0) return instance;
    const resolved = await resolveLegacyIds({
        client: client,
        instance: instance,
        pending: pending
    }), unresolved = [ ...pending ].filter(id => !resolved.has(id));
    return unresolved.length > 0 ? (warnUnresolvedOnce({
        client: client,
        instance: instance,
        unresolved: unresolved
    }), instance) : rewriteIdentitySlots(instance, id => resolved.get(id));
}

async function resolveLegacyIds(args) {
    const resolved = /* @__PURE__ */ new Map, directoryIds = [];
    for (const id of args.pending) {
        const {globalId: globalId} = classifyPrincipalId(id);
        globalId !== void 0 ? resolved.set(id, globalId) : directoryIds.push(id);
    }
    const projectId = anchorProjectId(args.instance.workflowResource);
    if (directoryIds.length === 0 || projectId === void 0) return resolved;
    const fromDirectory = await resolveThroughDirectory({
        client: args.client,
        projectId: projectId,
        ids: directoryIds
    });
    for (const [id, globalId] of fromDirectory) resolved.set(id, globalId);
    return resolved;
}

function warnUnresolvedOnce(args) {
    const key = unwrapRequestTag(args.client);
    let warned = warnedInstances.get(key);
    warned === void 0 && (warned = /* @__PURE__ */ new Set, warnedInstances.set(key, warned)), 
    !warned.has(args.instance._id) && (warned.add(args.instance._id), console.warn(`workflow: instance "${args.instance._id}" carries ${args.unresolved.length} legacy project-scoped principal id(s) the project-users directory could not resolve (${args.unresolved.join(", ")}). Its identity values are left as stored — comparisons against them fail closed — and the document does not self-heal until they resolve.`));
}

function anchorProjectId(resource) {
    if (resource.type === "dataset") return datasetResourceParts(resource.id).projectId;
}

function collectLegacyIds(instance) {
    const found = /* @__PURE__ */ new Set;
    return rewriteIdentitySlots(instance, id => {
        classifyPrincipalId(id).namespace === "project" && found.add(id);
    }), found;
}

function rewriteIdentitySlots(instance, visit) {
    const record = instance, fields = walkEntries(record.fields, visit), stages = walkStages(record.stages, visit);
    return fields === record.fields && stages === record.stages ? instance : {
        ...record,
        fields: fields,
        stages: stages
    };
}

function walkStages(stages, visit) {
    return Array.isArray(stages) ? shareMap(stages, stage => {
        const walked = walkEntryRow({
            row: stage,
            visit: visit
        });
        if (walked === null || typeof walked != "object") return walked;
        const record = walked, activities = walkActivities(record.activities, visit);
        return activities === record.activities ? walked === stage ? stage : walked : {
            ...record,
            activities: activities
        };
    }) : stages;
}

function walkActivities(activities, visit) {
    return Array.isArray(activities) ? shareMap(activities, activity => walkEntryRow({
        row: activity,
        visit: visit
    })) : activities;
}

function walkEntryRow(args) {
    const {row: row, visit: visit} = args;
    if (row === null || typeof row != "object") return row;
    const record = row, patch = {}, fields = walkEntries(record.fields, visit);
    if (fields !== record.fields && (patch.fields = fields), typeof record.completedBy == "string") {
        const stamped = visit(record.completedBy) ?? record.completedBy;
        stamped !== record.completedBy && (patch.completedBy = stamped);
    }
    return Object.keys(patch).length === 0 ? row : {
        ...record,
        ...patch
    };
}

function walkEntries(entries, visit) {
    return Array.isArray(entries) ? shareMap(entries, entry => {
        if (entry === null || typeof entry != "object") return entry;
        const record = entry, value = walkDeclaredValue({
            type: record._type,
            fields: record.fields,
            of: record.of,
            value: record.value,
            visit: visit
        });
        return value === record.value ? entry : {
            ...record,
            value: value
        };
    }) : entries;
}

function collectDeclaredGlobalIds(args) {
    const found = /* @__PURE__ */ new Set;
    return walkDeclaredValue({
        type: args.type,
        fields: args.fields,
        of: args.of,
        value: args.value,
        visit: id => {
            classifyPrincipalId(id).namespace === "global" && found.add(id);
        }
    }), found;
}

function rewriteDeclaredPrincipalIds(args) {
    return walkDeclaredValue({
        type: args.type,
        fields: args.fields,
        of: args.of,
        value: args.value,
        visit: id => args.map.get(id)
    });
}

function walkDeclaredValue(args) {
    const {type: type, value: value, visit: visit} = args;
    return type === "actor" || type === "assignee" ? visitPrincipalObject(value, visit) : type === "assignees" ? Array.isArray(value) ? shareMap(value, item => visitPrincipalObject(item, visit)) : value : type === "object" ? walkDeclaredObject({
        value: value,
        shapes: args.fields,
        visit: visit
    }) : type === "array" && Array.isArray(value) ? shareMap(value, row => walkDeclaredObject({
        value: row,
        shapes: args.of,
        visit: visit
    })) : value;
}

function walkDeclaredObject(args) {
    const {value: value, shapes: shapes, visit: visit} = args;
    if (value === null || typeof value != "object" || Array.isArray(value) || !Array.isArray(shapes)) return value;
    const record = value, patch = {};
    for (const shape of shapes) {
        const step = walkDeclaredProperty({
            record: record,
            shape: shape,
            visit: visit
        });
        step !== void 0 && (patch[step.name] = step.value);
    }
    return Object.keys(patch).length === 0 ? value : {
        ...record,
        ...patch
    };
}

function walkDeclaredProperty(args) {
    const {record: record, shape: shape, visit: visit} = args;
    if (shape === null || typeof shape != "object") return;
    const name = shape.name;
    if (typeof name != "string" || !Object.hasOwn(record, name)) return;
    const walked = walkDeclaredValue({
        type: shape.type,
        fields: shape.fields,
        of: shape.of,
        value: record[name],
        visit: visit
    });
    return walked === record[name] ? void 0 : {
        name: name,
        value: walked
    };
}

function visitPrincipalObject(value, visit) {
    if (value === null || typeof value != "object" || Array.isArray(value)) return value;
    const record = value;
    if (typeof record.id != "string" || record.type !== "user" && !(typeof record.kind == "string" && ACTOR_KIND_SET.has(record.kind))) return value;
    const stamped = visit(record.id) ?? record.id;
    return stamped === record.id ? value : {
        ...record,
        id: stamped
    };
}

function shareMap(items, fn) {
    let changed = !1;
    const next = items.map(item => {
        const out = fn(item);
        return out !== item && (changed = !0), out;
    });
    return changed ? next : items;
}

const SAFE_ID = /^[A-Za-z0-9_-]+$/;

async function resolveThroughDirectory(args) {
    const {client: client, projectId: projectId, ids: ids} = args, cache = cacheFor(unwrapRequestTag(client)), out = /* @__PURE__ */ new Map, safeMisses = ids.filter(id => {
        const hit = cache.get(id);
        return typeof hit == "string" && out.set(id, hit), hit === void 0;
    }).filter(id => SAFE_ID.test(id) && SAFE_ID.test(projectId));
    if (safeMisses.length === 0 || client.request === void 0) return out;
    try {
        const response = await client.request({
            uri: `/projects/${encodeURIComponent(projectId)}/users/${safeMisses.map(encodeURIComponent).join(",")}`,
            tag: REQUEST_TAG.accessResolveActor
        });
        ingestDirectoryRows({
            response: response,
            requested: new Set(safeMisses),
            cache: cache,
            out: out
        });
        for (const id of safeMisses) out.has(id) || cache.set(id, null);
    } catch (err) {
        console.warn(`workflow: project-users directory lookup failed for project "${projectId}" — legacy principal ids stay unresolved this pass. Original error: ${errorMessage(err)}`);
    }
    return out;
}

function cacheFor(client) {
    let cache = resolutionCache.get(client);
    return cache === void 0 && (cache = /* @__PURE__ */ new Map, resolutionCache.set(client, cache)), 
    cache;
}

function ingestDirectoryRows(args) {
    const records = Array.isArray(args.response) ? args.response : [ args.response ];
    for (const row of records) {
        if (typeof row?.id != "string" || !args.requested.has(row.id)) continue;
        const globalId = directoryBridgeId(row.sanityUserId);
        globalId !== void 0 && (args.cache.set(row.id, globalId), args.out.set(row.id, globalId));
    }
}

async function getInstanceDocument(client, instanceId) {
    const doc = await client.getDocument(instanceId);
    if (doc) return normalizeInstanceIdentities({
        client: client,
        instance: readInstanceDoc(doc)
    });
}

function readInstanceDoc(doc) {
    return parseInstanceDocument(assertReadableModel(doc));
}

function readInstancePreviewDoc(doc) {
    return hasDocumentId(doc) ? parseInstancePreviewDocument(withoutProjectedNulls(assertReadableModel(doc))) : parseInstancePreviewDocument(doc);
}

function hasDocumentId(doc) {
    return typeof doc == "object" && doc !== null && typeof doc._id == "string";
}

function withoutProjectedNulls(doc) {
    const row = withoutNullMembers(doc);
    if (typeof row != "object" || row === null) return row;
    const out = {
        ...row
    };
    return Array.isArray(out.stages) && (out.stages = out.stages.map(withoutStageEntryNulls)), 
    Array.isArray(out.claimedEffects) && (out.claimedEffects = out.claimedEffects.map(withoutNullMembers)), 
    Array.isArray(out.failedEffects) && (out.failedEffects = out.failedEffects.map(withoutNullMembers)), 
    out;
}

function withoutStageEntryNulls(entry) {
    const stage = withoutNullMembers(entry);
    if (typeof stage != "object" || stage === null) return stage;
    const out = {
        ...stage
    };
    return Array.isArray(out.activities) && (out.activities = out.activities.map(withoutNullMembers)), 
    out;
}

function withoutNullMembers(value) {
    return value === null || typeof value != "object" || Array.isArray(value) ? value : Object.fromEntries(Object.entries(value).filter(([, member]) => member !== null));
}

async function reload({client: client, instanceId: instanceId, tag: tag}) {
    const doc = await getInstanceDocument(client, instanceId);
    if (!doc) throw new InstanceNotFoundError({
        instanceId: instanceId
    });
    if (doc.tag !== tag) throw new InstanceNotFoundError({
        instanceId: instanceId,
        detail: "not visible to this engine: tag mismatch"
    });
    return doc;
}

function contextEntry(name, value) {
    const _key = randomKey();
    return typeof value == "string" ? {
        _key: _key,
        _type: "context.string",
        name: name,
        value: value
    } : typeof value == "number" ? {
        _key: _key,
        _type: "context.number",
        name: name,
        value: value
    } : typeof value == "boolean" ? {
        _key: _key,
        _type: "context.boolean",
        name: name,
        value: value
    } : {
        _key: _key,
        _type: "context.ref",
        name: name,
        value: value
    };
}

function contextJsonEntry(name, value) {
    return {
        _key: randomKey(),
        _type: "context.json",
        name: name,
        value: serializeContextValue(name, value)
    };
}

function serializeContextValue(name, value) {
    let json;
    try {
        json = JSON.stringify(value);
    } catch (err) {
        rethrowWithContext(err, `context entry "${name}" holds unserializable JSON`);
    }
    if (json === void 0) throw new Error(`context entry "${name}" holds a non-JSON value (undefined, a function, or a symbol)`);
    return json;
}

function buildInstanceBase(args) {
    const {id: id, now: now, actor: actor, perspective: perspective, executionContext: executionContext} = args, entries = [ {
        _key: randomKey(),
        _type: "stageEntered",
        at: now,
        stage: args.initialStage,
        ...actor !== void 0 ? {
            actor: actor
        } : {}
    }, ...args.extraHistory ?? [] ], history = executionContext !== void 0 ? stampHistoryEntries(entries, executionContext) : entries, body = {
        _id: id,
        _type: WORKFLOW_INSTANCE_TYPE,
        _rev: "",
        _createdAt: now,
        _updatedAt: now,
        tag: args.tag,
        workflowResource: args.workflowResource,
        definition: args.definitionName,
        pinnedVersion: args.pinnedVersion,
        ...args.pinnedContentHash !== void 0 ? {
            pinnedContentHash: args.pinnedContentHash
        } : {},
        definitionSnapshot: JSON.stringify(args.definition),
        fields: args.fields,
        context: args.context,
        ancestors: args.ancestors,
        ...perspective !== void 0 ? {
            perspective: perspective
        } : {},
        currentStage: args.initialStage,
        stages: [],
        subworkflows: [],
        pendingEffects: [],
        effectHistory: [],
        history: history,
        startedAt: now,
        lastChangedAt: now
    };
    return {
        ...body,
        ...modelStampFor({
            documentType: "instance",
            document: body
        })
    };
}

function instanceDocumentForWrite(instance) {
    const listModel = writesSingularAssignmentLists(instance);
    return {
        ...instance,
        fields: assignmentFieldsForWrite(instance.fields, listModel),
        stages: assignmentStagesForWrite(instance.stages, listModel)
    };
}

function memoizedBy(compute) {
    const cache = /* @__PURE__ */ new Map;
    return key => {
        const hit = cache.get(key);
        if (hit !== void 0) return hit;
        const value = (async () => compute(key))();
        return cache.set(key, value), value;
    };
}

async function hydrateSnapshot(args) {
    const {client: client, clientForGdr: clientForGdr, instance: instance, overlay: overlay} = args, loaded = [], visited = /* @__PURE__ */ new Set;
    loaded.push({
        doc: instance,
        resource: instance.workflowResource
    }), visited.add(selfGdr(instance));
    const {pending: pending, ordered: ordered} = planReads({
        client: client,
        clientForGdr: clientForGdr,
        instance: instance,
        overlay: overlay,
        visited: visited
    }), fetched = await readPending(pending);
    for (const entry of ordered) if (!isPendingRead(entry)) loaded.push(entry); else {
        const doc = fetched.get(entry);
        doc !== void 0 && loaded.push({
            doc: doc,
            resource: entry.resource
        });
    }
    return buildSnapshot({
        docs: loaded
    });
}

function planReads(args) {
    const {client: client, clientForGdr: clientForGdr, instance: instance, overlay: overlay, visited: visited} = args, pending = [], ordered = [];
    for (const ref of collectWatchRefs(instance)) {
        if (visited.has(ref.id)) continue;
        visited.add(ref.id);
        const held = overlay?.get(ref.id);
        if (held !== void 0) ordered.push(held); else {
            const read = routeRead({
                defaultClient: client,
                clientForGdr: clientForGdr,
                defaultResource: instance.workflowResource,
                uri: ref.id,
                perspective: readsRaw(ref) ? "raw" : instance.perspective ?? DEFAULT_CONTENT_PERSPECTIVE
            });
            pending.push(read), ordered.push(read);
        }
    }
    return {
        pending: pending,
        ordered: ordered
    };
}

function isPendingRead(entry) {
    return "client" in entry;
}

const SNAPSHOT_READ_BATCH_SIZE = 100, SNAPSHOT_READ_CONCURRENCY = 4, snapshotDocsQuery = "*[_id in $ids]";

function routeRead({defaultClient: defaultClient, clientForGdr: clientForGdr, defaultResource: defaultResource, uri: uri, perspective: perspective}) {
    const parsed = tryParseGdr(uri);
    return parsed === void 0 ? {
        client: defaultClient,
        id: uri,
        perspective: perspective,
        resource: defaultResource
    } : {
        client: clientForGdr(parsed),
        id: parsed.documentId,
        perspective: perspective,
        resource: resourceFromParsed(parsed)
    };
}

async function readPending(reads) {
    const batches = groupReads(reads).flatMap(group => chunks(group.reads, SNAPSHOT_READ_BATCH_SIZE).map(batch => ({
        group: group,
        batch: batch
    }))), results = [];
    for (const wave of chunks(batches, SNAPSHOT_READ_CONCURRENCY)) results.push(...await Promise.all(wave.map(readBatch)));
    return new Map(results.flat());
}

async function readBatch(args) {
    const {group: group, batch: batch} = args, ids = batch.map(read => read.id), docs = await group.client.fetch(snapshotDocsQuery, {
        ids: ids
    }, {
        perspective: group.perspective
    }), byId = new Map(docs.map(doc => [ doc._id, validateRawDoc(doc, group.perspective) ]));
    return batch.flatMap(read => {
        const doc = byId.get(read.id);
        return doc === void 0 ? [] : [ [ read, doc ] ];
    });
}

function validateRawDoc(doc, perspective) {
    return perspective !== "raw" ? doc : doc._type === WORKFLOW_INSTANCE_TYPE ? readInstanceDoc(doc) : assertReadableModel(doc);
}

function groupReads(reads) {
    const byClient = /* @__PURE__ */ new Map;
    for (const read of reads) {
        let clientGroups = byClient.get(read.client);
        clientGroups === void 0 && (clientGroups = /* @__PURE__ */ new Map, byClient.set(read.client, clientGroups));
        const key = JSON.stringify(read.perspective);
        let group = clientGroups.get(key);
        group === void 0 && (group = {
            client: read.client,
            perspective: read.perspective,
            reads: []
        }, clientGroups.set(key, group)), group.reads.push(read);
    }
    return [ ...byClient.values() ].flatMap(groups => [ ...groups.values() ]);
}

function chunks(values, size) {
    return Array.from({
        length: Math.ceil(values.length / size)
    }, (_, index) => values.slice(index * size, (index + 1) * size));
}

function collectEntryDocUris(resolvedFieldEntries) {
    return entryDocRefs(resolvedFieldEntries).map(ref => ref.id);
}

function isEngineContext(ctx) {
    return "client" in ctx;
}

function loadCallContext({client: client, instanceId: instanceId, options: options}) {
    return loadContext({
        client: client,
        instanceId: instanceId,
        options: {
            clientForGdr: options.clientForGdr,
            refSurface: options.refSurface,
            ...options.actor ? {
                actor: options.actor
            } : {},
            ...options.clock ? {
                clock: options.clock
            } : {},
            ...options.executionContext ? {
                executionContext: options.executionContext
            } : {},
            ...options.telemetry ? {
                telemetry: options.telemetry
            } : {},
            memberRolesLoader: options.memberRolesLoader
        }
    });
}

async function retryOnRevisionConflict(args) {
    const {client: client, instanceId: instanceId, options: options, commit: commit, onExhausted: onExhausted} = args;
    return retryCommit({
        load: () => loadCallContext({
            client: client,
            instanceId: instanceId,
            options: options
        }),
        commit: commit,
        shouldRetry: isInstanceCommitRevisionConflict,
        onExhausted: onExhausted
    });
}

async function retryCommit(args) {
    const {load: load, commit: commit, shouldRetry: shouldRetry, onExhausted: onExhausted} = args;
    for (let attempt = 1; attempt <= CONCURRENT_COMMIT_MAX_ATTEMPTS; attempt++) {
        const ctx = await load();
        try {
            return await commit(ctx);
        } catch (error) {
            if (!shouldRetry(error)) throw error;
        }
    }
    throw onExhausted();
}

async function loadContext({client: client, instanceId: instanceId, options: options}) {
    const instance = await getInstanceDocument(client, instanceId);
    if (!instance) throw new InstanceNotFoundError({
        instanceId: instanceId
    });
    return buildEngineContext({
        client: client,
        clientForGdr: options.clientForGdr,
        refSurface: options.refSurface,
        instance: instance,
        definition: parseDefinitionSnapshot(instance),
        ...options.actor !== void 0 ? {
            actor: options.actor
        } : {},
        ...options.clock !== void 0 ? {
            clock: options.clock
        } : {},
        ...options.executionContext !== void 0 ? {
            executionContext: options.executionContext
        } : {},
        ...options.telemetry !== void 0 ? {
            telemetry: options.telemetry
        } : {},
        memberRolesLoader: options.memberRolesLoader,
        ...options.overlay !== void 0 ? {
            overlay: options.overlay
        } : {},
        ...options.settlingCohorts !== void 0 ? {
            settlingCohorts: options.settlingCohorts
        } : {}
    });
}

async function ctxConditionParams(ctx, opts) {
    return renderConditionScope({
        instance: ctx.instance,
        definition: ctx.definition,
        snapshot: ctx.snapshot,
        now: ctx.now
    }, opts);
}

async function ctxNextEvaluationAt(ctx) {
    if (ctx.instance.completedAt !== void 0) return;
    const scopeFor = memoizedBy(activityName => ctxConditionParams(ctx, activityName === void 0 ? void 0 : {
        activityName: activityName
    })), sites = await stageTimeSites({
        definition: ctx.definition,
        stage: ctx.instance.currentStage,
        scopeFor: scopeFor
    });
    if (sites.length !== 0) return nextEvaluationInstant({
        sites: sites,
        dataset: ctx.snapshot.docs,
        now: ctx.now
    });
}

async function ctxEvaluateConditionOutcome({ctx: ctx, condition: condition, opts: opts}) {
    return condition === void 0 ? "satisfied" : evaluateConditionOutcome({
        condition: condition,
        snapshot: ctx.snapshot,
        params: await ctxConditionParams(ctx, opts)
    });
}

async function ctxEvaluateCondition({ctx: ctx, condition: condition, opts: opts}) {
    return await ctxEvaluateConditionOutcome({
        ctx: ctx,
        condition: condition,
        opts: opts
    }) === "satisfied";
}

async function buildEngineContext(args) {
    const {client: client, clientForGdr: clientForGdr, refSurface: refSurface, instance: instance, definition: definition} = args, clock = args.clock ?? wallClock, memberRolesLoader = args.memberRolesLoader, memberRoles = await memberRolesLoader({
        workflowResource: instance.workflowResource,
        definition: definition
    });
    return {
        client: client,
        clientForGdr: clientForGdr,
        refSurface: refSurface,
        clock: clock,
        now: clock(),
        executionContext: resolveExecutionContext(args.executionContext),
        telemetry: resolveTelemetry(args.telemetry),
        ...args.settlingCohorts !== void 0 ? {
            settlingCohorts: args.settlingCohorts
        } : {},
        ...args.actor !== void 0 ? {
            actor: args.actor
        } : {},
        instance: instance,
        definition: definition,
        memberRolesLoader: memberRolesLoader,
        ...memberRoles !== void 0 ? {
            memberRoles: memberRoles
        } : {},
        snapshot: await hydrateSnapshot({
            client: client,
            clientForGdr: clientForGdr,
            instance: instance,
            ...args.overlay !== void 0 ? {
                overlay: args.overlay
            } : {}
        })
    };
}

function createAssignmentMemberRolesLoader(client) {
    const byProjectId = /* @__PURE__ */ new Map;
    return async ({workflowResource: workflowResource, definition: definition}) => {
        if (!hasRoleConstrainedAssignments(definition)) return;
        assertRoleConstrainedAssignmentResource({
            context: "workflow",
            definition: definition,
            workflowResource: workflowResource
        });
        const projectId = datasetResourceParts(workflowResource.id).projectId;
        let pending = byProjectId.get(projectId);
        return pending === void 0 && (pending = client.request === void 0 ? Promise.reject(new Error(`Definition "${definition.name}" has role-constrained assignments, which require project-directory access for project "${projectId}"; the client cannot issue requests`)) : loadProjectMemberRoles({
            request: client.request,
            projectId: projectId
        }), byProjectId.set(projectId, pending)), pending;
    };
}

async function loadProjectMemberRoles(args) {
    const {request: request, projectId: projectId} = args, snapshot = await requestProjectMemberDirectory({
        request: request,
        projectId: projectId
    }), rolesByProjectId = projectMemberRoles({
        members: snapshot.members,
        projectId: projectId
    });
    return globalizeMemberRoles({
        snapshot: snapshot,
        rolesByProjectId: rolesByProjectId
    });
}

function globalizeMemberRoles(args) {
    const {snapshot: snapshot, rolesByProjectId: rolesByProjectId} = args, humanIds = [ ...rolesByProjectId.keys() ].filter(id => classifyPrincipalId(id).namespace === "project");
    for (const {globalId: globalId, memberId: memberId} of snapshot.globalUsers) {
        const roles = rolesByProjectId.get(memberId);
        roles !== void 0 && rolesByProjectId.set(globalId, roles);
    }
    for (const id of humanIds) rolesByProjectId.delete(id);
    return Object.fromEntries(rolesByProjectId);
}

function projectMemberRoles(args) {
    const result = /* @__PURE__ */ new Map;
    for (const member of args.members) {
        const {id: id, roles: roles} = member;
        if (!Array.isArray(roles) || !roles.every(isProjectMemberRole)) throw new Error(`Project member role directory response is invalid for project "${args.projectId}" and member "${id}"`);
        result.set(id, roles.map(role => role.name));
    }
    return result;
}

function isProjectMemberRole(role) {
    return isRecord(role) && typeof role.name == "string";
}

function findStage(definition, stageName) {
    const stage = definition.stages.find(s => s.name === stageName);
    if (stage === void 0) throw new Error(`Stage "${stageName}" not found in definition ${definition.name}`);
    return stage;
}

async function resolveStageFieldEntries(args) {
    const {client: client, instance: instance, stage: stage, now: now, refSurface: refSurface, initialFields: initialFields, recordDiscard: recordDiscard} = args;
    return resolveDeclaredFields({
        entryDefs: stage.fields,
        initialFields: initialFields ?? [],
        ctx: {
            client: client,
            now: now,
            refSurface: refSurface,
            selfId: instance._id,
            tag: instance.tag,
            stageName: stage.name,
            workflowResource: instance.workflowResource,
            memberRoles: args.memberRoles,
            roleAliases: args.roleAliases,
            workflowFields: instance.fields ?? [],
            ...instance.perspective !== void 0 ? {
                perspective: instance.perspective
            } : {},
            ...recordDiscard !== void 0 ? {
                recordDiscard: recordDiscard
            } : {}
        },
        randomKey: randomKey
    });
}

async function resolveActivityFieldEntries(args) {
    const {client: client, instance: instance, stage: stage, activity: activity, now: now, refSurface: refSurface, recordDiscard: recordDiscard} = args;
    return resolveDeclaredFields({
        entryDefs: activity.fields,
        initialFields: [],
        ctx: {
            client: client,
            now: now,
            refSurface: refSurface,
            selfId: instance._id,
            tag: instance.tag,
            stageName: stage.name,
            workflowResource: instance.workflowResource,
            activityName: activity.name,
            memberRoles: args.memberRoles,
            roleAliases: args.roleAliases,
            workflowFields: instance.fields ?? [],
            ...instance.perspective !== void 0 ? {
                perspective: instance.perspective
            } : {},
            ...recordDiscard !== void 0 ? {
                recordDiscard: recordDiscard
            } : {}
        },
        randomKey: randomKey
    });
}

async function resolveBindings(args) {
    const resolved = {};
    for (const [key, groq] of Object.entries(args.bindings ?? {})) resolved[key] = await runGroq$1({
        groq: groq,
        params: args.params,
        snapshot: args.snapshot
    });
    return {
        ...resolved,
        ...args.staticInput
    };
}

const DEFAULT_IDEMPOTENCY_TTL_MS = 1440 * 60 * 1e3;

class RequestAlreadyProcessedError extends Error {
    processed;
    constructor(args) {
        super(`Request "${args.processed.key}" (${args.processed.op}) was already processed on ${args.instanceId} at ${args.processed.processedAt}`), 
        this.name = "RequestAlreadyProcessedError", this.processed = args.processed;
    }
}

function isExpired(row, now) {
    return hasPassed(row.expiresAt, now);
}

function findProcessedRequest({instance: instance, record: record, now: now}) {
    const row = (instance.processedRequests ?? []).find(r => r.key === record.key && !isExpired(r, now));
    if (row !== void 0) {
        if (row.op !== record.op) throw new ContractViolationError(`Idempotency key "${record.key}" was already used for "${row.op}" on ${instance._id}; it cannot be reused for "${record.op}" while the recorded entry lives (expires ${row.expiresAt})`);
        return row;
    }
}

function assertRequestUnprocessed({instance: instance, record: record, now: now}) {
    if (record === void 0) return;
    const row = findProcessedRequest({
        instance: instance,
        record: record,
        now: now
    });
    if (row !== void 0) throw new RequestAlreadyProcessedError({
        instanceId: instance._id,
        processed: row
    });
}

function recordProcessedRequest({mutation: mutation, record: record, now: now}) {
    if (record === void 0) return;
    const expiresAt = addMs(now, record.ttlMs);
    mutation.processedRequests = [ ...mutation.processedRequests.filter(r => !isExpired(r, now)), {
        _key: randomKey(),
        _type: "processedRequest",
        key: record.key,
        op: record.op,
        processedAt: now,
        expiresAt: expiresAt
    } ];
}

function resolveGuardRead(args) {
    const {expr: expr, ctx: ctx, deref: deref} = args;
    if (expr === "$self") return selfGdr(ctx.instance);
    if (expr === "$now") return ctx.now;
    const fieldRead = FIELD_READ.exec(expr);
    if (fieldRead !== null) {
        const entry = (ctx.instance.fields ?? []).find(s => s.name === fieldRead[1]);
        return entry === void 0 ? void 0 : deref ? resolveFieldRead({
            kind: entry._type,
            value: entry.value,
            path: fieldRead[2],
            snapshot: ctx.snapshot
        }) : fieldRead[2] !== void 0 ? getPath(entry.value, fieldRead[2]) : entry.value;
    }
    const effectsRead = EFFECTS_READ.exec(expr);
    if (effectsRead !== null) {
        const outputs = effectOutputsMap(ctx.instance)[effectsRead[1]];
        return effectsRead[2] !== void 0 ? getPath(outputs, effectsRead[2]) : outputs;
    }
    throw new Error(`Guard read "${expr}" is not a supported deploy-time value — use "$self", "$now", "$fields.<name>[.path]", or "$effects['<name>'][.path]" (guards store resolved values; the lake cannot see $fields or $effects)`);
}

function resolveIdRefTarget(expr, ctx) {
    const value = resolveGuardRead({
        expr: expr,
        ctx: ctx,
        deref: !1
    });
    if (typeof value == "string" && isGdrUri(value)) return {
        parsed: parseGdr(value)
    };
    if (value && typeof value == "object") {
        const v2 = value;
        if (typeof v2.id == "string" && isGdrUri(v2.id)) return typeof v2.type == "string" ? {
            parsed: parseGdr(v2.id),
            type: v2.type
        } : {
            parsed: parseGdr(v2.id)
        };
    }
    return null;
}

function resolveIdRefTargets(idRefs, ctx) {
    const targets = [];
    for (const expr of idRefs ?? []) {
        const target = resolveIdRefTarget(expr, ctx);
        if (target === null) return null;
        targets.push(target);
    }
    return targets.length === 0 ? null : targets;
}

function assertSingleResource(targets) {
    const resource = resourceFromParsed(targets[0].parsed);
    for (const g of targets) {
        const r = resourceFromParsed(g.parsed);
        if (r.type !== resource.type || r.id !== resource.id) throw new Error(`Guard targets span multiple resources (${resource.type}:${resource.id} vs ${r.type}:${r.id}); a guard is single-resource.`);
    }
    return resource;
}

function bareIdRefs(targets) {
    return targets.flatMap(g => [ g.parsed.documentId, `drafts.${g.parsed.documentId}` ]);
}

function resolveMatchTypes(targets, authorTypes) {
    if (authorTypes !== void 0) return authorTypes;
    const inferred = [ ...new Set(targets.map(g => g.type).filter(t => !!t)) ];
    return inferred.length > 0 ? inferred : void 0;
}

function resolveMetadata(metadata, ctx) {
    const out = {};
    for (const [k, expr] of Object.entries(metadata ?? {})) out[k] = resolveGuardRead({
        expr: expr,
        ctx: ctx,
        deref: !0
    });
    return out;
}

const IDENTITY_KINDS = /* @__PURE__ */ new Set([ "actor", "assignee", "assignees" ]);

async function localizeGuardMetadata(args) {
    const {doc: doc, guard: guard, client: client, instance: instance, definition: definition} = args;
    if (doc.resourceType !== "dataset" || client.request === void 0) return doc;
    const request = client.request, {projectId: projectId} = datasetResourceParts(doc.resourceId), shapes = metadataShapes({
        guard: guard,
        instance: instance,
        definition: definition
    }), globals = collectMetadataGlobals({
        metadata: doc.metadata,
        shapes: shapes
    });
    if (globals.size === 0) return doc;
    const map = /* @__PURE__ */ new Map;
    for (const id of globals) {
        const lookup = await resolveGlobalProjectUser({
            client: client,
            request: request,
            projectId: projectId,
            id: id
        });
        lookup.status === "resolved" && map.set(id, lookup.user.id);
    }
    return map.size === 0 ? doc : {
        ...doc,
        metadata: rewriteMetadata({
            metadata: doc.metadata,
            shapes: shapes,
            map: map
        })
    };
}

function metadataShapes(args) {
    const out = /* @__PURE__ */ new Map;
    for (const [key, expr] of Object.entries(args.guard.metadata ?? {})) {
        const shape = shapeOfRead({
            expr: expr,
            instance: args.instance,
            definition: args.definition
        });
        shape !== void 0 && out.set(key, shape);
    }
    return out;
}

function shapeOfRead(args) {
    const fieldRead = FIELD_READ.exec(args.expr);
    if (fieldRead !== null) return shapeOfFieldRead(args.instance, fieldRead);
    const effectsRead = EFFECTS_READ.exec(args.expr);
    if (effectsRead !== null) return shapeOfEffectsRead(args.definition, effectsRead);
}

function shapeOfFieldRead(instance, fieldRead) {
    const entry = (instance.fields ?? []).find(s => s.name === fieldRead[1]);
    if (entry !== void 0) return narrowByPath({
        type: entry._type,
        fields: Reflect.get(entry, "fields"),
        of: Reflect.get(entry, "of")
    }, fieldRead[2]);
}

function shapeOfEffectsRead(definition, effectsRead) {
    const outputs = findEffect(definition, effectsRead[1])?.outputs ?? [], path = effectsRead[2];
    if (path === void 0) return;
    const [head, ...rest] = path.split("."), output = outputs.find(shape => shape.name === head);
    if (output !== void 0) return narrowByPath({
        type: output.type,
        fields: output.fields,
        of: output.of
    }, rest.length > 0 ? rest.join(".") : void 0);
}

function narrowByPath(shape, path) {
    if (path === void 0) return shape;
    if (typeof shape.type != "string" || !IDENTITY_KINDS.has(shape.type)) return;
    const steps = identityPathSteps(shape.type, path);
    if (steps === void 0) return;
    const itemType = shape.type === "assignees" ? "assignee" : shape.type;
    if (steps.length === 0) return {
        type: itemType
    };
    if (steps.length === 1 && steps[0] === "id") return {
        type: itemType,
        idLeaf: !0
    };
}

function identityPathSteps(kind, path) {
    const segments = path.split(".");
    return kind !== "assignees" ? segments : /^\d+$/.test(segments[0] ?? "") ? segments.slice(1) : void 0;
}

function collectMetadataGlobals(args) {
    const found = /* @__PURE__ */ new Set;
    for (const [key, shape] of args.shapes) {
        const value = args.metadata[key];
        if (shape.idLeaf === !0) {
            typeof value == "string" && classifyPrincipalId(value).namespace === "global" && found.add(value);
            continue;
        }
        for (const id of collectDeclaredGlobalIds({
            ...shape,
            value: value
        })) found.add(id);
    }
    return found;
}

function rewriteMetadata(args) {
    const out = {
        ...args.metadata
    };
    for (const [key, shape] of args.shapes) {
        const value = out[key];
        if (shape.idLeaf === !0) {
            typeof value == "string" && (out[key] = args.map.get(value) ?? value);
            continue;
        }
        out[key] = rewriteDeclaredPrincipalIds({
            ...shape,
            value: value,
            map: args.map
        });
    }
    return out;
}

const MutationGuardMatchSchema = tolerantObject()({
    types: v.exactOptional(v.array(v.string())),
    idRefs: v.exactOptional(v.array(v.string())),
    idPatterns: v.exactOptional(v.array(v.string())),
    actions: v.array(v.picklist(MUTATION_GUARD_ACTIONS))
}), MutationGuardDocSchema = tolerantObject()({
    _id: NonEmptyString,
    _type: v.literal(GUARD_DOC_TYPE),
    _rev: v.exactOptional(v.string()),
    _createdAt: v.exactOptional(v.string()),
    _updatedAt: v.exactOptional(v.string()),
    resourceType: NonEmptyString,
    resourceId: NonEmptyString,
    owner: v.string(),
    sourceInstanceId: NonEmptyString,
    sourceDefinition: NonEmptyString,
    sourceStage: NonEmptyString,
    name: v.exactOptional(v.string()),
    description: v.exactOptional(v.string()),
    match: MutationGuardMatchSchema,
    predicate: v.string(),
    metadata: v.record(v.string(), v.unknown())
});

function parseGuardDocument(doc) {
    return parsePersistedDoc({
        schema: MutationGuardDocSchema,
        doc: doc,
        docType: GUARD_DOC_TYPE
    });
}

async function fetchGuards(args) {
    return (await args.client.fetch(args.query, args.params, {
        tag: REQUEST_TAG.guardQuery
    })).map(parseGuardDocument);
}

function guardsForResource(client) {
    return fetchGuards({
        client: client,
        query: "*[_type == $t] | order(_id asc)",
        params: {
            t: GUARD_DOC_TYPE
        }
    });
}

function instancesGuardQuery(instanceIds) {
    return {
        query: "*[_type == $guardType && sourceInstanceId in $instanceIds] | order(_id asc)",
        params: {
            guardType: GUARD_DOC_TYPE,
            instanceIds: [ ...instanceIds ]
        }
    };
}

function instanceGuardQuery(instanceId) {
    return instancesGuardQuery([ instanceId ]);
}

function verdictGuardsForInstance(client, instanceId) {
    const {query: query, params: params} = instanceGuardQuery(instanceId);
    return fetchGuards({
        client: client,
        query: query,
        params: params
    });
}

async function guardsForInstance(args) {
    const {client: client, clientForGdr: clientForGdr, instance: instance} = args, stateUris = [ ...collectEntryDocUris(instance.fields), ...instance.stages.flatMap(stage => collectEntryDocUris(stage.fields)), ...instance.stages.flatMap(stage => stage.activities.flatMap(activity => collectEntryDocUris(activity.fields))) ], clients = resourceClientMap({
        base: instance.workflowResource,
        baseClient: client,
        clientForGdr: clientForGdr,
        gdrs: stateUris.map(uri => tryParseGdr(uri))
    }), {query: query, params: params} = instanceGuardQuery(instance._id);
    return queryGuardsAcross({
        clients: clients.values(),
        query: query,
        params: params
    });
}

async function guardsForDefinition(args) {
    const perClient = await guardsForDefinitionByClient(args);
    return dedupById(perClient.flatMap(entry => entry.guards));
}

async function guardsForDefinitionByClient(args) {
    const {client: client, clientForGdr: clientForGdr, workflowResource: workflowResource, definition: definition, definitions: definitions} = args, gdrs = definitions.flatMap(def => guardIdRefs(def).map(({idRef: idRef, stage: stage}) => staticIdRefGdr({
        idRef: idRef,
        stage: stage,
        definition: def
    }))), clients = resourceClientMap({
        base: workflowResource,
        baseClient: client,
        clientForGdr: clientForGdr,
        gdrs: gdrs
    }), query = "*[_type == $t && sourceDefinition == $definition]", params = {
        t: GUARD_DOC_TYPE,
        definition: definition
    };
    return Promise.all([ ...clients.values() ].map(async resourceClient => ({
        client: resourceClient,
        guards: await fetchGuards({
            client: resourceClient,
            query: query,
            params: params
        })
    })));
}

function guardIdRefs(definition) {
    return definition.stages.flatMap(stage => (stage.guards ?? []).flatMap(guard => (guard.match.idRefs ?? []).map(idRef => ({
        idRef: idRef,
        stage: stage
    }))));
}

function staticIdRefGdr({idRef: idRef, stage: stage, definition: definition}) {
    const read = /^\$fields\.([\w-]+)/.exec(idRef);
    if (read === null) return;
    const seed = entriesInScope(stage, definition).find(e => e.name === read[1])?.initialValue;
    return seed?.type === "literal" ? gdrFromValue(seed.value) : void 0;
}

function entriesInScope(stage, definition) {
    return [ ...definition.fields ?? [], ...stage.fields ?? [], ...(stage.activities ?? []).flatMap(activity => activity.fields ?? []) ];
}

function gdrFromValue(value) {
    if (typeof value == "string") return tryParseGdr(value);
    if (typeof value == "object" && value !== null && "id" in value) {
        const id = value.id;
        return typeof id == "string" ? tryParseGdr(id) : void 0;
    }
}

function resourceClientMap({base: base, baseClient: baseClient, clientForGdr: clientForGdr, gdrs: gdrs}) {
    const map = /* @__PURE__ */ new Map([ [ resourceGdr(base), baseClient ] ]);
    for (const parsed of gdrs) {
        if (parsed === void 0) continue;
        const key = resourceGdr(resourceFromParsed(parsed));
        map.has(key) || map.set(key, clientForGdr(parsed));
    }
    return map;
}

async function queryGuardsAcross({clients: clients, query: query, params: params}) {
    const perResource = await Promise.all([ ...clients ].map(client => fetchGuards({
        client: client,
        query: query,
        params: params
    })));
    return dedupById(perResource.flat());
}

function dedupById(guards) {
    return [ ...new Map(guards.map(g => [ g._id, g ])).values() ].sort((a, b) => a._id.localeCompare(b._id));
}

const GUARD_OWNER = "robot:workflow-engine", DELETE_CHUNK_SIZE = 200;

function resolveGuardRoute(guard, ctx) {
    const targets = resolveIdRefTargets(guard.match.idRefs, ctx);
    return targets === null ? null : {
        guardId: lakeGuardId({
            instanceDocId: ctx.instance._id,
            guardName: guard.name
        }),
        routeGdr: targets[0].parsed,
        targets: targets
    };
}

function resolveGuard({guard: guard, instance: instance, stageName: stageName, now: now, snapshot: snapshot}) {
    const ctx = {
        instance: instance,
        stageName: stageName,
        now: now,
        snapshot: snapshot
    }, route = resolveGuardRoute(guard, ctx);
    if (route === null) return null;
    const resource = assertSingleResource(route.targets), types = resolveMatchTypes(route.targets, guard.match.types);
    return {
        docs: compileGuards({
            id: route.guardId,
            resourceType: resource.type,
            resourceId: resource.id,
            owner: GUARD_OWNER,
            sourceInstanceId: instance._id,
            sourceDefinition: instance.definition,
            sourceStage: stageName,
            name: guard.name,
            ...guard.description !== void 0 ? {
                description: guard.description
            } : {},
            match: {
                ...types !== void 0 ? {
                    types: types
                } : {},
                idRefs: bareIdRefs(route.targets),
                ...guard.match.idPatterns !== void 0 ? {
                    idPatterns: guard.match.idPatterns
                } : {},
                actions: guard.match.actions
            },
            predicate: guard.predicate ?? "",
            metadata: resolveMetadata(guard.metadata, ctx)
        }),
        routeGdr: route.routeGdr
    };
}

async function upsertGuard(args) {
    const {client: client, doc: doc, exists: exists} = args;
    if (!exists) try {
        return {
            created: await client.create(doc, {
                ...SYNC_COMMIT,
                tag: REQUEST_TAG.guardDeploy
            })
        };
    } catch (error) {
        if (!isCreateIdCollision(error)) throw error;
    }
    const {_id: _id, _type: _type, _rev: _rev, _createdAt: _createdAt, _updatedAt: _updatedAt, ...body} = doc;
    return await client.patch(doc._id).set(body).commit({
        ...SYNC_COMMIT,
        tag: REQUEST_TAG.guardRefresh
    }), {
        created: void 0
    };
}

async function retractCreatedGuards(guards) {
    for (const {client: client, doc: doc} of guards.toReversed()) {
        if (doc._rev === void 0) return new Error(`Cannot roll back guard ${doc._id}: created document has no revision`);
        try {
            const revisionCheck = client.patch(doc._id).set({
                predicate: doc.predicate
            }).ifRevisionId(doc._rev);
            await client.transaction().patch(revisionCheck).delete(doc._id).commit({
                tag: REQUEST_TAG.guardDeploy
            });
        } catch (error) {
            return error;
        }
    }
}

async function deployResolvedGuard(args) {
    const {client: client, resourceKey: resourceKey, doc: doc, guard: guard, stageArgs: stageArgs, existingByResource: existingByResource} = args, localized = await localizeGuardMetadata({
        doc: doc,
        guard: guard,
        client: client,
        instance: stageArgs.instance,
        definition: stageArgs.definition
    });
    return (await upsertGuard({
        client: client,
        doc: localized,
        exists: existingByResource.get(resourceKey)?.has(doc._id) ?? !1
    })).created;
}

async function failGuardDeploy(args) {
    const {cause: cause, stageName: stageName, deployed: deployed, createdGuards: createdGuards, updatedExistingGuard: updatedExistingGuard} = args, rollbackError = updatedExistingGuard ? void 0 : await retractCreatedGuards(createdGuards);
    throw deployed > 0 && (updatedExistingGuard || rollbackError !== void 0) ? new PartialGuardDeployError({
        stageName: stageName,
        deployed: deployed,
        cause: cause,
        rollbackError: rollbackError
    }) : cause;
}

function resolvedStageGuards(args) {
    const stage = args.definition.stages.find(s => s.name === args.stageName), out = [];
    for (const guard of stage?.guards ?? []) {
        const resolved = resolveGuard({
            guard: guard,
            instance: args.instance,
            stageName: args.stageName,
            now: args.now,
            snapshot: args.snapshot
        });
        if (resolved === null) continue;
        const client = args.clientForGdr(resolved.routeGdr), resourceKey = resourceGdr(resourceFromParsed(resolved.routeGdr));
        for (const doc of resolved.docs) out.push({
            client: client,
            resourceKey: resourceKey,
            value: doc,
            guard: guard
        });
    }
    return out;
}

function resolvedStageGuardRoutes(args) {
    const stage = args.definition.stages.find(s => s.name === args.stageName), ctx = {
        instance: args.instance,
        stageName: args.stageName,
        now: args.now,
        snapshot: args.snapshot
    }, out = [];
    for (const guard of stage?.guards ?? []) {
        const route = resolveGuardRoute(guard, ctx);
        if (route === null) continue;
        const client = args.clientForGdr(route.routeGdr), resourceKey = resourceGdr(resourceFromParsed(route.routeGdr));
        for (const guardId of compiledGuardIds({
            id: route.guardId,
            actions: guard.match.actions
        })) out.push({
            client: client,
            resourceKey: resourceKey,
            value: guardId
        });
    }
    return out;
}

function groupByResource(routed) {
    const groups = /* @__PURE__ */ new Map;
    for (const item of routed) {
        const existing = groups.get(item.resourceKey);
        if (existing !== void 0) {
            existing.values.push(item.value);
            continue;
        }
        groups.set(item.resourceKey, {
            client: item.client,
            resourceKey: item.resourceKey,
            values: [ item.value ]
        });
    }
    return [ ...groups.values() ];
}

async function existingGuardIds(groups) {
    const entries = await Promise.all(groups.map(async ({client: client, resourceKey: resourceKey, values: values}) => {
        const ids = values.map(doc => doc._id), existing = await client.fetch("*[_id in $ids]._id", {
            ids: ids
        }, {
            tag: REQUEST_TAG.guardDeploy
        });
        return [ resourceKey, new Set(existing) ];
    }));
    return new Map(entries);
}

async function observedGuards(groups) {
    const entries = await Promise.all(groups.map(async ({client: client, resourceKey: resourceKey, values: ids}) => {
        const guards = await client.fetch("*[_id in $ids]{_id, predicate, _rev}", {
            ids: ids
        }, {
            tag: REQUEST_TAG.guardRetract
        });
        return [ resourceKey, new Map(guards.map(guard => [ guard._id, guard ])) ];
    }));
    return new Map(entries);
}

async function committedInstance(args) {
    return getInstanceDocument(args.client, args.instance._id);
}

async function deployStageGuards(args) {
    const live = await committedInstance(args);
    if (live === void 0 || live.currentStage !== args.stageName || live.abortedAt !== void 0) return;
    const routed = resolvedStageGuards(args), existingByResource = await existingGuardIds(groupByResource(routed));
    let deployed = 0;
    const createdGuards = [];
    let updatedExistingGuard = !1;
    for (const {client: client, resourceKey: resourceKey, value: doc, guard: guard} of routed) {
        try {
            const created = await deployResolvedGuard({
                client: client,
                resourceKey: resourceKey,
                doc: doc,
                guard: guard,
                stageArgs: args,
                existingByResource: existingByResource
            });
            created === void 0 ? updatedExistingGuard = !0 : createdGuards.push({
                client: client,
                doc: created
            });
        } catch (cause) {
            await failGuardDeploy({
                cause: cause,
                stageName: args.stageName,
                deployed: deployed,
                createdGuards: createdGuards,
                updatedExistingGuard: updatedExistingGuard
            });
        }
        deployed += 1;
    }
}

async function retractStageGuards(args) {
    const observed = [], routed = resolvedStageGuardRoutes(args), guardsByResource = await observedGuards(groupByResource(routed));
    for (const {client: client, resourceKey: resourceKey, value: guardId} of routed) {
        const guard = guardsByResource.get(resourceKey)?.get(guardId);
        if (guard) {
            if (guard._rev === void 0 || guard._rev === null) throw new Error(`Cannot retract guard ${guardId}: persisted document has no revision`);
            observed.push({
                client: client,
                guardId: guardId,
                predicate: guard.predicate,
                revision: guard._rev
            });
        }
    }
    const live = await committedInstance(args);
    if (live !== void 0 && !(live.currentStage === args.stageName && live.abortedAt === void 0)) for (const {client: client, guardId: guardId, predicate: predicate, revision: revision} of observed) {
        const revisionCheck = client.patch(guardId).set({
            predicate: predicate
        }).ifRevisionId(revision);
        await client.transaction().patch(revisionCheck).delete(guardId).commit({
            tag: REQUEST_TAG.guardRetract
        });
    }
}

async function deleteOrphanedDefinitionGuards(args) {
    const {client: client, clientForGdr: clientForGdr, workflowResource: workflowResource, definition: definition, definitions: definitions, tag: tag} = args, perClient = await guardsForDefinitionByClient({
        client: client,
        clientForGdr: clientForGdr,
        workflowResource: workflowResource,
        definition: definition,
        definitions: definitions
    }), ownPartitionPrefix = `${tag}.`;
    let count = 0;
    for (const {client: resourceClient, guards: guards} of perClient) {
        const own = guards.filter(guard => guard.sourceInstanceId.startsWith(ownPartitionPrefix));
        if (own.length !== 0) for (let start = 0; start < own.length; start += DELETE_CHUNK_SIZE) {
            const chunk = own.slice(start, start + DELETE_CHUNK_SIZE), tx = resourceClient.transaction();
            for (const guard of chunk) tx.delete(guard._id);
            await tx.commit(), count += chunk.length;
        }
    }
    return count;
}

async function abortInstance(args) {
    const {client: client, instanceId: instanceId, reason: reason, requestRecord: requestRecord, options: options} = args, ctx = await loadCallContext({
        client: client,
        instanceId: instanceId,
        options: options
    });
    return commitAbort({
        ctx: ctx,
        reason: reason,
        requestRecord: requestRecord,
        actor: options?.actor
    });
}

async function commitAbort({ctx: ctx, reason: reason, requestRecord: requestRecord, actor: actor}) {
    if (assertRequestUnprocessed({
        instance: ctx.instance,
        record: requestRecord,
        now: ctx.now
    }), ctx.instance.completedAt !== void 0) return {
        fired: !1
    };
    const stage = findStage(ctx.definition, ctx.instance.currentStage), mutation = startMutation(ctx.instance);
    recordProcessedRequest({
        mutation: mutation,
        record: requestRecord,
        now: ctx.now
    });
    const at = ctx.now, openEntry = findOpenStageEntry(mutation);
    openEntry !== void 0 && (openEntry.exitedAt = at);
    for (const row of liveSubworkflows(mutation)) condemnSubworkflow(row, {
        at: at,
        reason: "parent instance aborted"
    });
    const cancelled = mutation.pendingEffects.map(pending => ({
        effect: pending.name,
        origin: pending.origin.kind
    }));
    for (const pending of mutation.pendingEffects) {
        const {run: run, event: event} = buildEffectSettlement(pending, {
            status: "cancelled",
            ranAt: at,
            actor: actor,
            detail: "instance aborted before dispatch",
            error: void 0,
            durationMs: void 0,
            outputs: void 0
        });
        mutation.effectHistory.push(run), mutation.history.push(event);
    }
    mutation.pendingEffects = [], mutation.history.push({
        _key: randomKey(),
        _type: "aborted",
        at: at,
        stage: stage.name,
        ...reason !== void 0 ? {
            reason: reason
        } : {},
        ...actor !== void 0 ? {
            actor: actor
        } : {}
    }), mutation.completedAt = at, mutation.abortedAt = at;
    const instance = readInstanceDoc(await persist(ctx, mutation));
    await retractStageGuards({
        client: ctx.client,
        clientForGdr: ctx.clientForGdr,
        instance: ctx.instance,
        definition: ctx.definition,
        stageName: stage.name,
        now: ctx.now,
        snapshot: ctx.snapshot
    });
    for (const {effect: effect, origin: origin} of cancelled) ctx.telemetry.log(WorkflowEffectCompleted, {
        ...definitionHashFragment(ctx.instance.pinnedContentHash),
        instanceId: ctx.instance._id,
        effect: effect,
        status: "cancelled",
        origin: origin,
        cascaded: 0
    });
    return {
        fired: !0,
        stage: stage.name,
        instance: instance
    };
}

async function buildStageActivities({ctx: ctx, stage: stage, recordDiscard: recordDiscard}) {
    const entries = [];
    for (const activity of stage.activities ?? []) entries.push(await buildActivityEntry({
        ctx: ctx,
        stage: stage,
        activity: activity,
        recordDiscard: recordDiscard
    }));
    return entries;
}

async function buildActivityEntry({ctx: ctx, stage: stage, activity: activity, recordDiscard: recordDiscard}) {
    const outcome = await ctxEvaluateConditionOutcome({
        ctx: ctx,
        condition: activity.filter,
        opts: {
            activityName: activity.name
        }
    }), inScope = outcome !== "unsatisfied", entry = {
        _key: randomKey(),
        name: activity.name,
        status: inScope ? "active" : "skipped",
        ...activity.filter !== void 0 ? {
            filterEvaluation: buildFilterEvaluation({
                at: ctx.now,
                filter: activity.filter,
                outcome: outcome
            })
        } : {}
    };
    return inScope && (entry.startedAt = ctx.now, activity.fields !== void 0 && activity.fields.length > 0 && (entry.fields = await resolveActivityFieldEntries({
        client: ctx.client,
        instance: ctx.instance,
        stage: stage,
        activity: activity,
        now: ctx.now,
        refSurface: ctx.refSurface,
        memberRoles: ctx.memberRoles,
        roleAliases: ctx.definition.roleAliases,
        ...recordDiscard !== void 0 ? {
            recordDiscard: recordDiscard
        } : {}
    }))), entry;
}

function buildFilterEvaluation({at: at, filter: filter, outcome: outcome}) {
    return outcome === "unevaluable" ? {
        at: at,
        truthy: !1,
        unevaluable: !0,
        detail: `Filter "${filter}" could not be evaluated (GROQ null — a referenced operand was missing or incomparable). Activity kept in scope so the gate is not silently skipped.`
    } : {
        at: at,
        truthy: outcome === "satisfied"
    };
}

async function deployOrRollback(args) {
    try {
        await args.deploy();
    } catch (guardError) {
        if (!args.reversible) throw new WorkflowStateDivergedError({
            instanceId: args.instanceId,
            guardError: guardError,
            reason: "the move created child instances a parent-only rollback cannot undo"
        });
        try {
            let patch = args.client.patch(args.instanceId).set(args.restore);
            args.unset && args.unset.length > 0 && (patch = patch.unset(args.unset)), args.committedRev !== void 0 && (patch = patch.ifRevisionId(args.committedRev)), 
            await patch.commit(SYNC_COMMIT);
        } catch (rollbackError) {
            throw new WorkflowStateDivergedError({
                instanceId: args.instanceId,
                guardError: guardError,
                rollbackError: rollbackError,
                reason: "rollback of the committed move failed"
            });
        }
        throw guardError instanceof PartialGuardDeployError ? new WorkflowStateDivergedError({
            instanceId: args.instanceId,
            guardError: guardError,
            reason: "a multi-guard deploy partially applied; the rolled-back move left orphaned guard locks"
        }) : guardError;
    }
}

async function persistThenDeploy({ctx: ctx, mutation: mutation, deploy: deploy}) {
    const spawned = mutation.pendingCreates.length > 0, committed = await persist(ctx, mutation);
    await deployOrRollback({
        client: ctx.client,
        instanceId: ctx.instance._id,
        committedRev: committed._rev,
        restore: instanceStateFields({
            ...ctx.instance,
            minReaderModel: minReaderModelOf(ctx.instance),
            definitionModelVersion: definitionAssignmentModel(ctx.instance)
        }),
        unset: ctx.instance.completedAt === void 0 ? [ "completedAt" ] : [],
        reversible: !spawned,
        deploy: deploy
    });
}

async function persistThenMaybeRefresh({ctx: ctx, mutation: mutation, stageName: stageName, didChangeState: didChangeState}) {
    await persistThenDeploy({
        ctx: ctx,
        mutation: mutation,
        deploy: () => didChangeState ? refreshStageGuards({
            ctx: ctx,
            mutation: mutation,
            stageName: stageName
        }) : Promise.resolve()
    });
}

async function refreshStageGuards({ctx: ctx, mutation: mutation, stageName: stageName}) {
    await deployStageGuards({
        client: ctx.client,
        clientForGdr: ctx.clientForGdr,
        instance: materializeInstance(ctx.instance, mutation),
        definition: ctx.definition,
        stageName: stageName,
        now: ctx.now,
        snapshot: ctx.snapshot
    });
}

async function discoverItems(args) {
    const {client: client, groq: groq, params: params, perspective: perspective} = args;
    return client.fetch(groq, paramsForLake(params), {
        tag: REQUEST_TAG.discover,
        ...perspective !== void 0 ? {
            perspective: perspective
        } : {}
    });
}

function clientForDiscovery({defaultClient: defaultClient, clientForGdr: clientForGdr, subjectGdrUri: subjectGdrUri}) {
    if (subjectGdrUri === void 0) return defaultClient;
    const parsed = tryParseGdr(subjectGdrUri);
    return parsed !== void 0 ? clientForGdr(parsed) : defaultClient;
}

const MAX_SPAWN_DEPTH = 6;

function subworkflowsOnExit(sub) {
    return sub.onExit ?? "detach";
}

async function spawnSubworkflows({ctx: ctx, mutation: mutation, activity: activity, action: action, sub: sub, actor: actor}) {
    assertSpawnDepth(ctx, activity);
    const openEntry = findOpenStageEntry(mutation);
    if (openEntry === void 0) throw new Error(`Mutation invariant broken: no open StageEntry while spawning on activity "${activity.name}" of ${ctx.instance._id}`);
    const definition = await requireChildDefinition({
        ctx: ctx,
        activity: activity,
        sub: sub
    }), parentScope = await ctxConditionParams(ctx, {
        activityName: activity.name,
        ...actor ? {
            actor: actor
        } : {}
    }), {rows: rows, discoveryResource: discoveryResource} = await resolveSpawnRows(ctx, sub), keyedRows = keyRows(rows, {
        activity: activity.name,
        action: action.name,
        instanceId: ctx.instance._id
    }), live = liveSubworkflows(mutation).filter(r => r.activity === activity.name && r.action === action.name && r.definition === sub.definition.name && r.abortPending === void 0);
    adoptRediscoveredRows({
        mutation: mutation,
        live: live,
        keyedRows: keyedRows,
        openEntry: openEntry,
        activity: activity,
        now: ctx.now
    });
    const context = await resolveContextHandoff({
        ctx: ctx,
        sub: sub,
        parentScope: parentScope
    }), liveKeys = new Set(live.map(r => r.rowKey));
    for (const keyed of keyedRows) liveKeys.has(keyed.rowKey) || await spawnRow({
        ctx: ctx,
        mutation: mutation,
        activity: activity,
        action: action,
        sub: sub,
        definition: definition,
        parentScope: parentScope,
        context: context,
        discoveryResource: discoveryResource,
        openEntry: openEntry,
        keyed: keyed,
        actor: actor
    });
}

function assertSpawnDepth(ctx, activity) {
    if (ctx.instance.ancestors.length + 1 <= MAX_SPAWN_DEPTH) return;
    const chain = [ ...ctx.instance.ancestors.map(a => a.id), selfGdr(ctx.instance) ].join(" → ");
    throw new Error(`Subworkflow depth limit (${MAX_SPAWN_DEPTH}) exceeded on activity "${activity.name}" of ${ctx.instance._id}. Ancestor chain: ${chain}`);
}

async function requireChildDefinition({ctx: ctx, activity: activity, sub: sub}) {
    const definition = await resolveDefinitionRef({
        client: ctx.client,
        ref: sub.definition,
        tag: ctx.instance.tag
    });
    if (definition !== null) return definition;
    const versionLabel = sub.definition.version === void 0 || sub.definition.version === "latest" ? "latest" : `v${sub.definition.version}`;
    throw new Error(`Subworkflow definition "${sub.definition.name}" ${versionLabel} not deployed (parent ${ctx.instance._id}, activity ${activity.name})`);
}

function adoptRediscoveredRows({mutation: mutation, live: live, keyedRows: keyedRows, openEntry: openEntry, activity: activity, now: now}) {
    for (const row of live) keyedRows.some(k => k.rowKey === row.rowKey) && row.stageEntry !== openEntry._key && (row.stageEntry = openEntry._key, 
    row.cohortStage = openEntry.name, mutation.history.push({
        _key: randomKey(),
        _type: "subworkflowAdopted",
        at: now,
        stage: openEntry.name,
        activity: activity.name,
        instanceRef: row.ref,
        rowKey: row.rowKey
    }));
}

async function spawnRow({ctx: ctx, mutation: mutation, activity: activity, action: action, sub: sub, definition: definition, parentScope: parentScope, context: context, discoveryResource: discoveryResource, openEntry: openEntry, keyed: keyed, actor: actor}) {
    const now = ctx.now, initialFields = await projectRowFields({
        ctx: ctx,
        sub: sub,
        childDefinition: definition,
        parentScope: parentScope,
        row: keyed.row,
        discoveryResource: discoveryResource
    }), {ref: ref, body: body} = await prepareChildInstance({
        client: ctx.client,
        parent: ctx.instance,
        definition: definition,
        initialFields: initialFields,
        context: context,
        actor: actor,
        now: now,
        refSurface: ctx.refSurface,
        memberRolesLoader: ctx.memberRolesLoader
    });
    mutation.pendingCreates.push({
        body: body,
        started: instanceStartedData({
            definition: definition,
            instanceId: body._id,
            initialFieldCount: initialFields.length,
            viaSpawn: !0
        })
    }), mutation.subworkflows.push({
        _key: randomKey(),
        activity: activity.name,
        action: action.name,
        definition: sub.definition.name,
        stageEntry: openEntry._key,
        cohortStage: openEntry.name,
        rowKey: keyed.rowKey,
        ref: ref,
        spawnedAt: now
    }), mutation.history.push({
        _key: randomKey(),
        _type: "spawned",
        at: now,
        activity: activity.name,
        instanceRef: ref,
        rowKey: keyed.rowKey,
        ...actor !== void 0 ? {
            actor: actor
        } : {}
    });
}

function keyRows(rows, cx) {
    const keyed = rows.map(row => ({
        row: row,
        rowKey: spawnRowKey(row, cx)
    })), seen = /* @__PURE__ */ new Set;
    for (const {rowKey: rowKey} of keyed) {
        if (seen.has(rowKey)) throw new Error(`spawn.forEach on action "${cx.action}" (activity "${cx.activity}") of ${cx.instanceId} produced two rows with the same identity "${rowKey}" — adopt-matching cannot tell them apart. Make each row's identity unique: distinct \`_key\`/\`_id\`/GDR \`id\` values for object rows, distinct values for scalar rows.`);
        seen.add(rowKey);
    }
    return keyed;
}

function spawnRowKey(row, cx) {
    if (typeof row == "string" || typeof row == "number" || typeof row == "boolean") return String(row);
    if (typeof row == "object" && row !== null) {
        const {_key: _key, _id: _id} = row;
        if (typeof _key == "string" && _key.length > 0) return _key;
        if (typeof _id == "string" && _id.length > 0) return _id;
        if (isGdr(row) && isGdrUri(row.id)) return row.id;
    }
    throw new Error(`spawn.forEach on action "${cx.action}" (activity "${cx.activity}") of ${cx.instanceId} produced a row without an identity, so re-entering the stage could not tell "same row" from "new row". Project \`_id\` (document rows) or \`_key\` (array items), keep GDR rows whole (a \`{id, type}\` row with a GDR-URI \`id\` — the shape a \`doc.refs\` field stores — keys on its \`id\`), or mint a \`_key\` for synthetic rows (e.g. \`{"_key": lang + "-" + market, ...}\`); scalar rows key on their value. Row: ${JSON.stringify(row)}`);
}

function condemnOnExitCohorts({mutation: mutation, stage: stage, exitedEntry: exitedEntry, at: at}) {
    const abortCohorts = (stage.activities ?? []).flatMap(activity => (activity.actions ?? []).filter(action => action.spawn !== void 0 && subworkflowsOnExit(action.spawn) === "abort").map(action => ({
        activity: activity.name,
        action: action.name
    })));
    for (const cohort of abortCohorts) {
        const owed = liveSubworkflows(mutation).filter(row => row.activity === cohort.activity && row.action === cohort.action && row.stageEntry === exitedEntry._key);
        for (const row of owed) condemnSubworkflow(row, {
            at: at,
            reason: `stage "${stage.name}" exited with the child still live (onExit: "abort" on action "${cohort.action}" of activity "${cohort.activity}")`
        });
    }
}

async function resolveSpawnRows(ctx, sub) {
    const params = buildParams({
        instance: ctx.instance,
        now: ctx.now,
        snapshot: ctx.snapshot
    }), groq = sub.forEach;
    let value, discoveryResource;
    if (groq.includes("*")) {
        const routingUri = entrySubjectRefs(ctx.instance.fields)[0]?.id, client = clientForDiscovery({
            defaultClient: ctx.client,
            clientForGdr: ctx.clientForGdr,
            subjectGdrUri: routingUri
        });
        client !== ctx.client && routingUri !== void 0 && (discoveryResource = resourceFromGdrUri(routingUri)), 
        value = await discoverItems({
            client: client,
            groq: groq,
            params: params,
            perspective: ctx.instance.perspective ?? DEFAULT_CONTENT_PERSPECTIVE
        });
    } else {
        const tree = parse(groq, {
            params: params
        });
        value = await (await evaluate(tree, {
            dataset: [ ctx.instance ],
            params: params,
            root: ctx.instance
        })).get();
    }
    return Array.isArray(value) ? {
        rows: value,
        discoveryResource: discoveryResource
    } : value == null ? {
        rows: [],
        discoveryResource: discoveryResource
    } : {
        rows: [ value ],
        discoveryResource: discoveryResource
    };
}

async function projectRowFields({ctx: ctx, sub: sub, childDefinition: childDefinition, parentScope: parentScope, row: row, discoveryResource: discoveryResource}) {
    const out = [];
    for (const [name, groq] of Object.entries(sub.with ?? {})) {
        const declared = (childDefinition.fields ?? []).find(e => e.name === name);
        if (declared === void 0) throw new Error(`spawn.with["${name}"]: subworkflow "${childDefinition.name}" declares no field entry "${name}"`);
        const raw = await runGroq$1({
            groq: groq,
            params: {
                ...parentScope,
                row: row
            },
            snapshot: ctx.snapshot
        }), value = canonicaliseSpawnValue({
            kind: declared.type,
            value: raw,
            cx: {
                parentResource: ctx.instance.workflowResource,
                discoveryResource: discoveryResource,
                entryName: name,
                childName: childDefinition.name
            }
        });
        if (value == null) continue;
        const typeIssues = refTypeIssues({
            entryType: declared.type,
            types: declared.types,
            value: value
        });
        if (typeIssues !== void 0) {
            const hint = rejectedRefTypes({
                entryType: declared.type,
                types: declared.types,
                value: value
            }).includes("document") ? ' A projected bare id or `_ref` canonicalises with the generic type "document" — project the target\'s schema type alongside its id (e.g. {"id": _id, "type": _type}).' : "";
            throw new Error(`spawn.with["${name}"] on subworkflow "${childDefinition.name}": ${typeIssues.join("; ")}.${hint}`);
        }
        out.push({
            type: declared.type,
            name: name,
            value: value
        });
    }
    return out;
}

async function resolveContextHandoff({ctx: ctx, sub: sub, parentScope: parentScope}) {
    const out = {};
    for (const [name, groq] of Object.entries(sub.context ?? {})) {
        const v2 = await runGroq$1({
            groq: groq,
            params: parentScope,
            snapshot: ctx.snapshot
        });
        if (v2 != null) {
            if (typeof v2 == "string" || typeof v2 == "number" || typeof v2 == "boolean") {
                out[name] = v2;
                continue;
            }
            if (isGdr(v2) && isGdrUri(v2.id)) {
                out[name] = {
                    id: v2.id,
                    type: v2.type
                };
                continue;
            }
            throw new Error(`spawn.context["${name}"] evaluated to ${JSON.stringify(v2)}, which the handoff cannot carry. A context entry seeds the child's $context bag with a string, number, boolean, or a document reference — a full GDR \`{id, type}\` whose \`id\` is a "<scheme>:..." URI (parent-scope ids are already canonical).`);
        }
    }
    return out;
}

function canonicaliseSpawnValue({kind: kind, value: value, cx: cx}) {
    return isSingleDocRefKind(kind) ? coerceSpawnGdr(value, cx) : kind === "doc.refs" && Array.isArray(value) ? value.map(v2 => coerceSpawnGdr(v2, cx)).filter(v2 => v2 !== null) : value;
}

function assertBareIdRootsCorrectly(id, cx) {
    if (!(cx.discoveryResource === void 0 || sameResource(cx.discoveryResource, cx.parentResource))) throw new Error(`spawn.with["${cx.entryName}"]: subworkflow "${cx.childName}" projected the bare id "${id}", which roots at the parent resource "${cx.parentResource.id}", but spawn discovery ran against "${cx.discoveryResource.id}". The child would reference a document that doesn't exist in the parent resource. Project an explicit GDR URI in the with-GROQ (e.g. "dataset:<projectId>:<dataset>:" + _id) so the child's subject points at the resource the discovered rows came from.`);
}

function coerceSpawnGdr(raw, cx) {
    const toUri = id => isGdrUri(id) ? id : (assertBareIdRootsCorrectly(id, cx), gdrFromResource(cx.parentResource, id));
    if (raw == null) return null;
    if (isGdr(raw)) return {
        id: toUri(raw.id),
        type: raw.type
    };
    if (typeof raw == "object" && "_ref" in raw) {
        const r = raw;
        if (typeof r._ref == "string") return {
            id: toUri(r._ref),
            type: "document"
        };
    }
    return typeof raw == "string" && raw.length > 0 ? {
        id: toUri(raw),
        type: "document"
    } : null;
}

async function resolveDefinitionRef({client: client, ref: ref, tag: tag}) {
    const wantsExplicit = typeof ref.version == "number", params = {
        definition: ref.name,
        tag: tag
    };
    wantsExplicit && (params.version = ref.version);
    const result = await client.fetch(definitionLookupGroq(wantsExplicit), params);
    return result ? assertReadableModel(result) : null;
}

async function prepareChildInstance(args) {
    const {client: client, parent: parent, definition: definition, initialFields: initialFields, context: context, actor: actor, now: now, refSurface: refSurface, memberRolesLoader: memberRolesLoader} = args, childTag = parent.tag, workflowResource = parent.workflowResource, childDocId = instanceDocId(childTag), childRef = {
        id: gdrFromResource(workflowResource, childDocId),
        type: WORKFLOW_INSTANCE_TYPE
    }, ancestors = [ ...parent.ancestors, {
        id: selfGdr(parent),
        type: WORKFLOW_INSTANCE_TYPE
    } ], inheritedPerspective = parent.perspective, fieldDiscards = [], memberRoles = await memberRolesLoader({
        workflowResource: workflowResource,
        definition: definition
    }), childFields = await resolveDeclaredFields({
        entryDefs: definition.fields,
        initialFields: initialFields,
        ctx: {
            client: client,
            now: now,
            selfId: childDocId,
            tag: childTag,
            workflowResource: workflowResource,
            definitionName: definition.name,
            refSurface: refSurface,
            memberRoles: memberRoles,
            roleAliases: definition.roleAliases,
            inputProvenance: "definition",
            ...inheritedPerspective !== void 0 ? {
                perspective: inheritedPerspective
            } : {},
            recordDiscard: recordFieldDiscards({
                target: fieldDiscards,
                scope: "workflow",
                at: now
            })
        },
        randomKey: randomKey
    }), childPerspective = derivePerspectiveFromFields(childFields) ?? inheritedPerspective, contextEntries = Object.entries(context).map(([key, value]) => contextEntry(key, value)), base = buildInstanceBase({
        id: childDocId,
        now: now,
        tag: childTag,
        workflowResource: workflowResource,
        definitionName: definition.name,
        pinnedVersion: definition.version,
        pinnedContentHash: definition.contentHash,
        definition: definition,
        fields: childFields,
        context: contextEntries,
        ancestors: ancestors,
        perspective: childPerspective,
        initialStage: definition.initialStage,
        actor: actor,
        ...fieldDiscards.length > 0 ? {
            extraHistory: fieldDiscards
        } : {}
    });
    return {
        ref: childRef,
        body: base
    };
}

function isTerminalStage(stage) {
    return (stage.transitions ?? []).length === 0;
}

async function setStage(args) {
    const {client: client, instanceId: instanceId, targetStage: targetStage, reason: reason, requestRecord: requestRecord, options: options} = args, ctx = await loadCallContext({
        client: client,
        instanceId: instanceId,
        options: options
    });
    return commitSetStage({
        ctx: ctx,
        targetStage: targetStage,
        reason: reason,
        requestRecord: requestRecord,
        actor: options?.actor
    });
}

async function enterStage({ctx: ctx, mutation: mutation, nextStage: nextStage, at: at}) {
    mutation.currentStage = nextStage.name;
    const entryCtx = {
        ...ctx,
        instance: materializeInstance(ctx.instance, mutation)
    }, nextStageEntry = {
        _key: randomKey(),
        name: nextStage.name,
        enteredAt: at,
        fields: await resolveStageFieldEntries({
            client: ctx.client,
            instance: entryCtx.instance,
            stage: nextStage,
            now: at,
            refSurface: ctx.refSurface,
            memberRoles: ctx.memberRoles,
            roleAliases: ctx.definition.roleAliases,
            recordDiscard: recordFieldDiscards({
                target: mutation.history,
                scope: "stage",
                at: at
            })
        }),
        activities: []
    };
    mutation.stages.push(nextStageEntry), nextStageEntry.activities = await buildStageActivities({
        ctx: entryCtx,
        stage: nextStage,
        recordDiscard: recordFieldDiscards({
            target: mutation.history,
            scope: "activity",
            at: at
        })
    }), isTerminalStage(nextStage) && (mutation.completedAt = at);
}

function isTerminal(ctx) {
    return ctx.instance.completedAt !== void 0;
}

async function commitSetStage({ctx: ctx, targetStage: targetStage, reason: reason, requestRecord: requestRecord, actor: actor}) {
    if (assertRequestUnprocessed({
        instance: ctx.instance,
        record: requestRecord,
        now: ctx.now
    }), isTerminal(ctx)) return {
        fired: !1
    };
    const currentStage = findStage(ctx.definition, ctx.instance.currentStage), nextStage = findStage(ctx.definition, targetStage);
    if (currentStage.name === nextStage.name) return {
        fired: !1
    };
    const mutation = startMutation(ctx.instance);
    recordProcessedRequest({
        mutation: mutation,
        record: requestRecord,
        now: ctx.now
    });
    const transitionName = `setStage:${currentStage.name}->${nextStage.name}`;
    return await commitStageMove({
        ctx: ctx,
        mutation: mutation,
        fromStage: currentStage,
        toStage: nextStage,
        transition: transitionName,
        via: "setStage",
        actor: actor,
        reason: reason
    }), {
        fired: !0,
        fromStage: currentStage.name,
        toStage: nextStage.name,
        transition: transitionName
    };
}

async function commitStageMove({ctx: ctx, mutation: mutation, fromStage: fromStage, toStage: toStage, transition: transition, via: via, actor: actor, reason: reason}) {
    const at = ctx.now;
    mutation.history.push(...stageTransitionHistory({
        fromStage: fromStage.name,
        toStage: toStage.name,
        at: at,
        transition: transition,
        ...via === "setStage" ? {
            via: via
        } : {},
        ...actor !== void 0 ? {
            actor: actor
        } : {},
        ...reason !== void 0 ? {
            reason: reason
        } : {}
    })), exitOpenStage({
        mutation: mutation,
        stage: fromStage,
        at: at
    }), await enterStage({
        ctx: ctx,
        mutation: mutation,
        nextStage: toStage,
        at: at
    }), await persistStageMove({
        ctx: ctx,
        mutation: mutation,
        exitedStage: fromStage.name,
        enteredStage: toStage.name
    }), emitStageTransitioned({
        ctx: ctx,
        nextStage: toStage,
        at: at,
        via: via
    });
}

function emitStageTransitioned({ctx: ctx, nextStage: nextStage, at: at, via: via}) {
    ctx.telemetry.log(WorkflowStageTransitioned, stageTransitionedData({
        instance: ctx.instance,
        definition: ctx.definition,
        toStage: nextStage.name,
        toIsTerminal: isTerminalStage(nextStage),
        at: at,
        via: via
    }));
}

function exitOpenStage({mutation: mutation, stage: stage, at: at}) {
    const priorEntry = findOpenStageEntry(mutation);
    priorEntry !== void 0 && (priorEntry.exitedAt = at, condemnOnExitCohorts({
        mutation: mutation,
        stage: stage,
        exitedEntry: priorEntry,
        at: at
    }));
}

async function persistStageMove({ctx: ctx, mutation: mutation, exitedStage: exitedStage, enteredStage: enteredStage}) {
    await persistThenDeploy({
        ctx: ctx,
        mutation: mutation,
        deploy: () => deployStageGuards({
            client: ctx.client,
            clientForGdr: ctx.clientForGdr,
            instance: materializeInstance(ctx.instance, mutation),
            definition: ctx.definition,
            stageName: enteredStage,
            now: ctx.now,
            snapshot: ctx.snapshot
        })
    }), await retractStageGuards({
        client: ctx.client,
        clientForGdr: ctx.clientForGdr,
        instance: ctx.instance,
        definition: ctx.definition,
        stageName: exitedStage,
        now: ctx.now,
        snapshot: ctx.snapshot
    });
}

async function pickTransition(ctx, stage) {
    for (const candidate of stage.transitions ?? []) {
        const outcome = await ctxEvaluateConditionOutcome({
            ctx: ctx,
            condition: candidate.when
        });
        if (outcome === "satisfied") return candidate;
        if (outcome === "unevaluable") return;
    }
}

const parsedFilters = /* @__PURE__ */ new Map;

async function matchesFilter(args) {
    const {document: document, filter: filter, userId: userId} = args;
    parsedFilters.has(filter) || parsedFilters.set(filter, parse(`*[${filter}]`));
    const ast = parsedFilters.get(filter), data = await (await evaluate(ast, {
        dataset: [ document ],
        ...userId !== void 0 ? {
            identity: userId
        } : {}
    })).get();
    return Array.isArray(data) && data.length === 1;
}

async function grantsPermissionOn(args) {
    const {document: document, grants: grants, permission: permission, userId: userId} = args;
    if (!document || grants.length === 0) return !1;
    for (const grant of grants) if (grant.permissions.includes(permission) && await matchesFilter({
        document: document,
        filter: grant.filter,
        ...userId !== void 0 ? {
            userId: userId
        } : {}
    })) return !0;
    return !1;
}

function aclPathForResource(res) {
    if (res.type === "dataset") {
        const {projectId: projectId, dataset: dataset} = datasetResourceParts(res.id);
        return `/projects/${projectId}/datasets/${dataset}/acl`;
    }
    if (res.type === "canvas") return `/canvases/${res.id}/acl`;
}

async function fetchGrants(args) {
    const {client: client, resourcePath: resourcePath, signal: signal} = args;
    return client.request({
        url: resourcePath,
        tag: REQUEST_TAG.accessGrants,
        ...signal ? {
            signal: signal
        } : {}
    });
}

async function advisoryCanForCall(args) {
    const actor = args.options?.actor;
    if (!(args.options?.grants === void 0 || actor === void 0)) return advisoryCan({
        instance: args.instance,
        identity: lakePrincipalId({
            actor: actor,
            localPrincipalId: args.options.localPrincipalId
        }),
        grants: args.options.grants
    });
}

async function advisoryCan({instance: instance, identity: identity, grants: grants}) {
    if (grants === void 0) return;
    const can = {};
    for (const permission of DOCUMENT_VALUE_PERMISSIONS) can[permission] = await grantsPermissionOn({
        document: instance,
        grants: grants,
        permission: permission,
        userId: identity
    });
    return can;
}

const DANGEROUS_ATTRIBUTE_KEYS = /* @__PURE__ */ new Set([ "__proto__", "constructor", "prototype" ]);

function attributeEntry(row) {
    if (!(!isRecord(row) || typeof row.key != "string" || row.key.length === 0) && !DANGEROUS_ATTRIBUTE_KEYS.has(row.key) && !(!("activeValue" in row) || row.activeValue === void 0)) return [ row.key, row.activeValue ];
}

function normalizeUserAttributes(response) {
    if (!isRecord(response)) return;
    const rows = response.attributes;
    if (!Array.isArray(rows)) return;
    const out = {};
    for (const row of rows) {
        const entry = attributeEntry(row);
        entry !== void 0 && (out[entry[0]] = entry[1]);
    }
    return out;
}

const actorCache = /* @__PURE__ */ new WeakMap, grantsCache = /* @__PURE__ */ new WeakMap, orgIdCache = /* @__PURE__ */ new WeakMap, attributesCache = /* @__PURE__ */ new WeakMap;

async function resolveAccess(taggedClient, args = {}) {
    const client = unwrapRequestTag(taggedClient), requestFn = lazyRequest(client);
    if (requestFn === void 0) throw new ContractViolationError("workflow: no actor available. The engine resolves the actor from the client's token via `client.request({ uri: '/users/me' })`. Supply a real `@sanity/client` configured with a token (the test bench serves these endpoints per registered token).");
    const grantsPromise = args.grantsFromPath !== void 0 ? cachedGrants({
        client: client,
        requestFn: requestFn,
        resourcePath: args.grantsFromPath
    }) : Promise.resolve(void 0), [identity, grants] = await Promise.all([ cachedActor(client, requestFn), grantsPromise ]);
    if (identity === void 0) throw new ContractViolationError("workflow: failed to resolve actor from `/users/me`. The client is configured but the endpoint returned no usable identity — check the token.");
    return {
        actor: identity.actor,
        ...identity.localPrincipalId !== void 0 ? {
            localPrincipalId: identity.localPrincipalId
        } : {},
        ...grants !== void 0 ? {
            grants: grants
        } : {}
    };
}

async function resolveUserAttributes(taggedClient) {
    const client = unwrapRequestTag(taggedClient), requestFn = lazyRequest(client);
    if (requestFn !== void 0) return cachedAttributes({
        client: client,
        requestFn: requestFn
    });
}

function cachedActor(client, requestFn) {
    const cached = actorCache.get(client);
    if (cached !== void 0) return cached;
    const pending = fetchActor(client, requestFn).catch(err => {
        throw actorCache.get(client) === pending && actorCache.delete(client), err;
    });
    return actorCache.set(client, pending), pending;
}

function grantsForClientPath(taggedClient, resourcePath) {
    const client = unwrapRequestTag(taggedClient), requestFn = lazyRequest(client);
    return requestFn === void 0 ? Promise.resolve(void 0) : cachedGrants({
        client: client,
        requestFn: requestFn,
        resourcePath: resourcePath
    });
}

function lazyRequest(client) {
    return client.request === void 0 ? void 0 : opts => client.request(opts);
}

function cachedGrants({client: client, requestFn: requestFn, resourcePath: resourcePath}) {
    return cachedByKey({
        store: grantsCache,
        client: client,
        key: resourcePath,
        load: () => fetchGrantsCached(requestFn, resourcePath)
    });
}

function cachedByKey(args) {
    let byKey = args.store.get(args.client);
    byKey === void 0 && (byKey = /* @__PURE__ */ new Map, args.store.set(args.client, byKey));
    const cached = byKey.get(args.key);
    if (cached !== void 0) return cached;
    const pending = args.evictOnRejection ? args.load().catch(err => {
        throw byKey.get(args.key) === pending && byKey.delete(args.key), err;
    }) : args.load();
    return byKey.set(args.key, pending), pending;
}

async function fetchActor(client, requestFn) {
    const [resourceUser, globalUser] = await Promise.all([ fetchCurrentUser(requestFn, "the workflow resource host"), fetchGlobalUser(client) ]), resourceId = usableId(resourceUser), globalHostId = usableId(globalUser.user), carriedId = firstCarriedGlobalId([ globalHostId, resourceId ]), sessionId = carriedId ?? globalHostId ?? resourceId;
    if (sessionId === void 0) return;
    const bridge = carriedId === void 0 ? await bridgeProjectPrincipal({
        client: client,
        sessionId: sessionId,
        resourceId: resourceId
    }) : NO_BRIDGE, id = bridge.status === "resolved" ? bridge.globalId : sessionId;
    refuseProjectScopedActor({
        id: id,
        globalUser: globalUser,
        globalHostId: globalHostId,
        bridgeFailure: bridge.status === "unavailable" ? bridge : void 0
    });
    const roleNames = roleNamesFor(resourceUser, globalUser);
    return {
        actor: {
            kind: "person",
            id: id,
            ...roleNames.length > 0 ? {
                roles: roleNames
            } : {}
        },
        ...resourceId !== void 0 && resourceId !== id ? {
            localPrincipalId: resourceId
        } : {}
    };
}

function roleNamesFor(resourceUser, globalUser) {
    return (resourceUser?.roles?.length ? resourceUser : globalUser.user)?.roles?.map(r => r.name).filter(n => !!n) ?? [];
}

const NO_BRIDGE = {
    status: "not-applicable"
};

function globalRouteReason(globalUser, globalHostId) {
    return "reason" in globalUser ? globalUser.reason : globalHostId === void 0 ? "no record" : `the global host answered with project-scoped principal "${globalHostId}"`;
}

async function bridgeProjectPrincipal(args) {
    const {client: client, sessionId: sessionId, resourceId: resourceId} = args;
    return classifyPrincipalId(sessionId).namespace !== "project" ? NO_BRIDGE : resourceId === void 0 || classifyPrincipalId(resourceId).namespace !== "project" ? {
        status: "unavailable",
        reason: "no project-scoped principal from the resource host to address the directory with"
    } : directoryGlobalId({
        client: client,
        principalId: resourceId
    });
}

async function directoryGlobalId(args) {
    const {client: client, principalId: principalId} = args, projectId = projectIdOf(client);
    if (projectId === void 0) return {
        status: "unavailable",
        reason: "the client reports no projectId, so its user directory has no address"
    };
    const lookup = await clientProjectUserDirectory(withRequestTag(client, REQUEST_TAG.accessResolveActor), projectId).findById(principalId);
    if (lookup.status !== "resolved") return {
        status: "unavailable",
        reason: `project "${projectId}"'s user directory reported the principal ${lookup.status}`,
        ...lookup.status === "inaccessible" && lookup.cause !== void 0 ? {
            cause: lookup.cause
        } : {}
    };
    const globalId = directoryBridgeId(lookup.user.sanityUserId);
    return globalId === void 0 ? {
        status: "unavailable",
        reason: `project "${projectId}"'s user directory row for the principal carries no account-global sanityUserId`
    } : {
        status: "resolved",
        globalId: globalId
    };
}

function refuseProjectScopedActor(args) {
    const {id: id, globalUser: globalUser, globalHostId: globalHostId, bridgeFailure: bridgeFailure} = args;
    if (classifyPrincipalId(id).namespace !== "project") return;
    const routes = [ `The account-global record: ${globalRouteReason(globalUser, globalHostId)}.` ];
    bridgeFailure !== void 0 && routes.push(`The project user directory: ${bridgeFailure.reason}.`);
    const cause = bridgeFailure?.cause ?? ("cause" in globalUser ? globalUser.cause : void 0);
    throw new Error(`workflow: the caller's identity resolves to the project-scoped principal "${id}" and its account-global id could not be resolved. ${routes.join(" ")} The engine speaks account-global user ids only and never acts as a project-scoped principal.`, cause === void 0 ? void 0 : {
        cause: cause
    });
}

function usableId(user) {
    if (!(!user || typeof user.id != "string" || user.id.length === 0)) return user.id;
}

async function fetchCurrentUser(requestFn, hostDescription) {
    try {
        return await requestFn({
            uri: "/users/me",
            tag: REQUEST_TAG.accessResolveActor
        });
    } catch (err) {
        throw new Error(`workflow: /users/me request against ${hostDescription} failed. The engine resolves the actor from the client's token via \`client.request({ uri: '/users/me' })\`. Check the token/connectivity.`, {
            cause: err
        });
    }
}

async function fetchGlobalUser(client) {
    const sibling = globalHostRequestOf(client);
    if ("reason" in sibling) return {
        user: void 0,
        reason: sibling.reason,
        ...sibling.cause !== void 0 ? {
            cause: sibling.cause
        } : {}
    };
    try {
        const user = await sibling.request({
            uri: "/users/me",
            tag: REQUEST_TAG.accessResolveActor
        }), id = usableId(user);
        return sibling.requireGlobalPrincipal && id !== void 0 && classifyPrincipalId(id).namespace === "project" ? {
            user: void 0,
            reason: `global-host /users/me returned project-scoped principal "${id}"; the sibling client is still bound to a project API host`
        } : {
            user: user
        };
    } catch (err) {
        return {
            user: void 0,
            reason: `global-host /users/me failed: ${errorMessage(err)}`,
            cause: err
        };
    }
}

function globalHostRequestOf(client) {
    if (typeof client.withConfig != "function") return {
        reason: "the client cannot reach the global API host (no withConfig)"
    };
    const hostResolution = resolveGlobalApiHost(client);
    try {
        const globalClient = client.withConfig({
            useProjectHostname: !1,
            apiVersion: ENGINE_API_VERSION,
            ...hostResolution.apiHost === void 0 ? {} : {
                apiHost: hostResolution.apiHost
            }
        }), request = lazyRequest(globalClient);
        return request === void 0 ? {
            reason: "the global-host sibling client cannot issue requests"
        } : {
            request: request,
            requireGlobalPrincipal: hostResolution.requireGlobalPrincipal
        };
    } catch (err) {
        return {
            reason: `building the global-host sibling client failed: ${errorMessage(err)}`,
            cause: err
        };
    }
}

function resolveGlobalApiHost(client) {
    if (typeof client.config != "function") return {
        requireGlobalPrincipal: !1
    };
    const apiHost = client.config().apiHost;
    if (apiHost === void 0) return {
        requireGlobalPrincipal: !1
    };
    let parsed;
    try {
        parsed = new URL(apiHost);
    } catch {
        return {
            requireGlobalPrincipal: !0
        };
    }
    if (/^api\.sanity\.(io|work)$/.test(parsed.hostname)) return {
        requireGlobalPrincipal: !1
    };
    const match = /^[^.]+\.api\.sanity\.(io|work)$/.exec(parsed.hostname);
    return match === null ? {
        requireGlobalPrincipal: !0
    } : (parsed.hostname = `api.sanity.${match[1]}`, {
        apiHost: parsed.origin,
        requireGlobalPrincipal: !0
    });
}

async function fetchGrantsCached(requestFn, resourcePath) {
    try {
        return await fetchGrants({
            client: {
                request: requestFn
            },
            resourcePath: resourcePath
        });
    } catch (err) {
        console.warn(`workflow: failed to fetch grants from "${resourcePath}"; advisory permission reads that depend on them are skipped — a rendered $can stays undefined (conditions referencing it fail closed) and the subject-write forecast omits this resource. The lake still enforces writes. Original error: ${errorMessage(err)}`);
        return;
    }
}

function projectIdOf(client) {
    if (typeof client.config != "function") return;
    const projectId = client.config().projectId;
    return typeof projectId == "string" && projectId.length > 0 ? projectId : void 0;
}

function cachedAttributes(args) {
    const projectId = projectIdOf(args.client);
    return projectId === void 0 ? Promise.resolve(void 0) : resolveOrganizationId({
        client: args.client,
        requestFn: args.requestFn,
        projectId: projectId
    }).then(orgId => {
        if (orgId !== void 0) return cachedByKey({
            store: attributesCache,
            client: args.client,
            key: orgId,
            evictOnRejection: !0,
            load: () => fetchAttributesCached({
                client: args.client,
                orgId: orgId
            })
        });
    });
}

async function resolveOrganizationId(args) {
    const {client: client, requestFn: requestFn, projectId: projectId} = args;
    return cachedByKey({
        store: orgIdCache,
        client: client,
        key: projectId,
        evictOnRejection: !0,
        load: async () => {
            try {
                const project = await requestFn({
                    uri: `/projects/${encodeURIComponent(projectId)}`,
                    tag: REQUEST_TAG.accessResolveOrg
                });
                if (!isRecord(project)) return;
                const orgId = project.organizationId;
                return typeof orgId == "string" && orgId.length > 0 ? orgId : void 0;
            } catch (err) {
                const status = httpStatusOf(err);
                if (status !== void 0 && EXPECTED_ATTRIBUTES_ABSENCE.has(status)) return;
                throw new Error(`workflow: failed to resolve organizationId for project "${projectId}"; advisory $attributes cannot be bound. Original error: ${errorMessage(err)}`, {
                    cause: err
                });
            }
        }
    });
}

const ATTRIBUTES_FETCH_LIMIT = 100, EXPECTED_ATTRIBUTES_ABSENCE = /* @__PURE__ */ new Set([ 401, 402, 403, 404 ]);

function httpStatusOf(err) {
    if (!isRecord(err)) return;
    const status = err.statusCode;
    return typeof status == "number" ? status : void 0;
}

async function fetchAttributesCached(args) {
    const {client: client, orgId: orgId} = args, sibling = globalHostRequestOf(client);
    if ("reason" in sibling) {
        console.warn(`workflow: skipped org attributes fetch (${sibling.reason}); advisory $attributes stays undefined (conditions referencing it fail closed).`);
        return;
    }
    try {
        const response = await sibling.request({
            uri: `/organizations/${encodeURIComponent(orgId)}/users/me/attributes?limit=${ATTRIBUTES_FETCH_LIMIT}`,
            tag: REQUEST_TAG.accessAttributes
        }), envelope = isRecord(response) ? response : void 0;
        if (envelope?.hasMore === !0) {
            const page = Array.isArray(envelope.attributes) ? envelope.attributes : [];
            console.warn(`workflow: org attributes for organization "${orgId}" report hasMore=true (bound ${page.length} of the requested limit ${ATTRIBUTES_FETCH_LIMIT}); advisory $attributes may be incomplete — the engine fetches no further pages. Conditions that read missing keys fail closed.`);
        }
        const normalized = normalizeUserAttributes(response);
        return normalized === void 0 && console.warn(`workflow: org attributes response for organization "${orgId}" was unusable; advisory $attributes stays undefined (conditions referencing it fail closed).`), 
        normalized;
    } catch (err) {
        const status = httpStatusOf(err);
        if (status !== void 0 && EXPECTED_ATTRIBUTES_ABSENCE.has(status)) return;
        throw new Error(`workflow: failed to fetch org attributes for organization "${orgId}". Original error: ${errorMessage(err)}`, {
            cause: err
        });
    }
}

function callerBoundVars(args) {
    const vars = {
        ...args.can !== void 0 ? {
            can: args.can
        } : {},
        ...args.attributes !== void 0 ? {
            attributes: args.attributes
        } : {}
    };
    return Object.keys(vars).length > 0 ? vars : void 0;
}

async function callerBoundVarsForCall(args) {
    const can = await advisoryCanForCall({
        instance: args.instance,
        options: args.options
    }), attributes = args.options?.attributes !== void 0 ? args.options.attributes : await resolveUserAttributes(args.client);
    return callerBoundVars({
        ...can !== void 0 ? {
            can: can
        } : {},
        ...attributes !== void 0 ? {
            attributes: attributes
        } : {}
    });
}

async function fireAction(args) {
    const {client: client, instanceId: instanceId, activity: activity, action: action, params: params, requestRecord: requestRecord, options: options} = args;
    return retryOnRevisionConflict({
        client: client,
        instanceId: instanceId,
        options: options,
        commit: ctx => commitAction({
            ctx: ctx,
            activityName: activity,
            actionName: action,
            callerParams: params,
            requestRecord: requestRecord,
            options: options
        }),
        onExhausted: () => new ConcurrentFireActionError({
            instanceId: instanceId,
            activity: activity,
            action: action,
            attempts: CONCURRENT_COMMIT_MAX_ATTEMPTS
        })
    });
}

async function resolveActionCommit({ctx: ctx, activityName: activityName, actionName: actionName, callerParams: callerParams, options: options}) {
    const actor = options?.actor, {stage: stage, activity: activity} = findActivityInCurrentStage(ctx, activityName), action = (activity.actions ?? []).find(a => a.name === actionName);
    if (action === void 0) throw new ContractViolationError(`Action "${actionName}" not declared on activity "${activityName}"`);
    if (action.when !== void 0) throw new ActionDisabledError({
        activity: activityName,
        action: actionName,
        reason: {
            kind: "cascade-fired",
            when: action.when
        }
    });
    if (action.filter !== void 0) {
        const vars = await callerBoundVarsForCall({
            client: ctx.client,
            instance: ctx.instance,
            options: options
        });
        if (!await ctxEvaluateCondition({
            ctx: ctx,
            condition: action.filter,
            opts: {
                activityName: activityName,
                ...actor !== void 0 ? {
                    actor: actor
                } : {},
                ...vars !== void 0 ? {
                    vars: vars
                } : {}
            }
        })) throw new ActionDisabledError({
            activity: activityName,
            action: actionName,
            reason: {
                kind: "filter-failed",
                filter: action.filter,
                detail: "commit re-check"
            }
        });
    }
    const entry = findCurrentActivities(ctx.instance).find(t => t.name === activityName);
    if (entry === void 0 || entry.status !== "active") throw notActiveAtCommit({
        activityName: activityName,
        actionName: actionName,
        status: entry?.status
    });
    const params = validateActionParams({
        action: action,
        activityName: activityName,
        callerParams: callerParams
    });
    return {
        stage: stage,
        activity: activity,
        action: action,
        params: params
    };
}

function notActiveAtCommit(args) {
    const {activityName: activityName, actionName: actionName, status: status} = args;
    return status !== void 0 && isTerminalActivityStatus(status) ? new ActionDisabledError({
        activity: activityName,
        action: actionName,
        reason: {
            kind: "activity-not-active",
            status: status
        }
    }) : new Error(`Activity "${activityName}" must be active to fire action "${actionName}"; status is ${status ?? "missing"}`);
}

async function applyActionFire({ctx: ctx, mutation: mutation, activity: activity, action: action, params: params, actor: actor, triggered: triggered}) {
    mutation.history.push({
        _key: randomKey(),
        _type: "actionFired",
        at: ctx.now,
        stage: mutation.currentStage,
        activity: activity.name,
        action: action.name,
        ...actor !== void 0 ? {
            actor: actor,
            driverKind: driverKind(actor)
        } : {},
        ...triggered ? {
            triggered: !0
        } : {}
    });
    const ranOps = await runOps({
        ops: action.ops,
        mutation: mutation,
        stage: mutation.currentStage,
        origin: {
            activity: activity.name,
            action: action.name
        },
        params: params,
        actor: actor,
        self: selfGdr(ctx.instance),
        now: ctx.now,
        snapshot: ctx.snapshot,
        refSurface: ctx.refSurface,
        memberRoles: ctx.memberRoles,
        roleAliases: ctx.definition.roleAliases
    });
    if (await queueEffects({
        ctx: ctx,
        mutation: mutation,
        effects: action.effects,
        origin: {
            kind: "action",
            name: action.name
        },
        actor: actor,
        opts: {
            callerParams: params,
            activityName: activity.name
        }
    }), action.spawn !== void 0) {
        if (!isEngineContext(ctx)) throw new ContractViolationError(`Action "${action.name}" declares spawn, which requires a lake-capable engine context`);
        await spawnSubworkflows({
            ctx: ctx,
            mutation: mutation,
            activity: activity,
            action: action,
            sub: action.spawn,
            actor: actor
        });
    }
    return {
        ranOps: ranOps
    };
}

async function commitAction({ctx: ctx, activityName: activityName, actionName: actionName, callerParams: callerParams, requestRecord: requestRecord, options: options}) {
    assertRequestUnprocessed({
        instance: ctx.instance,
        record: requestRecord,
        now: ctx.now
    });
    const actor = options?.actor, {stage: stage, activity: activity, action: action, params: params} = await resolveActionCommit({
        ctx: ctx,
        activityName: activityName,
        actionName: actionName,
        callerParams: callerParams,
        options: options
    }), mutation = startRevisionRetryMutation(ctx.instance);
    recordProcessedRequest({
        mutation: mutation,
        record: requestRecord,
        now: ctx.now
    });
    const mutEntry = requireMutationActivityEntry(mutation, activityName), statusBefore = mutEntry.status, {ranOps: ranOps} = await applyActionFire({
        ctx: ctx,
        mutation: mutation,
        activity: activity,
        action: action,
        params: params,
        actor: actor,
        triggered: !1
    }), newStatus = mutEntry.status !== statusBefore ? mutEntry.status : void 0;
    return await persistThenMaybeRefresh({
        ctx: ctx,
        mutation: mutation,
        stageName: stage.name,
        didChangeState: ranOps.some(isFieldOp)
    }), {
        fired: !0,
        activity: activityName,
        action: actionName,
        ...newStatus !== void 0 ? {
            newStatus: newStatus
        } : {},
        ...ranOps.length > 0 ? {
            ranOps: ranOps
        } : {}
    };
}

async function runTriggeredActions({ctx: ctx, mutation: mutation, stage: stage}) {
    let fires = 0, ranFieldOps = !1;
    const liveCtx = liveViewContext(ctx, mutation);
    let firedThisPass = !0;
    for (;firedThisPass; ) {
        firedThisPass = !1;
        for (const activity of stage.activities ?? []) {
            const fired = await runActivityTriggers({
                ctx: liveCtx,
                mutation: mutation,
                activity: activity
            });
            fires += fired.fires, ranFieldOps = ranFieldOps || fired.ranFieldOps, fired.fires > 0 && (firedThisPass = !0);
        }
    }
    return {
        fires: fires,
        ranFieldOps: ranFieldOps
    };
}

async function runActivityTriggers({ctx: ctx, mutation: mutation, activity: activity}) {
    let fires = 0, ranFieldOps = !1;
    for (const action of activity.actions ?? []) {
        if (!isCascadeFired(action)) continue;
        const entry = currentActivities(mutation).find(e => e.name === activity.name);
        if (entry === void 0 || entry.status !== "active") break;
        if ((entry.firedActions ?? []).includes(action.name) || !await triggerIsLive({
            ctx: ctx,
            activity: activity,
            action: action
        })) continue;
        const result = await fireTriggeredAction({
            ctx: ctx,
            mutation: mutation,
            activity: activity,
            action: action,
            entry: entry
        });
        if (fires++, ranFieldOps = ranFieldOps || result.ranFieldOps, isTerminalActivityStatus(entry.status)) break;
    }
    return {
        fires: fires,
        ranFieldOps: ranFieldOps
    };
}

async function triggerIsLive({ctx: ctx, activity: activity, action: action}) {
    if (!tokenMayExecute({
        actor: ctx.actor,
        action: action,
        ctx: ctx
    })) return !1;
    const scope = {
        activityName: activity.name
    };
    return action.filter !== void 0 && await ctxEvaluateConditionOutcome({
        ctx: ctx,
        condition: action.filter,
        opts: scope
    }) !== "satisfied" ? !1 : await ctxEvaluateConditionOutcome({
        ctx: ctx,
        condition: action.when,
        opts: scope
    }) === "satisfied";
}

function tokenMayExecute({actor: actor, action: action, ctx: ctx}) {
    const roles = action.roles;
    return roles === void 0 || roles.length === 0 ? !0 : actor === void 0 ? !1 : roles.some(required => actorFulfillsRole({
        actorRoles: actor.roles,
        required: required,
        aliases: ctx.definition.roleAliases
    }));
}

async function fireTriggeredAction({ctx: ctx, mutation: mutation, activity: activity, action: action, entry: entry}) {
    entry.firedActions = [ ...entry.firedActions ?? [], action.name ];
    const {ranOps: ranOps} = await applyActionFire({
        ctx: ctx,
        mutation: mutation,
        activity: activity,
        action: action,
        params: {},
        actor: ctx.actor,
        triggered: !0
    });
    return {
        ranFieldOps: ranOps.some(isFieldOp)
    };
}

async function primeInitialStage(args) {
    const {client: client, instanceId: instanceId, refSurface: refSurface, clientForGdr: clientForGdr} = args, instance = await getInstanceDocument(client, instanceId);
    if (!instance || instance.stages.length > 0) return;
    const ctx = await buildCascadeStepContext(args, instance), {definition: definition} = ctx, stage = definition.stages.find(s => s.name === instance.currentStage);
    if (stage === void 0) return;
    const now = ctx.now, discards = [], initialStageEntry = {
        _key: randomKey(),
        name: stage.name,
        enteredAt: now,
        fields: await resolveStageFieldEntries({
            client: client,
            instance: instance,
            stage: stage,
            now: now,
            refSurface: refSurface,
            memberRoles: ctx.memberRoles,
            roleAliases: ctx.definition.roleAliases,
            recordDiscard: recordFieldDiscards({
                target: discards,
                scope: "stage",
                at: now
            })
        }),
        activities: []
    }, primedCtx = {
        ...ctx,
        instance: {
            ...instance,
            stages: [ initialStageEntry ]
        }
    };
    initialStageEntry.activities = await buildStageActivities({
        ctx: primedCtx,
        stage: stage,
        recordDiscard: recordFieldDiscards({
            target: discards,
            scope: "activity",
            at: now
        })
    });
    const terminal = isTerminalStage(stage), committed = await client.patch(instance._id).set({
        stages: assignmentStagesForWrite([ initialStageEntry ], writesSingularAssignmentLists(instance)),
        lastChangedAt: now,
        ...terminal ? {
            completedAt: now
        } : {},
        ...discards.length > 0 ? {
            history: [ ...instance.history, ...stampHistoryEntries(discards, ctx.executionContext) ]
        } : {}
    }).ifRevisionId(instance._rev).commit(SYNC_COMMIT);
    await deployOrRollback({
        client: client,
        instanceId: instance._id,
        committedRev: committed._rev,
        restore: {
            stages: instance.stages,
            history: instance.history
        },
        ...terminal ? {
            unset: [ "completedAt" ]
        } : {},
        reversible: !0,
        deploy: () => deployStageGuards({
            client: client,
            clientForGdr: clientForGdr,
            instance: instance,
            definition: definition,
            stageName: stage.name,
            now: now,
            snapshot: ctx.snapshot
        })
    });
}

const CASCADE_LIMIT = 100;

async function runCascadeHop({client: client, instanceId: instanceId, actor: actor, clientForGdr: clientForGdr, refSurface: refSurface, memberRolesLoader: memberRolesLoader, clock: clock, executionContext: executionContext, telemetry: telemetry, overlay: overlay, settlingCohorts: settlingCohorts, instance: instance}) {
    const contextOptions = {
        clientForGdr: clientForGdr,
        refSurface: refSurface,
        ...actor ? {
            actor: actor
        } : {},
        ...clock ? {
            clock: clock
        } : {},
        ...executionContext ? {
            executionContext: executionContext
        } : {},
        ...telemetry ? {
            telemetry: telemetry
        } : {},
        ...overlay ? {
            overlay: overlay
        } : {},
        ...settlingCohorts ? {
            settlingCohorts: settlingCohorts
        } : {},
        memberRolesLoader: memberRolesLoader
    };
    let preloadedInstance = instance;
    return retryCommit({
        load: async () => {
            if (preloadedInstance === void 0) return loadContext({
                client: client,
                instanceId: instanceId,
                options: contextOptions
            });
            const loaded = preloadedInstance;
            return preloadedInstance = void 0, buildEngineContext({
                client: client,
                instance: loaded,
                definition: parseDefinitionSnapshot(loaded),
                ...contextOptions
            });
        },
        commit: ctx => commitCascadeHop({
            ctx: ctx,
            actor: actor
        }),
        shouldRetry: isInstanceCommitRevisionConflict,
        onExhausted: () => new ConcurrentCascadeError({
            instanceId: instanceId,
            attempts: CONCURRENT_COMMIT_MAX_ATTEMPTS
        })
    });
}

async function commitCascadeHop({ctx: ctx, actor: actor}) {
    if (isTerminal(ctx)) return {
        moved: !1
    };
    const stage = findStage(ctx.definition, ctx.instance.currentStage), mutation = startRevisionRetryMutation(ctx.instance), fired = await runTriggeredActions({
        ctx: ctx,
        mutation: mutation,
        stage: stage
    }), hopCtx = liveViewContext(ctx, mutation), transition = await pickTransition(hopCtx, stage);
    if (transition === void 0) {
        const nextEvaluationAt = await ctxNextEvaluationAt(hopCtx);
        return fired.fires > 0 && await persistThenMaybeRefresh({
            ctx: ctx,
            mutation: mutation,
            stageName: stage.name,
            didChangeState: fired.ranFieldOps
        }), {
            moved: !1,
            ...nextEvaluationAt !== void 0 ? {
                nextEvaluationAt: nextEvaluationAt
            } : {}
        };
    }
    return await commitStageMove({
        ctx: ctx,
        mutation: mutation,
        fromStage: stage,
        toStage: findStage(ctx.definition, transition.to),
        transition: transition.name,
        via: "transition",
        actor: actor
    }), {
        moved: !0
    };
}

async function cascadeAutoTransitions({client: client, instanceId: instanceId, actor: actor, clientForGdr: clientForGdr, refSurface: refSurface, memberRolesLoader: memberRolesLoader, clock: clock, executionContext: executionContext, telemetry: telemetry, overlay: overlay, settlingCohorts: settlingCohorts}) {
    let count = 0;
    for (;;) {
        const drained = await drainCondemnedChildren({
            client: client,
            instanceId: instanceId,
            actor: actor,
            clientForGdr: clientForGdr,
            refSurface: refSurface,
            memberRolesLoader: memberRolesLoader,
            clock: clock,
            executionContext: executionContext,
            telemetry: telemetry
        }), hop = await runCascadeHop({
            client: client,
            instanceId: instanceId,
            actor: actor,
            clientForGdr: clientForGdr,
            refSurface: refSurface,
            memberRolesLoader: memberRolesLoader,
            clock: clock,
            executionContext: executionContext,
            telemetry: telemetry,
            overlay: overlay,
            settlingCohorts: settlingCohorts,
            ...drained.drained ? {} : {
                instance: drained.instance
            }
        });
        if (!hop.moved) return {
            cascaded: count,
            ...hop.nextEvaluationAt !== void 0 ? {
                nextEvaluationAt: hop.nextEvaluationAt
            } : {}
        };
        if (count++, count >= CASCADE_LIMIT) throw new CascadeLimitError({
            instanceId: instanceId,
            limit: CASCADE_LIMIT
        });
    }
}

async function drainCondemnedChildren({client: client, instanceId: instanceId, actor: actor, clientForGdr: clientForGdr, refSurface: refSurface, memberRolesLoader: memberRolesLoader, clock: clock, executionContext: executionContext, telemetry: telemetry, draining: draining = /* @__PURE__ */ new Set, instance: preloadedInstance}) {
    if (draining.has(instanceId)) return {
        instance: preloadedInstance,
        drained: !1
    };
    draining.add(instanceId);
    const instance = preloadedInstance ?? await getInstanceDocument(client, instanceId);
    if (!instance) return {
        instance: void 0,
        drained: !1
    };
    const condemned = condemnedSubworkflows(instance);
    if (condemned.length === 0) return {
        instance: instance,
        drained: !1
    };
    for (const row of condemned) await settleCondemnedRow({
        client: client,
        ownerId: instanceId,
        row: row,
        actor: actor,
        clientForGdr: clientForGdr,
        refSurface: refSurface,
        memberRolesLoader: memberRolesLoader,
        clock: clock,
        executionContext: executionContext,
        telemetry: telemetry,
        draining: draining
    });
    return await stampDrainedRows({
        client: client,
        instance: instance,
        condemned: condemned,
        clock: clock,
        executionContext: executionContext
    }), {
        instance: instance,
        drained: !0
    };
}

async function settleCondemnedRow({client: client, ownerId: ownerId, row: row, actor: actor, clientForGdr: clientForGdr, refSurface: refSurface, memberRolesLoader: memberRolesLoader, clock: clock, executionContext: executionContext, telemetry: telemetry, draining: draining}) {
    const childId = toBareId(row.ref.id);
    try {
        const result = await abortInstance({
            client: client,
            instanceId: childId,
            reason: row.abortPending?.reason ?? "condemned by parent",
            options: {
                ...actor ? {
                    actor: actor
                } : {},
                clientForGdr: clientForGdr,
                refSurface: refSurface,
                memberRolesLoader: memberRolesLoader,
                ...clock ? {
                    clock: clock
                } : {},
                ...executionContext ? {
                    executionContext: executionContext
                } : {},
                ...telemetry ? {
                    telemetry: telemetry
                } : {}
            }
        });
        await drainCondemnedChildren({
            client: client,
            instanceId: childId,
            actor: actor,
            clientForGdr: clientForGdr,
            refSurface: refSurface,
            memberRolesLoader: memberRolesLoader,
            clock: clock,
            executionContext: executionContext,
            telemetry: telemetry,
            draining: draining,
            ...result.fired ? {
                instance: result.instance
            } : {}
        });
    } catch (cause) {
        if (cause instanceof InstanceNotFoundError) return;
        throw cause instanceof WorkflowStateDivergedError ? cause : new WorkflowStateDivergedError({
            instanceId: ownerId,
            guardError: cause,
            reason: `owed child abort of "${childId}" failed while draining condemned rows`
        });
    }
}

async function stampDrainedRows({client: client, instance: instance, condemned: condemned, clock: clock, executionContext: executionContext}) {
    const now = (clock ?? wallClock)(), stamp = resolveExecutionContext(executionContext), ids = condemned.map(row => toBareId(row.ref.id)), byId = await loadInstancesById(client, ids), condemnedKeys = new Set(condemned.map(row => row._key)), history = [ ...instance.history ], subworkflows = (instance.subworkflows ?? []).map(row => {
        if (row.resolved !== void 0 || !condemnedKeys.has(row._key)) return row;
        const child = byId.get(toBareId(row.ref.id)), resolved = child !== void 0 ? terminalResolution(child) : {
            at: now,
            aborted: !0
        };
        return resolved === void 0 ? row : (history.push(...stampHistoryEntries([ subworkflowResolvedEntry({
            row: row,
            at: now,
            status: resolved.aborted === !0 ? "aborted" : "done"
        }) ], stamp)), {
            ...row,
            resolved: resolved
        });
    });
    await client.patch(instance._id).set({
        subworkflows: subworkflows,
        history: history,
        lastChangedAt: now
    }).ifRevisionId(instance._rev).commit(SYNC_COMMIT);
}

async function loadInstancesById(client, ids) {
    const children = await client.fetch("*[_id in $ids]", {
        ids: ids
    });
    return new Map(children.filter(child => child._type === WORKFLOW_INSTANCE_TYPE).map(child => {
        const instance = readInstanceDoc(child);
        return [ instance._id, instance ];
    }));
}

function terminalResolution(child) {
    const status = resolvedChildStatus(child);
    if (!(status === void 0 || child.completedAt === void 0 || child.completedAt === null)) return {
        at: child.completedAt,
        stage: child.currentStage,
        ...status === "aborted" ? {
            aborted: !0
        } : {}
    };
}

function subworkflowResolvedEntry({row: row, at: at, status: status}) {
    return {
        _key: randomKey(),
        _type: "subworkflowResolved",
        at: at,
        activity: row.activity,
        instanceRef: row.ref,
        status: status
    };
}

async function propagateToAncestors(args) {
    const {instance: instance, ...stepArgs} = args, {client: client, instanceId: instanceId} = stepArgs, loaded = await loadPropagationPair({
        client: client,
        instanceId: instanceId,
        ...instance !== void 0 ? {
            instance: instance
        } : {}
    });
    if (loaded === void 0) return;
    const {child: child, parent: parent} = loaded;
    if (stepArgs.settlingCohorts?.has(parent._id)) return;
    const ctx = await buildCascadeStepContext(stepArgs, parent), mutation = startMutation(parent), row = mutation.subworkflows.find(r => toBareId(r.ref.id) === child._id);
    if (row === void 0) {
        await recordOrphanedPropagation({
            ctx: ctx,
            mutation: mutation,
            child: child
        });
        return;
    }
    const changed = stampResolvedChild({
        mutation: mutation,
        row: row,
        child: child,
        now: ctx.now
    }), parentTerminal = parent.completedAt !== void 0;
    changed && await persist(ctx, mutation), !parentTerminal && await cascadeAndPropagateParent(stepArgs, parent);
}

async function loadPropagationPair({client: client, instanceId: instanceId, instance: instance}) {
    const child = instance ?? await getInstanceDocument(client, instanceId);
    if (!child) return;
    const parentGdr = parentRef(child);
    if (parentGdr === void 0) return;
    const parentDoc = await client.getDocument(toBareId(parentGdr.id));
    if (!(!parentDoc || parentDoc._type !== WORKFLOW_INSTANCE_TYPE)) return {
        child: child,
        parent: readInstanceDoc(parentDoc)
    };
}

async function propagateSpawnBatch(args) {
    const {childIds: childIds, ...stepArgs} = args, {client: client, instanceId: instanceId} = stepArgs, parent = await getInstanceDocument(client, instanceId);
    if (parent === void 0) return;
    const children = await loadInstancesById(client, childIds);
    if (children.size !== childIds.length) throw new Error(`Spawn batch for ${instanceId} could not reload every child`);
    const ctx = await buildCascadeStepContext(stepArgs, parent), mutation = startMutation(parent);
    stampSpawnBatch({
        mutation: mutation,
        parent: parent,
        children: children,
        childIds: childIds,
        now: ctx.now
    }) && await persist(ctx, mutation), parent.completedAt === void 0 && await cascadeAndPropagateParent(stepArgs, parent);
}

async function buildCascadeStepContext(args, instance) {
    return buildEngineContext({
        client: args.client,
        clientForGdr: args.clientForGdr,
        refSurface: args.refSurface,
        instance: instance,
        definition: parseDefinitionSnapshot(instance),
        ...args.clock ? {
            clock: args.clock
        } : {},
        ...args.actor ? {
            actor: args.actor
        } : {},
        ...args.executionContext ? {
            executionContext: args.executionContext
        } : {},
        ...args.telemetry ? {
            telemetry: args.telemetry
        } : {},
        ...args.settlingCohorts ? {
            settlingCohorts: args.settlingCohorts
        } : {},
        memberRolesLoader: args.memberRolesLoader
    });
}

async function cascadeAndPropagateParent(args, parent) {
    await cascadeAutoTransitions({
        ...args,
        instanceId: parent._id
    }), await propagateToAncestors({
        ...args,
        instanceId: parent._id
    });
}

function stampSpawnBatch({mutation: mutation, parent: parent, children: children, childIds: childIds, now: now}) {
    let changed = !1;
    for (const childId of childIds) {
        const child = requireSpawnBatchChild({
            parent: parent,
            children: children,
            childId: childId
        }), row = mutation.subworkflows.find(entry => toBareId(entry.ref.id) === childId);
        if (row === void 0) throw new Error(`Spawn batch child ${childId} has no registry row on parent ${parent._id}`);
        changed = stampResolvedChild({
            mutation: mutation,
            row: row,
            child: child,
            now: now
        }) || changed;
    }
    return changed;
}

function requireSpawnBatchChild({parent: parent, children: children, childId: childId}) {
    const child = children.get(childId);
    if (child === void 0) throw new Error(`Spawn batch child ${childId} disappeared`);
    const expectedParent = parentRef(child);
    if (expectedParent === void 0 || toBareId(expectedParent.id) !== parent._id) throw new Error(`Spawn batch child ${childId} does not belong to parent ${parent._id}`);
    return child;
}

function stampResolvedChild({mutation: mutation, row: row, child: child, now: now}) {
    if (row.resolved !== void 0) return !1;
    const resolved = terminalResolution(child);
    return resolved === void 0 ? !1 : (row.resolved = resolved, mutation.history.push(subworkflowResolvedEntry({
        row: row,
        at: now,
        status: resolved.aborted === !0 ? "aborted" : "done"
    })), !0);
}

async function recordOrphanedPropagation({ctx: ctx, mutation: mutation, child: child}) {
    const childUri = selfGdr(child);
    mutation.history.some(h => h._type === "subworkflowOrphaned" && h.instanceRef.id === childUri) || (mutation.history.push({
        _key: randomKey(),
        _type: "subworkflowOrphaned",
        at: ctx.now,
        instanceRef: {
            id: childUri,
            type: WORKFLOW_INSTANCE_TYPE
        },
        detail: `Instance "${child._id}" names this instance as its parent but no subworkflow-registry row matches it, so its state cannot drive any gate here.`
    }), await persist(ctx, mutation));
}

function startMutation(instance) {
    return {
        minReaderModel: minReaderModelOf(instance),
        definitionSnapshot: instance.definitionSnapshot,
        definitionModelVersion: definitionAssignmentModel(instance),
        currentStage: instance.currentStage,
        fields: (instance.fields ?? []).map(s => ({
            ...s
        })),
        stages: instance.stages.map(s => ({
            ...s,
            fields: (s.fields ?? []).map(entry => ({
                ...entry
            })),
            activities: s.activities.map(t => ({
                ...t,
                ...t.fields !== void 0 ? {
                    fields: t.fields.map(entry => ({
                        ...entry
                    }))
                } : {}
            }))
        })),
        subworkflows: (instance.subworkflows ?? []).map(row => ({
            ...row,
            ...row.abortPending !== void 0 ? {
                abortPending: {
                    ...row.abortPending
                }
            } : {},
            ...row.resolved !== void 0 ? {
                resolved: {
                    ...row.resolved
                }
            } : {}
        })),
        pendingEffects: [ ...instance.pendingEffects ],
        effectHistory: [ ...instance.effectHistory ],
        context: [ ...instance.context ],
        history: [ ...instance.history ],
        processedRequests: [ ...instance.processedRequests ?? [] ],
        lastChangedAt: instance.lastChangedAt,
        ...instance.completedAt !== void 0 ? {
            completedAt: instance.completedAt
        } : {},
        ...instance.abortedAt !== void 0 ? {
            abortedAt: instance.abortedAt
        } : {},
        pendingCreates: []
    };
}

function startRevisionRetryMutation(instance) {
    const mutation = startMutation(instance);
    return mutation.commitErrorMapper = error => isRetryableInstanceCommitConflict(error) ? new InstanceCommitRevisionConflict(error) : error, 
    mutation;
}

function instanceStateFields(src) {
    const listModel = isAssignmentListModel(src.definitionModelVersion), state = {
        currentStage: src.currentStage,
        fields: assignmentFieldsForWrite(src.fields, listModel),
        stages: assignmentStagesForWrite(src.stages, listModel),
        subworkflows: src.subworkflows ?? [],
        pendingEffects: src.pendingEffects,
        effectHistory: src.effectHistory,
        context: src.context,
        history: src.history,
        processedRequests: src.processedRequests ?? [],
        ...src.completedAt !== void 0 ? {
            completedAt: src.completedAt
        } : {},
        ...src.abortedAt !== void 0 ? {
            abortedAt: src.abortedAt
        } : {}
    };
    return {
        ...modelStampFor({
            documentType: "instance",
            document: {
                ...state,
                definitionSnapshot: src.definitionSnapshot
            },
            storedMinReaderModel: src.minReaderModel
        }),
        ...state
    };
}

function liveViewContext(ctx, mutation) {
    const materialized = materializeInstance(ctx.instance, mutation);
    return {
        ...ctx,
        instance: materialized,
        snapshot: overlayInstanceInSnapshot(ctx.snapshot, materialized)
    };
}

function materializeInstance(base, mutation) {
    return {
        ...base,
        currentStage: mutation.currentStage,
        fields: mutation.fields,
        stages: mutation.stages,
        subworkflows: mutation.subworkflows,
        context: mutation.context,
        pendingEffects: mutation.pendingEffects,
        effectHistory: mutation.effectHistory
    };
}

async function persist(ctx, mutation) {
    stampExecutionContext(ctx, mutation);
    const set = {
        ...instanceStateFields(mutation),
        lastChangedAt: ctx.now
    }, pendingCreates = mutation.pendingCreates;
    if (pendingCreates.length === 0) return commitInstanceWrite(() => ctx.client.patch(ctx.instance._id).set(set).ifRevisionId(ctx.instance._rev).commit(SYNC_COMMIT), mutation.commitErrorMapper);
    const tx = ctx.client.transaction();
    for (const {body: body} of pendingCreates) tx.create(instanceDocumentForWrite(body));
    tx.patch(ctx.client.patch(ctx.instance._id).set(set).ifRevisionId(ctx.instance._rev)), 
    await commitInstanceWrite(() => tx.commit(), mutation.commitErrorMapper), mutation.pendingCreates = [];
    const actorForPriming = ctx.actor, settlingCohorts = new Set(ctx.settlingCohorts).add(ctx.instance._id);
    for (const {body: body} of pendingCreates) try {
        const stepArgs = spawnStepArgs(ctx, {
            instanceId: body._id,
            actor: actorForPriming,
            settlingCohorts: settlingCohorts
        });
        await primeInitialStage(stepArgs), await cascadeAutoTransitions(stepArgs);
    } catch (cause) {
        throw cause instanceof WorkflowStateDivergedError ? cause : new WorkflowStateDivergedError({
            instanceId: ctx.instance._id,
            guardError: cause,
            reason: `spawned child "${body._id}" failed to settle after the spawn transaction committed`
        });
    }
    await settleSpawnBatch(ctx, {
        pendingCreates: pendingCreates,
        actor: actorForPriming,
        settlingCohorts: settlingCohorts
    });
    for (const {started: started} of pendingCreates) ctx.telemetry.log(WorkflowInstanceStarted, started);
    const reloaded = await getInstanceDocument(ctx.client, ctx.instance._id);
    if (!reloaded) throw new Error(`Instance ${ctx.instance._id} disappeared after transaction commit`);
    return reloaded;
}

async function commitInstanceWrite(write, mapError) {
    try {
        return await write();
    } catch (error) {
        throw mapError?.(error) ?? error;
    }
}

async function settleSpawnBatch(ctx, args) {
    try {
        await propagateSpawnBatch({
            ...spawnStepArgs(ctx, {
                instanceId: ctx.instance._id,
                actor: args.actor,
                settlingCohorts: args.settlingCohorts
            }),
            childIds: args.pendingCreates.map(({body: body}) => body._id)
        });
    } catch (cause) {
        throw cause instanceof WorkflowStateDivergedError ? cause : new WorkflowStateDivergedError({
            instanceId: ctx.instance._id,
            guardError: cause,
            reason: "spawned children failed to propagate after the spawn transaction committed"
        });
    }
}

function spawnStepArgs(ctx, args) {
    return {
        client: ctx.client,
        instanceId: args.instanceId,
        actor: args.actor,
        clientForGdr: ctx.clientForGdr,
        refSurface: ctx.refSurface,
        memberRolesLoader: ctx.memberRolesLoader,
        clock: ctx.clock,
        executionContext: ctx.executionContext,
        telemetry: ctx.telemetry,
        settlingCohorts: args.settlingCohorts ?? ctx.settlingCohorts
    };
}

function stampExecutionContext(ctx, mutation) {
    const stamp = ctx.executionContext, appended = mutation.history.slice(ctx.instance.history.length);
    mutation.history = [ ...mutation.history.slice(0, ctx.instance.history.length), ...stampHistoryEntries(appended, stamp) ];
    for (const {body: body} of mutation.pendingCreates) body.history = stampHistoryEntries(body.history, stamp);
}

function currentStageEntry(mutation) {
    const entry = findOpenStageEntry(mutation);
    if (entry === void 0) throw new Error(`Mutation invariant broken: no current (un-exited) StageEntry for currentStage "${mutation.currentStage}"`);
    return entry;
}

function currentActivities(mutation) {
    return currentStageEntry(mutation).activities;
}

function findActivityInCurrentStage(ctx, activityName) {
    const stage = findStage(ctx.definition, ctx.instance.currentStage), activity = (stage.activities ?? []).find(t => t.name === activityName);
    if (activity === void 0) throw new ContractViolationError(`Activity "${activityName}" not found in current stage "${stage.name}" of ${ctx.definition.name}`);
    return {
        stage: stage,
        activity: activity
    };
}

function requireMutationActivityEntry(mutation, activity) {
    const mutEntry = currentActivities(mutation).find(t => t.name === activity);
    if (mutEntry === void 0) throw new Error(`Activity "${activity}" disappeared from mutation copy — invariant broken`);
    return mutEntry;
}

function findCurrentStageEntry(instance) {
    return findOpenStageEntry(instance);
}

function findCurrentActivities(instance) {
    return findCurrentStageEntry(instance)?.activities ?? [];
}

async function completeEffect(args) {
    const {client: client, instanceId: instanceId, effectKey: effectKey, status: status, outputs: outputs, ops: ops, detail: detail, error: error, durationMs: durationMs, requestRecord: requestRecord, options: options} = args;
    if (status !== "done" && status !== "failed") throw new ContractViolationError(`completeEffect: status must be "done" or "failed", got ${JSON.stringify(status)} — "cancelled" is engine-stamped by abort, never reportable`);
    return retryOnRevisionConflict({
        client: client,
        instanceId: instanceId,
        options: options,
        commit: ctx => commitCompleteEffect({
            ctx: ctx,
            effectKey: effectKey,
            status: status,
            outputs: outputs,
            ops: ops,
            detail: detail,
            error: error,
            durationMs: durationMs,
            requestRecord: requestRecord,
            actor: options.actor
        }),
        onExhausted: () => new ConcurrentCompleteEffectError({
            instanceId: instanceId,
            effectKey: effectKey,
            attempts: CONCURRENT_COMMIT_MAX_ATTEMPTS
        })
    });
}

function buildEffectHistoryEntry(pending, outcome) {
    const {status: status, ranAt: ranAt, actor: actor, detail: detail, error: error, durationMs: durationMs, outputs: outputs} = outcome, resolvedActor = actor ?? pending.actor;
    return {
        _key: pending._key,
        name: pending.name,
        ...pending.title !== void 0 ? {
            title: pending.title
        } : {},
        ...pending.description !== void 0 ? {
            description: pending.description
        } : {},
        params: pending.params,
        origin: pending.origin,
        ...resolvedActor !== void 0 ? {
            actor: resolvedActor
        } : {},
        ...pending.stageEntryKey !== void 0 ? {
            stageEntryKey: pending.stageEntryKey
        } : {},
        ranAt: ranAt,
        ...durationMs !== void 0 ? {
            durationMs: durationMs
        } : {},
        status: status,
        ...detail !== void 0 ? {
            detail: detail
        } : {},
        ...error !== void 0 ? {
            error: error
        } : {},
        ...outputs !== void 0 ? {
            outputs: outputs
        } : {}
    };
}

function buildEffectSettlement(pending, outcome) {
    const {status: status, ranAt: ranAt, actor: actor, detail: detail, outputs: outputs} = outcome;
    return {
        run: buildEffectHistoryEntry(pending, outcome),
        event: {
            _key: randomKey(),
            _type: "effectCompleted",
            at: ranAt,
            effectKey: pending._key,
            effect: pending.name,
            status: status,
            ...outputs !== void 0 ? {
                outputs: outputs
            } : {},
            ...detail !== void 0 ? {
                detail: detail
            } : {},
            ...actor !== void 0 ? {
                actor: actor
            } : {}
        }
    };
}

function validateEffectOutputs(args) {
    const {outputs: outputs, declared: declared, effectName: effectName} = args, byName = new Map(declared.map(shape => [ shape.name, shape ])), issues = [], normalized = {};
    for (const [key, value] of Object.entries(outputs)) {
        const shape = byName.get(key);
        if (shape === void 0) {
            issues.push(`"${key}" is not a declared output`);
            continue;
        }
        const check = effectOutputCheck({
            shape: shape,
            value: value,
            eligibility: args
        });
        "issues" in check ? issues.push(...check.issues.map(i => `"${key}": ${i}`)) : normalized[key] = check.output;
    }
    if (issues.length > 0) throw new EffectOutputsInvalidError({
        effect: effectName,
        issues: issues
    });
    return normalized;
}

function effectOutputCheck(args) {
    const {shape: shape, value: value, eligibility: eligibility} = args;
    return parseFieldValue({
        entryType: shape.type,
        value: value,
        ...shape.fields !== void 0 ? {
            fields: shape.fields
        } : {},
        ...shape.of !== void 0 ? {
            of: shape.of
        } : {},
        ...shape.options !== void 0 ? {
            options: shape.options
        } : {},
        ...shape.validation !== void 0 ? {
            validation: shape.validation
        } : {},
        ...shape.roles !== void 0 ? {
            roles: shape.roles
        } : {},
        memberRoles: eligibility.memberRoles,
        roleAliases: eligibility.roleAliases
    });
}

function requirePendingEffect(instance, effectKey) {
    const pending = instance.pendingEffects.find(e => e._key === effectKey);
    if (pending !== void 0) return pending;
    const run = instance.effectHistory.find(e => e._key === effectKey), settled = run !== void 0 ? {
        status: run.status,
        ranAt: run.ranAt,
        ...run.detail !== void 0 ? {
            detail: run.detail
        } : {}
    } : void 0;
    throw new EffectNotFoundError({
        instanceId: instance._id,
        effectKey: effectKey,
        ...settled !== void 0 ? {
            settled: settled
        } : {}
    });
}

function validateCompletionInput({pending: pending, definition: definition, status: status, ops: ops, outputs: outputs, memberRoles: memberRoles, roleAliases: roleAliases}) {
    if (status === "failed" && ops !== void 0 && ops.length > 0) throw new EffectOpsInvalidError({
        effect: pending.name,
        issues: [ "ops cannot accompany a failed completion — field.set the outcome on a done completion instead" ]
    });
    if (status === "failed" && outputs !== void 0) throw new EffectOutputsInvalidError({
        effect: pending.name,
        issues: [ "outputs cannot accompany a failed completion — report outputs on a done completion instead" ]
    });
    const normalizedOutputs = outputs !== void 0 ? validateEffectOutputs({
        outputs: outputs,
        declared: findEffect(definition, pending.name)?.outputs ?? [],
        effectName: pending.name,
        memberRoles: memberRoles,
        roleAliases: roleAliases
    }) : void 0;
    return {
        ops: ops !== void 0 ? validateEffectOps(ops, pending.name) : [],
        outputs: normalizedOutputs
    };
}

async function commitCompleteEffect({ctx: ctx, effectKey: effectKey, status: status, outputs: outputs, ops: ops, detail: detail, error: error, durationMs: durationMs, requestRecord: requestRecord, actor: actor}) {
    assertRequestUnprocessed({
        instance: ctx.instance,
        record: requestRecord,
        now: ctx.now
    });
    const pending = requirePendingEffect(ctx.instance, effectKey), {ops: validatedOps, outputs: validatedOutputs} = validateCompletionInput({
        pending: pending,
        definition: ctx.definition,
        status: status,
        ops: ops,
        outputs: outputs,
        memberRoles: ctx.memberRoles,
        roleAliases: ctx.definition.roleAliases
    }), mutation = startRevisionRetryMutation(ctx.instance);
    recordProcessedRequest({
        mutation: mutation,
        record: requestRecord,
        now: ctx.now
    }), mutation.pendingEffects = mutation.pendingEffects.filter(e => e._key !== effectKey);
    const ranAt = ctx.now, settlement = buildEffectSettlement(pending, {
        status: status,
        ranAt: ranAt,
        actor: actor,
        detail: detail,
        error: error,
        durationMs: durationMs,
        outputs: validatedOutputs
    });
    mutation.effectHistory.push(settlement.run);
    const wroteEffectOutputs = status === "done" && outputs !== void 0;
    mutation.history.push(settlement.event);
    const ranOps = await runOps({
        ops: validatedOps,
        mutation: mutation,
        stage: ctx.instance.currentStage,
        origin: {
            effect: pending.name
        },
        params: pending.params,
        actor: actor,
        self: selfGdr(ctx.instance),
        now: ranAt,
        snapshot: ctx.snapshot,
        refSurface: ctx.refSurface,
        memberRoles: ctx.memberRoles,
        roleAliases: ctx.definition.roleAliases
    }), needsGuardRefresh = wroteEffectOutputs || ranOps.some(isFieldOp);
    return await persistThenMaybeRefresh({
        ctx: ctx,
        mutation: mutation,
        stageName: ctx.instance.currentStage,
        didChangeState: needsGuardRefresh
    }), {
        effectKey: effectKey,
        effect: pending.name,
        status: status,
        origin: pending.origin.kind
    };
}

function buildQueuedEffect({effect: effect, origin: origin, params: params, actor: actor, now: now, stageEntryKey: stageEntryKey}) {
    const key = randomKey(), pending = {
        _key: key,
        _type: "pendingEffect",
        name: effect.name,
        ...effect.title !== void 0 ? {
            title: effect.title
        } : {},
        ...effect.description !== void 0 ? {
            description: effect.description
        } : {},
        ...effect.bindings !== void 0 ? {
            bindings: effect.bindings
        } : {},
        params: params,
        origin: origin,
        ...actor !== void 0 ? {
            actor: actor
        } : {},
        queuedAt: now,
        ...stageEntryKey !== void 0 ? {
            stageEntryKey: stageEntryKey
        } : {}
    }, history = {
        _key: randomKey(),
        _type: "effectQueued",
        at: now,
        effectKey: key,
        effect: effect.name,
        origin: origin
    };
    return {
        pending: pending,
        history: history
    };
}

async function queueEffects({ctx: ctx, mutation: mutation, effects: effects, origin: origin, actor: actor, opts: opts}) {
    if (!effects || effects.length === 0) return;
    const now = ctx.now, liveCtx = {
        ...ctx,
        instance: materializeInstance(ctx.instance, mutation)
    }, stageEntryKey = opts?.stageEntryKey ?? findOpenStageEntry(liveCtx.instance)?._key, params = await ctxConditionParams(liveCtx, {
        ...opts?.activityName !== void 0 ? {
            activityName: opts.activityName
        } : {},
        ...actor !== void 0 ? {
            actor: actor
        } : {},
        vars: {
            params: opts?.callerParams ?? {}
        }
    });
    for (const effect of effects) {
        const resolved = await resolveBindings({
            bindings: effect.bindings,
            staticInput: effect.input,
            snapshot: ctx.snapshot,
            params: params
        }), {pending: pending, history: history} = buildQueuedEffect({
            effect: effect,
            origin: origin,
            params: resolved,
            actor: actor,
            now: now,
            stageEntryKey: stageEntryKey
        });
        mutation.pendingEffects.push(pending), mutation.history.push(history);
    }
}

async function commitEffectOps(args) {
    const {client: client, instanceId: instanceId, effectKey: effectKey, claimToken: claimToken, ops: ops, requestRecord: requestRecord, leaseMs: leaseMs, options: options} = args;
    return retryOnRevisionConflict({
        client: client,
        instanceId: instanceId,
        options: options,
        commit: ctx => commitReport({
            ctx: ctx,
            effectKey: effectKey,
            claimToken: claimToken,
            ops: ops,
            requestRecord: requestRecord,
            leaseMs: leaseMs
        }),
        onExhausted: () => new ConcurrentCommitEffectOpsError({
            instanceId: instanceId,
            effectKey: effectKey,
            attempts: CONCURRENT_COMMIT_MAX_ATTEMPTS
        })
    });
}

function staleClaimReason(args) {
    const claim = args.pending.claim;
    if (claim === void 0) return "unclaimed";
    if (claim.claimToken !== args.claimToken) return "claim-superseded";
    if (isClaimExpired(claim, args.now)) return "lease-expired";
}

function renewClaimLease(args) {
    const {mutation: mutation, effectKey: effectKey, now: now, leaseMs: leaseMs} = args;
    mutation.pendingEffects = mutation.pendingEffects.map(entry => entry._key === effectKey && entry.claim !== void 0 ? {
        ...entry,
        claim: {
            ...entry.claim,
            leaseExpiresAt: addMs(now, leaseMs)
        }
    } : entry);
}

async function commitReport({ctx: ctx, effectKey: effectKey, claimToken: claimToken, ops: ops, requestRecord: requestRecord, leaseMs: leaseMs}) {
    assertRequestUnprocessed({
        instance: ctx.instance,
        record: requestRecord,
        now: ctx.now
    });
    const pending = requirePendingEffect(ctx.instance, effectKey), reason = staleClaimReason({
        pending: pending,
        claimToken: claimToken,
        now: ctx.now
    });
    if (reason !== void 0) throw new StaleEffectClaimError({
        instanceId: ctx.instance._id,
        effectKey: effectKey,
        reason: reason
    });
    const validatedOps = validateEffectOps(ops, pending.name);
    if (validatedOps.length === 0) throw new EffectOpsInvalidError({
        effect: pending.name,
        issues: [ "a mid-dispatch report must carry at least one field op — there is nothing to commit" ]
    });
    const mutation = startRevisionRetryMutation(ctx.instance);
    return recordProcessedRequest({
        mutation: mutation,
        record: requestRecord,
        now: ctx.now
    }), renewClaimLease({
        mutation: mutation,
        effectKey: effectKey,
        now: ctx.now,
        leaseMs: leaseMs
    }), await runOps({
        ops: validatedOps,
        mutation: mutation,
        stage: ctx.instance.currentStage,
        origin: {
            effect: pending.name
        },
        params: pending.params,
        actor: ctx.actor,
        self: selfGdr(ctx.instance),
        now: ctx.now,
        snapshot: ctx.snapshot,
        refSurface: ctx.refSurface,
        memberRoles: ctx.memberRoles,
        roleAliases: ctx.definition.roleAliases
    }), await persistThenMaybeRefresh({
        ctx: ctx,
        mutation: mutation,
        stageName: ctx.instance.currentStage,
        didChangeState: !0
    }), {
        effectKey: effectKey,
        effect: pending.name
    };
}

const RESET_ACTIVITY_TARGETS = _exhaustiveOptions()([ "active", "skipped" ]);

function isResetActivityTarget(value) {
    return RESET_ACTIVITY_TARGETS.includes(value);
}

async function resetActivity(args) {
    const {client: client, instanceId: instanceId, activity: activity, to: to, requestRecord: requestRecord, options: options} = args, ctx = await loadCallContext({
        client: client,
        instanceId: instanceId,
        options: options
    });
    return commitResetActivity({
        ctx: ctx,
        activity: activity,
        to: to,
        requestRecord: requestRecord,
        actor: options?.actor
    });
}

async function commitResetActivity({ctx: ctx, activity: activity, to: to, requestRecord: requestRecord, actor: actor}) {
    if (assertRequestUnprocessed({
        instance: ctx.instance,
        record: requestRecord,
        now: ctx.now
    }), isTerminal(ctx)) return {
        fired: !1
    };
    const mutation = startMutation(ctx.instance), openStage2 = findOpenStageEntry(mutation), entry = findCurrentActivityEntry(mutation, activity);
    if (openStage2 === void 0 || entry === void 0) throw new ContractViolationError(`resetActivity: activity "${activity}" is not in the current stage of instance "${ctx.instance._id}"`);
    const from = entry.status;
    if (from === to) return {
        fired: !1
    };
    if (!isTerminalActivityStatus(from)) throw new ContractViolationError(`resetActivity: activity "${activity}" is "${from}", not a terminal status — a reset only recovers a resolved (typically failed) activity. Use setStage to force past a live stage.`);
    return recordProcessedRequest({
        mutation: mutation,
        record: requestRecord,
        now: ctx.now
    }), applyActivityStatusChange({
        entry: entry,
        history: mutation.history,
        stage: openStage2.name,
        to: to,
        at: ctx.now,
        ...actor !== void 0 ? {
            actor: actor
        } : {}
    }), await persist(ctx, mutation), {
        fired: !0,
        stage: openStage2.name,
        activity: activity,
        from: from,
        to: to
    };
}

const REPLAY_SURFACE = {
    allows: () => !0,
    description: "any resource (in-memory replay; the committing surface enforces)"
};

async function whatIfFireAction(args) {
    const {instance: instance, definition: definition, snapshot: snapshot, now: now, actor: actor, stage: stage, activity: activity, action: action} = args;
    if (!replayable({
        instance: instance,
        stage: stage,
        action: action
    })) return;
    const ctx = {
        now: now,
        instance: instance,
        definition: definition,
        snapshot: snapshot,
        refSurface: REPLAY_SURFACE,
        actor: actor
    }, mutation = startMutation(instance);
    try {
        await applyActionFire({
            ctx: ctx,
            mutation: mutation,
            activity: activity,
            action: action,
            params: {},
            actor: actor,
            triggered: !1
        }), await runTriggeredActions({
            ctx: ctx,
            mutation: mutation,
            stage: stage
        });
    } catch (err) {
        if (err instanceof FieldValueShapeError) return;
        throw err;
    }
    const transition = await pickTransition(liveViewContext(ctx, mutation), stage);
    return transition === void 0 ? {
        exitsStage: !1
    } : {
        exitsStage: !0,
        transition: transition.name
    };
}

function replayable(args) {
    const {instance: instance, stage: stage, action: action} = args;
    if (terminalState(instance) !== "in-flight" || (action.params ?? []).length > 0 || action.spawn !== void 0) return !1;
    const primed = new Set((findOpenStageEntry(instance)?.activities ?? []).map(e => e.name));
    return (stage.activities ?? []).every(declared => primed.has(declared.name)) ? !(stage.activities ?? []).flatMap(declared => declared.actions ?? []).some(sibling => isCascadeFired(sibling) && sibling.spawn !== void 0) : !1;
}

function clientGuardDereference(client) {
    return async ({_ref: _ref}) => await client.getDocument(toBareId(_ref)) ?? null;
}

async function buildFieldInsights({sites: sites, snapshot: snapshot}) {
    const insights = [];
    for (const field of fieldsReadAcross(sites)) {
        const involved = sites.filter(entry => readsField(entry.insight, field));
        insights.push({
            field: field,
            reads: dedupeReads(involved.flatMap(entry => fieldReads(entry.insight, field))),
            involvedIn: involved.map(entry => entry.site),
            proposals: await verifyProposals({
                field: field,
                involved: involved,
                snapshot: snapshot
            })
        });
    }
    return insights;
}

function fieldsReadAcross(sites) {
    const names = sites.flatMap(entry => entry.insight.analysis.reads).filter(read => read.variable === "fields").map(read => read.path[0]).filter(head => typeof head == "string");
    return [ ...new Set(names) ];
}

function fieldReads(insight, field) {
    return insight.analysis.reads.filter(read => read.variable === "fields" && read.path[0] === field);
}

function readsField(insight, field) {
    return fieldReads(insight, field).length > 0;
}

async function verifyProposals({field: field, involved: involved, snapshot: snapshot}) {
    const proposals = [];
    for (const assign of candidateAssignments(involved, field)) {
        const consequences = [];
        for (const entry of involved) {
            const result = await whatIfCondition({
                condition: entry.condition,
                dataset: snapshot.docs,
                params: entry.params,
                assign: assign
            });
            result.changed && consequences.push({
                site: entry.site,
                before: result.before,
                after: result.after
            });
        }
        consequences.length > 0 && proposals.push({
            assign: assign,
            consequences: consequences
        });
    }
    return proposals;
}

function candidateAssignments(involved, field) {
    const candidates = involved.flatMap(entry => entry.insight.blockedBy).flatMap(atom => atom.requirement !== void 0 ? [ atom.requirement ] : []).filter(requirement => requirement.target.variable === "fields").filter(requirement => requirement.target.path[0] === field).flatMap(assignmentFor).filter(fabricatable);
    return dedupeBy(candidates, assignment => JSON.stringify([ assignment.target.path, assignment.value ]));
}

function assignmentFor(requirement) {
    switch (requirement.kind) {
      case "equals":
        return [ {
            target: requirement.target,
            value: requirement.value
        } ];

      case "truthy":
        return [ {
            target: requirement.target,
            value: !0
        } ];

      case "falsy":
        return [ {
            target: requirement.target,
            value: !1
        } ];

      case "differs":
      case "defined":
      case "undefined":
      case "compares":
        return [];
    }
}

function fabricatable(assignment) {
    return assignment.target.path.every(segment => typeof segment != "number" || segment <= MAX_COUNTERFACTUAL_INDEX);
}

async function subjectResourceGrants(args) {
    const {clientForGdr: clientForGdr, instance: instance} = args, clients = /* @__PURE__ */ new Map;
    for (const {parsed: parsed, resource: resource} of foreignSubjectRefs(instance)) {
        const key = resourceGdr(resource);
        clients.has(key) || clients.set(key, {
            resource: resource,
            client: clientForGdr(parsed)
        });
    }
    const resolved = await Promise.all([ ...clients.entries() ].map(async ([key, entry]) => {
        const path = aclPathForResource(entry.resource);
        if (path === void 0) return;
        const [grants, actorId] = await Promise.all([ grantsForClientPath(entry.client, path), resourceActorId(entry.client) ]);
        if (!(grants === void 0 || actorId === void 0)) return [ key, {
            grants: grants,
            actorId: actorId
        } ];
    }));
    return new Map(resolved.filter(entry => entry !== void 0));
}

async function resourceActorId(client) {
    try {
        const access = await resolveAccess(client);
        return lakePrincipalId(access);
    } catch (err) {
        console.warn(`workflow: failed to resolve the actor identity on a subject resource; the subject-write forecast omits this resource (degrade open — the lake still enforces writes). Original error: ${errorMessage(err)}`);
        return;
    }
}

async function evaluateInstance(args) {
    const {client: client, tag: tag, workflowResource: workflowResource, instanceId: instanceId, resourceClients: resourceClients} = args, now = (args.clock ?? wallClock)();
    validateTag(tag);
    const [access, instance, attributes] = await Promise.all([ resolveAccess(client, {
        ...args.grantsFromPath !== void 0 ? {
            grantsFromPath: args.grantsFromPath
        } : {}
    }), reload({
        client: client,
        instanceId: instanceId,
        tag: tag
    }), resolveUserAttributes(client) ]), {actor: actor, grants: grants, localPrincipalId: localPrincipalId} = access, definition = parseDefinitionSnapshot(instance), clientForGdr = buildClientForGdr({
        client: client,
        workflowResource: workflowResource,
        resourceClients: resourceClients
    }), [snapshot, guards, resourceGrants] = await Promise.all([ hydrateSnapshot({
        client: client,
        clientForGdr: clientForGdr,
        instance: instance
    }), verdictGuardsForInstance(client, instance._id), subjectResourceGrants({
        clientForGdr: clientForGdr,
        instance: instance
    }) ]);
    return evaluateFromSnapshot({
        instance: instance,
        definition: definition,
        actor: actor,
        snapshot: snapshot,
        guards: guards,
        now: now,
        resourceGrants: resourceGrants,
        guardDereference: clientGuardDereference(client),
        ...localPrincipalId !== void 0 ? {
            localPrincipalId: localPrincipalId
        } : {},
        ...grants !== void 0 ? {
            grants: grants
        } : {},
        ...attributes !== void 0 ? {
            attributes: attributes
        } : {}
    });
}

async function callerBoundProjectionScopes(args) {
    const can = await advisoryCan({
        instance: args.instance,
        identity: args.identity,
        grants: args.grants
    }), vars = callerBoundVars({
        ...can !== void 0 ? {
            can: can
        } : {},
        ...args.attributes !== void 0 ? {
            attributes: args.attributes
        } : {}
    }), opts = {
        actor: args.actor,
        ...vars !== void 0 ? {
            vars: vars
        } : {}
    };
    return {
        scope: await renderConditionScope(args.scopeSource, opts),
        scopeForActivity: memoizedBy(activityName => renderConditionScope(args.scopeSource, {
            activityName: activityName,
            ...opts
        }))
    };
}

function clockSitesOf(instance, sites) {
    return terminalState(instance) !== "in-flight" ? [] : sites.filter(entry => readsNowIn(entry.insight.analysis.reads));
}

function currentStageOf(instance, definition) {
    try {
        return findStage(definition, instance.currentStage);
    } catch (err) {
        throw new Error(`Instance "${instance._id}" currentStage "${instance.currentStage}" not in definition`, {
            cause: err
        });
    }
}

async function explainSite(args) {
    const {site: site, condition: condition, params: params, snapshot: snapshot, sites: sites} = args, insight = await explainCondition({
        condition: condition,
        dataset: snapshot.docs,
        params: params
    });
    return sites.push({
        site: site,
        condition: condition,
        params: params,
        insight: insight
    }), insight;
}

async function evaluateFromSnapshot(args) {
    const {instance: instance, definition: definition, actor: actor, grants: grants, attributes: attributes, snapshot: snapshot} = args, now = args.now ?? wallClock(), stage = currentStageOf(instance, definition), autonomy = autonomyOf(definition), stageAutonomy2 = stageAutonomyOf(autonomy, stage.name), fireConsequence = (activity, action) => whatIfFireAction({
        instance: instance,
        definition: definition,
        snapshot: snapshot,
        now: now,
        actor: actor,
        stage: stage,
        activity: activity,
        action: action
    }), scopeSource = {
        instance: instance,
        definition: definition,
        snapshot: snapshot,
        now: now
    }, anchorIdentity = lakePrincipalId({
        actor: actor,
        localPrincipalId: args.localPrincipalId
    }), {scope: scope, scopeForActivity: scopeForActivity} = await callerBoundProjectionScopes({
        scopeSource: scopeSource,
        actor: actor,
        instance: instance,
        grants: grants,
        attributes: attributes,
        identity: anchorIdentity
    }), cascadeScopeForActivity = memoizedBy(activityName => renderConditionScope(scopeSource, {
        activityName: activityName
    })), currentActivityEntries = findOpenStageEntry(instance)?.activities ?? [], guardDenial = await instanceGuardReason({
        instance: instance,
        identity: anchorIdentity,
        guards: args.guards,
        dereference: args.guardDereference
    }), subjectDenials = await forecastSubjectDenials({
        instance: instance,
        snapshot: snapshot,
        resourceGrants: args.resourceGrants
    }), subjectPermissionReason = subjectDenials.length > 0 ? {
        kind: "subject-permission-denied",
        denied: subjectDenials
    } : void 0, sites = [], activityEvaluations = [];
    for (const activity of stage.activities ?? []) activityEvaluations.push(await evaluateActivity({
        activity: activity,
        statusEntry: currentActivityEntries.find(t => t.name === activity.name),
        instance: instance,
        snapshot: snapshot,
        activityScope: await scopeForActivity(activity.name),
        cascadeActivityScope: () => cascadeScopeForActivity(activity.name),
        stageHasExits: !isTerminalStage(stage),
        guardDenial: guardDenial,
        subjectPermissionReason: subjectPermissionReason,
        sites: sites,
        autonomy: activityAutonomyOf(stageAutonomy2, activity.name),
        fireConsequence: fireConsequence
    }));
    const cascadeParams = await renderConditionScope(scopeSource), transitionEvaluations = [];
    for (const transition of stage.transitions ?? []) {
        const insight = await explainSite({
            site: {
                kind: "transition",
                transition: transition.name
            },
            condition: transition.when,
            params: cascadeParams,
            snapshot: snapshot,
            sites: sites
        });
        transitionEvaluations.push({
            transition: transition,
            whenSatisfied: insight.outcome === "satisfied",
            unevaluable: insight.outcome === "unevaluable",
            insight: insight
        });
    }
    const currentStage = {
        stage: stage,
        ...semanticsOf(stage),
        activities: activityEvaluations,
        transitions: transitionEvaluations,
        autonomy: stageAutonomy2
    }, pendingOnYou = activityEvaluations.filter(t => t.pendingOnActor), canInteract = activityEvaluations.some(t => t.actions.some(a => a.allowed)), editGuardDenial = guardDenial?.kind === "mutation-guard-denied" ? guardDenial : void 0, editableFields = await evaluateEditableFields({
        instance: instance,
        definition: definition,
        stage: stage,
        snapshot: snapshot,
        scope: scope,
        scopeForActivity: scopeForActivity,
        guardDenial: editGuardDenial,
        sites: sites
    }), nextEvaluationAt = await nextEvaluationInstant({
        sites: clockSitesOf(instance, sites),
        dataset: snapshot.docs,
        now: now
    });
    return {
        instance: instance,
        definition: definition,
        ...semanticsOf(definition),
        actor: actor,
        currentStage: currentStage,
        pendingOnYou: pendingOnYou,
        canInteract: canInteract,
        editableFields: editableFields,
        fieldInsights: await buildFieldInsights({
            sites: sites,
            snapshot: snapshot
        }),
        autonomy: autonomy,
        ...nextEvaluationAt !== void 0 ? {
            nextEvaluationAt: nextEvaluationAt
        } : {}
    };
}

const AUTONOMY_CACHE = /* @__PURE__ */ new WeakMap;

function autonomyOf(definition) {
    const hit = AUTONOMY_CACHE.get(definition);
    if (hit !== void 0) return hit;
    const derived = deriveWorkflowAutonomy(definition);
    return AUTONOMY_CACHE.set(definition, derived), derived;
}

async function evaluateEditableFields(args) {
    const {instance: instance, definition: definition, stage: stage, snapshot: snapshot, scope: scope, scopeForActivity: scopeForActivity, guardDenial: guardDenial, sites: sites} = args, fields = [];
    for (const site of editableFieldsInStage(definition, stage)) {
        const window = fieldWindowOpen(instance, site), insight = await editPredicateInsight({
            site: site,
            snapshot: snapshot,
            scope: scope,
            scopeForActivity: scopeForActivity,
            sites: sites
        }), predicateSatisfied = insight === void 0 || insight.outcome === "satisfied", reason = editDisabledReason({
            effective: site.effective,
            instance: instance,
            window: window,
            guardDenial: guardDenial,
            predicateSatisfied: predicateSatisfied
        }), value = readFieldValue(instance, site);
        fields.push({
            scope: site.scope,
            ...site.activity !== void 0 ? {
                activity: site.activity
            } : {},
            name: site.name,
            type: site.type,
            ...site.title !== void 0 ? {
                title: site.title
            } : {},
            ...site.validation !== void 0 ? {
                validation: site.validation
            } : {},
            ...site.roles !== void 0 ? {
                roles: site.roles,
                ...definition.roleAliases !== void 0 ? {
                    roleAliases: definition.roleAliases
                } : {}
            } : {},
            value: value,
            editable: reason === void 0,
            ...reason !== void 0 ? {
                disabledReason: reason
            } : {},
            ...fieldProvenance(instance, site.ref),
            ...insight !== void 0 ? {
                insight: insight
            } : {}
        });
    }
    return fields;
}

async function editPredicateInsight(args) {
    const {site: site, snapshot: snapshot, scope: scope, scopeForActivity: scopeForActivity, sites: sites} = args;
    if (typeof site.effective != "string") return;
    const params = site.scope === "activity" && site.activity !== void 0 ? await scopeForActivity(site.activity) : scope;
    return explainSite({
        site: {
            kind: "editable-field",
            scope: site.scope,
            name: site.name,
            ...site.activity !== void 0 ? {
                activity: site.activity
            } : {}
        },
        condition: site.effective,
        params: params,
        snapshot: snapshot,
        sites: sites
    });
}

async function forecastSubjectDenials(args) {
    const {instance: instance, snapshot: snapshot, resourceGrants: resourceGrants} = args;
    if (resourceGrants === void 0 || resourceGrants.size === 0) return [];
    const denials = [], seen = /* @__PURE__ */ new Set;
    for (const {ref: ref, parsed: parsed, resource: resource} of foreignSubjectRefs(instance)) {
        if (seen.has(ref.id)) continue;
        seen.add(ref.id);
        const access = resourceGrants.get(resourceGdr(resource));
        if (access === void 0) continue;
        const doc = snapshot.docs.find(d => d._id === ref.id);
        doc !== void 0 && await subjectUpdateAllowed({
            doc: doc,
            parsed: parsed,
            grants: access.grants,
            actorId: access.actorId
        }) === !1 && denials.push({
            subject: ref.id,
            resource: resourceGdr(resource),
            permission: "update"
        });
    }
    return denials;
}

async function subjectUpdateAllowed(args) {
    const {doc: doc, parsed: parsed, grants: grants, actorId: actorId} = args;
    try {
        return await grantsPermissionOn({
            document: {
                ...doc,
                _id: parsed.documentId
            },
            grants: grants,
            permission: "update",
            userId: actorId
        });
    } catch (err) {
        console.warn(`workflow: subject-write forecast skipped for "${doc._id}" — evaluating that resource's grants failed (the lake still enforces the write). Original error: ${errorMessage(err)}`);
        return;
    }
}

async function evaluateActivity(args) {
    const {activity: activity, statusEntry: statusEntry, instance: instance, snapshot: snapshot, activityScope: activityScope, cascadeActivityScope: cascadeActivityScope, stageHasExits: stageHasExits, guardDenial: guardDenial, subjectPermissionReason: subjectPermissionReason, sites: sites, autonomy: autonomy, fireConsequence: fireConsequence} = args, status = statusEntry?.status ?? "skipped", assigned = activityScope.assigned === !0, {unmetRequirements: unmetRequirements, ...conditionInsights} = await explainActivityConditions({
        activity: activity,
        activityScope: activityScope,
        cascadeActivityScope: cascadeActivityScope,
        snapshot: snapshot,
        sites: sites
    }), requirementsReason = unmetRequirements.length > 0 ? {
        kind: "requirements-unmet",
        unmetRequirements: unmetRequirements
    } : void 0, actions = [];
    for (const action of activity.actions ?? []) actions.push(await evaluateAction({
        action: action,
        activityName: activity.name,
        status: status,
        instance: instance,
        snapshot: snapshot,
        activityScope: activityScope,
        cascadeActivityScope: cascadeActivityScope,
        stageHasExits: stageHasExits,
        guardDenial: guardDenial,
        subjectPermissionReason: subjectPermissionReason,
        requirementsReason: requirementsReason,
        sites: sites,
        fireConsequence: a => fireConsequence(activity, a),
        assignment: assignmentMembers(statusEntry?.fields ?? [])
    }));
    return {
        activity: activity,
        ...semanticsOf(activity),
        status: status,
        kind: deriveActivityKind(activity),
        classification: deriveExecutorClassification(activity),
        autonomy: autonomy,
        pendingOnActor: status === "active" && assigned,
        scopedOut: isFilterScopedOut({
            status: status,
            startedAt: statusEntry?.startedAt
        }),
        ...unmetRequirements.length > 0 ? {
            unmetRequirements: unmetRequirements
        } : {},
        ...conditionInsights,
        actions: actions
    };
}

async function explainActivityConditions({activity: activity, activityScope: activityScope, cascadeActivityScope: cascadeActivityScope, snapshot: snapshot, sites: sites}) {
    const explainAt = (site, condition) => explainSite({
        site: site,
        condition: condition,
        params: activityScope,
        snapshot: snapshot,
        sites: sites
    }), requirementEntries = [];
    for (const requirement of activity.requirements ?? []) requirementEntries.push([ requirement, await explainAt({
        kind: "requirement",
        activity: activity.name,
        requirement: requirement.name
    }, requirement.query) ]);
    const requirementInsights = Object.fromEntries(requirementEntries.map(([requirement, insight]) => [ requirement.name, insight ])), unmetRequirements = requirementEntries.filter(([, insight]) => insight.outcome !== "satisfied").map(([requirement]) => requirementDescriptor(requirement)), filterInsight = activity.filter !== void 0 ? await explainSite({
        site: {
            kind: "activity-filter",
            activity: activity.name
        },
        condition: activity.filter,
        params: await cascadeActivityScope(),
        snapshot: snapshot,
        sites: sites
    }) : void 0;
    return {
        unmetRequirements: unmetRequirements,
        ...activity.requirements !== void 0 ? {
            requirementInsights: requirementInsights
        } : {},
        ...filterInsight !== void 0 ? {
            filterInsight: filterInsight
        } : {}
    };
}

async function evaluateAction(args) {
    const {action: action, activityName: activityName, snapshot: snapshot, activityScope: activityScope, sites: sites} = args, conditionScope = isCascadeFired(action) ? await args.cascadeActivityScope() : activityScope, insights = await explainActionGates({
        action: action,
        activityName: activityName,
        conditionScope: conditionScope,
        snapshot: snapshot,
        sites: sites
    });
    if (action.when !== void 0) return triggeredActionVerdict({
        action: action,
        when: action.when,
        insights: insights
    });
    const verdict = fireableActionVerdict({
        args: args,
        insights: insights
    }), firing = await args.fireConsequence(action);
    return firing !== void 0 ? {
        ...verdict,
        firing: firing
    } : verdict;
}

async function explainActionGates({action: action, activityName: activityName, conditionScope: conditionScope, snapshot: snapshot, sites: sites}) {
    const insight = action.filter !== void 0 ? await explainSite({
        site: {
            kind: "action",
            activity: activityName,
            action: action.name
        },
        condition: action.filter,
        params: conditionScope,
        snapshot: snapshot,
        sites: sites
    }) : void 0, whenInsight = action.when !== void 0 ? await explainSite({
        site: {
            kind: "action-when",
            activity: activityName,
            action: action.name
        },
        condition: action.when,
        params: conditionScope,
        snapshot: snapshot,
        sites: sites
    }) : void 0;
    return {
        ...insight !== void 0 ? {
            insight: insight
        } : {},
        ...whenInsight !== void 0 ? {
            whenInsight: whenInsight
        } : {}
    };
}

function triggeredActionVerdict({action: action, when: when, insights: insights}) {
    return insights.insight !== void 0 && insights.insight.outcome === "unsatisfied" ? disabled({
        action: action,
        reason: {
            kind: "filter-failed",
            filter: action.filter ?? ""
        },
        ...insights
    }) : {
        ...actionEvaluationIdentity(action),
        allowed: !1,
        triggered: !0,
        disabledReason: {
            kind: "cascade-fired",
            when: when
        },
        ...insights
    };
}

function fireableActionVerdict({args: args, insights: insights}) {
    const {action: action, status: status, instance: instance, stageHasExits: stageHasExits, guardDenial: guardDenial} = args, lifecycle = lifecycleReason({
        instance: instance,
        status: status,
        stageHasExits: stageHasExits
    });
    if (lifecycle !== void 0) return disabled({
        action: action,
        reason: lifecycle,
        ...insights
    });
    if (guardDenial !== void 0) return disabled({
        action: action,
        reason: guardDenial,
        ...insights
    });
    if (args.subjectPermissionReason !== void 0 && (action.effects?.length ?? 0) > 0) return disabled({
        action: action,
        reason: args.subjectPermissionReason,
        ...insights
    });
    if (args.requirementsReason !== void 0) return disabled({
        action: action,
        reason: args.requirementsReason,
        ...insights
    });
    const filterFailure = filterFailureVerdict(args, insights);
    return filterFailure !== void 0 ? filterFailure : {
        ...actionEvaluationIdentity(action),
        allowed: !0,
        ...insights
    };
}

function filterFailureVerdict(args, insights) {
    const {action: action} = args, {insight: insight} = insights;
    if (action.filter === void 0 || insight === void 0 || insight.outcome === "satisfied") return;
    const holderGate = insight.blockedBy.some(entry => isAssignedGate(entry.atom)) ? {
        holders: [ ...activeAssignmentMembers(args.assignment) ]
    } : void 0;
    return disabled({
        action: action,
        reason: {
            kind: "filter-failed",
            filter: action.filter
        },
        ...insights,
        ...holderGate === void 0 ? {} : {
            holderGate: holderGate
        }
    });
}

function actionEvaluationIdentity(action) {
    return {
        action: action,
        ...semanticsOf(action)
    };
}

function semanticsOf(node) {
    return node.semantics === void 0 ? {} : {
        semantics: node.semantics
    };
}

async function instanceGuardReason({instance: instance, identity: identity, guards: guards, dereference: dereference}) {
    if (guards === void 0 || guards.length === 0) return;
    const denied = await instanceWriteDenials({
        instance: instance,
        guards: guards,
        identity: identity,
        dereference: dereference ?? (() => Promise.resolve(null))
    });
    if (denied.length !== 0) return {
        kind: "mutation-guard-denied",
        denied: deniedGuardRefs(denied)
    };
}

function lifecycleReason({instance: instance, status: status, stageHasExits: stageHasExits}) {
    const terminalReason = instanceTerminalReason(instance);
    if (terminalReason !== void 0) return terminalReason;
    if (!stageHasExits) return {
        kind: "stage-terminal",
        stage: instance.currentStage
    };
    if (isTerminalActivityStatus(status)) return {
        kind: "activity-not-active",
        status: status
    };
}

function disabled(args) {
    return {
        ...actionEvaluationIdentity(args.action),
        allowed: !1,
        disabledReason: args.reason,
        ...args.insight !== void 0 ? {
            insight: args.insight
        } : {},
        ...args.whenInsight !== void 0 ? {
            whenInsight: args.whenInsight
        } : {},
        ...args.holderGate !== void 0 ? {
            holderGate: args.holderGate
        } : {}
    };
}

const ASSIGNMENT_KINDS = '["assignee", "assignees"]', HAS_USER = '(count(value[@.type == "user"]) > 0 || value.type == "user")', HAS_MEMBER = "(count(value) > 0 || defined(value.type))";

function assignmentActivityArm(state) {
    const assignmentFields = `fields[_type in ${ASSIGNMENT_KINDS}]`;
    return state === "unrouted" ? `count(${assignmentFields}[${HAS_MEMBER}]) == 0` : state === "held" ? `count(${assignmentFields}[${HAS_USER} && (count(value[@.type == "user" && id == $assignmentUserId]) > 0 || (value.type == "user" && value.id == $assignmentUserId))]) > 0` : `count(${assignmentFields}[${HAS_USER}]) == 0 && count(${assignmentFields}[count(value[@.type == "role" && role in $assignmentRoles]) > 0 || (value.type == "role" && value.role in $assignmentRoles)]) > 0`;
}

function assignmentPrefilter(assignment, params) {
    if (assignment.userId.length === 0) throw new ContractViolationError("instancesQuery: assignment.userId must be non-empty");
    const states = [ ...new Set(assignment.states ?? [ "unrouted", "routed", "held" ]) ];
    if (states.length === 0) throw new ContractViolationError("instancesQuery: assignment.states must not be empty");
    if (states.some(state => state !== "unrouted" && state !== "routed" && state !== "held")) throw new ContractViolationError("instancesQuery: assignment.states contains an unknown state");
    return params.assignmentUserId = assignment.userId, params.assignmentRoles = [ ...new Set(assignment.roles ?? []) ].sort(), 
    `count(stages[!defined(exitedAt)].activities[status == "active" && (${states.map(assignmentActivityArm).join(" || ")})]) > 0`;
}

function inFlightFilter() {
    return "!defined(completedAt)";
}

const FIELD_REFS_DOC = "count(fields[value.id in $documents]) > 0 || count(fields[count(value[id in $documents]) > 0]) > 0", OPEN_STAGE_REFS_DOC = `count(stages[!defined(exitedAt) && (${FIELD_REFS_DOC} || count(activities[${FIELD_REFS_DOC}]) > 0)]) > 0`, LIVE_SUBWORKFLOW_REFS_DOC = "count(subworkflows[!defined(resolved) && ref.id in $documents]) > 0";

function documentPrefilter(documents, params) {
    for (const document of documents) if (!isGdrUri(document)) throw new ContractViolationError(`every document must be a resource-qualified GDR URI (e.g. "dataset:project:dataset:doc-id"); got: ${JSON.stringify(document)}`);
    return params.documents = [ ...documents ], params.bareIds = documents.map(extractDocumentId), 
    `_id in $bareIds || count(ancestors[id in $documents]) > 0 || ${LIVE_SUBWORKFLOW_REFS_DOC} || ${FIELD_REFS_DOC} || ${OPEN_STAGE_REFS_DOC}`;
}

function documentArm(filter, params) {
    if (filter.documents === void 0 && filter.document === void 0) return;
    const documents = [ .../* @__PURE__ */ new Set([ ...filter.documents ?? [], ...filter.document !== void 0 ? [ filter.document ] : [] ]) ].sort();
    return documentPrefilter(documents, params);
}

function idsArm(filter, params) {
    if (filter.ids !== void 0) {
        for (const id of filter.ids) if (isGdrUri(id)) throw new ContractViolationError(`instancesQuery: every id must be a bare instance document id (an instance's _id is never resource-qualified); got a GDR URI: ${JSON.stringify(id)}`);
        return params.ids = [ ...new Set(filter.ids) ].sort(), "_id in $ids";
    }
}

function beforeArm(filter, params) {
    const before = filter.before;
    if (before !== void 0) {
        if (filter.limit === void 0) throw new ContractViolationError("instancesQuery: `before` requires `limit` — a cursor pages the newest-first sliced read");
        if (before.id === "" || before.startedAt === "") throw new ContractViolationError(`instancesQuery: \`before\` needs a non-empty id and startedAt; got ${JSON.stringify(before)}`);
        return params.beforeStartedAt = before.startedAt, params.beforeId = before.id, "(startedAt < $beforeStartedAt || (startedAt == $beforeStartedAt && _id > $beforeId))";
    }
}

function compiledConditions(filter, params) {
    if (filter.limit !== void 0 && (!Number.isInteger(filter.limit) || filter.limit <= 0)) throw new ContractViolationError(`instancesQuery: limit must be a positive integer; got ${JSON.stringify(filter.limit)}`);
    const conditions = [ `_type == "${WORKFLOW_INSTANCE_TYPE}"`, tagScopeFilter() ];
    filter.includeCompleted !== !0 && conditions.push(inFlightFilter()), filter.definition !== void 0 && (conditions.push("definition == $definition"), 
    params.definition = filter.definition), filter.stage !== void 0 && (conditions.push("currentStage == $stage"), 
    params.stage = filter.stage), filter.assignment !== void 0 && conditions.push(assignmentPrefilter(filter.assignment, params));
    const cursor = beforeArm(filter, params);
    cursor !== void 0 && conditions.push(cursor);
    const arms = [ documentArm(filter, params), idsArm(filter, params) ].filter(arm => arm !== void 0);
    return arms.length > 0 && conditions.push(`(${arms.join(" || ")})`), conditions;
}

function orderedSlice(filter) {
    return filter.limit !== void 0 ? ` | order(startedAt desc, _id asc) [0...${filter.limit}]` : " | order(startedAt asc, _id asc)";
}

function instancesQuery(args) {
    const {tag: tag, filter: filter = {}} = args;
    validateTag(tag);
    const params = {
        tag: tag
    };
    return {
        query: `*[${compiledConditions(filter, params).join(" && ")}]${orderedSlice(filter)}`,
        params: params
    };
}

function instanceChangesQuery(args) {
    return validateTag(args.tag), {
        query: `*[_type == "${WORKFLOW_INSTANCE_TYPE}" && ${tagScopeFilter()}]`,
        params: {
            tag: args.tag
        }
    };
}

const PREVIEW_FIELD_TYPES = [ "actor", "assignee", "assignees", "date", "datetime", "doc.ref", "doc.refs", "dueDate", "dueDatetime", "release.ref", "subject" ], PREVIEW_PROJECTION = `{\n  _id, _type, modelVersion, minReaderModel, workflowResource, definition, pinnedVersion,\n  currentStage, startedAt, completedAt, abortedAt, perspective, ancestors,\n  "fields": fields[_type in $previewFieldTypes],\n  "stages": stages[!defined(exitedAt) && name == ^.currentStage]{\n    _key, name, enteredAt,\n    "fields": fields[_type in $previewFieldTypes],\n    "activities": activities[]{\n      _key, name, status, completedBy,\n      "fields": fields[_type in $previewFieldTypes]\n    }\n  },\n  "claimedEffects": pendingEffects[defined(claim)]{name, title},\n  "failedEffects": effectHistory[status == "failed"]{name, title}\n}`.replaceAll(/\s+/g, " ").trim();

function instancePreviewsQuery(args) {
    const {tag: tag, filter: filter = {}} = args;
    validateTag(tag);
    const params = {
        tag: tag,
        previewFieldTypes: PREVIEW_FIELD_TYPES
    };
    return {
        query: `*[${compiledConditions(filter, params).join(" && ")}]${orderedSlice(filter)} ${PREVIEW_PROJECTION}`,
        params: params
    };
}

const DEFAULT_EFFECT_LEASE_MS = 300 * 1e3;

function claimReleasedEntry(args) {
    return {
        _key: randomKey(),
        _type: "effectClaimReleased",
        at: args.at,
        effectKey: args.pending._key,
        effect: args.pending.name,
        claim: args.claim,
        via: args.via,
        actor: args.actor
    };
}

const SWEEP_MAX_ATTEMPTS = 5;

async function sweepStaleClaims(args) {
    const {tag: tag, instanceId: instanceId} = args, client = pinApiVersion(args.client);
    validateTag(tag);
    const clock = args.clock ?? wallClock, {actor: actor} = await resolveAccess(client), stamp = resolveExecutionContext(args.executionContext);
    for (let attempt = 1; attempt <= SWEEP_MAX_ATTEMPTS; attempt++) {
        const now = clock(), instance = await reload({
            client: client,
            instanceId: instanceId,
            tag: tag
        }), expired = instance.pendingEffects.filter(e => e.claim !== void 0 && isClaimExpired(e.claim, now));
        if (expired.length === 0) return {
            released: []
        };
        const expiredKeys = new Set(expired.map(e => e._key)), releasedQueue = instance.pendingEffects.map(e => expiredKeys.has(e._key) ? stripClaim(e) : e), auditRows = stampHistoryEntries(expired.map(pending => claimReleasedEntry({
            pending: pending,
            claim: pending.claim,
            at: now,
            via: "sweep",
            actor: actor
        })), stamp);
        try {
            return await client.patch(instance._id).ifRevisionId(instance._rev).set({
                pendingEffects: releasedQueue,
                history: [ ...instance.history, ...auditRows ]
            }).commit(SYNC_COMMIT), {
                released: expired
            };
        } catch (error) {
            if (!isRevisionConflict(error)) throw error;
        }
    }
    throw new Error(`sweepStaleClaims lost the optimistic-locking race ${SWEEP_MAX_ATTEMPTS} attempts running on ${instanceId} — a concurrent writer kept committing first. Retry later, or investigate a write storm on this instance.`);
}

function stripClaim(entry) {
    const {claim: _claim, ...rest} = entry;
    return rest;
}

function isProjectRole(value) {
    return isRecord(value) && typeof value.name == "string";
}

async function projectRoleNames(request, projectId) {
    const result = await request({
        uri: `/projects/${encodeURIComponent(projectId)}/roles`
    });
    if (!Array.isArray(result) || !result.every(isProjectRole)) throw new Error(`Project role catalog response is invalid for project "${projectId}"`);
    return new Set(result.map(role => role.name));
}

function memberRoleNames(member, projectId) {
    if (!Array.isArray(member.roles) || !member.roles.every(isProjectRole)) throw new Error(`Project member role directory response is invalid for project "${projectId}" and member "${member.id}"`);
    return member.roles.map(role => role.name);
}

function heldRoleNames(members, projectId) {
    return new Set(members.flatMap(member => member.isRobot === !0 ? [] : memberRoleNames(member, projectId)));
}

function addRoles(collector, args) {
    for (const role of args.roles ?? []) collector.references.push({
        definition: collector.definition,
        path: args.path,
        role: role
    });
}

function visitAssigneeValue(collector, args) {
    const members = Array.isArray(args.value) ? args.value : [ args.value ];
    for (const [index, member] of members.entries()) !isRecord(member) || member.type !== "role" || typeof member.role != "string" || collector.references.push({
        definition: collector.definition,
        path: `${args.path}[${index}].role`,
        role: member.role
    });
}

function visitObjectValue(collector, args) {
    for (const shape of args.fields ?? []) visitShapeValue(collector, {
        path: `${args.path}.${shape.name}`,
        shape: shape,
        value: args.value[shape.name]
    });
}

function visitArrayValue(collector, args) {
    for (const [index, row] of args.rows.entries()) if (isRecord(row)) for (const shape of args.shapes ?? []) visitShapeValue(collector, {
        path: `${args.path}[${index}].${shape.name}`,
        shape: shape,
        value: row[shape.name]
    });
}

function visitAssignmentValue(collector, args) {
    args.kind === "assignee" || args.kind === "assignees" ? visitAssigneeValue(collector, args) : args.kind === "object" && isRecord(args.value) ? visitObjectValue(collector, {
        ...args,
        value: args.value
    }) : args.kind === "array" && Array.isArray(args.value) && visitArrayValue(collector, {
        path: args.path,
        rows: args.value,
        shapes: args.of
    });
}

function visitShapeValue(collector, args) {
    visitAssignmentValue(collector, {
        path: args.path,
        kind: args.shape.type,
        value: args.value,
        fields: args.shape.fields,
        of: args.shape.of
    });
}

function visitField(collector, args) {
    addRoles(collector, {
        path: `${args.path}.roles`,
        roles: args.field.roles
    });
    for (const shape of args.field.fields ?? args.field.of ?? []) visitFieldShape(collector, {
        parentPath: args.path,
        shape: shape
    });
    args.field.initialValue?.type === "literal" && visitAssignmentValue(collector, {
        path: `${args.path}.initialValue.value`,
        kind: args.field.type,
        value: args.field.initialValue.value,
        fields: args.field.fields,
        of: args.field.of
    });
}

function visitFieldShape(collector, args) {
    const path = `${args.parentPath}.${args.shape.name}`;
    addRoles(collector, {
        path: `${path}.roles`,
        roles: args.shape.roles
    });
    for (const child of args.shape.fields ?? args.shape.of ?? []) visitFieldShape(collector, {
        parentPath: path,
        shape: child
    });
}

function rolesFromConditions(definition) {
    return conditionSitesOf(definition).flatMap(site => {
        try {
            return analyzeCondition(site.condition).atoms.flatMap(atom => rolesGateOf(atom) ?? []);
        } catch {
            return [];
        }
    });
}

function visitRoleAliases(collector, aliases) {
    for (const [role, fulfillers] of Object.entries(aliases ?? {})) role !== UNIVERSAL_ROLE_ALIAS_KEY && collector.references.push({
        definition: collector.definition,
        path: `roleAliases.${role}`,
        role: role
    }), addRoles(collector, {
        path: `roleAliases.${role}`,
        roles: fulfillers
    });
}

function visitFields(collector, args) {
    for (const [index, field] of (args.fields ?? []).entries()) visitField(collector, {
        path: `${args.parentPath}[${index}]`,
        field: field
    });
}

function visitAction(collector, args) {
    addRoles(collector, {
        path: `${args.path}.roles`,
        roles: args.action.roles
    });
    for (const [effectIndex, effect] of (args.action.effects ?? []).entries()) for (const shape of effect.outputs ?? []) visitFieldShape(collector, {
        parentPath: `${args.path}.effects[${effectIndex}].outputs`,
        shape: shape
    });
}

function visitStage(collector, args) {
    const stagePath = `stages[${args.stageIndex}]`;
    visitFields(collector, {
        fields: args.stage.fields,
        parentPath: `${stagePath}.fields`
    });
    for (const [activityIndex, activity] of (args.stage.activities ?? []).entries()) {
        const activityPath = `${stagePath}.activities[${activityIndex}]`;
        visitFields(collector, {
            fields: activity.fields,
            parentPath: `${activityPath}.fields`
        });
        for (const [actionIndex, action] of (activity.actions ?? []).entries()) visitAction(collector, {
            action: action,
            path: `${activityPath}.actions[${actionIndex}]`
        });
    }
}

function addConditionReferences(collector, definition) {
    for (const role of rolesFromConditions(definition)) collector.references.push({
        definition: collector.definition,
        path: "generated role condition",
        role: role
    });
}

function definitionRoleReferences(definition) {
    const collector = {
        references: [],
        definition: definition.name
    };
    visitRoleAliases(collector, definition.roleAliases), visitFields(collector, {
        fields: definition.fields,
        parentPath: "fields"
    });
    for (const [stageIndex, stage] of definition.stages.entries()) visitStage(collector, {
        stage: stage,
        stageIndex: stageIndex
    });
    return addConditionReferences(collector, definition), collector.references;
}

function definitionRoleNames(definition) {
    return [ ...new Set(definitionRoleReferences(definition).map(reference => reference.role)) ];
}

function roleCatalogProjectId(client, resource) {
    if (resource.type === "dataset") return datasetResourceParts(resource.id).projectId;
    const projectId = client.config?.().projectId;
    if (typeof projectId == "string" && projectId.length > 0) return projectId;
    throw new Error(`workflow.deployDefinitions: definitions reference project roles on ${resource.type} workflow resources, but the client does not expose a projectId for the project role catalog`);
}

async function validateDefinitionRoles(args) {
    const references = args.definitions.flatMap(definitionRoleReferences);
    if (references.length === 0) /* @__PURE__ */ return new Map;
    if (args.client.request === void 0) throw new Error("workflow.deployDefinitions: definitions reference project roles, but the client cannot read the project role catalog");
    const projectId = roleCatalogProjectId(args.client, args.workflowResource), [known, members] = await Promise.all([ projectRoleNames(args.client.request, projectId), requestProjectMembers(args.client.request, projectId) ]), unknown = references.filter(reference => !known.has(reference.role));
    if (unknown.length > 0) {
        const lines = unknown.map(({definition: definition, path: path, role: role}) => `  - ${definition}: ${path} references unknown role "${role}"`);
        throw new Error(`workflow.deployDefinitions: unknown project roles for project "${projectId}":\n${lines.join(`\n`)}`);
    }
    const held = heldRoleNames(members, projectId), warnings = /* @__PURE__ */ new Map;
    for (const {definition: definition, role: role} of references) {
        if (held.has(role)) continue;
        const message = `Project role "${role}" has no current user holder; work routed to it has no recipient.`, definitionWarnings = warnings.get(definition) ?? [];
        definitionWarnings.includes(message) || definitionWarnings.push(message), warnings.set(definition, definitionWarnings);
    }
    return warnings;
}

async function sortByDependencies({client: client, definitions: definitions, tag: tag}) {
    const byName = /* @__PURE__ */ new Map;
    for (const def of definitions) {
        if (byName.has(def.name)) throw new Error(`workflow.deployDefinitions: duplicate definition name "${def.name}" in batch — names identify a batch member (versions are deploy-assigned, not authored)`);
        byName.set(def.name, def);
    }
    const findInBatch = ref => findRefInBatch(byName, ref);
    return await assertSpawnContracts({
        client: client,
        definitions: definitions,
        ctx: {
            tag: tag,
            findInBatch: findInBatch
        }
    }), topoSortDefinitions(definitions, {
        findInBatch: findInBatch
    });
}

function findRefInBatch(byName, ref) {
    if (typeof ref.version != "number") return byName.get(ref.name);
}

async function assertSpawnContracts({client: client, definitions: definitions, ctx: ctx}) {
    const missing = [], invalidHandoffs = [];
    for (const def of definitions) for (const spawn of spawnSitesOf(def)) {
        const ref = logicalRefOf(spawn.definition), child = ctx.findInBatch(ref) ?? await resolveDeployedRef({
            client: client,
            ref: ref,
            tag: ctx.tag
        });
        if (child === void 0) {
            missing.push({
                reason: "unresolved-definition",
                from: def.name,
                ref: logicalRefLabel(ref)
            });
            continue;
        }
        for (const field of Object.keys(spawn.with ?? {})) {
            const entry = (child.fields ?? []).find(candidate => candidate.name === field);
            entry === void 0 ? invalidHandoffs.push({
                reason: "unconsumable-field",
                from: def.name,
                child: child.name,
                field: field,
                detail: "the child declares no workflow field with that name"
            }) : isInputSourced(entry) || invalidHandoffs.push({
                reason: "unconsumable-field",
                from: def.name,
                child: child.name,
                field: field,
                detail: `the child field is ${entry.initialValue?.type ?? "working-memory"}-sourced, not input-sourced`
            });
        }
    }
    if (missing.length === 0 && invalidHandoffs.length === 0) return;
    if (invalidHandoffs.length === 0) {
        const lines2 = missing.map(issue => `  - ${issue.from} → ${issue.ref} (neither in batch nor deployed)`);
        throw new Error(`workflow.deployDefinitions: ${missing.length} unresolved reference${missing.length === 1 ? "" : "s"}:\n` + lines2.join(`\n`));
    }
    const issues = [ ...missing, ...invalidHandoffs ], lines = [ ...missing.map(m => `  - ${m.from} → ${m.ref} (neither in batch nor deployed)`), ...invalidHandoffs.map(issue => `  - ${issue.from} → ${issue.child}: spawn.with["${issue.field}"] cannot be consumed — ${issue.detail}`) ], count = lines.length;
    throw new SpawnContractsInvalidError({
        issues: issues,
        message: `workflow.deployDefinitions: ${count} invalid spawn contract${count === 1 ? "" : "s"}:\n` + lines.join(`\n`)
    });
}

async function resolveDeployedRef({client: client, ref: ref, tag: tag}) {
    const wantsExplicit = typeof ref.version == "number", params = {
        definition: ref.name,
        tag: tag
    };
    wantsExplicit && (params.version = ref.version);
    const doc = await client.fetch(definitionLookupGroq(wantsExplicit), params);
    if (doc) return parseDefinitionInput(doc, `workflow.deployDefinitions child "${logicalRefLabel(ref)}"`);
}

function logicalRefLabel(ref) {
    return typeof ref.version == "number" ? `${ref.name} v${ref.version}` : ref.name;
}

function logicalRefOf(ref) {
    return {
        name: ref.name,
        ...ref.version !== void 0 ? {
            version: ref.version
        } : {}
    };
}

function spawnSitesOf(def) {
    return def.stages.flatMap(stage => stage.activities ?? []).flatMap(activity => activity.actions ?? []).flatMap(action => action.spawn === void 0 ? [] : [ action.spawn ]);
}

function topoSortDefinitions(definitions, ctx) {
    const visited = /* @__PURE__ */ new Set, visiting = /* @__PURE__ */ new Set, ordered = [], visit = def => {
        if (!visited.has(def.name)) {
            if (visiting.has(def.name)) throw new Error(`workflow.deployDefinitions: dependency cycle involving "${def.name}"`);
            visiting.add(def.name);
            for (const ref of refsOf(def)) {
                const child = ctx.findInBatch(ref);
                child !== void 0 && visit(child);
            }
            visiting.delete(def.name), visited.add(def.name), ordered.push(def);
        }
    };
    for (const def of definitions) visit(def);
    return ordered;
}

function refsOf(def) {
    return spawnSitesOf(def).map(spawn => logicalRefOf(spawn.definition));
}

function hashDefinitionContent(def) {
    const canonical = stableStringify(def);
    let hash = 0xcbf29ce484222325n;
    for (let i = 0; i < canonical.length; i++) hash ^= BigInt(canonical.charCodeAt(i)), 
    hash = hash * 0x100000001b3n & 0xffffffffffffffffn;
    return hash.toString(16).padStart(16, "0");
}

function planDefinitionDeploy({def: def, latest: latest, target: target}) {
    assertRoleConstrainedAssignmentResource({
        context: "workflow.deployDefinitions",
        definition: def,
        workflowResource: target.workflowResource
    });
    const expanded = expandResourceAliases(def, target.resourceAliases), contentHash = hashDefinitionContent(expanded), unchanged = latest !== void 0 && latest.contentHash === contentHash, version = unchanged ? latest.version : (latest?.version ?? 0) + 1, docId = definitionDocId({
        tag: target.tag,
        definition: expanded.name,
        version: version
    }), document = {
        ...expanded,
        _id: docId,
        _type: WORKFLOW_DEFINITION_TYPE,
        tag: target.tag,
        version: version,
        contentHash: contentHash,
        ...modelStampFor({
            documentType: "definition",
            document: expanded
        })
    };
    return {
        status: unchanged ? "unchanged" : "create",
        version: version,
        contentHash: contentHash,
        docId: docId,
        document: document
    };
}

const RESOURCE_ALIAS_REF_RE = new RegExp(`@(${RESOURCE_ALIAS_NAME_SOURCE}):`, "g"), STANDALONE_RESOURCE_ALIAS_RE = new RegExp(`^@${RESOURCE_ALIAS_NAME_SOURCE}:`), PROSE_KEYS = /* @__PURE__ */ new Set([ "title", "description" ]);

function expandResourceAliases(definition, resourceAliases) {
    assertResourceAliasNamesValid(resourceAliases);
    const map = resourceAliases ?? {};
    return mapJsonStrings(definition, (value, key) => key !== void 0 && PROSE_KEYS.has(key) ? value : expandAliasString({
        value: value,
        map: map,
        definitionName: definition.name
    }));
}

function expandAliasString({value: value, map: map, definitionName: definitionName}) {
    const expanded = value.replace(RESOURCE_ALIAS_REF_RE, (_match, alias) => {
        if (!Object.hasOwn(map, alias)) throw new Error(`workflow.deployDefinitions: definition "${definitionName}" references unbound resource alias "@${alias}". Bind it in the deploy target's resource map, or remove the reference.`);
        return gdrResourcePrefix(map[alias]);
    });
    if (expanded === value) return value;
    if (STANDALONE_RESOURCE_ALIAS_RE.test(value) && !isGdrUri(expanded)) throw new Error(`workflow.deployDefinitions: definition "${definitionName}" reference "${value}" expands to the malformed GDR "${expanded}". A resource alias reference must be "@<alias>:<documentId>" with a single document id.`);
    return expanded;
}

function assertResourceAliasNamesValid(resourceAliases) {
    for (const name of Object.keys(resourceAliases ?? {})) validateResourceAliasName(name);
}

const DEPLOY_STAMPED_KEYS = /* @__PURE__ */ new Set([ "tag", "version", "contentHash", "modelVersion", "minReaderModel" ]);

function isDocumentEnvelopeKey(key) {
    return key.startsWith("_") || DEPLOY_STAMPED_KEYS.has(key);
}

function parseDefinitionInput(def, caller) {
    const label = labelFor(caller, def);
    if (def._type !== void 0 && def._type !== WORKFLOW_DEFINITION_TYPE) throw new Error(`${label}: expected a workflow definition or a "${WORKFLOW_DEFINITION_TYPE}" document, got _type "${String(def._type)}"`);
    const content = Object.fromEntries(Object.entries(def).filter(([key]) => !isDocumentEnvelopeKey(key)));
    return parseStoredDefinition(content, label);
}

function stripSystemFields(doc) {
    const out = {};
    for (const [k, v2] of Object.entries(doc)) k === "_rev" || k === "_createdAt" || k === "_updatedAt" || (out[k] = v2);
    return out;
}

function stableStringify(value) {
    if (value === null || typeof value != "object") return JSON.stringify(value);
    if (Array.isArray(value)) return `[${value.map(stableStringify).join(",")}]`;
    const record = value;
    return `{${Object.keys(record).toSorted().map(k => `${JSON.stringify(k)}:${stableStringify(record[k])}`).join(",")}}`;
}

async function loadDefinition({client: client, definition: definition, version: version, tag: tag}) {
    if (version !== void 0) {
        const doc = await client.fetch(definitionLookupGroq(!0), {
            definition: definition,
            version: version,
            tag: tag
        });
        if (!doc) throw new DefinitionNotFoundError({
            definition: definition,
            version: version
        });
        return assertReadableModel(doc);
    }
    const latest = await loadLatestDeployed({
        client: client,
        definition: definition,
        tag: tag
    });
    if (!latest) throw new DefinitionNotFoundError({
        definition: definition
    });
    return latest;
}

async function loadLatestDeployed({client: client, definition: definition, tag: tag}) {
    const doc = await client.fetch(definitionLookupGroq(!1), {
        definition: definition,
        tag: tag
    });
    return doc ? assertReadableModel(doc) : void 0;
}

async function loadDefinitionVersions({client: client, definition: definition, tag: tag}) {
    return (await client.fetch(`*[_type == "${WORKFLOW_DEFINITION_TYPE}" && name == $definition && ${tagScopeFilter()}] | order(version desc)`, {
        definition: definition,
        tag: tag
    })).map(assertReadableModel);
}

async function loadDefinitionVersionsOrThrow({client: client, definition: definition, tag: tag}) {
    const versions = await loadDefinitionVersions({
        client: client,
        definition: definition,
        tag: tag
    });
    if (versions.length === 0) throw new DefinitionNotFoundError({
        definition: definition
    });
    return versions;
}

async function editField(args) {
    const {client: client, instanceId: instanceId, target: target, mode: mode = DEFAULT_EDIT_MODE, value: value, requestRecord: requestRecord, options: options} = args;
    return retryOnRevisionConflict({
        client: client,
        instanceId: instanceId,
        options: options,
        commit: ctx => commitEdit({
            ctx: ctx,
            target: target,
            mode: mode,
            value: value,
            requestRecord: requestRecord,
            options: options
        }),
        onExhausted: () => new ConcurrentEditFieldError({
            instanceId: instanceId,
            target: labelTarget(target),
            attempts: CONCURRENT_COMMIT_MAX_ATTEMPTS
        })
    });
}

async function commitEdit({ctx: ctx, target: target, mode: mode, value: value, requestRecord: requestRecord, options: options}) {
    assertRequestUnprocessed({
        instance: ctx.instance,
        record: requestRecord,
        now: ctx.now
    });
    const actor = options?.actor, stage = findStage(ctx.definition, ctx.instance.currentStage), site = resolveEditTarget({
        definition: ctx.definition,
        stage: stage,
        target: target
    });
    if (site === void 0) throw new ContractViolationError(`No field "${describeTarget(target)}" resolves in current stage "${stage.name}" of ${ctx.definition.name}`);
    await assertFieldEditable({
        ctx: ctx,
        site: site,
        options: options
    });
    const op = buildEditOp({
        site: site,
        mode: mode,
        value: value
    }), mutation = startRevisionRetryMutation(ctx.instance);
    recordProcessedRequest({
        mutation: mutation,
        record: requestRecord,
        now: ctx.now
    });
    const ranOps = await runOps({
        ops: [ op ],
        mutation: mutation,
        stage: stage.name,
        origin: {
            edit: !0,
            ...site.scope === "activity" && site.activity !== void 0 ? {
                activity: site.activity
            } : {}
        },
        params: {},
        actor: actor,
        self: selfGdr(ctx.instance),
        now: ctx.now,
        snapshot: ctx.snapshot,
        refSurface: ctx.refSurface,
        memberRoles: ctx.memberRoles,
        roleAliases: ctx.definition.roleAliases
    });
    return await persistThenMaybeRefresh({
        ctx: ctx,
        mutation: mutation,
        stageName: stage.name,
        didChangeState: ranOps.some(isFieldOp)
    }), {
        edited: !0,
        target: fieldTarget(site),
        ...ranOps.length > 0 ? {
            ranOps: ranOps
        } : {}
    };
}

async function assertFieldEditable({ctx: ctx, site: site, options: options}) {
    const actor = options?.actor, window = fieldWindowOpen(ctx.instance, site), vars = await callerBoundVarsForCall({
        client: ctx.client,
        instance: ctx.instance,
        options: options
    }), predicateSatisfied = window.open && site.effective !== !0 && site.effective !== void 0 ? await ctxEvaluateCondition({
        ctx: ctx,
        condition: site.effective,
        opts: {
            ...site.activity !== void 0 ? {
                activityName: site.activity
            } : {},
            ...actor !== void 0 ? {
                actor: actor
            } : {},
            ...vars !== void 0 ? {
                vars: vars
            } : {}
        }
    }) : site.effective === !0, reason = editDisabledReason({
        effective: site.effective,
        instance: ctx.instance,
        window: window,
        guardDenial: void 0,
        predicateSatisfied: predicateSatisfied
    });
    if (reason !== void 0) throw new EditFieldDeniedError({
        target: fieldTarget(site),
        reason: reason
    });
}

function buildEditOp({site: site, mode: mode, value: value}) {
    if (mode === "unset") return {
        type: "field.unset",
        target: site.ref
    };
    if (value === void 0) throw new ContractViolationError(`editField mode "${mode}" requires a value for field "${site.name}"`);
    return {
        type: mode === "append" ? "field.append" : "field.set",
        target: site.ref,
        value: {
            type: "literal",
            value: value
        }
    };
}

function fieldTarget(site) {
    return {
        scope: site.scope,
        field: site.name,
        ...site.activity !== void 0 ? {
            activity: site.activity
        } : {}
    };
}

function labelTarget(target) {
    return {
        scope: target.scope ?? (target.activity !== void 0 ? "activity" : "workflow"),
        field: target.field,
        ...target.activity !== void 0 ? {
            activity: target.activity
        } : {}
    };
}

function describeTarget(target) {
    const where = target.activity !== void 0 ? `${target.activity}.${target.field}` : target.field;
    return target.scope !== void 0 ? `${target.scope}:${where}` : where;
}

async function resolveOperationContext(args) {
    validateTag(args.tag);
    const access = await resolveAccess(args.client, {
        ...args.grantsFromPath !== void 0 ? {
            grantsFromPath: args.grantsFromPath
        } : {}
    });
    return {
        access: access,
        actor: access.actor,
        memberRolesLoader: createAssignmentMemberRolesLoader(args.client),
        clientForGdr: buildClientForGdr({
            client: args.client,
            workflowResource: args.workflowResource,
            resourceClients: args.resourceClients
        }),
        refSurface: buildResourceSurface({
            workflowResource: args.workflowResource,
            resourceClients: args.resourceClients
        })
    };
}

async function runCommitVerb(params) {
    const {args: args, op: op, run: run} = params, {client: client, tag: tag, instanceId: instanceId} = args, context = await resolveOperationContext(args), clock = args.clock ?? wallClock, record = requestRecordFor({
        idempotencyKey: args.idempotencyKey,
        op: op,
        idempotencyTtlMs: args.idempotencyTtlMs
    }), before = await reload({
        client: client,
        instanceId: instanceId,
        tag: tag
    }), now = clock();
    return runDeduped({
        client: client,
        tag: tag,
        instanceId: instanceId,
        record: record,
        before: before,
        now: now,
        actor: context.actor,
        clientForGdr: context.clientForGdr,
        refSurface: context.refSurface,
        memberRolesLoader: context.memberRolesLoader,
        clock: clock,
        executionContext: args.executionContext,
        telemetry: args.telemetry,
        run: () => run({
            ...context,
            clock: clock,
            now: now,
            record: record,
            before: before
        })
    });
}

function requestRecordFor(args) {
    if (args.idempotencyKey !== void 0) {
        if (args.idempotencyKey.trim() === "") throw new ContractViolationError(`${args.op}: idempotencyKey must be a non-empty string; omit it to opt out of dedup`);
        return {
            key: args.idempotencyKey,
            op: args.op,
            ttlMs: args.idempotencyTtlMs ?? DEFAULT_IDEMPOTENCY_TTL_MS
        };
    }
}

async function runDeduped(args) {
    const {client: client, tag: tag, instanceId: instanceId, record: record, before: before, now: now, actor: actor, clientForGdr: clientForGdr, refSurface: refSurface, memberRolesLoader: memberRolesLoader, run: run} = args, {clock: clock, executionContext: executionContext, telemetry: telemetry} = args, replay = async () => {
        const pre = await reload({
            client: client,
            instanceId: instanceId,
            tag: tag
        }), settled = await cascadeAndReload({
            client: client,
            tag: tag,
            instanceId: instanceId,
            actor: actor,
            clientForGdr: clientForGdr,
            refSurface: refSurface,
            memberRolesLoader: memberRolesLoader,
            clock: clock,
            executionContext: executionContext,
            telemetry: telemetry
        });
        return settledResult({
            ...settled,
            changed: settled.instance._rev !== pre._rev
        });
    };
    if (record !== void 0 && before !== void 0 && findProcessedRequest({
        instance: before,
        record: record,
        now: now
    }) !== void 0) return replay();
    try {
        return await run();
    } catch (error) {
        if (error instanceof RequestAlreadyProcessedError) return replay();
        throw error;
    }
}

async function cascade({client: client, instanceId: instanceId, actor: actor, clientForGdr: clientForGdr, refSurface: refSurface, memberRolesLoader: memberRolesLoader, clock: clock, executionContext: executionContext, telemetry: telemetry, overlay: overlay}) {
    const settled = await cascadeAutoTransitions({
        client: client,
        instanceId: instanceId,
        actor: actor,
        clientForGdr: clientForGdr,
        refSurface: refSurface,
        memberRolesLoader: memberRolesLoader,
        ...clock !== void 0 ? {
            clock: clock
        } : {},
        ...executionContext !== void 0 ? {
            executionContext: executionContext
        } : {},
        ...telemetry !== void 0 ? {
            telemetry: telemetry
        } : {},
        ...overlay !== void 0 ? {
            overlay: overlay
        } : {}
    });
    return await propagateToAncestors({
        client: client,
        instanceId: instanceId,
        actor: actor,
        clientForGdr: clientForGdr,
        refSurface: refSurface,
        memberRolesLoader: memberRolesLoader,
        ...clock !== void 0 ? {
            clock: clock
        } : {},
        ...executionContext !== void 0 ? {
            executionContext: executionContext
        } : {},
        ...telemetry !== void 0 ? {
            telemetry: telemetry
        } : {}
    }), settled;
}

async function cascadeAndReload(args) {
    const {client: client, tag: tag, instanceId: instanceId, actor: actor, clientForGdr: clientForGdr, refSurface: refSurface, memberRolesLoader: memberRolesLoader, clock: clock} = args;
    return {
        ...await cascade({
            client: client,
            instanceId: instanceId,
            actor: actor,
            clientForGdr: clientForGdr,
            refSurface: refSurface,
            memberRolesLoader: memberRolesLoader,
            clock: clock,
            ...args.executionContext !== void 0 ? {
                executionContext: args.executionContext
            } : {},
            ...args.telemetry !== void 0 ? {
                telemetry: args.telemetry
            } : {}
        }),
        instance: await reload({
            client: client,
            instanceId: instanceId,
            tag: tag
        })
    };
}

async function unchangedNextEvaluationAt(args) {
    const {client: client, instance: instance, actor: actor, clientForGdr: clientForGdr, refSurface: refSurface, memberRolesLoader: memberRolesLoader, now: now} = args;
    if (terminalState(instance) !== "in-flight") return;
    const ctx = await buildEngineContext({
        client: client,
        instance: instance,
        definition: parseDefinitionSnapshot(instance),
        clientForGdr: clientForGdr,
        refSurface: refSurface,
        memberRolesLoader: memberRolesLoader,
        clock: () => now,
        ...actor !== void 0 ? {
            actor: actor
        } : {},
        ...args.executionContext !== void 0 ? {
            executionContext: args.executionContext
        } : {},
        ...args.telemetry !== void 0 ? {
            telemetry: args.telemetry
        } : {}
    });
    return ctxNextEvaluationAt(ctx);
}

async function adminOverrideResult(args) {
    const {client: client, tag: tag, instanceId: instanceId, fired: fired, now: now, executionContext: executionContext, telemetry: telemetry, ...scope} = args, optional = {
        ...executionContext !== void 0 ? {
            executionContext: executionContext
        } : {},
        ...telemetry !== void 0 ? {
            telemetry: telemetry
        } : {}
    }, settled = fired ? await cascade({
        client: client,
        instanceId: instanceId,
        ...scope,
        ...optional
    }) : {
        cascaded: 0
    }, instance = await reload({
        client: client,
        instanceId: instanceId,
        tag: tag
    });
    return settledResult({
        ...settled,
        instance: instance,
        changed: fired,
        ...fired ? {} : {
            nextEvaluationAt: await unchangedNextEvaluationAt({
                client: client,
                instance: instance,
                now: now,
                actor: scope.actor,
                clientForGdr: scope.clientForGdr,
                refSurface: scope.refSurface,
                memberRolesLoader: scope.memberRolesLoader,
                ...optional
            })
        }
    });
}

function settledResult(args) {
    const {instance: instance, cascaded: cascaded, changed: changed, ranOps: ranOps, nextEvaluationAt: nextEvaluationAt} = args;
    return {
        instance: instance,
        cascaded: cascaded,
        changed: changed,
        ...ranOps !== void 0 ? {
            ranOps: ranOps
        } : {},
        ...nextEvaluationAt !== void 0 ? {
            nextEvaluationAt: nextEvaluationAt
        } : {}
    };
}

async function settleStart(args) {
    const runArgs = {
        client: args.client,
        instanceId: args.instanceId,
        actor: args.actor,
        clientForGdr: args.clientForGdr,
        refSurface: args.refSurface,
        memberRolesLoader: args.memberRolesLoader,
        clock: args.clock,
        ...args.executionContext !== void 0 ? {
            executionContext: args.executionContext
        } : {},
        ...args.telemetry !== void 0 ? {
            telemetry: args.telemetry
        } : {}
    };
    try {
        await primeInitialStage(runArgs);
    } catch (cause) {
        throw cause instanceof WorkflowStateDivergedError ? cause : new StartNotPrimedError({
            instanceId: args.instanceId,
            cause: cause
        });
    }
    args.emitStarted?.();
    try {
        return await cascade(runArgs);
    } catch (cause) {
        if (cause instanceof WorkflowStateDivergedError) throw cause;
        const instance = await reload({
            client: args.client,
            instanceId: args.instanceId,
            tag: args.tag
        }).catch(() => {});
        throw new StartNotSettledError({
            instanceId: args.instanceId,
            ...instance !== void 0 ? {
                instance: instance
            } : {},
            cause: cause
        });
    }
}

function resumeMismatch(args) {
    if (args.existing.definition !== args.definition) return `holds workflow "${args.existing.definition}", not "${args.definition}"`;
    if (args.version !== void 0 && args.existing.pinnedVersion !== args.version) return `is pinned to version ${args.existing.pinnedVersion}, not ${args.version}`;
}

async function resumeStart(args) {
    const {existing: existing, definition: definition, version: version, initialFieldCount: initialFieldCount, ...settlementArgs} = args, mismatch = resumeMismatch({
        existing: existing,
        definition: definition,
        version: version
    });
    if (mismatch !== void 0) throw new ContractViolationError(`startInstance: instanceId "${existing._id}" already exists and ${mismatch}. A supplied instanceId is start's idempotency key — reuse one only to retry the same start.`);
    if (existing.stages.length === 0 && terminalState(existing) !== "in-flight") throw new ContractViolationError(`startInstance: instanceId "${existing._id}" belongs to a start that was discarded (aborted before it finished starting) — mint a new id to start again.`);
    const completesStart = isUnprimed(existing), emitStarted = () => resolveTelemetry(args.telemetry).log(WorkflowInstanceStarted, instanceStartedDataFor({
        instance: existing,
        initialFieldCount: initialFieldCount,
        viaSpawn: !1
    })), settled = await settleStart({
        ...settlementArgs,
        instanceId: existing._id,
        ...completesStart ? {
            emitStarted: emitStarted
        } : {}
    }), instance = await reload({
        client: args.client,
        instanceId: existing._id,
        tag: args.tag
    });
    return settledResult({
        ...settled,
        instance: instance,
        changed: instance._rev !== existing._rev
    });
}

function engineOptionsForActor({actor: actor, clock: clock, clientForGdr: clientForGdr, refSurface: refSurface, memberRolesLoader: memberRolesLoader, executionContext: executionContext, telemetry: telemetry}) {
    return {
        ...actor !== void 0 ? {
            actor: actor
        } : {},
        clock: clock,
        clientForGdr: clientForGdr,
        refSurface: refSurface,
        memberRolesLoader: memberRolesLoader,
        ...executionContext !== void 0 ? {
            executionContext: executionContext
        } : {},
        ...telemetry !== void 0 ? {
            telemetry: telemetry
        } : {}
    };
}

async function abortAndPropagate(args) {
    const {client: client, instanceId: instanceId, reason: reason, actor: actor, clientForGdr: clientForGdr, refSurface: refSurface, memberRolesLoader: memberRolesLoader, clock: clock, executionContext: executionContext, telemetry: telemetry, requestRecord: requestRecord} = args, result = await abortInstance({
        client: client,
        instanceId: instanceId,
        ...reason !== void 0 ? {
            reason: reason
        } : {},
        ...requestRecord !== void 0 ? {
            requestRecord: requestRecord
        } : {},
        options: engineOptionsForActor({
            actor: actor,
            clock: clock,
            clientForGdr: clientForGdr,
            refSurface: refSurface,
            memberRolesLoader: memberRolesLoader,
            executionContext: executionContext,
            telemetry: telemetry
        })
    }), drained = await drainCondemnedChildren({
        client: client,
        instanceId: instanceId,
        actor: actor,
        clientForGdr: clientForGdr,
        refSurface: refSurface,
        memberRolesLoader: memberRolesLoader,
        clock: clock,
        ...executionContext !== void 0 ? {
            executionContext: executionContext
        } : {},
        ...telemetry !== void 0 ? {
            telemetry: telemetry
        } : {},
        ...result.fired ? {
            instance: result.instance
        } : {}
    });
    return await propagateToAncestors({
        client: client,
        instanceId: instanceId,
        actor: actor,
        clientForGdr: clientForGdr,
        refSurface: refSurface,
        memberRolesLoader: memberRolesLoader,
        clock: clock,
        ...executionContext !== void 0 ? {
            executionContext: executionContext
        } : {},
        ...telemetry !== void 0 ? {
            telemetry: telemetry
        } : {},
        ...result.fired && !drained.drained ? {
            instance: drained.instance
        } : {}
    }), result.fired;
}

function toContextEntries(ctx) {
    return Object.entries(ctx).map(([id, value]) => toContextSeedEntry(id, value));
}

function toContextSeedEntry(name, value) {
    return typeof value == "string" || typeof value == "number" || typeof value == "boolean" || isGdr(value) ? contextEntry(name, value) : contextJsonEntry(name, value);
}

async function dispatchGatedWrite(args) {
    const {client: client, tag: tag, workflowResource: workflowResource, instanceId: instanceId, resourceClients: resourceClients, grantsFromPath: grantsFromPath, apply: apply} = args, evaluation = await evaluateInstance({
        client: client,
        tag: tag,
        workflowResource: workflowResource,
        instanceId: instanceId,
        clock: args.clock,
        ...grantsFromPath !== void 0 ? {
            grantsFromPath: grantsFromPath
        } : {},
        ...resourceClients !== void 0 ? {
            resourceClients: resourceClients
        } : {}
    }), ranOps = await apply(args.before, evaluation), settled = await cascadeAndReload({
        client: client,
        tag: tag,
        instanceId: instanceId,
        actor: args.actor,
        clientForGdr: args.clientForGdr,
        refSurface: args.refSurface,
        memberRolesLoader: args.memberRolesLoader,
        clock: args.clock,
        executionContext: args.executionContext,
        telemetry: args.telemetry
    });
    return settledResult({
        ...settled,
        changed: !0,
        ...ranOps !== void 0 ? {
            ranOps: ranOps
        } : {}
    });
}

function guardDeniedError(instanceId, reason) {
    return new MutationGuardDeniedError({
        documentId: instanceId,
        action: "update",
        denied: reason.denied
    });
}

function assertActionAllowed({evaluation: evaluation, activity: activity, action: action}) {
    const actionEval = evaluation.currentStage.activities.find(t => t.activity.name === activity)?.actions.find(a => a.action.name === action);
    if (actionEval?.allowed === !1 && actionEval.disabledReason) {
        const reason = actionEval.disabledReason;
        throw reason.kind === "mutation-guard-denied" ? guardDeniedError(evaluation.instance._id, reason) : new ActionDisabledError({
            activity: activity,
            action: action,
            reason: reason
        });
    }
}

function assertEditAllowed(evaluation, target) {
    const field = evaluation.editableFields.filter(s => editTargetMatches(s, target)).sort((a, b) => SCOPE_PRECEDENCE[a.scope] - SCOPE_PRECEDENCE[b.scope])[0];
    if (field !== void 0 && !field.editable && field.disabledReason !== void 0) {
        const reason = field.disabledReason;
        throw reason.kind === "mutation-guard-denied" ? guardDeniedError(evaluation.instance._id, reason) : new EditFieldDeniedError({
            target: {
                scope: field.scope,
                field: field.name,
                ...field.activity !== void 0 ? {
                    activity: field.activity
                } : {}
            },
            reason: reason
        });
    }
}

async function applyEdit(args) {
    const {client: client, instance: instance, target: target, mode: mode, value: value, requestRecord: requestRecord, ...options} = args;
    return (await editField({
        client: client,
        instanceId: instance._id,
        target: target,
        ...mode !== void 0 ? {
            mode: mode
        } : {},
        ...value !== void 0 ? {
            value: value
        } : {},
        ...requestRecord !== void 0 ? {
            requestRecord: requestRecord
        } : {},
        options: buildEngineCallOptions(options)
    })).ranOps;
}

function buildEngineCallOptions(args) {
    return {
        actor: args.actor,
        clientForGdr: args.clientForGdr,
        refSurface: args.refSurface,
        memberRolesLoader: args.memberRolesLoader,
        ...args.localPrincipalId !== void 0 ? {
            localPrincipalId: args.localPrincipalId
        } : {},
        ...args.grants !== void 0 ? {
            grants: args.grants
        } : {},
        ...args.attributes !== void 0 ? {
            attributes: args.attributes
        } : {},
        ...args.clock !== void 0 ? {
            clock: args.clock
        } : {},
        ...args.executionContext !== void 0 ? {
            executionContext: args.executionContext
        } : {},
        ...args.telemetry !== void 0 ? {
            telemetry: args.telemetry
        } : {}
    };
}

async function applyAction(args) {
    const {client: client, instance: instance, activity: activity, action: action, params: params, requestRecord: requestRecord, ...rest} = args, options = buildEngineCallOptions(rest);
    return (await fireAction({
        client: client,
        instanceId: instance._id,
        activity: activity,
        action: action,
        ...params !== void 0 ? {
            params: params
        } : {},
        ...requestRecord !== void 0 ? {
            requestRecord: requestRecord
        } : {},
        options: options
    })).ranOps;
}

async function deleteDefinition(args) {
    const {client: client, tag: tag, definition: definition, version: version, cascade: cascade2, reason: reason, executionContext: executionContext, telemetry: telemetry} = args, {actor: actor, clientForGdr: clientForGdr, refSurface: refSurface, memberRolesLoader: memberRolesLoader} = await resolveOperationContext(args), clock = args.clock ?? wallClock, versions = await loadDefinitionVersionsOrThrow({
        client: client,
        definition: definition,
        tag: tag
    }), targets = version === void 0 ? versions : versions.filter(d => d.version === version);
    if (targets.length === 0) throw new DefinitionNotFoundError({
        definition: definition,
        ...version !== void 0 ? {
            version: version
        } : {}
    });
    const lastVersionGoes = targets.length === versions.length;
    await assertNoSpawnReferrers({
        client: client,
        tag: tag,
        definition: definition,
        targets: targets,
        lastVersionGoes: lastVersionGoes
    });
    const instanceIds = await nonTerminalInstanceIds({
        client: client,
        tag: tag,
        definition: definition,
        version: version
    });
    if (instanceIds.length > 0 && cascade2 !== !0) throw new DefinitionInUseError({
        definition: definition,
        blockedBy: {
            reason: "non-terminal-instances",
            instanceIds: instanceIds
        }
    });
    const abortedInstanceIds = await abortInstances({
        client: client,
        instanceIds: instanceIds,
        reason: reason,
        actor: actor,
        clientForGdr: clientForGdr,
        refSurface: refSurface,
        memberRolesLoader: memberRolesLoader,
        clock: clock,
        executionContext: executionContext,
        telemetry: telemetry
    }), tx = client.transaction();
    for (const target of targets) tx.delete(target._id);
    await tx.commit();
    const deletedGuardCount = lastVersionGoes ? await deleteOrphanedDefinitionGuards({
        client: client,
        clientForGdr: clientForGdr,
        workflowResource: args.workflowResource,
        definition: definition,
        definitions: versions,
        tag: tag
    }) : 0;
    return {
        name: definition,
        deletedVersions: targets.map(d => d.version),
        abortedInstanceIds: abortedInstanceIds,
        deletedGuardCount: deletedGuardCount
    };
}

async function assertNoSpawnReferrers({client: client, tag: tag, definition: definition, targets: targets, lastVersionGoes: lastVersionGoes}) {
    const deployed = (await client.fetch(`*[_type == "${WORKFLOW_DEFINITION_TYPE}" && ${tagScopeFilter()}]`, {
        tag: tag
    })).map(assertReadableModel), targetIds = new Set(targets.map(d => d._id)), targetVersions = new Set(targets.map(d => d.version)), referrers = deployed.filter(d => !targetIds.has(d._id)).filter(d => refsOf(d).some(ref => ref.name === definition && (typeof ref.version == "number" ? targetVersions.has(ref.version) : lastVersionGoes)));
    if (referrers.length > 0) throw new DefinitionInUseError({
        definition: definition,
        blockedBy: {
            reason: "spawn-referrers",
            referrers: referrers.map(d => ({
                definition: d.name,
                version: d.version
            }))
        }
    });
}

async function nonTerminalInstanceIds({client: client, tag: tag, definition: definition, version: version}) {
    const versionFilter = version === void 0 ? "" : " && pinnedVersion == $version";
    return (await client.fetch(`*[_type == "${WORKFLOW_INSTANCE_TYPE}" && definition == $definition && ${tagScopeFilter()} && ${inFlightFilter()}${versionFilter}] | order(_id asc){_id}`, {
        definition: definition,
        tag: tag,
        ...version !== void 0 ? {
            version: version
        } : {}
    })).map(doc => doc._id);
}

async function abortInstances(args) {
    const {client: client, instanceIds: instanceIds, reason: reason, actor: actor, clientForGdr: clientForGdr, clock: clock, executionContext: executionContext, telemetry: telemetry} = args, aborted = [];
    for (const instanceId of instanceIds) await abortAndPropagate({
        client: client,
        instanceId: instanceId,
        reason: reason,
        actor: actor,
        clientForGdr: clientForGdr,
        refSurface: args.refSurface,
        memberRolesLoader: args.memberRolesLoader,
        clock: clock,
        ...executionContext !== void 0 ? {
            executionContext: executionContext
        } : {},
        ...telemetry !== void 0 ? {
            telemetry: telemetry
        } : {}
    }) && aborted.push(instanceId);
    return aborted;
}

function lintDeadActivityInputSeeds(definition) {
    return definition.stages.flatMap(stage => (stage.activities ?? []).flatMap(activity => (activity.fields ?? []).filter(field => field.initialValue?.type === "input").map(field => `Activity field "${stage.name}.${activity.name}.${field.name}" uses an input seed, but start/spawn inputs address workflow-scope fields only; this field will always materialize empty. Use a literal, query, field read, or working-memory field instead.`)));
}

function projectStartSliceRow(instance) {
    const subject = (instance.fields ?? []).find(field => field._type === "subject")?.value;
    return {
        definition: instance.definition,
        subject: typeof subject == "object" && subject !== null && "id" in subject && isGdrUri(subject.id) ? subject.id : null,
        completedAt: instance.completedAt ?? null
    };
}

async function fetchStartSlice(args) {
    const {client: client, tag: tag} = args, {query: query, params: params} = instancesQuery({
        tag: tag,
        filter: {
            includeCompleted: !0
        }
    });
    return (await client.fetch(query, params)).map(assertReadableModel).map(projectStartSliceRow);
}

const workflow = {
    deployDefinitions: async rawArgs => {
        const args = taggedScope(rawArgs, REQUEST_TAG.deploy), {client: client, tag: tag, workflowResource: workflowResource, resourceAliases: resourceAliases} = args;
        validateTag(tag);
        const definitions = args.definitions.map(def => parseDefinitionInput(def, "workflow.deployDefinitions"));
        definitions.forEach(validateDefinition), assertReaderModelAcknowledgement(args.expectedMinReaderModel, {
            requiredMinReaderModel: requiredDefinitionReaderModel(definitions)
        });
        const roleWarnings = await validateDefinitionRoles({
            client: client,
            definitions: definitions,
            workflowResource: workflowResource
        }), ordered = await sortByDependencies({
            client: client,
            definitions: definitions,
            tag: tag
        }), results = [], deployed = [], tx = client.transaction();
        let hasWrites = !1;
        for (const def of ordered) {
            const lint = [ ...lintEffectOutputs(def), ...lintDeadActivityInputSeeds(def), ...roleWarnings.get(def.name) ?? [] ], warnings = lint.length > 0 ? {
                warnings: lint
            } : {}, latest = await loadLatestDeployed({
                client: client,
                definition: def.name,
                tag: tag
            }), plan = planDefinitionDeploy({
                def: def,
                latest: latest,
                target: {
                    tag: tag,
                    workflowResource: workflowResource,
                    ...resourceAliases !== void 0 ? {
                        resourceAliases: resourceAliases
                    } : {}
                }
            });
            if (plan.status === "unchanged") {
                results.push({
                    name: def.name,
                    version: plan.version,
                    status: "unchanged",
                    ...warnings
                }), deployed.push({
                    def: def,
                    contentHash: plan.contentHash,
                    status: "unchanged"
                });
                continue;
            }
            tx.create(plan.document), hasWrites = !0, results.push({
                name: def.name,
                version: plan.version,
                status: "created",
                ...warnings
            }), deployed.push({
                def: def,
                contentHash: plan.contentHash,
                status: "created"
            });
        }
        hasWrites && await tx.commit();
        const deployId = randomKey(), telemetry = resolveTelemetry(args.telemetry);
        for (const {def: def, contentHash: contentHash, status: status} of deployed) telemetry.log(WorkflowDefinitionDeployed, definitionDeployedData(def, {
            contentHash: contentHash,
            status: status,
            deployId: deployId
        }));
        return {
            results: results,
            deployId: deployId
        };
    },
    deleteDefinition: async rawArgs => {
        const args = taggedScope(rawArgs, REQUEST_TAG.deleteDefinition), result = await deleteDefinition(args);
        return resolveTelemetry(args.telemetry).log(WorkflowDefinitionDeleted, {
            deletedVersionCount: result.deletedVersions.length,
            cascadeAbortedCount: result.abortedInstanceIds.length,
            deletedGuardCount: result.deletedGuardCount
        }), result;
    },
    startInstance: async rawArgs => {
        const args = taggedScope(rawArgs, REQUEST_TAG.start), {client: client, tag: tag, definition: definitionName, version: version, initialFields: initialFields, instanceId: instanceId, executionContext: executionContext} = args, operationContext = await resolveOperationContext(args), {actor: actor, clientForGdr: clientForGdr, refSurface: refSurface, memberRolesLoader: memberRolesLoader} = operationContext, clock = args.clock ?? wallClock, seedFields = initialFields ?? [], existing = instanceId !== void 0 ? await getInstanceDocument(client, instanceId) : void 0;
        return existing !== void 0 && existing.tag === tag ? resumeStart({
            client: client,
            tag: tag,
            existing: existing,
            definition: definitionName,
            version: version,
            initialFieldCount: seedFields.length,
            actor: actor,
            clientForGdr: clientForGdr,
            refSurface: refSurface,
            memberRolesLoader: memberRolesLoader,
            clock: clock,
            ...executionContext !== void 0 ? {
                executionContext: executionContext
            } : {},
            ...args.telemetry !== void 0 ? {
                telemetry: args.telemetry
            } : {}
        }) : startFreshInstance({
            args: args,
            operationContext: operationContext,
            clock: clock,
            seedFields: seedFields
        });
    },
    fireAction: async rawArgs => {
        const args = taggedScope(rawArgs, REQUEST_TAG.fireAction), {client: client, tag: tag, workflowResource: workflowResource, instanceId: instanceId, activity: activity, action: action, params: params, idempotent: idempotent, resourceClients: resourceClients, grantsFromPath: grantsFromPath, executionContext: executionContext} = args;
        return runCommitVerb({
            args: args,
            op: "fireAction",
            run: async ({access: access, actor: actor, clientForGdr: clientForGdr, refSurface: refSurface, memberRolesLoader: memberRolesLoader, clock: clock, now: now, record: record, before: before}) => {
                if (findCurrentActivityEntry(before, activity) === void 0 && idempotent === !0) return settledResult({
                    instance: before,
                    cascaded: 0,
                    changed: !1,
                    nextEvaluationAt: await unchangedNextEvaluationAt({
                        client: client,
                        instance: before,
                        actor: actor,
                        clientForGdr: clientForGdr,
                        refSurface: refSurface,
                        memberRolesLoader: memberRolesLoader,
                        now: now,
                        ...executionContext !== void 0 ? {
                            executionContext: executionContext
                        } : {},
                        ...args.telemetry !== void 0 ? {
                            telemetry: args.telemetry
                        } : {}
                    })
                });
                const result = await dispatchGatedWrite({
                    client: client,
                    tag: tag,
                    workflowResource: workflowResource,
                    instanceId: instanceId,
                    actor: actor,
                    clientForGdr: clientForGdr,
                    refSurface: refSurface,
                    memberRolesLoader: memberRolesLoader,
                    clock: clock,
                    before: before,
                    ...grantsFromPath !== void 0 ? {
                        grantsFromPath: grantsFromPath
                    } : {},
                    ...executionContext !== void 0 ? {
                        executionContext: executionContext
                    } : {},
                    ...resourceClients !== void 0 ? {
                        resourceClients: resourceClients
                    } : {},
                    ...args.telemetry !== void 0 ? {
                        telemetry: args.telemetry
                    } : {},
                    apply: (instance, evaluation) => (assertActionAllowed({
                        evaluation: evaluation,
                        activity: activity,
                        action: action
                    }), applyAction({
                        client: client,
                        instance: instance,
                        activity: activity,
                        action: action,
                        actor: actor,
                        ...access.grants !== void 0 ? {
                            grants: access.grants
                        } : {},
                        ...access.localPrincipalId !== void 0 ? {
                            localPrincipalId: access.localPrincipalId
                        } : {},
                        clock: clock,
                        clientForGdr: clientForGdr,
                        refSurface: refSurface,
                        memberRolesLoader: memberRolesLoader,
                        ...executionContext !== void 0 ? {
                            executionContext: executionContext
                        } : {},
                        ...args.telemetry !== void 0 ? {
                            telemetry: args.telemetry
                        } : {},
                        ...params !== void 0 ? {
                            params: params
                        } : {},
                        ...record !== void 0 ? {
                            requestRecord: record
                        } : {}
                    }))
                });
                return resolveTelemetry(args.telemetry).log(WorkflowActionFired, actionFiredData({
                    instance: before,
                    activity: activity,
                    params: params,
                    cascaded: result.cascaded
                })), result;
            }
        });
    },
    editField: async rawArgs => {
        const args = taggedScope(rawArgs, REQUEST_TAG.editField), {client: client, tag: tag, workflowResource: workflowResource, instanceId: instanceId, target: target, mode: mode, value: value, resourceClients: resourceClients, grantsFromPath: grantsFromPath, executionContext: executionContext} = args;
        return runCommitVerb({
            args: args,
            op: "editField",
            run: async ({access: access, actor: actor, clientForGdr: clientForGdr, refSurface: refSurface, memberRolesLoader: memberRolesLoader, clock: clock, record: record, before: before}) => {
                const result = await dispatchGatedWrite({
                    client: client,
                    tag: tag,
                    workflowResource: workflowResource,
                    instanceId: instanceId,
                    actor: actor,
                    clientForGdr: clientForGdr,
                    refSurface: refSurface,
                    memberRolesLoader: memberRolesLoader,
                    clock: clock,
                    before: before,
                    ...grantsFromPath !== void 0 ? {
                        grantsFromPath: grantsFromPath
                    } : {},
                    ...executionContext !== void 0 ? {
                        executionContext: executionContext
                    } : {},
                    ...resourceClients !== void 0 ? {
                        resourceClients: resourceClients
                    } : {},
                    ...args.telemetry !== void 0 ? {
                        telemetry: args.telemetry
                    } : {},
                    apply: (instance, evaluation) => (assertEditAllowed(evaluation, target), applyEdit({
                        client: client,
                        instance: instance,
                        target: target,
                        ...mode !== void 0 ? {
                            mode: mode
                        } : {},
                        ...value !== void 0 ? {
                            value: value
                        } : {},
                        actor: actor,
                        ...access.grants !== void 0 ? {
                            grants: access.grants
                        } : {},
                        ...access.localPrincipalId !== void 0 ? {
                            localPrincipalId: access.localPrincipalId
                        } : {},
                        clock: clock,
                        clientForGdr: clientForGdr,
                        refSurface: refSurface,
                        memberRolesLoader: memberRolesLoader,
                        ...executionContext !== void 0 ? {
                            executionContext: executionContext
                        } : {},
                        ...args.telemetry !== void 0 ? {
                            telemetry: args.telemetry
                        } : {},
                        ...record !== void 0 ? {
                            requestRecord: record
                        } : {}
                    }))
                });
                return resolveTelemetry(args.telemetry).log(WorkflowFieldEdited, fieldEditedData({
                    instance: before,
                    target: target,
                    mode: mode
                })), result;
            }
        });
    },
    completeEffect: async rawArgs => {
        const args = taggedScope(rawArgs, REQUEST_TAG.completeEffect), {client: client, tag: tag, instanceId: instanceId, effectKey: effectKey, status: status, outputs: outputs, ops: ops, detail: detail, error: error, durationMs: durationMs, executionContext: executionContext} = args, operationContext = await resolveOperationContext(args), clock = args.clock ?? wallClock, now = clock(), record = requestRecordFor({
            idempotencyKey: args.idempotencyKey,
            op: "completeEffect",
            idempotencyTtlMs: args.idempotencyTtlMs
        });
        return runDeduped({
            client: client,
            tag: tag,
            instanceId: instanceId,
            record: record,
            now: now,
            ...operationContext,
            clock: clock,
            executionContext: executionContext,
            telemetry: args.telemetry,
            run: async () => {
                const completion = await completeEffect({
                    client: client,
                    instanceId: instanceId,
                    effectKey: effectKey,
                    status: status,
                    ...outputs !== void 0 ? {
                        outputs: outputs
                    } : {},
                    ...ops !== void 0 ? {
                        ops: ops
                    } : {},
                    ...detail !== void 0 ? {
                        detail: detail
                    } : {},
                    ...error !== void 0 ? {
                        error: error
                    } : {},
                    ...durationMs !== void 0 ? {
                        durationMs: durationMs
                    } : {},
                    ...record !== void 0 ? {
                        requestRecord: record
                    } : {},
                    options: engineOptionsForActor({
                        ...operationContext,
                        clock: clock,
                        executionContext: executionContext,
                        telemetry: args.telemetry
                    })
                }), settled = await cascadeAndReload({
                    client: client,
                    tag: tag,
                    instanceId: instanceId,
                    ...operationContext,
                    clock: clock,
                    executionContext: executionContext,
                    telemetry: args.telemetry
                });
                return resolveTelemetry(args.telemetry).log(WorkflowEffectCompleted, {
                    ...definitionHashFragment(settled.instance.pinnedContentHash),
                    instanceId: instanceId,
                    effect: completion.effect,
                    status: status,
                    origin: completion.origin,
                    cascaded: settled.cascaded
                }), settledResult({
                    ...settled,
                    changed: !0
                });
            }
        });
    },
    commitEffectOps: async rawArgs => {
        const args = taggedScope(rawArgs, REQUEST_TAG.commitEffectOps), {client: client, tag: tag, instanceId: instanceId, effectKey: effectKey, claimToken: claimToken, ops: ops, executionContext: executionContext} = args, operationContext = await resolveOperationContext(args), clock = args.clock ?? wallClock, now = clock(), record = requestRecordFor({
            idempotencyKey: args.idempotencyKey,
            op: "commitEffectOps",
            idempotencyTtlMs: args.idempotencyTtlMs
        });
        if (record === void 0) throw new ContractViolationError("commitEffectOps: idempotencyKey is required — the engine cannot assume a supplied field op is idempotent, so every mid-dispatch report must be retry-safe");
        return runDeduped({
            client: client,
            tag: tag,
            instanceId: instanceId,
            record: record,
            now: now,
            ...operationContext,
            clock: clock,
            executionContext: executionContext,
            telemetry: args.telemetry,
            run: async () => {
                const report = await commitEffectOps({
                    client: client,
                    instanceId: instanceId,
                    effectKey: effectKey,
                    claimToken: claimToken,
                    ops: ops,
                    requestRecord: record,
                    leaseMs: args.leaseMs ?? DEFAULT_EFFECT_LEASE_MS,
                    options: engineOptionsForActor({
                        ...operationContext,
                        clock: clock,
                        executionContext: executionContext,
                        telemetry: args.telemetry
                    })
                }), settled = await cascadeAndReload({
                    client: client,
                    tag: tag,
                    instanceId: instanceId,
                    ...operationContext,
                    clock: clock,
                    executionContext: executionContext,
                    telemetry: args.telemetry
                });
                return resolveTelemetry(args.telemetry).log(WorkflowEffectStateReported, {
                    ...definitionHashFragment(settled.instance.pinnedContentHash),
                    instanceId: instanceId,
                    effect: report.effect,
                    cascaded: settled.cascaded
                }), settledResult({
                    ...settled,
                    changed: !0
                });
            }
        });
    },
    tick: async rawArgs => {
        const args = taggedScope(rawArgs, REQUEST_TAG.tick), {client: client, tag: tag, instanceId: instanceId, executionContext: executionContext} = args, operationContext = await resolveOperationContext(args), {access: access} = operationContext, clock = args.clock ?? wallClock, current = await reload({
            client: client,
            instanceId: instanceId,
            tag: tag
        }), guards = await verdictGuardsForInstance(client, current._id);
        await assertInstanceWriteAllowed({
            instance: current,
            guards: guards,
            identity: lakePrincipalId(access),
            dereference: clientGuardDereference(client)
        });
        const settled = await cascadeAndReload({
            client: client,
            tag: tag,
            instanceId: instanceId,
            ...operationContext,
            clock: clock,
            executionContext: executionContext,
            telemetry: args.telemetry
        });
        return resolveTelemetry(args.telemetry).log(WorkflowInstanceTicked, {
            ...definitionHashFragment(settled.instance.pinnedContentHash),
            instanceId: instanceId,
            cascaded: settled.cascaded
        }), settledResult({
            ...settled,
            changed: settled.instance._rev !== current._rev
        });
    },
    setStage: async rawArgs => {
        const args = taggedScope(rawArgs, REQUEST_TAG.setStage), {client: client, tag: tag, instanceId: instanceId, targetStage: targetStage, reason: reason, executionContext: executionContext} = args;
        return runCommitVerb({
            args: args,
            op: "setStage",
            run: async ({actor: actor, clientForGdr: clientForGdr, refSurface: refSurface, memberRolesLoader: memberRolesLoader, clock: clock, now: now, record: record, before: before}) => {
                const result = await setStage({
                    client: client,
                    instanceId: instanceId,
                    targetStage: targetStage,
                    ...reason !== void 0 ? {
                        reason: reason
                    } : {},
                    ...record !== void 0 ? {
                        requestRecord: record
                    } : {},
                    options: engineOptionsForActor({
                        actor: actor,
                        clock: clock,
                        clientForGdr: clientForGdr,
                        refSurface: refSurface,
                        memberRolesLoader: memberRolesLoader,
                        executionContext: executionContext,
                        telemetry: args.telemetry
                    })
                });
                return resolveTelemetry(args.telemetry).log(WorkflowStageSet, {
                    ...definitionHashFragment(before.pinnedContentHash),
                    instanceId: instanceId,
                    changed: result.fired
                }), adminOverrideResult({
                    client: client,
                    tag: tag,
                    instanceId: instanceId,
                    fired: result.fired,
                    actor: actor,
                    clientForGdr: clientForGdr,
                    refSurface: refSurface,
                    memberRolesLoader: memberRolesLoader,
                    clock: clock,
                    now: now,
                    ...executionContext !== void 0 ? {
                        executionContext: executionContext
                    } : {},
                    ...args.telemetry !== void 0 ? {
                        telemetry: args.telemetry
                    } : {}
                });
            }
        });
    },
    abortInstance: async rawArgs => {
        const args = taggedScope(rawArgs, REQUEST_TAG.abort), {client: client, tag: tag, instanceId: instanceId, reason: reason, executionContext: executionContext} = args;
        return runCommitVerb({
            args: args,
            op: "abortInstance",
            run: async ({actor: actor, clientForGdr: clientForGdr, refSurface: refSurface, memberRolesLoader: memberRolesLoader, clock: clock, record: record, before: before}) => {
                const changed = await abortAndPropagate({
                    client: client,
                    instanceId: instanceId,
                    reason: reason,
                    actor: actor,
                    clientForGdr: clientForGdr,
                    refSurface: refSurface,
                    memberRolesLoader: memberRolesLoader,
                    clock: clock,
                    ...executionContext !== void 0 ? {
                        executionContext: executionContext
                    } : {},
                    ...args.telemetry !== void 0 ? {
                        telemetry: args.telemetry
                    } : {},
                    ...record !== void 0 ? {
                        requestRecord: record
                    } : {}
                });
                return resolveTelemetry(args.telemetry).log(WorkflowInstanceAborted, {
                    ...definitionHashFragment(before.pinnedContentHash),
                    instanceId: instanceId,
                    changed: changed
                }), settledResult({
                    instance: await reload({
                        client: client,
                        instanceId: instanceId,
                        tag: tag
                    }),
                    cascaded: 0,
                    changed: changed
                });
            }
        });
    },
    resetActivity: async rawArgs => {
        const args = taggedScope(rawArgs, REQUEST_TAG.resetActivity), {client: client, tag: tag, instanceId: instanceId, activity: activity, executionContext: executionContext} = args, to = args.to ?? "active";
        if (!isResetActivityTarget(to)) throw new ContractViolationError(`resetActivity: "to" must be "active" or "skipped"; got ${JSON.stringify(args.to)}`);
        return runCommitVerb({
            args: args,
            op: "resetActivity",
            run: async ({actor: actor, clientForGdr: clientForGdr, refSurface: refSurface, memberRolesLoader: memberRolesLoader, clock: clock, now: now, record: record, before: before}) => {
                const result = await resetActivity({
                    client: client,
                    instanceId: instanceId,
                    activity: activity,
                    to: to,
                    ...record !== void 0 ? {
                        requestRecord: record
                    } : {},
                    options: engineOptionsForActor({
                        actor: actor,
                        clock: clock,
                        clientForGdr: clientForGdr,
                        refSurface: refSurface,
                        memberRolesLoader: memberRolesLoader,
                        executionContext: executionContext,
                        telemetry: args.telemetry
                    })
                });
                return resolveTelemetry(args.telemetry).log(WorkflowActivityReset, {
                    ...definitionHashFragment(before.pinnedContentHash),
                    instanceId: instanceId,
                    changed: result.fired
                }), adminOverrideResult({
                    client: client,
                    tag: tag,
                    instanceId: instanceId,
                    fired: result.fired,
                    actor: actor,
                    clientForGdr: clientForGdr,
                    refSurface: refSurface,
                    memberRolesLoader: memberRolesLoader,
                    clock: clock,
                    now: now,
                    ...executionContext !== void 0 ? {
                        executionContext: executionContext
                    } : {},
                    ...args.telemetry !== void 0 ? {
                        telemetry: args.telemetry
                    } : {}
                });
            }
        });
    },
    getInstance: async rawArgs => {
        const args = taggedScope(rawArgs, REQUEST_TAG.getInstance), {client: client, tag: tag, instanceId: instanceId} = args;
        return validateTag(tag), reload({
            client: client,
            instanceId: instanceId,
            tag: tag
        });
    },
    guardsForInstance: async rawArgs => {
        const args = taggedScope(rawArgs, REQUEST_TAG.guardQuery), {client: client, tag: tag, workflowResource: workflowResource, instanceId: instanceId, resourceClients: resourceClients} = args;
        validateTag(tag);
        const instance = await reload({
            client: client,
            instanceId: instanceId,
            tag: tag
        });
        return guardsForInstance({
            client: client,
            clientForGdr: buildClientForGdr({
                client: client,
                workflowResource: workflowResource,
                resourceClients: resourceClients
            }),
            instance: instance
        });
    },
    guardsForDefinition: async rawArgs => {
        const args = taggedScope(rawArgs, REQUEST_TAG.guardQuery), {client: client, tag: tag, workflowResource: workflowResource, definition: definition, resourceClients: resourceClients} = args;
        validateTag(tag);
        const definitions = await loadDefinitionVersionsOrThrow({
            client: client,
            definition: definition,
            tag: tag
        });
        return guardsForDefinition({
            client: client,
            clientForGdr: buildClientForGdr({
                client: client,
                workflowResource: workflowResource,
                resourceClients: resourceClients
            }),
            workflowResource: workflowResource,
            definition: definition,
            definitions: definitions
        });
    },
    query: async rawArgs => {
        const args = taggedScope(rawArgs, REQUEST_TAG.query), {client: client, tag: tag, groq: groq, params: params} = args;
        if (validateTag(tag), !/\$tag\b/.test(groq)) throw new ContractViolationError(`workflow.query: query must filter on $tag to stay tag-scoped (e.g. "tag == $tag"); got: ${groq}`);
        return client.fetch(groq, {
            ...params,
            tag: tag
        });
    },
    queryInScope: async rawArgs => {
        const args = taggedScope(rawArgs, REQUEST_TAG.query), {client: client, tag: tag, workflowResource: workflowResource, instanceId: instanceId, groq: groq, params: params, resourceClients: resourceClients} = args, clock = args.clock ?? wallClock;
        validateTag(tag);
        const instance = await reload({
            client: client,
            instanceId: instanceId,
            tag: tag
        }), clientForGdr = buildClientForGdr({
            client: client,
            workflowResource: workflowResource,
            resourceClients: resourceClients
        }), snapshot = await hydrateSnapshot({
            client: client,
            clientForGdr: clientForGdr,
            instance: instance
        }), reserved = await renderConditionScope({
            instance: instance,
            definition: parseDefinitionSnapshot(instance),
            snapshot: snapshot,
            now: clock()
        }), tree = parse(groq, {
            params: {
                ...reserved,
                ...params
            }
        });
        return await (await evaluate(tree, {
            dataset: snapshot.docs,
            params: {
                ...reserved,
                ...params
            }
        })).get();
    },
    listPendingEffects: async rawArgs => {
        const args = taggedScope(rawArgs, REQUEST_TAG.effectList);
        return (await reload({
            client: args.client,
            instanceId: args.instanceId,
            tag: args.tag
        })).pendingEffects;
    },
    findPendingEffects: async rawArgs => {
        const args = taggedScope(rawArgs, REQUEST_TAG.effectFind);
        return (await reload({
            client: args.client,
            instanceId: args.instanceId,
            tag: args.tag
        })).pendingEffects.filter(e => !(args.claimed === !0 && e.claim === void 0 || args.claimed === !1 && e.claim !== void 0 || args.names !== void 0 && !args.names.includes(e.name)));
    },
    evaluate: async rawArgs => {
        const args = taggedScope(rawArgs, REQUEST_TAG.evaluate);
        return evaluateInstance(args);
    },
    diagnose: async rawArgs => {
        const args = taggedScope(rawArgs, REQUEST_TAG.diagnose), evaluation = await evaluateInstance(args), diagnosis = diagnoseInstance(diagnoseInputFromEvaluation(evaluation));
        return {
            evaluation: evaluation,
            diagnosis: diagnosis,
            remediations: remediationsFor(diagnosis)
        };
    },
    availableActions: async rawArgs => {
        const args = taggedScope(rawArgs, REQUEST_TAG.availableActions), evaluation = await evaluateInstance(args);
        return {
            evaluation: evaluation,
            actions: availableActions(evaluation.currentStage.activities)
        };
    },
    children: async rawArgs => {
        const args = taggedScope(rawArgs, REQUEST_TAG.children), {client: client, tag: tag, instanceId: instanceId, activity: activity} = args;
        validateTag(tag);
        const parent = await reload({
            client: client,
            instanceId: instanceId,
            tag: tag
        }), isSpawned = h => h._type === "spawned", ids = parent.history.filter(isSpawned).filter(h => activity === void 0 || h.activity === activity).map(h => extractDocumentId(h.instanceRef.id));
        return ids.length === 0 ? [] : (await client.fetch(`*[_id in $ids && ${tagScopeFilter()}] | order(startedAt asc)`, {
            ids: ids,
            tag: tag
        })).map(readInstanceDoc);
    },
    instancesForDocument: async rawArgs => {
        const args = taggedScope(rawArgs, REQUEST_TAG.instancesForDocument), {client: client, tag: tag, document: document} = args, {query: query, params: params} = instancesQuery({
            tag: tag,
            filter: {
                document: document
            }
        });
        return (await client.fetch(query, params)).map(readInstanceDoc).filter(instance => instanceWatchesDocument(instance, document));
    },
    definitionsForDocument: async rawArgs => {
        const args = taggedScope(rawArgs, REQUEST_TAG.definitionsForDocument), {client: client, tag: tag, document: document} = args;
        validateTag(tag);
        const clock = args.clock ?? wallClock, deployed = (await client.fetch(latestDefinitionsGroq(), {
            tag: tag
        })).map(assertReadableModel);
        let slice;
        return applicableDefinitions({
            definitions: deployed,
            document: document,
            scope: {
                tag: tag,
                now: clock(),
                fetchDataset: () => slice ??= fetchStartSlice({
                    client: client,
                    tag: tag
                })
            }
        });
    },
    evaluateStart: async rawArgs => {
        const args = taggedScope(rawArgs, REQUEST_TAG.evaluateStart), {client: client, tag: tag, definition: definitionName, version: version, initialFields: initialFields} = args;
        validateTag(tag);
        const clock = args.clock ?? wallClock, definition = await loadDefinition({
            client: client,
            definition: definitionName,
            version: version,
            tag: tag
        }), invalidInitialFields = initialFieldIssues({
            workflowFields: definition.fields ?? [],
            stages: definition.stages,
            initialFields: initialFields ?? []
        }), missingRequired = missingRequiredInputs({
            entryDefs: definition.fields ?? [],
            initialFields: initialFields ?? []
        }), verdict = await startRequirementVerdict({
            client: client,
            tag: tag,
            definition: definition,
            initialFields: initialFields ?? [],
            now: clock(),
            preflight: !0
        });
        return {
            ...verdict,
            allowed: invalidInitialFields.length === 0 && verdict.allowed,
            missingRequired: missingRequired,
            invalidInitialFields: invalidInitialFields
        };
    },
    permissions: {
        matchesFilter: matchesFilter,
        grantsPermissionOn: grantsPermissionOn,
        fetchGrants: fetchGrants
    }
};

function startPredicateContext(args) {
    const {client: client, tag: tag, definition: definition, initialFields: initialFields, now: now} = args;
    let slice;
    return {
        fields: startFieldsParam({
            entryDefs: definition.fields ?? [],
            initialFields: initialFields
        }),
        scope: {
            tag: tag,
            now: now,
            fetchDataset: () => slice ??= fetchStartSlice({
                client: client,
                tag: tag
            })
        }
    };
}

async function startRequirementVerdict(args) {
    const {client: client, tag: tag, definition: definition, initialFields: initialFields, now: now, preflight: preflight = !1} = args, declared = definition.start?.requirements ?? [];
    if (declared.length === 0) return {
        allowed: !0,
        outcome: "satisfied",
        requirements: [],
        unboundReads: []
    };
    const {fields: fields, scope: scope} = startPredicateContext({
        client: client,
        tag: tag,
        definition: definition,
        initialFields: initialFields,
        now: now
    }), subjectEntry = (definition.fields ?? []).find(isSubjectEntry)?.name, requirements = [], unbound = /* @__PURE__ */ new Set;
    for (const requirement of declared) {
        const descriptor = requirementDescriptor(requirement);
        if (requirement.type === "singleSubject") {
            if (preflight && subjectEntry !== void 0 && !Object.hasOwn(fields, subjectEntry)) unbound.add(subjectEntry), 
            requirements.push({
                ...descriptor,
                outcome: "unevaluable"
            }); else {
                const refused = await singleSubjectRequirementRefused({
                    definition: definition,
                    fields: fields,
                    scope: scope
                });
                requirements.push({
                    ...descriptor,
                    outcome: refused ? "unsatisfied" : "satisfied"
                });
            }
            continue;
        }
        const missing = unboundRequirementReads(requirement.query, fields);
        missing.forEach(name => unbound.add(name));
        const insight = await explainStartRequirement({
            query: requirement.query,
            definition: definition,
            fields: fields,
            scope: scope
        });
        requirements.push({
            ...descriptor,
            outcome: preflight && missing.length > 0 ? "unevaluable" : insight.outcome,
            insight: insight
        });
    }
    let outcome = "satisfied";
    return requirements.some(requirement => requirement.outcome === "unsatisfied") ? outcome = "unsatisfied" : requirements.some(requirement => requirement.outcome === "unevaluable") && (outcome = "unevaluable"), 
    {
        allowed: outcome === "satisfied",
        outcome: outcome,
        requirements: requirements,
        unboundReads: [ ...unbound ]
    };
}

async function startFreshInstance(args) {
    const {args: startArgs, operationContext: operationContext, clock: clock, seedFields: seedFields} = args, {client: client, tag: tag, workflowResource: workflowResource, definition: definitionName, version: version, ancestors: ancestors, context: context, instanceId: instanceId, perspective: perspective, executionContext: executionContext} = startArgs, {actor: actor, clientForGdr: clientForGdr, refSurface: refSurface, memberRolesLoader: memberRolesLoader} = operationContext, definition = await loadDefinition({
        client: client,
        definition: definitionName,
        version: version,
        tag: tag
    }), id = instanceId ?? instanceDocId(tag);
    assertInitialStageExists(definition);
    const now = clock();
    await assertStartInputs({
        client: client,
        tag: tag,
        definition: definition,
        initialFields: seedFields,
        now: now
    });
    const {resolvedFields: resolvedFields, fieldDiscards: fieldDiscards} = await resolveStartFields({
        client: client,
        tag: tag,
        workflowResource: workflowResource,
        definition: definition,
        initialFields: seedFields,
        id: id,
        now: now,
        refSurface: refSurface,
        memberRolesLoader: memberRolesLoader,
        ...perspective !== void 0 ? {
            perspective: perspective
        } : {}
    }), base = buildInstanceBase({
        id: id,
        now: now,
        tag: tag,
        workflowResource: workflowResource,
        definitionName: definition.name,
        pinnedVersion: definition.version,
        pinnedContentHash: definition.contentHash,
        definition: definition,
        fields: resolvedFields,
        context: context ? toContextEntries(context) : [],
        ancestors: ancestors ?? [],
        perspective: perspective ?? derivePerspectiveFromFields(resolvedFields),
        initialStage: definition.initialStage,
        actor: actor,
        executionContext: resolveExecutionContext(executionContext),
        ...fieldDiscards.length > 0 ? {
            extraHistory: fieldDiscards
        } : {}
    });
    await client.create(instanceDocumentForWrite(base), SYNC_COMMIT);
    const settled = await settleStart({
        client: client,
        tag: tag,
        instanceId: id,
        actor: actor,
        clientForGdr: clientForGdr,
        refSurface: refSurface,
        memberRolesLoader: memberRolesLoader,
        clock: clock,
        ...executionContext !== void 0 ? {
            executionContext: executionContext
        } : {},
        ...startArgs.telemetry !== void 0 ? {
            telemetry: startArgs.telemetry
        } : {},
        emitStarted: () => resolveTelemetry(startArgs.telemetry).log(WorkflowInstanceStarted, instanceStartedData({
            definition: definition,
            instanceId: id,
            initialFieldCount: seedFields.length,
            viaSpawn: !1
        }))
    });
    return settledResult({
        ...settled,
        instance: await reload({
            client: client,
            instanceId: id,
            tag: tag
        }),
        changed: !0
    });
}

function assertInitialStageExists(definition) {
    if (!definition.stages.some(stage => stage.name === definition.initialStage)) throw new Error(`Initial stage "${definition.initialStage}" missing in ${definition.name} v${definition.version}`);
}

async function assertStartInputs(args) {
    const {client: client, tag: tag, definition: definition, initialFields: initialFields, now: now} = args;
    assertInitialFieldsConsumable({
        workflowFields: definition.fields ?? [],
        stages: definition.stages,
        initialFields: initialFields,
        definitionName: definition.name
    }), assertRequiredInputProvided({
        entryDefs: definition.fields ?? [],
        initialFields: initialFields,
        definitionName: definition.name
    });
    const unmetRequirements = (await startRequirementVerdict({
        client: client,
        tag: tag,
        definition: definition,
        initialFields: initialFields,
        now: now
    })).requirements.filter(requirement => requirement.outcome !== "satisfied").map(requirementDescriptor);
    if (unmetRequirements.length > 0) throw new StartNotAllowedError({
        definition: definition.name,
        unmetRequirements: unmetRequirements
    });
}

async function resolveStartFields(args) {
    const {client: client, tag: tag, workflowResource: workflowResource, definition: definition, initialFields: initialFields, id: id, now: now, refSurface: refSurface, memberRolesLoader: memberRolesLoader, perspective: perspective} = args, fieldDiscards = [], memberRoles = await memberRolesLoader({
        workflowResource: workflowResource,
        definition: definition
    });
    return {
        resolvedFields: await resolveDeclaredFields({
            entryDefs: definition.fields ?? [],
            initialFields: initialFields,
            ctx: {
                client: client,
                now: now,
                selfId: id,
                tag: tag,
                workflowResource: workflowResource,
                definitionName: definition.name,
                refSurface: refSurface,
                memberRoles: memberRoles,
                roleAliases: definition.roleAliases,
                ...perspective !== void 0 ? {
                    perspective: perspective
                } : {},
                recordDiscard: recordFieldDiscards({
                    target: fieldDiscards,
                    scope: "workflow",
                    at: now
                })
            },
            randomKey: randomKey
        }),
        fieldDiscards: fieldDiscards
    };
}

const EFFECT_COMMIT_QUEUE_DEPTH = 32, EFFECT_COMMIT_DISPATCH_CAP = 200;

class EffectCommitQueueOverflowError extends WorkflowError {
    effectKey;
    bound;
    limit;
    constructor(args) {
        super("effect-commit-queue-overflow", args.bound === "queue-depth" ? `Effect dispatch "${args.effectKey}" has ${args.limit} mid-dispatch commits queued — await your ctx.commitOps/ctx.setProgress calls (or report less often); the queue serializes real engine commits and cannot grow without bound` : `Effect dispatch "${args.effectKey}" already made ${args.limit} mid-dispatch commits — the per-dispatch cap bounds instance history growth; report more coarsely`), 
        this.name = "EffectCommitQueueOverflowError", this.effectKey = args.effectKey, this.bound = args.bound, 
        this.limit = args.limit;
    }
}

function isClaimLoss(err) {
    return err instanceof StaleEffectClaimError || err instanceof EffectNotFoundError;
}

function pendingEntry(args) {
    let resolve, reject;
    const promise = new Promise((res, rej) => {
        resolve = res, reject = rej;
    });
    return promise.catch(() => {}), {
        ...args,
        resolve: resolve,
        reject: reject,
        promise: promise
    };
}

function createEffectCommitQueue(args) {
    const {effectKey: effectKey, claimToken: claimToken, commit: commit} = args, pending = [];
    let admitted = 0, dead, closed = !1, tail = Promise.resolve();
    function assertOpen() {
        if (dead !== void 0) throw dead;
        if (closed) throw new Error(`Effect dispatch "${effectKey}" already settled — a mid-dispatch report after the handler returned cannot commit; await your reports inside the handler`);
    }
    function admit(entry) {
        if (assertOpen(), pending.length >= EFFECT_COMMIT_QUEUE_DEPTH) throw new EffectCommitQueueOverflowError({
            effectKey: effectKey,
            bound: "queue-depth",
            limit: EFFECT_COMMIT_QUEUE_DEPTH
        });
        if (admitted >= EFFECT_COMMIT_DISPATCH_CAP) throw new EffectCommitQueueOverflowError({
            effectKey: effectKey,
            bound: "dispatch-cap",
            limit: EFFECT_COMMIT_DISPATCH_CAP
        });
        return admitted += 1, pending.push(entry), tail = tail.then(runNext), entry.promise;
    }
    function die(err) {
        dead = err;
        for (const entry of pending.splice(0)) entry.reject(err);
    }
    async function runNext() {
        const entry = pending.shift();
        if (entry !== void 0) try {
            await commit({
                ops: entry.buildOps(),
                idempotencyKey: entry.idempotencyKey
            }), entry.resolve();
        } catch (err) {
            entry.reject(err), die(err);
        }
    }
    return {
        commitOps: req => admit(pendingEntry({
            buildOps: () => req.ops,
            idempotencyKey: req.idempotencyKey
        })),
        setProgress: (target, value) => {
            assertOpen();
            const ref = typeof target == "string" ? {
                scope: "workflow",
                field: target
            } : target, coalesceKey = `${ref.scope}:${ref.field}`, queued = pending.find(entry2 => entry2.coalesceKey === coalesceKey);
            if (queued !== void 0) return queued.latestValue = value, queued.promise;
            const entry = pendingEntry({
                buildOps: () => [ {
                    type: "field.set",
                    target: ref,
                    value: {
                        type: "literal",
                        value: entry.latestValue
                    }
                } ],
                idempotencyKey: `effect:${effectKey}:${claimToken}:progress:${coalesceKey}:${admitted}`,
                coalesceKey: coalesceKey,
                latestValue: value
            });
            return admit(entry);
        },
        settle: async () => (closed = !0, await tail, dead !== void 0 && !isClaimLoss(dead) ? dead : void 0)
    };
}

class MissingHandlerError extends WorkflowError {
    info;
    constructor(args) {
        super("missing-effect-handler", missingHandlerMessage(args.info)), this.name = "MissingHandlerError", 
        this.info = args.info;
    }
}

function missingHandlerMessage(info) {
    return info.phase === "deploy" ? `verifyDeployedDefinitions: missing handler for "${info.name}" referenced by ${info.definitionId} at ${info.locations.join(", ")}` : `drainEffects: no handler registered for "${info.name}" (effectKey=${info.effectKey}, instanceId=${info.instanceId})`;
}

async function verifyDeployedDefinitionsInternal(args) {
    const {tag: tag, handlers: handlers, missingHandler: missingHandler, logger: logger} = args, client = withRequestTag(args.client, REQUEST_TAG.verifyDefinitions);
    validateTag(tag);
    const log = logger("verifyDeployedDefinitions"), definitions = (await client.fetch(definitionsListGroq("asc"), {
        tag: tag
    })).map(assertReadableModel), seen = [], missingByName = /* @__PURE__ */ new Map;
    for (const def of definitions) seen.push({
        name: def.name,
        version: def.version,
        _id: def._id
    }), walkEffectNames(def, (name, location) => {
        if (handlers[name] !== void 0) return;
        const key = `${name}@@${def._id}`;
        let bucket = missingByName.get(key);
        bucket === void 0 && (bucket = {
            definitionId: def._id,
            locations: /* @__PURE__ */ new Set
        }, missingByName.set(key, bucket)), bucket.locations.add(location);
    });
    const missing = [];
    for (const [key, bucket] of missingByName.entries()) {
        const name = key.split("@@")[0];
        missing.push({
            name: name,
            definitionId: bucket.definitionId,
            locations: [ ...bucket.locations ].toSorted()
        });
    }
    for (const m of missing) {
        const info = {
            phase: "deploy",
            name: m.name,
            locations: m.locations,
            definitionId: m.definitionId
        };
        if (await applyMissingHandler({
            policy: missingHandler,
            info: info,
            log: log
        }) === "fail") throw new MissingHandlerError({
            info: info
        });
    }
    return {
        definitions: seen,
        missing: missing
    };
}

function walkEffectNames(def, visit) {
    for (const {effect: effect, location: location} of effectSites(def)) visit(effect.name, location);
}

function noteDrainedInstanceHash(buckets, instance) {
    instance.pinnedContentHash !== void 0 && (buckets.definitionContentHash = instance.pinnedContentHash);
}

function drainCascadeTelemetry(telemetry) {
    if (telemetry !== void 0) return {
        log: (event, data) => {
            event.name === WorkflowEffectCompleted.name && !isCancelledCompletion(data) || telemetry.log(event, data);
        }
    };
}

function isCancelledCompletion(data) {
    return typeof data == "object" && data !== null && data.status === "cancelled";
}

async function drainEffectsInternal(args) {
    const {tag: tag, workflowResource: workflowResource, instanceId: instanceId, handlers: handlers, missingHandler: missingHandler, logger: logger, handlerClient: rawHandlerClient, handlerResourceClients: rawHandlerResourceClients} = args, cascadeTelemetry = drainCascadeTelemetry(args.telemetry), {client: client, resourceClients: resourceClients} = taggedScope(args, REQUEST_TAG.drain), leaseMs = args.leaseMs ?? DEFAULT_EFFECT_LEASE_MS, clock = args.clock ?? wallClock, handlerClient = effectHandlerClient(rawHandlerClient), handlerResourceClients = effectHandlerResolver(rawHandlerResourceClients), routeGdr = buildClientForGdr({
        client: handlerClient,
        workflowResource: workflowResource,
        resourceClients: handlerResourceClients
    }), clientFor = ref => routeGdr(parseGdr(typeof ref == "string" ? ref : ref.id)), {actor: drainerActor} = await resolveAccess(client), executionContext = args.executionContext;
    validateTag(tag);
    const log = logger("drainEffects"), buckets = {
        drained: [],
        failed: [],
        skipped: [],
        lost: []
    }, pass = {
        client: client,
        handlerClient: handlerClient,
        tag: tag,
        workflowResource: workflowResource,
        ...resourceClients !== void 0 ? {
            resourceClients: resourceClients
        } : {},
        instanceId: instanceId,
        handlers: handlers,
        missingHandler: missingHandler,
        logger: logger,
        log: log,
        leaseMs: leaseMs,
        clock: clock,
        ...executionContext !== void 0 ? {
            executionContext: executionContext
        } : {},
        telemetry: cascadeTelemetry,
        drainerActor: drainerActor,
        clientFor: clientFor,
        buckets: buckets,
        skippedKeys: /* @__PURE__ */ new Set
    };
    for (;await drainOneCandidate(pass); ) ;
    return buckets;
}

async function drainOneCandidate(pass) {
    const {client: client, tag: tag, instanceId: instanceId, buckets: buckets, skippedKeys: skippedKeys} = pass, now = pass.clock(), before = await reload({
        client: client,
        instanceId: instanceId,
        tag: tag
    });
    noteDrainedInstanceHash(buckets, before);
    const candidate = findClaimableCandidate({
        instance: before,
        now: now,
        skippedKeys: skippedKeys
    });
    if (candidate === void 0) return !1;
    const handler = pass.handlers[candidate.name];
    if (handler === void 0) return await assertSkippableOrThrow({
        missingHandler: pass.missingHandler,
        candidate: candidate,
        instanceId: instanceId,
        log: pass.log
    }), buckets.skipped.push(candidate), skippedKeys.add(candidate._key), !0;
    const claimed = await claimPendingEffect({
        client: client,
        instance: before,
        candidate: candidate,
        drainerActor: pass.drainerActor,
        now: now,
        leaseMs: pass.leaseMs,
        ...pass.executionContext !== void 0 ? {
            executionContext: pass.executionContext
        } : {}
    });
    if (claimed === void 0) return !0;
    const outcome = await dispatchAndReport({
        handler: handler,
        candidate: candidate,
        claimToken: claimed.claimToken,
        leaseMs: pass.leaseMs,
        client: client,
        handlerClient: pass.handlerClient,
        tag: tag,
        workflowResource: pass.workflowResource,
        ...pass.resourceClients !== void 0 ? {
            resourceClients: pass.resourceClients
        } : {},
        instanceId: instanceId,
        clientFor: pass.clientFor,
        logger: pass.logger,
        log: pass.log,
        ...pass.executionContext !== void 0 ? {
            executionContext: pass.executionContext
        } : {},
        telemetry: pass.telemetry,
        clock: pass.clock
    });
    return buckets[outcome].push(candidate), !0;
}

function findClaimableCandidate({instance: instance, now: now, skippedKeys: skippedKeys}) {
    return instance.pendingEffects.find(e => (e.claim === void 0 || isClaimExpired(e.claim, now)) && !skippedKeys.has(e._key));
}

async function dispatchAndReport(args) {
    const {handler: handler, candidate: candidate, claimToken: claimToken, leaseMs: leaseMs, client: client, handlerClient: handlerClient, tag: tag, workflowResource: workflowResource, resourceClients: resourceClients, instanceId: instanceId, clientFor: clientFor, logger: logger, log: log, executionContext: executionContext, telemetry: telemetry, clock: clock} = args, commitQueue = createEffectCommitQueue({
        effectKey: candidate._key,
        claimToken: claimToken,
        commit: async req => {
            await workflow.commitEffectOps({
                client: client,
                tag: tag,
                workflowResource: workflowResource,
                ...resourceClients !== void 0 ? {
                    resourceClients: resourceClients
                } : {},
                ...executionContext !== void 0 ? {
                    executionContext: executionContext
                } : {},
                ...telemetry !== void 0 ? {
                    telemetry: telemetry
                } : {},
                instanceId: instanceId,
                effectKey: candidate._key,
                claimToken: claimToken,
                ops: req.ops,
                idempotencyKey: req.idempotencyKey,
                leaseMs: leaseMs,
                clock: clock
            });
        }
    }), {outputs: outputs, ops: ops, dispatchError: dispatchError} = await dispatchEffect({
        handler: handler,
        candidate: candidate,
        queue: commitQueue,
        ctx: {
            client: handlerClient,
            clientFor: clientFor,
            instanceId: instanceId,
            logger: logger
        }
    });
    try {
        await reportEffectOutcome({
            client: client,
            tag: tag,
            workflowResource: workflowResource,
            ...resourceClients !== void 0 ? {
                resourceClients: resourceClients
            } : {},
            ...executionContext !== void 0 ? {
                executionContext: executionContext
            } : {},
            ...telemetry !== void 0 ? {
                telemetry: telemetry
            } : {},
            instanceId: instanceId,
            effectKey: candidate._key,
            outputs: outputs,
            ops: ops,
            dispatchError: dispatchError,
            clock: clock
        });
    } catch (error) {
        if (!(error instanceof EffectNotFoundError)) throw error;
        return log.warn(error.settled?.status === "cancelled" ? `Effect "${candidate.name}" was cancelled by an abort during dispatch — reported outcome discarded` : `Effect "${candidate.name}" was completed by another party during dispatch — completion lost`, {
            effectKey: candidate._key,
            instanceId: instanceId
        }), "lost";
    }
    return dispatchError === void 0 ? "drained" : "failed";
}

async function reportEffectOutcome(args) {
    const {outputs: outputs, ops: ops, dispatchError: dispatchError, resourceClients: resourceClients, ...rest} = args;
    await workflow.completeEffect({
        ...rest,
        ...resourceClients !== void 0 ? {
            resourceClients: resourceClients
        } : {},
        status: dispatchError === void 0 ? "done" : "failed",
        ...outputs !== void 0 ? {
            outputs: outputs
        } : {},
        ...ops !== void 0 ? {
            ops: ops
        } : {},
        ...dispatchError !== void 0 ? {
            error: dispatchError
        } : {}
    });
}

async function assertSkippableOrThrow({missingHandler: missingHandler, candidate: candidate, instanceId: instanceId, log: log}) {
    if (await applyMissingHandler({
        policy: missingHandler,
        info: {
            phase: "drain",
            name: candidate.name,
            effectKey: candidate._key,
            instanceId: instanceId
        },
        log: log
    }) === "fail") throw new MissingHandlerError({
        info: {
            phase: "drain",
            name: candidate.name,
            effectKey: candidate._key,
            instanceId: instanceId
        }
    });
}

async function claimPendingEffect({client: client, instance: instance, candidate: candidate, drainerActor: drainerActor, now: now, leaseMs: leaseMs, executionContext: executionContext}) {
    const priorClaim = candidate.claim, claimToken = randomKey(), claim = {
        _type: "pendingEffect.claim",
        claimedAt: now,
        claimedBy: drainerActor,
        leaseExpiresAt: addMs(now, leaseMs),
        claimToken: claimToken
    }, takeoverAudit = priorClaim !== void 0 ? stampHistoryEntries([ claimReleasedEntry({
        pending: candidate,
        claim: priorClaim,
        at: now,
        via: "drain-takeover",
        actor: drainerActor
    }) ], resolveExecutionContext(executionContext))[0] : void 0;
    try {
        return await client.patch(instance._id).ifRevisionId(instance._rev).set({
            [`pendingEffects[_key=="${candidate._key}"].claim`]: claim,
            ...takeoverAudit !== void 0 ? {
                history: [ ...instance.history, takeoverAudit ]
            } : {}
        }).commit(SYNC_COMMIT), {
            claimToken: claimToken
        };
    } catch (error) {
        if (!isRevisionConflict(error)) throw error;
        return;
    }
}

async function invokeHandler({handler: handler, candidate: candidate, queue: queue, ctx: ctx}) {
    try {
        const result = await handler(candidate.params, {
            client: ctx.client,
            clientFor: ctx.clientFor,
            instanceId: ctx.instanceId,
            effectKey: candidate._key,
            log: (message, extra) => ctx.logger(`effect.${candidate.name}`).info(message, extra),
            commitOps: queue.commitOps,
            setProgress: queue.setProgress
        });
        return result === void 0 ? {} : {
            result: result
        };
    } catch (err) {
        return {
            thrown: {
                err: err
            }
        };
    }
}

function shapeDispatchError(err) {
    const message = errorMessage(err), stack = err instanceof Error && err.stack !== void 0 ? err.stack : void 0;
    return stack !== void 0 ? {
        message: message,
        stack: stack
    } : {
        message: message
    };
}

async function dispatchEffect(args) {
    const {result: result, thrown: thrown} = await invokeHandler(args), settleFailure = await args.queue.settle(), failed = thrown ?? (settleFailure !== void 0 ? {
        err: settleFailure
    } : void 0);
    return failed !== void 0 ? {
        dispatchError: shapeDispatchError(failed.err)
    } : {
        ...result?.outputs !== void 0 ? {
            outputs: result.outputs
        } : {},
        ...result?.ops !== void 0 ? {
            ops: result.ops
        } : {}
    };
}

async function applyMissingHandler({policy: policy, info: info, log: log}) {
    if (policy === "fail") return "fail";
    if (policy === "skip") return log.warn(`Missing effect handler "${info.name}" — skipping`, {
        info: info
    }), "skip";
    try {
        return await policy(info), "skip";
    } catch {
        return "fail";
    }
}

function previewSiteOf(args) {
    const stage = findStage(args.definition, args.instance.currentStage), site = resolveEditTarget({
        definition: args.definition,
        stage: stage,
        target: args.target
    });
    if (site !== void 0) return fieldWindowOpen(args.instance, site).open ? site : void 0;
}

function previewOp(site, preview) {
    return buildEditOp({
        site: site,
        mode: preview.mode ?? DEFAULT_EDIT_MODE,
        value: preview.value
    });
}

async function applyEditPreviews(args) {
    const {instance: instance, definition: definition, previews: previews, now: now, refSurface: refSurface} = args;
    if (previews.length === 0) return instance;
    const stage = findStage(definition, instance.currentStage), mutation = startMutation(instance);
    for (const preview of previews) {
        const site = previewSiteOf({
            instance: instance,
            definition: definition,
            target: preview.target
        });
        if (site !== void 0) try {
            await applyOp(previewOp(site, preview), {
                mutation: mutation,
                stage: stage.name,
                activityName: site.activity,
                params: {},
                actor: void 0,
                self: selfGdr(instance),
                now: now,
                snapshot: void 0,
                refSurface: refSurface,
                opsFromDefinition: !1
            });
        } catch (err) {
            if (!(err instanceof FieldValueShapeError) && !(err instanceof RefResourceUndeclaredError)) throw err;
        }
    }
    return materializeInstance(instance, mutation);
}

function createInstanceSession(args) {
    const {client: client, tag: tag, clock: clock, executionContext: executionContext} = args, telemetry = resolveTelemetry(args.telemetry);
    let instance = asHeldInstance(args.instance);
    const opScope = opTag => {
        const scoped = taggedScope(args, opTag);
        return {
            client: scoped.client,
            clientForGdr: buildClientForGdr({
                client: scoped.client,
                workflowResource: instance.workflowResource,
                resourceClients: scoped.resourceClients
            }),
            refSurface: buildResourceSurface({
                workflowResource: instance.workflowResource,
                resourceClients: scoped.resourceClients
            })
        };
    }, tickScope = opScope(REQUEST_TAG.tick), fireScope = opScope(REQUEST_TAG.fireAction), editScope = opScope(REQUEST_TAG.editField), evalScope = opScope(REQUEST_TAG.evaluate);
    let overlay = /* @__PURE__ */ new Map;
    const previews = /* @__PURE__ */ new Map;
    let heldGuards = [], committing = !1, buffered;
    const bufferedDocuments = /* @__PURE__ */ new Map;
    let deferredUpdateError;
    const flushDeferredError = () => {
        if (deferredUpdateError === void 0) return;
        const failure = deferredUpdateError;
        throw deferredUpdateError = void 0, failure;
    }, selfUri = () => gdrFromResource(instance.workflowResource, instance._id), applyDocument = (ld, target) => {
        const owned = {
            doc: structuredClone(ld.doc),
            resource: ld.resource
        }, uri = gdrFromResource(owned.resource, owned.doc._id);
        if (uri === selfUri()) {
            const rawStamp = owned.doc._updatedAt;
            !isParseableInstant(rawStamp) || owned.doc._type !== WORKFLOW_INSTANCE_TYPE ? asHeldInstance(owned.doc) : Date.parse(rawStamp) > Date.parse(instance._updatedAt) && (instance = asHeldInstance(owned.doc));
            return;
        }
        target.set(uri, owned);
    }, applyUpdate = docs => {
        const next = /* @__PURE__ */ new Map;
        for (const ld of docs) applyDocument(ld, next);
        overlay = next;
    }, applyDocumentUpdate = doc => {
        applyDocument(doc, overlay);
    }, access = () => resolveAccess(client, {
        ...args.grantsFromPath !== void 0 ? {
            grantsFromPath: args.grantsFromPath
        } : {}
    }), snapshotFrom = (held, self) => buildSnapshot({
        docs: [ {
            doc: self,
            resource: instance.workflowResource
        }, ...held.values() ]
    });
    let parsedDefinition;
    const definitionOf = () => (parsedDefinition?.source !== instance && (parsedDefinition = {
        source: instance,
        definition: parseDefinitionSnapshot(instance)
    }), parsedDefinition.definition), siteKey = site => JSON.stringify([ site.scope, site.activity ?? null, site.name ]), stagePreview = ({target: target, mode: mode, value: value}) => {
        const site = previewSiteOf({
            instance: instance,
            definition: definitionOf(),
            target: target
        });
        if (site === void 0) return;
        const preview = {
            target: target,
            ...mode !== void 0 ? {
                mode: mode
            } : {},
            ...value !== void 0 ? {
                value: value
            } : {}
        };
        previewOp(site, preview);
        const visit = site.scope === "workflow" ? void 0 : findOpenStageEntry(instance)?._key, row = {
            preview: preview,
            ...visit !== void 0 ? {
                visit: visit
            } : {}
        }, key = siteKey(site), rows = previews.get(key) ?? [];
        return previews.set(key, preview.mode === "append" ? [ ...rows, row ] : [ row ]), 
        key;
    }, optimisticSelf = () => {
        const visit = findOpenStageEntry(instance)?._key;
        for (const [key, rows] of previews) {
            const live = rows.filter(row => row.visit === void 0 || row.visit === visit), target = live[0]?.preview.target;
            target === void 0 || previewSiteOf({
                instance: instance,
                definition: definitionOf(),
                target: target
            }) === void 0 ? previews.delete(key) : live.length < rows.length && previews.set(key, live);
        }
        return applyEditPreviews({
            instance: instance,
            definition: definitionOf(),
            previews: [ ...previews.values() ].flat().map(row => row.preview),
            now: (clock ?? wallClock)(),
            refSurface: evalScope.refSurface
        });
    }, evaluateWith = async ({held: held, guards: guards, self: self}) => {
        const [{actor: actor, localPrincipalId: localPrincipalId, grants: grants}, attributes] = await Promise.all([ access(), resolveUserAttributes(client) ]), resourceGrants = await subjectResourceGrants({
            clientForGdr: evalScope.clientForGdr,
            instance: instance
        }), normalizedSelf = await normalizeInstanceIdentities({
            client: evalScope.client,
            instance: self
        });
        return evaluateFromSnapshot({
            instance: normalizedSelf,
            definition: definitionOf(),
            actor: actor,
            snapshot: snapshotFrom(held, normalizedSelf),
            guards: guards,
            resourceGrants: resourceGrants,
            guardDereference: clientGuardDereference(evalScope.client),
            ...localPrincipalId !== void 0 ? {
                localPrincipalId: localPrincipalId
            } : {},
            ...clock !== void 0 ? {
                now: clock()
            } : {},
            ...grants !== void 0 ? {
                grants: grants
            } : {},
            ...attributes !== void 0 ? {
                attributes: attributes
            } : {}
        });
    }, settleAfterApply = async ({actor: actor, held: held, ranOps: ranOps, scope: scope, memberRolesLoader: memberRolesLoader}) => {
        const settled = await cascadeHeld({
            scope: scope,
            actor: actor,
            held: held,
            memberRolesLoader: memberRolesLoader
        });
        return instance = await reload({
            client: scope.client,
            instanceId: instance._id,
            tag: tag
        }), settledResult({
            ...settled,
            instance: instance,
            changed: !0,
            ...ranOps !== void 0 ? {
                ranOps: ranOps
            } : {}
        });
    }, cascadeHeld = ({scope: scope, actor: actor, held: held, memberRolesLoader: memberRolesLoader}) => cascade({
        client: scope.client,
        instanceId: instance._id,
        actor: actor,
        clientForGdr: scope.clientForGdr,
        refSurface: scope.refSurface,
        memberRolesLoader: memberRolesLoader,
        ...clock !== void 0 ? {
            clock: clock
        } : {},
        ...executionContext !== void 0 ? {
            executionContext: executionContext
        } : {},
        ...args.telemetry !== void 0 ? {
            telemetry: args.telemetry
        } : {},
        overlay: held
    }), commit = async run => {
        committing = !0;
        const held = new Map(overlay);
        try {
            const {actor: actor, localPrincipalId: localPrincipalId} = await access();
            return await run({
                actor: actor,
                ...localPrincipalId !== void 0 ? {
                    localPrincipalId: localPrincipalId
                } : {}
            }, held);
        } finally {
            if (committing = !1, buffered !== void 0) {
                const next = buffered;
                buffered = void 0;
                try {
                    applyUpdate(next);
                } catch (err) {
                    deferredUpdateError = err;
                }
            }
            for (const next of bufferedDocuments.values()) try {
                applyDocumentUpdate(next);
            } catch (err) {
                deferredUpdateError = err;
            }
            bufferedDocuments.clear();
        }
    };
    return {
        get subscriptionDocuments() {
            return subscriptionDocumentsForInstance(instance);
        },
        update(docs) {
            if (committing) buffered = docs, bufferedDocuments.clear(); else try {
                applyUpdate(docs);
            } catch (err) {
                throw deferredUpdateError = void 0, err;
            }
            flushDeferredError();
        },
        updateDocument(doc) {
            if (committing) {
                const uri = gdrFromResource(doc.resource, doc.doc._id);
                bufferedDocuments.set(uri, {
                    doc: structuredClone(doc.doc),
                    resource: doc.resource
                });
            } else try {
                applyDocumentUpdate(doc);
            } catch (err) {
                throw deferredUpdateError = void 0, err;
            }
            flushDeferredError();
        },
        updateGuards(guards) {
            heldGuards = guards.map(guard => parseGuardDocument(structuredClone(guard)));
        },
        async evaluate() {
            return evaluateWith({
                held: overlay,
                guards: heldGuards,
                self: await optimisticSelf()
            });
        },
        previewField({target: target, mode: mode, value: value}) {
            stagePreview({
                target: target,
                mode: mode,
                value: value
            });
        },
        discardFieldPreview(target) {
            if (target === void 0) {
                previews.clear();
                return;
            }
            const site = previewSiteOf({
                instance: instance,
                definition: definitionOf(),
                target: target
            });
            site !== void 0 && previews.delete(siteKey(site));
        },
        tick() {
            return commit(async ({actor: actor, localPrincipalId: localPrincipalId}, held) => {
                const memberRolesLoader = createAssignmentMemberRolesLoader(tickScope.client);
                await assertInstanceWriteAllowed({
                    instance: instance,
                    guards: heldGuards,
                    identity: lakePrincipalId({
                        actor: actor,
                        localPrincipalId: localPrincipalId
                    }),
                    dereference: clientGuardDereference(tickScope.client)
                });
                const before = await reload({
                    client: tickScope.client,
                    instanceId: instance._id,
                    tag: tag
                }), settled = await cascadeHeld({
                    scope: tickScope,
                    actor: actor,
                    held: held,
                    memberRolesLoader: memberRolesLoader
                });
                return instance = await reload({
                    client: tickScope.client,
                    instanceId: instance._id,
                    tag: tag
                }), telemetry.log(WorkflowInstanceTicked, {
                    ...definitionHashFragment(instance.pinnedContentHash),
                    instanceId: instance._id,
                    cascaded: settled.cascaded
                }), settledResult({
                    ...settled,
                    instance: instance,
                    changed: instance._rev !== before._rev
                });
            });
        },
        fireAction({activity: activity, action: action, params: params}) {
            return commit(async ({actor: actor, localPrincipalId: localPrincipalId}, held) => {
                const memberRolesLoader = createAssignmentMemberRolesLoader(fireScope.client);
                assertActionAllowed({
                    evaluation: await evaluateWith({
                        held: held,
                        guards: heldGuards,
                        self: instance
                    }),
                    activity: activity,
                    action: action
                });
                const preOp = instance, {grants: grants} = await access(), ranOps = await applyAction({
                    client: fireScope.client,
                    instance: instance,
                    activity: activity,
                    action: action,
                    actor: actor,
                    ...localPrincipalId !== void 0 ? {
                        localPrincipalId: localPrincipalId
                    } : {},
                    clientForGdr: fireScope.clientForGdr,
                    refSurface: fireScope.refSurface,
                    memberRolesLoader: memberRolesLoader,
                    ...grants !== void 0 ? {
                        grants: grants
                    } : {},
                    ...clock !== void 0 ? {
                        clock: clock
                    } : {},
                    ...executionContext !== void 0 ? {
                        executionContext: executionContext
                    } : {},
                    ...args.telemetry !== void 0 ? {
                        telemetry: args.telemetry
                    } : {},
                    ...params !== void 0 ? {
                        params: params
                    } : {}
                }), result = await settleAfterApply({
                    actor: actor,
                    held: held,
                    ranOps: ranOps,
                    scope: fireScope,
                    memberRolesLoader: memberRolesLoader
                });
                return telemetry.log(WorkflowActionFired, actionFiredData({
                    instance: preOp,
                    activity: activity,
                    params: params,
                    cascaded: result.cascaded
                })), result;
            });
        },
        editField({target: target, mode: mode, value: value}) {
            let stagedKey;
            try {
                stagedKey = stagePreview({
                    target: target,
                    mode: mode,
                    value: value
                });
            } catch (err) {
                return Promise.reject(err);
            }
            return commit(async ({actor: actor, localPrincipalId: localPrincipalId}, held) => {
                const memberRolesLoader = createAssignmentMemberRolesLoader(editScope.client);
                assertEditAllowed(await evaluateWith({
                    held: held,
                    guards: heldGuards,
                    self: instance
                }), target);
                const preOp = instance, {grants: grants} = await access(), ranOps = await applyEdit({
                    client: editScope.client,
                    instance: instance,
                    target: target,
                    ...mode !== void 0 ? {
                        mode: mode
                    } : {},
                    ...value !== void 0 ? {
                        value: value
                    } : {},
                    actor: actor,
                    ...localPrincipalId !== void 0 ? {
                        localPrincipalId: localPrincipalId
                    } : {},
                    clientForGdr: editScope.clientForGdr,
                    refSurface: editScope.refSurface,
                    memberRolesLoader: memberRolesLoader,
                    ...grants !== void 0 ? {
                        grants: grants
                    } : {},
                    ...clock !== void 0 ? {
                        clock: clock
                    } : {},
                    ...executionContext !== void 0 ? {
                        executionContext: executionContext
                    } : {},
                    ...args.telemetry !== void 0 ? {
                        telemetry: args.telemetry
                    } : {}
                }), result = await settleAfterApply({
                    actor: actor,
                    held: held,
                    ranOps: ranOps,
                    scope: editScope,
                    memberRolesLoader: memberRolesLoader
                });
                return telemetry.log(WorkflowFieldEdited, fieldEditedData({
                    instance: preOp,
                    target: target,
                    mode: mode
                })), result;
            }).finally(() => {
                stagedKey !== void 0 && previews.delete(stagedKey);
            });
        }
    };
}

function asHeldInstance(doc) {
    return readInstanceDoc(doc);
}

const defaultLoggerFactory = name => ({
    info: (message, extra) => console.error(`[${name}] ${message}`, extra !== void 0 ? extra : ""),
    warn: (message, extra) => console.warn(`[${name}] ${message}`, extra !== void 0 ? extra : ""),
    error: (message, extra) => console.error(`[${name}] ${message}`, extra !== void 0 ? extra : "")
});

function logDeployWarnings(result, loggerFactory) {
    for (const {name: name, warnings: warnings} of result.results) {
        if (warnings === void 0) continue;
        const log = loggerFactory(`deploy.${name}`);
        for (const warning of warnings) log.warn(warning);
    }
}

const silentLogger = {
    info: () => {},
    warn: () => {},
    error: () => {}
};

function createEngine(args) {
    const {workflowResource: workflowResource, clock: clock, tag: tag, executionContext: executionContext} = args, {client: client, resourceClients: resourceClients} = taggedScope(args, REQUEST_TAG.engine);
    validateTag(tag);
    const effects = {
        handlers: args.effects?.handlers ?? {},
        missingHandler: args.effects?.missingHandler ?? "fail"
    }, logger = args.loggerFactory ?? defaultLoggerFactory, telemetry = resolveTelemetry(args.telemetry), EMPTY_DRAIN_LOG_INTERVAL_MS = 6e4;
    let lastEmptyDrainLoggedAt;
    const emptyDrainDue = () => {
        const now = Date.parse((clock ?? wallClock)());
        return lastEmptyDrainLoggedAt !== void 0 && now - lastEmptyDrainLoggedAt < EMPTY_DRAIN_LOG_INTERVAL_MS ? !1 : (lastEmptyDrainLoggedAt = now, 
        !0);
    }, optionalScope = {
        ...resourceClients !== void 0 ? {
            resourceClients: resourceClients
        } : {},
        ...clock !== void 0 ? {
            clock: clock
        } : {},
        ...executionContext !== void 0 ? {
            executionContext: executionContext
        } : {}
    }, withScope = rest => ({
        ...rest,
        client: client,
        tag: tag,
        workflowResource: workflowResource,
        telemetry: telemetry,
        ...optionalScope,
        ...args.idempotencyTtlMs !== void 0 ? {
            idempotencyTtlMs: args.idempotencyTtlMs
        } : {}
    });
    return {
        client: client,
        tag: tag,
        workflowResource: workflowResource,
        effects: effects,
        logger: logger,
        telemetry: telemetry,
        resolveActor: rest => resolveClientActor(client, rest),
        deployDefinitions: async rest => {
            const result = await workflow.deployDefinitions(withScope(rest));
            return logDeployWarnings(result, logger), result;
        },
        startInstance: rest => workflow.startInstance(withScope(rest)),
        fireAction: rest => workflow.fireAction(withScope(rest)),
        editField: rest => workflow.editField(withScope(rest)),
        completeEffect: rest => workflow.completeEffect(withScope(rest)),
        commitEffectOps: rest => workflow.commitEffectOps(withScope(rest)),
        tick: rest => workflow.tick(withScope(rest)),
        evaluate: rest => workflow.evaluate(withScope(rest)),
        diagnose: rest => workflow.diagnose(withScope(rest)),
        availableActions: rest => workflow.availableActions(withScope(rest)),
        setStage: rest => workflow.setStage(withScope(rest)),
        abortInstance: rest => workflow.abortInstance(withScope(rest)),
        resetActivity: rest => workflow.resetActivity(withScope(rest)),
        deleteDefinition: rest => workflow.deleteDefinition(withScope(rest)),
        getInstance: rest => workflow.getInstance(withScope(rest)),
        subscriptionDocumentsForInstance: rest => workflow.getInstance(withScope(rest)).then(subscriptionDocumentsForInstance),
        session: ({instance: instance, grantsFromPath: grantsFromPath}) => createInstanceSession({
            client: client,
            tag: tag,
            instance: instance,
            telemetry: telemetry,
            ...optionalScope,
            ...grantsFromPath !== void 0 ? {
                grantsFromPath: grantsFromPath
            } : {}
        }),
        guardsForInstance: rest => workflow.guardsForInstance(withScope(rest)),
        guardsForDefinition: rest => workflow.guardsForDefinition(withScope(rest)),
        children: rest => workflow.children(withScope(rest)),
        instancesForDocument: rest => workflow.instancesForDocument(withScope(rest)),
        definitionsForDocument: rest => workflow.definitionsForDocument(withScope(rest)),
        evaluateStart: rest => workflow.evaluateStart(withScope(rest)),
        query: rest => workflow.query(withScope(rest)),
        queryInScope: rest => workflow.queryInScope(withScope(rest)),
        listPendingEffects: rest => workflow.listPendingEffects(withScope(rest)),
        findPendingEffects: rest => workflow.findPendingEffects(withScope(rest)),
        drainEffects: async ({instanceId: instanceId}) => {
            const result = await drainEffectsInternal({
                client: client,
                handlerClient: args.client,
                ...args.resourceClients !== void 0 ? {
                    handlerResourceClients: args.resourceClients
                } : {},
                tag: tag,
                workflowResource: workflowResource,
                ...optionalScope,
                instanceId: instanceId,
                ...effects,
                logger: logger,
                telemetry: telemetry,
                ...args.effects?.leaseMs !== void 0 ? {
                    leaseMs: args.effects.leaseMs
                } : {}
            }), drained = result.drained;
            return (drained.length > 0 || emptyDrainDue()) && telemetry.log(WorkflowEffectsDrained, {
                ...definitionHashFragment(result.definitionContentHash),
                instanceId: instanceId,
                drainedCount: drained.length,
                drainedEffects: drained.map(entry => entry.name)
            }), result;
        },
        verifyDeployedDefinitions: () => verifyDeployedDefinitionsInternal({
            client: client,
            tag: tag,
            ...effects,
            logger: logger
        })
    };
}

const CONSENT_TAG = "telemetry.consent", BATCH_TAG = "telemetry.batch";

function createTelemetryIntake(args) {
    const {client: client, projectId: projectId, denied: denied = !1} = args;
    let cachedConsent;
    return {
        resolveConsent: async () => denied ? {
            status: "denied"
        } : (cachedConsent !== void 0 || (cachedConsent = await client.request({
            uri: "/intake/telemetry-status",
            tag: CONSENT_TAG
        })), cachedConsent),
        sendEvents: batch => client.request({
            uri: "/intake/batch",
            method: "POST",
            body: {
                projectId: projectId,
                batch: batch
            },
            tag: BATCH_TAG
        })
    };
}

function isTelemetryEnvDenied(env) {
    return isTrueish(env.CI) || isTrueish(env.DO_NOT_TRACK);
}

function processShellUserProperties(surface) {
    const proc = globalThis.process;
    if (proc === void 0) throw new Error("processShellUserProperties is for process shells (CLI, MCP server) — no `process` global here");
    return {
        surface: surface,
        machinePlatform: proc.platform,
        cpuArchitecture: proc.arch,
        runtime: resolveExecutionContext().runtime,
        runtimeVersion: proc.version
    };
}

function isTrueish(value) {
    if (value === void 0 || value === "") return !1;
    const normalized = value.toLowerCase();
    return normalized !== "0" && normalized !== "false";
}

function groupSitesOf(definition) {
    const sites = [], workflow2 = declarationIndex({
        groups: definition.groups,
        level: "workflow",
        sites: sites
    });
    attributeFieldMembers({
        entries: definition.fields,
        scope: "workflow",
        chain: [ workflow2 ]
    });
    for (const stage of definition.stages) collectStageGroupSites({
        stage: stage,
        workflow: workflow2,
        sites: sites
    });
    return sites;
}

function collectStageGroupSites({stage: stage, workflow: workflow2, sites: sites}) {
    const enclosing = [ declarationIndex({
        groups: stage.groups,
        level: "stage",
        stage: stage.name,
        sites: sites
    }), workflow2 ];
    attributeFieldMembers({
        entries: stage.fields,
        scope: "stage",
        stage: stage.name,
        chain: enclosing
    });
    for (const activity of stage.activities ?? []) collectActivityGroupSites({
        activity: activity,
        stage: stage.name,
        enclosing: enclosing,
        sites: sites
    });
}

function collectActivityGroupSites({activity: activity, stage: stage, enclosing: enclosing, sites: sites}) {
    attributeMember({
        member: {
            kind: "activity",
            stage: stage,
            name: activity.name
        },
        group: activity.group,
        chain: enclosing
    });
    const chain = [ declarationIndex({
        groups: activity.groups,
        level: "activity",
        stage: stage,
        activity: activity.name,
        sites: sites
    }), ...enclosing ];
    attributeFieldMembers({
        entries: activity.fields,
        scope: "activity",
        stage: stage,
        activity: activity.name,
        chain: chain
    });
    for (const action of activity.actions ?? []) attributeMember({
        member: {
            kind: "action",
            stage: stage,
            activity: activity.name,
            name: action.name
        },
        group: action.group,
        chain: chain
    });
}

function declarationIndex({groups: groups, level: level, stage: stage, activity: activity, sites: sites}) {
    const index = /* @__PURE__ */ new Map;
    for (const group of groups ?? []) {
        const site = {
            group: group,
            level: level,
            ...stage !== void 0 ? {
                stage: stage
            } : {},
            ...activity !== void 0 ? {
                activity: activity
            } : {},
            members: []
        };
        sites.push(site), index.has(group.name) || index.set(group.name, site);
    }
    return index;
}

function attributeFieldMembers({entries: entries, scope: scope, stage: stage, activity: activity, chain: chain}) {
    for (const entry of entries ?? []) attributeMember({
        member: {
            kind: "field",
            scope: scope,
            ...stage !== void 0 ? {
                stage: stage
            } : {},
            ...activity !== void 0 ? {
                activity: activity
            } : {},
            name: entry.name
        },
        group: entry.group,
        chain: chain
    });
}

function attributeMember({member: member, group: group, chain: chain}) {
    for (const name of groupMembershipNames(group)) {
        const site = chain.find(index => index.has(name))?.get(name);
        site && site.members.push(member);
    }
}

function diffEntry({def: rawDef, latestRaw: latestRaw, target: target}) {
    const def = parseDefinitionInput(rawDef, "diffEntry");
    assertReaderModelAcknowledgement(target.expectedMinReaderModel, {
        requiredMinReaderModel: requiredDefinitionReaderModel([ def ])
    });
    const plan = planDefinitionDeploy({
        def: def,
        latest: asLatest(latestRaw),
        target: target
    }), existing = latestRaw ? stripSystemFields(latestRaw) : void 0;
    return {
        name: def.name,
        version: plan.version,
        status: plan.status,
        docId: plan.docId,
        expected: plan.document,
        ...existing !== void 0 ? {
            existing: existing
        } : {}
    };
}

const LatestDeployedSchema = tolerantObject()({
    version: v.number(),
    contentHash: v.exactOptional(v.string())
});

function asLatest(raw) {
    if (raw !== void 0) return parsePersistedDoc({
        schema: LatestDeployedSchema,
        doc: raw,
        docType: WORKFLOW_DEFINITION_TYPE
    });
}

async function computeDiffEntries({client: client, defs: defs, target: target}) {
    const definitions = defs.map(def => parseDefinitionInput(def, "computeDiffEntries"));
    assertReaderModelAcknowledgement(target.expectedMinReaderModel, {
        requiredMinReaderModel: requiredDefinitionReaderModel(definitions)
    });
    const entries = [];
    for (const def of definitions) {
        const latest = await loadLatestDeployed({
            client: client,
            definition: def.name,
            tag: target.tag
        });
        entries.push(diffEntry({
            def: def,
            latestRaw: latest,
            target: target
        }));
    }
    return entries;
}

function buildInitialFields({declared: declared, values: values}) {
    const settable = declared.filter(f => f.initialValue?.type === "input"), settableNames = settable.map(f => f.name).join(", ");
    return Object.entries(values).map(([name, value]) => {
        const entry = declared.find(f => f.name === name);
        if (entry === void 0) throw new Error(settable.length > 0 ? `workflow declares no field "${name}" — input fields: ${settableNames}` : `workflow declares no field "${name}" — it declares no input fields at all`);
        if (entry.initialValue?.type !== "input") throw new Error(`field "${name}" is not input-sourced and cannot accept a start value. ` + (settable.length > 0 ? `Input fields: ${settableNames}` : "This workflow has no input fields."));
        return {
            type: entry.type,
            name: name,
            value: value
        };
    });
}

function startRefusal(definition) {
    if (!isStartableDefinition(definition)) return "definition is spawn-only (lifecycle: 'child') — instances of it are spawned by a parent workflow's activity, not started standalone";
}

const HISTORY_DISPLAY = {
    stageEntered: {
        title: "Stage entered",
        description: "The instance crossed into a new stage."
    },
    stageExited: {
        title: "Stage exited",
        description: "The instance left a stage on the way to the next one."
    },
    activityStatusChanged: {
        title: "Activity status changed",
        description: "An action or op flipped an activity's status (active → done / skipped / failed)."
    },
    actionFired: {
        title: "Action fired",
        description: "An action fired against an activity — via fireAction, or cascade-fired when its `when` turned true (`triggered`) — ops ran, status may have flipped, effects queued, spawn reconciled."
    },
    transitionFired: {
        title: "Transition fired",
        description: "A transition committed and the instance moved to a new stage."
    },
    effectQueued: {
        title: "Effect queued",
        description: "A pending effect was added to the queue, waiting for a drainer to dispatch it."
    },
    effectCompleted: {
        title: "Effect settled",
        description: "A queued effect settled: a drainer ran its handler to completion (done or failed, outputs recorded on the run row and readable as $effects), or an abort cancelled it before dispatch."
    },
    effectClaimReleased: {
        title: "Stale claim released",
        description: "A pending effect's claim outlived its lease (the claimer is presumed dead) and was released — taken over by a drain for redispatch, or force-released by sweepStaleClaims."
    },
    spawned: {
        title: "Spawned child",
        description: "An action's `spawn` declaration spawned a child workflow instance."
    },
    subworkflowAdopted: {
        title: "Child adopted",
        description: "A re-entered stage's spawn reconciler rediscovered a live child's row and re-bound the child to the fresh cohort instead of duplicating it."
    },
    subworkflowResolved: {
        title: "Child resolved",
        description: "A spawned child was observed terminal (done or aborted); its registry row now carries the frozen terminal state and the child left the parent's watch-set."
    },
    subworkflowOrphaned: {
        title: "Child propagation orphaned",
        description: "An instance naming this one as its parent reached terminal, but no subworkflow-registry row matches it — its state cannot drive any gate here."
    },
    aborted: {
        title: "Instance aborted",
        description: "An admin hard-stopped the instance — pending effects cancelled, stage guards removed, abortedAt + completedAt stamped."
    },
    opApplied: {
        title: "Op applied",
        description: "An action, direct edit, or effect completion executed an operation and recorded its audit payload."
    },
    fieldQueryDiscarded: {
        title: "Query result discarded",
        description: "A query-sourced field's GROQ result didn't fit the field's declared kind, so it was discarded and the field fell back to its default."
    }
}, EXECUTOR_CLASSIFICATION_DISPLAY = {
    autonomous: {
        title: "Autonomous",
        description: "Every action is cascade-fired — the engine fires them; no caller fires anything."
    },
    interactive: {
        title: "Interactive",
        description: "Only fireAction-fired actions — a caller (editor, robot, agent) must act."
    },
    "off-system": {
        title: "Off-system",
        description: "The work happens outside the system (the `target` deep-link); someone records it here."
    },
    hybrid: {
        title: "Hybrid",
        description: "A mix of caller-fired actions and cascade-fired triggers."
    }
}, FIELD_KIND_DISPLAY = {
    "doc.ref": {
        title: "Document reference",
        description: "Single GDR pointer at a document in this or another resource."
    },
    "doc.refs": {
        title: "Document references",
        description: "Ordered list of GDR pointers — multi-doc selection."
    },
    subject: {
        title: "Subject document",
        description: "Single GDR pointer at THE document the workflow is about — same value as a document reference, elevated so surfaces identify the subject by kind."
    },
    "release.ref": {
        title: "Content Release reference",
        description: "GDR pointer at a Content Release system doc, carrying the release name the engine derives the instance's read perspective from."
    },
    string: {
        title: "String value",
        description: "Free-form single-line string entry."
    },
    text: {
        title: "Text value",
        description: "Free-form multiline string entry."
    },
    number: {
        title: "Number value",
        description: "Numeric entry."
    },
    progress: {
        title: "Progress",
        description: "Application-defined completion, 0–100 inclusive (fractions allowed)."
    },
    boolean: {
        title: "Boolean value",
        description: "True/false entry."
    },
    date: {
        title: "Date value",
        description: "Date-only (YYYY-MM-DD) entry, no time component."
    },
    dueDate: {
        title: "Due date",
        description: "THE due date for a level — same value as a date, elevated so surfaces identify the deadline by kind."
    },
    datetime: {
        title: "Date / time value",
        description: "ISO-8601 timestamp entry."
    },
    dueDatetime: {
        title: "Due date / time",
        description: "THE due datetime for a level — same value as a date/time, the timestamp counterpart of a due date."
    },
    url: {
        title: "URL value",
        description: "Validated URL entry."
    },
    actor: {
        title: "Actor value",
        description: "A typed (person|agent|system) actor identity — a concrete principal."
    },
    assignee: {
        title: "Assignee",
        description: "A single typed (user|role) assignee — who is expected/responsible."
    },
    assignees: {
        title: "Assignees",
        description: "Ordered list of typed (user|role) assignees."
    },
    object: {
        title: "Object",
        description: "An object with named sub-fields; the value is keyed by sub-field name."
    },
    array: {
        title: "Array",
        description: "An array of objects shaped by the declared `of` sub-fields; ops add/update/remove rows."
    }
}, OP_DISPLAY = {
    "field.set": {
        title: "Set field entry",
        description: "Overwrite a field entry's value with a resolved value expression."
    },
    "field.setIfMissing": {
        title: "Set field entry if missing",
        description: "Set a nullable field entry's value only when it is currently null or undefined; array, assignees, and doc.refs entries are always arrays and are not supported."
    },
    "field.unset": {
        title: "Unset field entry",
        description: "Reset a field entry to its default (null for scalars, [] for array kinds)."
    },
    "field.append": {
        title: "Append to field entry",
        description: "Push a resolved item onto an array-kind entry (array, assignees, doc.refs)."
    },
    "field.inc": {
        title: "Increment field entry",
        description: "Add a resolved numeric delta, defaulting to 1, to a number entry that already holds a finite number."
    },
    "field.dec": {
        title: "Decrement field entry",
        description: "Subtract a resolved numeric delta, defaulting to 1, from a number entry that already holds a finite number."
    },
    "field.updateWhere": {
        title: "Update matching rows",
        description: "Merge declared sub-fields into rows of an `array` entry that match the `where` condition (`array` only — `doc.refs`/`assignees` rows are engine-shaped)."
    },
    "field.removeWhere": {
        title: "Remove matching rows",
        description: "Drop rows of an array-kind entry that match the `where` condition."
    },
    "status.set": {
        title: "Set activity status",
        description: "Flip an activity's status explicitly (the action-level `status:` sugar desugars to this)."
    }
}, CONTEXT_ENTRY_DISPLAY = {
    "context.string": {
        title: "String",
        description: "Start-seeded string value, read as `$context.<name>`."
    },
    "context.number": {
        title: "Number",
        description: "Start-seeded numeric value, read as `$context.<name>`."
    },
    "context.boolean": {
        title: "Boolean",
        description: "Start-seeded boolean value, read as `$context.<name>`."
    },
    "context.ref": {
        title: "GDR reference",
        description: "Start-seeded GDR pointer, read as `$context.<name>`."
    },
    "context.json": {
        title: "JSON",
        description: "Start-seeded JSON payload, read as `$context.<name>`."
    }
}, AUTHORING_DISPLAY = {
    pendingEffect: {
        title: "Pending effect",
        description: "An effect that has been queued but not yet dispatched."
    },
    "pendingEffect.claim": {
        title: "Claim",
        description: "A drainer's lock on a pending effect while it dispatches."
    },
    [WORKFLOW_DEFINITION_TYPE]: {
        title: "Workflow definition",
        description: "An immutable workflow blueprint, addressed by `name` + `version`."
    },
    [WORKFLOW_INSTANCE_TYPE]: {
        title: "Workflow instance",
        description: "A running (or finished) workflow against its declared fields."
    }
}, ACTIVITY_KIND_DISPLAY = {
    user: {
        title: "User activity",
        description: "A person acts via the app — renders action buttons ranked by outcome."
    },
    service: {
        title: "Service activity",
        description: "An automated system or effect does the work — renders effect drain status."
    },
    script: {
        title: "Script activity",
        description: "The engine runs a step inline and resolves it in the same cascade hop — an audit row."
    },
    manual: {
        title: "Manual activity",
        description: "Work done off-system, acknowledged by a person or verified by a predicate — renders an instruction card with a deep-link."
    },
    receive: {
        title: "Receive activity",
        description: 'No actor; the engine waits on a condition — renders "waiting until …", no buttons.'
    }
}, GROUP_KIND_DISPLAY = {
    core: {
        title: "Core group",
        description: "Central to understanding the workflow — condensed views (cards, summaries, projections) include these first."
    },
    details: {
        title: "Details group",
        description: "Depth on demand — excluded from condensed views, disclosed when asked."
    }
}, DRIVER_KIND_DISPLAY = {
    person: {
        title: "Person",
        description: "A human fired this action."
    },
    agent: {
        title: "Agent",
        description: "An AI agent fired this action."
    },
    service: {
        title: "Service",
        description: "An external system or Function fired this action."
    },
    engine: {
        title: "Engine",
        description: "The workflow engine itself drove this — a gate, machine step, or housekeeping."
    }
}, DISPLAY = {
    ...HISTORY_DISPLAY,
    ...FIELD_KIND_DISPLAY,
    ...OP_DISPLAY,
    ...CONTEXT_ENTRY_DISPLAY,
    ...AUTHORING_DISPLAY
};

function displayTitle(typeKey) {
    return typeKey ? DISPLAY[typeKey]?.title ?? typeKey : "";
}

function displayDescription(typeKey) {
    if (typeKey) return DISPLAY[typeKey]?.description;
}

export { ACTION_SEMANTICS, ACTIVITY_KINDS, ACTIVITY_KIND_DISPLAY, ACTOR_KINDS, ANONYMOUS_IDENTITY, AUTHORING_DISPLAY, ActionDisabledError, ActionParamsInvalidError, CONDITION_VARS, CONTEXT_ENTRY_DISPLAY, CascadeLimitError, ConcurrentCascadeError, ConcurrentCommitEffectOpsError, ConcurrentCompleteEffectError, ConcurrentEditFieldError, ConcurrentFireActionError, ContractViolationError, DATA_MODEL_CHANGES, DATA_MODEL_MAX_READER, DATA_MODEL_MIN_READER, DATA_MODEL_VERSION, DECISION_SEMANTICS, DEFAULT_CONTENT_PERSPECTIVE, DEFAULT_EFFECT_LEASE_MS, DEFAULT_IDEMPOTENCY_TTL_MS, DEFAULT_TRANSITION_WHEN, DISPLAY, DOCUMENT_VALUE_PERMISSIONS, DRIVER_KINDS, DRIVER_KIND_DISPLAY, DefinitionInUseError, DefinitionNotFoundError, EFFECT_COMMIT_DISPATCH_CAP, EFFECT_COMMIT_QUEUE_DEPTH, ENGINE_API_VERSION, EXECUTION_KINDS, EXECUTOR_CLASSIFICATIONS, EXECUTOR_CLASSIFICATION_DISPLAY, EditFieldDeniedError, EffectCommitQueueOverflowError, EffectNotFoundError, EffectOpsInvalidError, EffectOutputsInvalidError, FIELD_KIND_DISPLAY, FILTER_SCOPE_VARS, FieldValueShapeError, GROUP_KINDS, GROUP_KIND_DISPLAY, GUARD_DOC_TYPE, GUARD_OWNER, GUARD_PREDICATE_VARS, HISTORY_DISPLAY, InitialFieldsInvalidError, InstanceNotFoundError, MAX_COUNTERFACTUAL_INDEX2 as MAX_COUNTERFACTUAL_INDEX, MissingHandlerError, ModelVersionAheadError, MutationGuardDeniedError, MutationGuardDocSchema, NEUTRAL_MARK, OP_DISPLAY, OUTCOME_MARKS, PartialGuardDeployError, PersistedDocShapeError, READER_MODEL_ROLLOUT_URL, RESERVED_CONDITION_VARS, ReaderModelAcknowledgementError, RefResourceUndeclaredError, RequiredFieldNotProvidedError, SIGNAL_SEMANTICS, START_FILTER_VARS, START_REQUIREMENT_VARS, SYSTEM_IDENTITY, SpawnContractsInvalidError, StaleEffectClaimError, StartNotAllowedError, StartNotPrimedError, StartNotSettledError, WORKFLOW_DEFINITION_TYPE, WORKFLOW_INSTANCE_TYPE, WorkflowActionFired, WorkflowActivityReset, WorkflowDefinitionDeleted, WorkflowDefinitionDeployed, WorkflowEffectCompleted, WorkflowEffectStateReported, WorkflowEffectsDrained, WorkflowError, WorkflowFieldEdited, WorkflowInstanceAborted, WorkflowInstancePreviewSchema, WorkflowInstanceSchema, WorkflowInstanceStarted, WorkflowInstanceTicked, WorkflowStageSet, WorkflowStageTransitioned, WorkflowStateDivergedError, abortReason, acceptsDocumentType, aclPathForResource, actionDisabledDetail, actionRendering, actionVerdict, activeAssignmentMembers, activityAutonomyOf, actorFulfillsRole, actorMatchesAssignment, analyzeCondition2 as analyzeCondition, applicableDefinitions, assertReadableModel, assertReaderModelAcknowledgement, assignmentMatch, assignmentMembers, assignmentPrefilter, assignmentState, assignmentStateCounts, atomReadsDataset2 as atomReadsDataset, autonomySummary, availableActions, buildInitialFields, buildSnapshot, checklistLines, classifyPrincipalId, clientConfigFromResource, clientGuardDereference, clientProjectUserDirectory, compileGuards, computeDiffEntries, conditionFieldReadNames, conditionSitesOf, contentDocQuery, contentDraftFallback, contentReleaseName, contextMap, createEngine, createTelemetryIntake, datasetResourceParts, defaultLoggerFactory, definitionDeployedData, definitionLookupGroq, definitionRoleNames, definitionTagsGroq, definitionsListGroq, deniedGuardLabels, deniedGuardRefs, denyingGuards, deployStageGuards, deployedTagsGroq, deriveActivityKind, deriveExecutorClassification, deriveWorkflowAutonomy, describeAtom, describeAutonomyWait, describeCondition, describeDefinition, describeFieldInsight, describeNode, describeSite, describeSiteHeading, diagnoseInputFromEvaluation, diagnoseInstance, diffEntry, displayDescription, displayTitle, documentActionDenials, documentPrefilter, documentStuckCause, driverKind, effectOutputsMap, entryDocRefs, errorMessage, evaluateFromSnapshot, evaluateMutationGuard, evaluateStartFilter, expandResourceAliases, explainCondition2 as explainCondition, explainStartRequirement, extractDocumentId, fieldTreeShape, findActivityNode, findCurrentActivityEntry, findOpenStageEntry, findStageNode, formatRead2 as formatRead, gdrFromResource, gdrRef, gdrUri, groupMembershipNames, groupSitesOf, guardMatches, guardsForDefinition, guardsForInstance, guardsForResource, guillemets2 as guillemets, hasSingleSubjectRequirement, hashDefinitionContent, humanize2 as humanize, identityMatchesAssignment, inFlightFilter, initialFieldIssues, instanceAssignmentStateCounts, instanceChangesQuery, instanceDocId, instanceGuardQuery, instancePreviewsQuery, instanceWatchesDocument, instancesGuardQuery, instancesQuery, isCascadeFired, isClaimExpired, isClientProjectUser, isComparisonOp, isDefinitionApplicable, isFilterScopedOut, isGdr, isInputSourced, isNotesEntry, isProjectUserNotFoundError, isRevisionConflict, isSingleDocRefEntry, isSingleDocRefKind, isStartableDefinition, isSubjectEntry, isTelemetryEnvDenied, isTerminalActivityStatus, isTerminalStage, isTodoListEntry, isTodoListItem, isUnprimed, lakeGuardId, lakePrincipalId, latestDefinitionsGroq, latestDeployedDefinitions, lintEffectOutputs, minReaderModelOf, missingRequiredInputs, modelVersionOf, narrateAutonomyWaits, noopTelemetry, openActivityAssignments, parentRef, parseDefinitionInput, parseDefinitionSnapshot, parseDefinitionSnapshotValue, parseGdr, parseGuardDocument, parseInstanceDocument, parseInstancePreviewDocument, parseResourceGdr, processShellUserProperties, projectStartSliceRow, projectToWatchRef, quoted2 as quoted, readInstanceDoc, readInstancePreviewDoc, readsRaw, readsRootDocument, refCanvas, refDashboard, refDataset, refKindAcceptsTypes, refMediaLibrary, refsOf, rejectedRefTypes, releaseDocId, releaseRef, remediationsFor, requiredDefinitionReaderModel, requiredModelFeatures, requiredReaderModel, resolveAccess, resolveActor, resolveClientActor, resolveFieldEntry$1 as resolveFieldEntry, resolveUserAttributes, resourceAliasesToMap, resourceFromParsed, resourceGdr, retractStageGuards, sameResource, scalarValidationIssues, schemaTreeShape, sentenceCase, silentLogger, singleSubjectRequirementRefused, snapshotGuardDereference, stageAutonomyOf, startFieldsParam, startKindOf, startRefusal, stripSystemFields, subjectDenialLabels, subscriptionDocument, subscriptionDocumentsForInstance, sweepStaleClaims, tagScopeFilter, terminalState, toBareId, tryParseGdr, unboundRequirementReads, unsatisfiedTransitionSummaries, userLoginProvider, validateDefinition, validateTag, verdictGuardsForInstance, wallClock, whatIfCondition2 as whatIfCondition, withAssignment, workflow };
