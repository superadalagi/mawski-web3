import { createHash } from 'node:crypto';
import { open } from 'node:fs/promises';
import { Readable } from 'node:stream';
import { finished } from 'node:stream/promises';
import { fileURLToPath } from 'node:url';
import { createRequester } from 'get-it';
import { retryOnFailure } from './retryOnFailure.js';
// Assets can be arbitrarily large (video, in particular), so there is no ceiling on how long the
// body may take to download - get-it would otherwise abort it after two minutes. A connection
// that never sends us response headers is still an error, though.
const request = createRequester({ as: 'stream', timeout: { headers: 30_000, total: false } });
export const getHashedBufferForUri = (uri, fetch) => retryOnFailure(() => getHashedBufferForUriInternal(uri, fetch));
async function getHashedBufferForUriInternal(uri, fetch) {
    if (/^file:\/\//i.test(uri)) {
        return getHashedBufferFromBytes(await readFileUri(uri));
    }
    if (/^data:/i.test(uri)) {
        return getHashedBufferFromBytes(await readDataUri(uri));
    }
    if (/^https?:\/\//i.test(uri)) {
        return getHashedBufferFromStream(await readHttpUri(uri, fetch));
    }
    throw new Error(`Unsupported URI scheme: ${uri}`);
}
function getHashedBufferFromBytes(buffer) {
    const hash = createHash('sha1');
    hash.update(buffer);
    return { buffer, sha1hash: hash.digest('hex') };
}
async function getHashedBufferFromStream(stream) {
    const hash = createHash('sha1');
    const chunks = [];
    try {
        stream.on('data', (chunk) => {
            chunks.push(chunk);
            hash.update(chunk);
        });
        await finished(stream);
        return {
            buffer: Buffer.concat(chunks),
            sha1hash: hash.digest('hex'),
        };
    }
    finally {
        if (stream instanceof Readable) {
            stream.destroy();
        }
    }
}
async function readFileUri(uri) {
    const filepath = fileURLToPath(uri);
    const fileHandle = await open(filepath, 'r');
    try {
        return await fileHandle.readFile();
    }
    finally {
        await fileHandle.close();
    }
}
async function readDataUri(uri) {
    const res = await fetch(uri);
    return Buffer.from(await res.arrayBuffer());
}
async function readHttpUri(uri, fetch) {
    const parsed = new URL(uri);
    const res = await request({ url: parsed.href, ...(fetch ? { fetch } : {}) });
    return Readable.fromWeb(res.body);
}
//# sourceMappingURL=getHashedBufferForUri.js.map