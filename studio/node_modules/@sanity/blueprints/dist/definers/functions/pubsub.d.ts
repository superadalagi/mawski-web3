import { type BlueprintPubSubFunctionConfig, type BlueprintPubSubFunctionResource } from '../../index.js';
/**
 * Defines a function that provide pubsub behaviour.
 *
 * @remarks
 * A barebones pubsub function
 * ```ts
 * definePubSubFunction({
 *   name: 'send-email',
 * })
 * ```
 * @public
 * @category Definers
 * @expandType BlueprintPubSubFunctionConfig
 * @param functionConfig The configuration for the pubsub function
 * @returns The validated pubsub function resource
 */
export declare function definePubSubFunction(functionConfig: BlueprintPubSubFunctionConfig): BlueprintPubSubFunctionResource;
