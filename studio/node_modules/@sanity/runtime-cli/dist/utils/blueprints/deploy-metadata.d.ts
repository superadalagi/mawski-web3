/**
 * Facts about the environment that runs a deploy.
 * Sent as the `client` part of deploy metadata. Keys with an `undefined` value drop out of the JSON body.
 */
export declare function getClientMetadata({ cliVersion, dir, }: {
    /** Version of the CLI that hosts this plugin, when known */
    cliVersion?: string;
    /** Directory of the Blueprint file, used to detect the package manager */
    dir: string;
}): Record<string, unknown>;
