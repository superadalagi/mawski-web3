import AdmZip from 'adm-zip';
import type { AssetPrepResult, FunctionResource, InstallerType } from '../types.js';
export declare function prepareAsset({ blueprintFileDir, resource, installer, }: {
    blueprintFileDir: string;
    resource: FunctionResource;
    installer?: InstallerType;
}): Promise<AssetPrepResult>;
export declare function pathToZip(path: string): Promise<AdmZip>;
