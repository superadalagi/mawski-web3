import type { DurableLogEvent, DurableRuntimeEvent, DurableStepEvent } from '../types.js';
type DurableLogLine = Pick<DurableLogEvent, 'level' | 'message'>;
type DurableJsonStep = DurableStepEvent & {
    logs: DurableLogLine[];
};
/**
 * Records a durable log event
 */
export declare const createDurableJsonCollector: () => {
    record: (event: DurableRuntimeEvent) => void;
    result: () => {
        steps: DurableJsonStep[];
        logs: DurableLogLine[];
    };
};
export {};
