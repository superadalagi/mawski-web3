import type { FunctionResource } from '../types.js';
export type ShouldTranspileResult = {
    transpile: true;
    entryPoint: string;
} | {
    transpile: false;
};
export declare function shouldTranspileFunction(resource: FunctionResource, functionPath: string): Promise<ShouldTranspileResult>;
