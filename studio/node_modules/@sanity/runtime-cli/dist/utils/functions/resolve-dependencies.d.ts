import type { FunctionResource, InstallerType } from '../types.js';
export declare function resolveResourceDependencies({ resource, transpiled, blueprintFileDir, functionPath, installer, }: {
    resource: FunctionResource;
    blueprintFileDir: string;
    functionPath: string;
    transpiled: boolean;
    installer?: InstallerType;
}): Promise<void>;
