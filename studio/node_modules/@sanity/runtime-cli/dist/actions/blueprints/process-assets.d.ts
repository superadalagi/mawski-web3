import type { BlueprintResource } from '@sanity/blueprints';
import type { AssetResource, AuthParams, InstallerType } from '../../utils/types.js';
type ProcessAssetsOptions<R extends BlueprintResource> = {
    assetResources: AssetResource<R>[];
    auth: AuthParams;
    blueprintFileDir: string;
    installer: InstallerType;
    fetchFn: typeof fetch;
    onProgress?: (progress: ProcessAssetsProgress) => void;
    onResult?: (result: ProcessedAsset<R>) => void;
};
export type ProcessAssetsProgress = {
    completed: number;
    total: number;
    slow: boolean;
};
export type ProcessedAsset<R extends BlueprintResource> = {
    success: false;
    resource: R;
    error: string;
    failureKind: 'processing' | 'upload';
} | {
    success: true;
    resource: R;
    assetParameter: string;
    hash: string;
    exists: boolean;
    metadata?: Record<string, unknown>;
};
export declare function processAssets<R extends BlueprintResource>(options: ProcessAssetsOptions<R>): Promise<ProcessedAsset<R>[]>;
export {};
