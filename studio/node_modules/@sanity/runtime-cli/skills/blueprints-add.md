# `npx @sanity/cli blueprints add`

Treat this command as legacy compatibility only. It is deprecated and supports only the `function` resource type. Prefer `npx @sanity/cli functions add` for new work.

## Agent guidance

- Warn that the command is deprecated before adding new usage to scripts or documentation.
- Migrate existing legacy invocations to `npx @sanity/cli functions add`.
