import { D as getAuthLogger, E as REQUEST_TAG_PREFIX, O as configureLogging$1, S as AuthStateType, T as DEFAULT_BASE, b as getTokenFromLocation, c as getDashboardOrganizationId, d as getTokenState, f as setAuthToken, h as getCleanedUrl, i as authStore, k as createLogger, l as getIsInDashboardState, m as getAuthCode, n as getClient, o as getAuthState, p as createLoggedInAuthState, r as getClientState, s as getCurrentUserState, u as getLoginUrlState, w as DEFAULT_API_VERSION, y as getDefaultLocation } from "./clientStore-BI4D8HhJ.js";
import { c as isReleasePerspective, d as isMediaLibraryResource, i as bindActionGlobally, l as isCanvasResource, n as bindActionByResource, o as defineStore, r as bindActionByResourceAndPerspective, t as createStateSourceAction, u as isDatasetResource } from "./createStateSourceAction-DdAhEfnZ.js";
import { A as clearPendingTransaction, B as assertDatasetResource, C as getCommentsState, D as toCommentsKeyParts, E as resolveComments, F as rollbackCommentUpdate, G as defineMutation, H as provisionAddonDataset, I as setCommentCreateError, K as randomId, L as setPendingTransaction, M as receiveComment, N as removeCommentById, O as addComment, P as restoreComments, R as normalizeComment, T as resolveCommentThreads, U as COMMENTS_API_VERSION, V as getAddonDatasetState, W as defineFetcher, _ as updateLastLoadMoreRequest, a as resolveQuery, b as commentsStore, c as getAllReleasesState, d as getUsersKey, f as initializeRequest, g as setUsersError, h as setUsersData, j as getCommentsKey, k as applyCommentUpdate, l as addSubscription, m as removeSubscription, o as getPerspectiveState, p as parseUsersKey, q as randomUuid, r as getQueryState, s as getActiveReleasesState, t as CORE_SDK_VERSION, u as cancelRequest, v as PROJECT_API_VERSION, x as getCommentThreadsState, y as USERS_STATE_CLEAR_DELAY, z as toCommentFieldPath } from "./version-CD4aBigm.js";
import { r as omitProperty, t as isDeepEqual } from "./object-CxXlSVg7.js";
import { n as setCleanupTimeout } from "./setCleanupTimeout-BiM6q1KF.js";
import { t as _defineProperty } from "./defineProperty-Bho9Ngsi.js";
import { t as getNodeState } from "./getNodeState-B4nG0sjm.js";
import { ClientError, CorsOriginError } from "@sanity/client";
import { EMPTY, NEVER, Observable, Subject, Subscription, auditTime, catchError, combineLatest, concat, concatMap, debounceTime, defer, distinctUntilChanged, filter, first, firstValueFrom, from, fromEvent, groupBy, map, merge, mergeMap, of, pairwise, partition, race, retry, share, shareReplay, skip, startWith, switchMap, take, takeUntil, tap, throttle, throwError, timer, withLatestFrom } from "rxjs";
import { isKeySegment, isKeyedObject } from "@sanity/types";
import { DocumentId, getDraftId, getPublishedId, getVersionId, isDraftId, isPublishedId, isVersionId } from "@sanity/id-utils";
import { getIndexForKey, getIndexForKey as getIndexForKey$1, getPathDepth, joinPaths, jsonMatch, jsonMatch as jsonMatch$1, slicePath, slicePath as slicePath$1, stringifyPath, stringifyPath as stringifyPath$1 } from "@sanity/json-match";
import { SanityEncoder } from "@sanity/mutate";
import { createDocumentLoaderFromClient } from "@sanity/mutate/_unstable_store";
import { evaluateSync, parse } from "groq-js";
import { catchError as catchError$1, distinctUntilChanged as distinctUntilChanged$1, map as map$1, mergeMap as mergeMap$1, retry as retry$1, scan, share as share$1, switchMap as switchMap$1 } from "rxjs/operators";
import { applyPatches, parsePatch } from "@sanity/diff-match-patch";
import { createSelector } from "reselect";
import { diffValue } from "@sanity/diff-patch";
import { SDK_CHANNEL_NAME, SDK_NODE_NAME } from "@sanity/message-protocol";
import { fromUrl } from "@sanity/bifur-client";
/**
* Creates a new Sanity resource instance
* @param config - Configuration for the instance (optional)
* @returns A configured SanityInstance
*
* @public
*/
function createSanityInstance(config = {}) {
	let instanceId = randomId(16), disposeListeners = /* @__PURE__ */ new Map(), disposed = { current: !1 }, instanceContext = {
		instanceId,
		projectId: config.projectId,
		dataset: config.dataset
	}, logger = createLogger("sdk", { instanceContext });
	return logger.info("Sanity instance created", {
		hasProjectId: !!config.projectId,
		hasDataset: !!config.dataset,
		hasAuth: !!config.auth,
		hasPerspective: !!config.perspective
	}), logger.debug("Instance configuration", {
		projectId: config.projectId,
		dataset: config.dataset,
		perspective: config.perspective,
		hasStudioConfig: !!config.studio,
		hasStudioTokenSource: !!config.studio?.auth?.token,
		hasAuthProviders: !!config.auth?.providers,
		hasAuthToken: !!config.auth?.token
	}), {
		instanceId,
		config,
		isDisposed: () => disposed.current,
		dispose: () => {
			if (disposed.current) {
				logger.trace("Dispose called on already disposed instance", { internal: !0 });
				return;
			}
			logger.trace("Disposing instance", {
				internal: !0,
				listenerCount: disposeListeners.size
			}), disposed.current = !0, disposeListeners.forEach((listener) => listener()), disposeListeners.clear(), logger.info("Instance disposed");
		},
		onDispose: (cb) => {
			let listenerId = randomId(16);
			return disposeListeners.set(listenerId, cb), () => {
				disposeListeners.delete(listenerId);
			};
		}
	};
}
const checkPermissions = defineFetcher({
	name: "checkPermissions",
	getKey: (_instance, resourceType, resourceId, permissions) => `${resourceType}:${resourceId}:${[...permissions].sort().join(",")}`,
	fetch: (instance) => (resourceType, resourceId, permissions) => getClientState(instance, {
		apiVersion: "v2025-07-11",
		scope: "global"
	}).observable.pipe(switchMap((client) => client.observable.request({
		url: `/access/${resourceType}/${resourceId}/user-permissions/me/check`,
		query: { permissions },
		tag: "access.check"
	}).pipe(map((response) => response.data)))),
	tags: (_data, resourceType, resourceId) => [{
		type: "access",
		id: `${resourceType}:${resourceId}`
	}]
});
/**
* Serialises store options into a `@sanity/client` request query object.
* Scalars are stringified and `undefined` values dropped; string arrays pass
* through untouched so the client serialises them as repeated keys
* (`permissions=a&permissions=b`).
*
* @internal
*/
function buildQuery(params) {
	return Object.fromEntries(Object.entries(params).filter(([, value]) => value !== void 0).map(([key, value]) => [key, Array.isArray(value) ? value : String(value)]));
}
/** The API takes `include` as a comma-separated list; sorted so key and query agree */
function serializeInclude$1(include) {
	return include?.length ? [...include].sort().join(",") : void 0;
}
/**
* Fetcher for an organization's applications (`GET /applications`), on the
* shared fetcher cache.
*
* @see https://www.sanity.io/docs/http-reference/applications-api
* @internal
*/
const applications = defineFetcher({
	name: "applications",
	getKey: (_instance, options) => [
		options.organizationId,
		options.type ?? "",
		serializeInclude$1(options.include) ?? "",
		options.limit ?? "",
		options.cursor ?? ""
	].join(":"),
	fetch: (instance) => (options) => getClientState(instance, {
		apiVersion: "vX",
		scope: "global"
	}).observable.pipe(switchMap((client) => client.observable.request({
		url: "/applications",
		query: buildQuery({
			organizationId: options.organizationId,
			type: options.type,
			include: serializeInclude$1(options.include),
			limit: options.limit,
			cursor: options.cursor
		}),
		tag: "applications.list"
	}))),
	tags: (data) => [{
		type: "application",
		id: "LIST"
	}, ...data.data.map((app) => ({
		type: "application",
		id: app.id
	}))]
}), application = defineFetcher({
	name: "application",
	getKey: (_instance, applicationId, options) => `${applicationId}:${serializeInclude$1(options?.include) ?? ""}`,
	fetch: (instance) => (applicationId, options) => getClientState(instance, {
		apiVersion: "vX",
		scope: "global"
	}).observable.pipe(switchMap((client) => client.observable.request({
		url: `/applications/${applicationId}`,
		query: buildQuery({ include: serializeInclude$1(options?.include) }),
		tag: "applications.get"
	}))),
	tags: (data) => [{
		type: "application",
		id: data.id
	}]
}), updateApplication = defineMutation({
	name: "updateApplication",
	mutationFn: (instance) => ({ applicationId, ...body }) => getClientState(instance, {
		apiVersion: "vX",
		scope: "global"
	}).observable.pipe(switchMap((client) => client.observable.request({
		url: `/applications/${applicationId}`,
		method: "PATCH",
		body,
		tag: "applications.update"
	}))),
	invalidates: (_result, { applicationId }) => [{
		type: "application",
		id: "LIST"
	}, {
		type: "application",
		id: applicationId
	}]
}), deleteApplication = defineMutation({
	name: "deleteApplication",
	mutationFn: (instance) => ({ applicationId }) => getClientState(instance, {
		apiVersion: "vX",
		scope: "global"
	}).observable.pipe(switchMap((client) => client.observable.request({
		url: `/applications/${applicationId}`,
		method: "DELETE",
		tag: "applications.delete"
	}))),
	invalidates: (_result, { applicationId }) => [{
		type: "application",
		id: "LIST"
	}, {
		type: "application",
		id: applicationId
	}]
});
/**
* Compares a project's actual organization ID with the expected organization ID.
* @public
*/
function compareProjectOrganization(projectId, projectOrganizationId, currentDashboardOrgId) {
	return projectOrganizationId === currentDashboardOrgId ? { error: null } : { error: `Project ${projectId} belongs to Organization ${projectOrganizationId ?? "unknown"}, but the Dashboard has Organization ${currentDashboardOrgId} selected` };
}
function normalizeProjectsOptions(options) {
	return {
		organizationId: options?.organizationId,
		includeMembers: options?.includeMembers ?? !1,
		includeFeatures: options?.includeFeatures ?? !0,
		onlyExplicitMembership: options?.onlyExplicitMembership ?? !1
	};
}
/** @internal */
function getProjectsCacheKey(_instance, options) {
	let { organizationId, includeMembers, includeFeatures, onlyExplicitMembership } = normalizeProjectsOptions(options);
	return `projects${organizationId ? `:org:${organizationId}` : ""}${includeMembers ? ":members" : ""}${includeFeatures ? ":features" : ""}${onlyExplicitMembership ? ":explicit" : ""}`;
}
/**
* Fetcher for the current user's projects (`GET /projects`), on the shared
* fetcher cache.
*
* @internal
*/
const projects = defineFetcher({
	name: "projects",
	getKey: getProjectsCacheKey,
	fetch: (instance) => (options) => getClientState(instance, {
		apiVersion: "v2025-02-19",
		scope: "global"
	}).observable.pipe(switchMap((client) => client.observable.request({
		url: "/projects",
		query: buildQuery(normalizeProjectsOptions(options)),
		tag: "projects.list"
	}))),
	tags: (data) => [{
		type: "project",
		id: "LIST"
	}, ...data.map((project) => ({
		type: "project",
		id: project.id
	}))]
});
function normalizeProjectOptions(options) {
	return {
		includeMembers: options?.includeMembers ?? !0,
		includeFeatures: options?.includeFeatures ?? !0
	};
}
function resolveProjectId$1(instance, options) {
	let projectId = options?.projectId ?? instance.config.projectId;
	if (!projectId) throw Error("A projectId is required to use the project API.");
	return projectId;
}
/** @internal */
function getProjectCacheKey(instance, options) {
	let projectId = resolveProjectId$1(instance, options), { includeMembers, includeFeatures } = normalizeProjectOptions(options);
	return `project:${projectId}${includeMembers ? ":members" : ""}${includeFeatures ? ":features" : ""}`;
}
/**
* Fetcher for a single project (`GET /projects/:id`), on the shared fetcher
* cache. Pre-seeds from the default {@link projects} list entry when it can
* satisfy the read — the default list carries features but not members, so
* only `includeMembers: false` reads seed.
*
* @internal
*/
const project = defineFetcher({
	name: "project",
	getKey: getProjectCacheKey,
	fetch: (instance) => (options) => getClientState(instance, {
		apiVersion: "v2025-02-19",
		scope: "global"
	}).observable.pipe(switchMap((client) => client.observable.request({
		url: `/projects/${resolveProjectId$1(instance, options)}`,
		query: buildQuery(normalizeProjectOptions(options)),
		tag: "projects.get"
	}))),
	tags: (data) => [{
		type: "project",
		id: data.id
	}],
	initialData: (instance, options) => {
		if (normalizeProjectOptions(options).includeMembers) return;
		let snapshot = projects.getState(instance).getCurrent();
		if (snapshot.status !== "success") return;
		let projectId = resolveProjectId$1(instance, options), match = snapshot.data.find((item) => item.id === projectId);
		return match && {
			data: match,
			dataUpdatedAt: snapshot.dataUpdatedAt
		};
	}
}), getDashboardOrganizationId$1 = bindActionGlobally(authStore, createStateSourceAction(({ state: { dashboardContext } }) => dashboardContext?.orgId));
/**
* Creates an observable that emits the organization verification state for a given instance.
* It combines the dashboard organization ID (from auth context) with the
* project's actual organization ID (fetched via the project fetcher) and compares them.
* @public
*/
function observeOrganizationVerificationState(instance, projectIds) {
	let dashboardOrgId$ = getDashboardOrganizationId$1(instance).observable.pipe(distinctUntilChanged()), projectOrgIdObservables = projectIds.map((id) => project.getState(instance, { projectId: id }).observable.pipe(map((snapshot) => ({
		projectId: id,
		orgId: snapshot.data?.organizationId ?? null
	})), distinctUntilChanged((prev, curr) => prev.orgId === curr.orgId))), allProjectOrgIds$ = projectOrgIdObservables.length > 0 ? combineLatest(projectOrgIdObservables) : of([]);
	return combineLatest([dashboardOrgId$, allProjectOrgIds$]).pipe(switchMap(([dashboardOrgId, projectOrgDataArray]) => {
		if (!dashboardOrgId || projectOrgDataArray.length === 0) return of({ error: null });
		for (let projectData of projectOrgDataArray) {
			if (!projectData.orgId) continue;
			let result = compareProjectOrganization(projectData.projectId, projectData.orgId, dashboardOrgId);
			if (result.error) return of(result);
		}
		return of({ error: null });
	}), distinctUntilChanged((prev, curr) => prev.error === curr.error));
}
/**
* @public
*/
const handleAuthCallback = bindActionGlobally(authStore, async ({ state, instance }, locationHref = getDefaultLocation()) => {
	let logger = getAuthLogger(instance), { providedToken, callbackUrl, clientFactory, apiHost, storageArea, storageKey } = state.get().options;
	if (providedToken) return logger.debug("Skipping auth callback - token already provided"), !1;
	let { authState } = state.get();
	if (authState.type === "logging-in" && authState.isExchangingToken) return logger.debug("Skipping auth callback - token exchange already in progress"), !1;
	let cleanedUrl = getCleanedUrl(locationHref), tokenFromUrl = getTokenFromLocation(locationHref);
	if (tokenFromUrl) return logger.info("Auth token found in URL, logging in"), state.set("setTokenFromUrl", { authState: createLoggedInAuthState(tokenFromUrl, null) }), cleanedUrl;
	let authCode = getAuthCode(callbackUrl, locationHref);
	if (!authCode) return logger.debug("No auth code found in callback URL"), !1;
	let parsedUrl = new URL(locationHref), dashboardContext = {};
	try {
		let contextParam = parsedUrl.searchParams.get("_context");
		if (contextParam) {
			let parsedContext = JSON.parse(contextParam);
			parsedContext && typeof parsedContext == "object" && (delete parsedContext.sid, dashboardContext = parsedContext, logger.debug("Dashboard context parsed from callback URL", { hasDashboardContext: !0 }));
		}
	} catch (err) {
		logger.warn("Failed to parse dashboard context from callback URL", { error: err });
	}
	logger.info("Exchanging auth code for token"), state.set("exchangeSessionForToken", {
		authState: {
			type: "logging-in",
			isExchangingToken: !0
		},
		dashboardContext
	});
	try {
		let client = clientFactory({
			apiVersion: DEFAULT_API_VERSION,
			requestTagPrefix: REQUEST_TAG_PREFIX,
			useProjectHostname: !1,
			useCdn: !1,
			...apiHost && { apiHost }
		});
		logger.debug("Fetching token from auth endpoint");
		let { token } = await client.request({
			method: "GET",
			url: "/auth/fetch",
			query: { sid: authCode },
			tag: "fetch-token"
		});
		return logger.info("Auth token obtained successfully, user logged in"), storageArea?.setItem(storageKey, JSON.stringify({ token })), state.set("setToken", { authState: createLoggedInAuthState(token, null) }), cleanedUrl;
	} catch (error) {
		return logger.error("Failed to exchange auth code for token", { error }), state.set("exchangeSessionForTokenError", { authState: {
			type: "error",
			error
		} }), cleanedUrl;
	}
}), logout = bindActionGlobally(authStore, async ({ state, instance }) => {
	let logger = getAuthLogger(instance), { clientFactory, apiHost, providedToken, storageArea, storageKey } = state.get().options;
	if (providedToken) {
		logger.debug("Skipping logout - token is statically provided");
		return;
	}
	let { authState } = state.get();
	if (authState.type === "logged-out" && authState.isDestroyingSession) {
		logger.debug("Skipping logout - already in progress");
		return;
	}
	let token = authState.type === "logged-in" && authState.token;
	try {
		if (token) {
			logger.info("Logging out user"), state.set("loggingOut", { authState: {
				type: "logged-out",
				isDestroyingSession: !0
			} });
			let client = clientFactory({
				token,
				requestTagPrefix: REQUEST_TAG_PREFIX,
				apiVersion: DEFAULT_API_VERSION,
				...apiHost && { apiHost },
				useProjectHostname: !1,
				useCdn: !1
			});
			logger.debug("Calling logout endpoint"), await client.request({
				url: "/auth/logout",
				method: "POST",
				tag: "logout"
			});
		} else logger.debug("No token to logout - already logged out");
	} catch (error) {
		throw logger.error("Logout request failed", { error }), error;
	} finally {
		logger.info("User logged out, clearing stored tokens"), state.set("logoutSuccess", {
			authState: {
				type: "logged-out",
				isDestroyingSession: !1
			},
			oauthTokens: void 0
		}), storageArea?.removeItem(storageKey), storageArea?.removeItem(`${storageKey}_last_refresh`);
	}
});
/**
* Encodes a byte array as a base64url string (RFC 4648 §5) with no padding,
* as required for PKCE `code_verifier`/`code_challenge` values.
*
* @internal
*/
function base64UrlEncode(bytes) {
	let binary = "";
	for (let byte of bytes) binary += String.fromCharCode(byte);
	return btoa(binary).replace(/\+/g, "-").replace(/\//g, "_").replace(/=+$/, "");
}
function randomString() {
	let bytes = /* @__PURE__ */ new Uint8Array(32);
	return crypto.getRandomValues(bytes), base64UrlEncode(bytes);
}
/**
* Generates a PKCE `code_verifier`
*
* @internal
*/
function generateCodeVerifier() {
	return randomString();
}
/**
* Generates the `state` parameter used to defend against CSRF on the OAuth
* callback.
*
* @internal
*/
function generateState() {
	return randomString();
}
/**
* Derives the PKCE `code_challenge` from a `code_verifier` using
* `base64url(sha256(verifier))` (the `S256` method).
*
* @internal
*/
async function generateCodeChallenge(codeVerifier) {
	let data = new TextEncoder().encode(codeVerifier), digest = await crypto.subtle.digest("SHA-256", data);
	return base64UrlEncode(new Uint8Array(digest));
}
/** sessionStorage key for the PKCE `code_verifier`. */
const OAUTH_VERIFIER_KEY = "__sanity_oauth_verifier", OAUTH_STATE_KEY = "__sanity_oauth_state";
/** Builds the RFC 8707 resource indicator for an organisation. */
function getResourceIndicator(organizationId) {
	return `urn:io.sanity:organization:${organizationId}`;
}
/**
* Serialises tokens for storage, converting `expiresAt` to an ISO string.
*
* @internal
*/
function serializeTokens(tokens) {
	return JSON.stringify({
		accessToken: tokens.accessToken,
		tokenType: tokens.tokenType,
		expiresIn: tokens.expiresIn,
		expiresAt: tokens.expiresAt.toISOString(),
		...tokens.refreshToken !== void 0 && { refreshToken: tokens.refreshToken }
	});
}
/**
* Reads the store options, throwing when the instance was not configured for
* OAuth.
*/
function getOAuthOptions(state) {
	let { options } = state;
	if (!options.oauth) throw Error("OAuth is not configured on this instance (missing `auth.oauth`).");
	return options;
}
/** Creates a client for the (unauthenticated) public OAuth token/revoke calls. */
function createOAuthClient(options) {
	return options.clientFactory({
		apiVersion: "v1",
		requestTagPrefix: REQUEST_TAG_PREFIX,
		useProjectHostname: !1,
		useCdn: !1,
		...options.apiHost && { apiHost: options.apiHost }
	});
}
/** Converts a token endpoint response to the {@link OAuthTokens} shape. */
function toOAuthTokens(response) {
	return {
		accessToken: response.access_token,
		tokenType: "bearer",
		expiresIn: response.expires_in,
		expiresAt: new Date(Date.now() + response.expires_in * 1e3),
		...response.refresh_token !== void 0 && { refreshToken: response.refresh_token }
	};
}
/**
* Starts the OAuth authorization-code + PKCE flow: generates a `code_verifier`,
* `code_challenge` and `state`, persists the verifier and state to
* `sessionStorage`, then navigates the browser to the authorize endpoint.
*
* @public
*/
const startOAuthAuthorization = bindActionGlobally(authStore, async ({ state, instance }) => {
	let logger = getAuthLogger(instance), options = getOAuthOptions(state.get()), codeVerifier = generateCodeVerifier(), oauthState = generateState(), codeChallenge = await generateCodeChallenge(codeVerifier), session = typeof sessionStorage < "u" ? sessionStorage : void 0;
	session?.setItem(OAUTH_VERIFIER_KEY, codeVerifier), session?.setItem(OAUTH_STATE_KEY, oauthState);
	let authorizeUrl = new URL("/v1/auth/oauth/authorize", options.apiHost ?? "https://api.sanity.io");
	authorizeUrl.searchParams.set("response_type", "code"), authorizeUrl.searchParams.set("client_id", options.oauth.clientId), authorizeUrl.searchParams.set("redirect_uri", options.oauth.redirectUri), authorizeUrl.searchParams.set("state", oauthState), authorizeUrl.searchParams.set("code_challenge", codeChallenge), authorizeUrl.searchParams.set("code_challenge_method", "S256"), authorizeUrl.searchParams.append("resource", getResourceIndicator(options.oauth.organizationId)), logger.info("Starting OAuth authorization"), typeof window < "u" && typeof window.location?.assign == "function" && window.location.assign(authorizeUrl.toString());
}), handleOAuthCallback = bindActionGlobally(authStore, async ({ state, instance }, locationHref = getDefaultLocation()) => {
	let logger = getAuthLogger(instance), options = getOAuthOptions(state.get()), { authState } = state.get();
	if (authState.type === "logging-in" && authState.isExchangingToken) return logger.debug("Skipping OAuth callback - token exchange already in progress"), !1;
	let callbackUrl = new URL(locationHref, DEFAULT_BASE), code = callbackUrl.searchParams.get("code"), returnedState = callbackUrl.searchParams.get("state"), error = callbackUrl.searchParams.get("error"), errorDescription = callbackUrl.searchParams.get("error_description"), session = typeof sessionStorage < "u" ? sessionStorage : void 0, cleanedUrlObj = new URL(locationHref, DEFAULT_BASE);
	for (let param of [
		"code",
		"state",
		"error",
		"error_description"
	]) cleanedUrlObj.searchParams.delete(param);
	let cleanedUrl = cleanedUrlObj.toString();
	if (error) return logger.warn("OAuth callback returned an error", {
		error,
		errorDescription
	}), clearOAuthArtifacts(session), state.set("oauthCallbackError", { authState: {
		type: "error",
		error: Error(errorDescription ? `${error}: ${errorDescription}` : error)
	} }), cleanedUrl;
	if (!code) return logger.debug("No OAuth code found in callback URL"), !1;
	let storedState = session?.getItem("__sanity_oauth_state") ?? null;
	if (!returnedState || !storedState || returnedState !== storedState) return logger.error("OAuth state mismatch — rejecting callback"), clearOAuthArtifacts(session), state.set("oauthStateMismatch", { authState: {
		type: "error",
		error: /* @__PURE__ */ Error("OAuth state mismatch")
	} }), cleanedUrl;
	let codeVerifier = session?.getItem("__sanity_oauth_verifier") ?? null;
	if (!codeVerifier) return logger.error("OAuth code verifier missing — cannot exchange code"), clearOAuthArtifacts(session), state.set("oauthVerifierMissing", { authState: {
		type: "error",
		error: /* @__PURE__ */ Error("OAuth code verifier missing")
	} }), cleanedUrl;
	logger.info("Exchanging OAuth code for tokens"), state.set("oauthExchange", { authState: {
		type: "logging-in",
		isExchangingToken: !0
	} });
	try {
		let client = createOAuthClient(options), params = new URLSearchParams({
			grant_type: "authorization_code",
			code,
			code_verifier: codeVerifier,
			redirect_uri: options.oauth.redirectUri,
			client_id: options.oauth.clientId,
			resource: getResourceIndicator(options.oauth.organizationId)
		}), tokens = toOAuthTokens(await client.request({
			method: "POST",
			url: "/auth/oauth/token",
			headers: { "content-type": "application/x-www-form-urlencoded" },
			body: params.toString(),
			tag: "oauth.token"
		}));
		return options.storageArea?.setItem(options.storageKey, serializeTokens(tokens)), clearOAuthArtifacts(session), logger.info("OAuth tokens obtained, user logged in"), state.set("oauthLoggedIn", {
			authState: createLoggedInAuthState(tokens.accessToken, null),
			oauthTokens: tokens
		}), cleanedUrl;
	} catch (exchangeError) {
		return logger.error("Failed to exchange OAuth code for tokens", { error: exchangeError }), clearOAuthArtifacts(session), state.set("oauthExchangeError", { authState: {
			type: "error",
			error: exchangeError
		} }), cleanedUrl;
	}
});
/**
* A refresh failure is unrecoverable only when the token endpoint rejects the
* refresh token itself (a 4xx). Rate-limit (429) and request-timeout (408)
* responses, 5xx errors and network failures are transient, so the session is
* kept intact for the caller to retry rather than forcing a logout.
*/
function isUnrecoverableRefreshError(error) {
	return error instanceof ClientError && error.statusCode !== 408 && error.statusCode !== 429;
}
let refreshInFlight = null;
/**
* Refreshes the OAuth tokens using the `refresh_token` grant. Concurrent
* callers share a single in-flight request. An unrecoverable failure (a 4xx
* rejecting the refresh token) clears the tokens and transitions to
* `LOGGED_OUT`; transient failures (network, 5xx, rate limits) leave the
* session intact and rethrow so the caller can retry.
*
* @public
*/
const refreshOAuthTokens = bindActionGlobally(authStore, (context) => refreshInFlight || (refreshInFlight = doRefreshOAuthTokens(context).finally(() => {
	refreshInFlight = null;
}), refreshInFlight));
async function doRefreshOAuthTokens({ state, instance }) {
	let logger = getAuthLogger(instance), options = getOAuthOptions(state.get()), current = state.get().oauthTokens;
	if (!current?.refreshToken) return logger.warn("No refresh token available — logging out"), options.storageArea?.removeItem(options.storageKey), state.set("oauthRefreshNoToken", {
		authState: {
			type: "logged-out",
			isDestroyingSession: !1
		},
		oauthTokens: void 0
	}), null;
	try {
		let client = createOAuthClient(options), params = new URLSearchParams({
			grant_type: "refresh_token",
			refresh_token: current.refreshToken,
			client_id: options.oauth.clientId,
			resource: getResourceIndicator(options.oauth.organizationId)
		}), tokens = toOAuthTokens(await client.request({
			method: "POST",
			url: "/auth/oauth/token",
			headers: { "content-type": "application/x-www-form-urlencoded" },
			body: params.toString(),
			tag: "oauth.refresh"
		}));
		return tokens.refreshToken ||= current.refreshToken, options.storageArea?.setItem(options.storageKey, serializeTokens(tokens)), logger.info("OAuth tokens refreshed"), state.set("oauthRefreshed", {
			authState: createLoggedInAuthState(tokens.accessToken, null),
			oauthTokens: tokens
		}), tokens;
	} catch (error) {
		throw isUnrecoverableRefreshError(error) ? (logger.error("OAuth token refresh failed — logging out", { error }), options.storageArea?.removeItem(options.storageKey), state.set("oauthRefreshFailed", {
			authState: {
				type: "logged-out",
				isDestroyingSession: !1
			},
			oauthTokens: void 0
		}), error) : (logger.warn("OAuth token refresh failed — keeping session for retry", { error }), error);
	}
}
/**
* Revokes the OAuth tokens then clears local storage and transitions to `LOGGED_OUT`.
* Local state is cleared even if the revoke request fails.
*
* @public
*/
const revokeOAuthTokens = bindActionGlobally(authStore, async ({ state, instance }) => {
	let logger = getAuthLogger(instance), options = getOAuthOptions(state.get()), current = state.get().oauthTokens, token = current?.refreshToken ?? current?.accessToken;
	try {
		if (token) {
			let client = createOAuthClient(options), params = new URLSearchParams({
				token,
				client_id: options.oauth.clientId
			});
			await client.request({
				method: "POST",
				url: "/auth/oauth/revoke",
				headers: { "content-type": "application/x-www-form-urlencoded" },
				body: params.toString(),
				tag: "oauth.revoke"
			}), logger.info("OAuth tokens revoked");
		}
	} catch (error) {
		logger.warn("OAuth revoke request failed — clearing local state anyway", { error });
	} finally {
		options.storageArea?.removeItem(options.storageKey), state.set("oauthRevoked", {
			authState: {
				type: "logged-out",
				isDestroyingSession: !1
			},
			oauthTokens: void 0
		});
	}
}), getOAuthTokensState = bindActionGlobally(authStore, createStateSourceAction(({ state }) => state.oauthTokens ?? null));
/** Removes the transient PKCE artifacts from session storage. */
function clearOAuthArtifacts(session) {
	session?.removeItem(OAUTH_VERIFIER_KEY), session?.removeItem(OAUTH_STATE_KEY);
}
function weaken(node) {
	return Array.isArray(node) ? node.map(weaken) : node && typeof node == "object" ? "_ref" in node ? {
		...node,
		_weak: !0
	} : Object.fromEntries(Object.entries(node).map(([key, value]) => [key, weaken(value)])) : node;
}
/**
* Marks every reference in a content snapshot weak.
*
* A comment's snapshot is written into the addon dataset, but its references
* point at documents in the main one. Left strong, Content Lake would refuse to
* delete any document a snapshot happens to mention, so an old comment could
* pin a document forever.
*
* @internal
*/
function weakenReferencesInContentSnapshot(snapshot) {
	return weaken(snapshot);
}
/**
* Refuses to write a comment that points at nothing.
*
* The type already requires `fieldPath`, but an empty string or an empty path
* array both survive that and normalise to `''`. Such a comment is not merely
* useless: the Studio's comment inspector calls `fromString` on the stored
* path, which throws on `''`, so the inspector crashes for everyone viewing
* that document until the comment is deleted. Cheaper to refuse the write.
*/
function requireFieldPath(fieldPath) {
	let normalized = toCommentFieldPath(fieldPath);
	if (!normalized) throw Error("A comment needs a field path. Comments attach to a field, not to a document as a whole.");
	return normalized;
}
function requireCurrentUserId(instance) {
	let userId = getCurrentUserState(instance).getCurrent()?.id;
	if (!userId) throw Error("Writing a comment requires a logged in user.");
	return userId;
}
/** Waits out discovery, then insists the addon dataset exists. */
async function requireAddonDataset(instance, resource) {
	let datasetName = await firstValueFrom(getAddonDatasetState(instance, { resource }).observable.pipe(filter((value) => value !== void 0), take(1)));
	if (!datasetName) throw Error("This project has no comments dataset, so there is no comment to change.");
	return datasetName;
}
async function getWritableClient(instance, resource, { createIfMissing }) {
	let { projectId } = assertDatasetResource(resource), dataset = createIfMissing ? await provisionAddonDataset(instance, { resource }) : await requireAddonDataset(instance, resource);
	return getClient(instance, {
		apiVersion: COMMENTS_API_VERSION,
		projectId,
		dataset
	});
}
function buildCommentPayload(options) {
	let { handle, resource } = options;
	return {
		_id: options.commentId,
		_type: "comment",
		authorId: options.authorId,
		message: options.message,
		threadId: options.threadId,
		...options.parentCommentId ? { parentCommentId: options.parentCommentId } : {},
		status: options.status,
		reactions: null,
		context: {
			tool: "",
			...options.context
		},
		...options.contentSnapshot === void 0 ? {} : { contentSnapshot: weakenReferencesInContentSnapshot(options.contentSnapshot) },
		target: {
			documentRevisionId: options.documentRevisionId ?? "",
			path: {
				field: options.fieldPath,
				...options.selection ? { selection: options.selection } : {}
			},
			document: {
				_dataset: resource.dataset,
				_projectId: resource.projectId,
				_ref: getPublishedId(DocumentId(handle.documentId)),
				_type: "crossDatasetReference",
				_weak: !0
			},
			documentType: handle.documentType,
			...options.documentVersionId ? { documentVersionId: options.documentVersionId } : {}
		}
	};
}
function findComment(state, commentsKey, commentId) {
	let comments = state.get().entries[commentsKey]?.comments;
	return comments && Object.hasOwn(comments, commentId) ? comments[commentId] : void 0;
}
function findCommentById(state, commentId) {
	for (let entry of Object.values(state.get().entries)) {
		let comments = entry?.comments, comment = comments && Object.hasOwn(comments, commentId) ? comments[commentId] : void 0;
		if (comment) return comment;
	}
}
async function postComment(context, handle, payload) {
	let { state, instance, key } = context, commentsKey = getCommentsKey(toCommentsKeyParts(instance, handle));
	state.set("addComment", addComment(commentsKey, payload));
	try {
		let created = await (await getWritableClient(instance, key.resource, { createIfMissing: !0 })).createIfNotExists(payload, { tag: "comments.create" });
		return state.set("receiveComment", receiveComment(commentsKey, created)), normalizeComment(created);
	} catch (error) {
		throw state.set("setCommentCreateError", setCommentCreateError(payload._id, error instanceof Error ? error : Error(String(error)))), error;
	}
}
/**
* Starts a thread on a document, or on one of its fields.
*
* Creates the project's comments dataset if this is the first comment anywhere
* in it. The comment appears locally before the server confirms it; if the
* write fails it stays, carrying `_state.createError`, and retrying with the
* same `commentId` replaces it.
*
* @beta
*/
const createComment = bindActionByResource(commentsStore, async (context, options) => {
	let { instance, key } = context, perspective = options.perspective ?? instance.config.perspective, fieldPath = requireFieldPath(options.fieldPath);
	return postComment(context, options, buildCommentPayload({
		authorId: requireCurrentUserId(instance),
		commentId: options.commentId ?? randomUuid(),
		contentSnapshot: options.contentSnapshot,
		context: options.context,
		documentRevisionId: options.documentRevisionId,
		handle: options,
		message: options.message,
		resource: assertDatasetResource(key.resource),
		fieldPath,
		selection: options.selection,
		status: options.status ?? "open",
		threadId: options.threadId ?? randomUuid(),
		...isReleasePerspective(perspective) ? { documentVersionId: perspective.releaseName } : {}
	}));
}), replyToComment = bindActionByResource(commentsStore, async (context, options) => {
	let { instance, key, state } = context, parent = findComment(state, getCommentsKey(toCommentsKeyParts(instance, options)), options.parentCommentId), threadId = options.threadId ?? parent?.threadId;
	if (!threadId) throw Error(`Cannot reply to "${options.parentCommentId}": it is not loaded, so pass its threadId.`);
	let status = parent?.status ?? options.status;
	if (!status) throw Error(`Cannot reply to "${options.parentCommentId}": it is not loaded, so pass its status.`);
	let perspective = options.perspective ?? instance.config.perspective;
	return postComment(context, options, buildCommentPayload({
		authorId: requireCurrentUserId(instance),
		commentId: options.commentId ?? randomUuid(),
		handle: options,
		message: options.message,
		parentCommentId: parent?.parentCommentId ?? options.parentCommentId,
		resource: assertDatasetResource(key.resource),
		fieldPath: requireFieldPath(options.fieldPath ?? parent?.target.path?.field ?? ""),
		status,
		threadId,
		...isReleasePerspective(perspective) ? { documentVersionId: perspective.releaseName } : {}
	}));
}), updateComment = bindActionByResource(commentsStore, async ({ state, instance, key }, { commentId, message }) => {
	let lastEditedAt = (/* @__PURE__ */ new Date()).toISOString(), transactionId = randomUuid(), previous = findCommentById(state, commentId);
	previous && (state.set("setPendingTransaction", setPendingTransaction(commentId, transactionId)), state.set("updateComment", applyCommentUpdate(commentId, {
		message,
		lastEditedAt
	})));
	try {
		let client = await getWritableClient(instance, key.resource, { createIfMissing: !1 });
		await client.transaction().transactionId(transactionId).patch(client.patch(commentId).set({
			message,
			lastEditedAt
		})).commit({ tag: "comments.update" }), previous && state.set("clearPendingTransaction", clearPendingTransaction(commentId));
	} catch (error) {
		throw previous && state.set("rollbackCommentUpdate", rollbackCommentUpdate(commentId, transactionId, previous)), error;
	}
}), setCommentStatus = bindActionByResource(commentsStore, async ({ state, instance, key }, { commentId, status }) => {
	let transactionId = randomUuid(), previousComments = [];
	for (let entry of Object.values(state.get().entries)) for (let candidate of Object.values(entry?.comments ?? {})) (candidate._id === commentId || candidate.parentCommentId === commentId) && (previousComments.push(candidate), state.set("setPendingTransaction", setPendingTransaction(candidate._id, transactionId)), state.set("updateCommentStatus", applyCommentUpdate(candidate._id, { status })));
	try {
		await (await getWritableClient(instance, key.resource, { createIfMissing: !1 })).mutate([{ patch: {
			id: commentId,
			set: { status }
		} }, { patch: {
			query: "*[_type == \"comment\" && parentCommentId == $commentId]",
			params: { commentId },
			set: { status }
		} }], {
			transactionId,
			tag: "comments.set-status"
		});
		for (let previous of previousComments) state.set("clearPendingTransaction", clearPendingTransaction(previous._id));
	} catch (error) {
		for (let previous of previousComments) state.set("rollbackCommentUpdate", rollbackCommentUpdate(previous._id, transactionId, previous));
		throw error;
	}
}), removeComment = bindActionByResource(commentsStore, async ({ state, instance, key }, { commentId }) => {
	let removed = Object.entries(state.get().entries).flatMap(([entryKey, entry]) => {
		let comments = Object.values(entry?.comments ?? {}).filter((comment) => comment._id === commentId || comment.parentCommentId === commentId);
		return comments.length ? [{
			key: entryKey,
			comments
		}] : [];
	});
	state.set("removeComment", removeCommentById(commentId));
	try {
		await (await getWritableClient(instance, key.resource, { createIfMissing: !1 })).mutate([{ delete: {
			query: "*[_type == \"comment\" && parentCommentId == $commentId]",
			params: { commentId }
		} }, { delete: { id: commentId } }], { tag: "comments.remove" });
	} catch (error) {
		throw state.set("restoreComments", restoreComments(removed)), error;
	}
});
/**
* Creates or validates a `DocumentHandle` object.
* Ensures the provided object conforms to the `DocumentHandle` interface.
* @param handle - The object containing document identification properties.
* @returns The validated `DocumentHandle` object.
* @public
*/
function createDocumentHandle(handle) {
	return handle;
}
/**
* Creates or validates a `DocumentTypeHandle` object.
* Ensures the provided object conforms to the `DocumentTypeHandle` interface.
* @param handle - The object containing document type identification properties.
* @returns The validated `DocumentTypeHandle` object.
* @public
*/
function createDocumentTypeHandle(handle) {
	return handle;
}
/**
* Creates or validates a `ProjectHandle` object.
* Ensures the provided object conforms to the `ProjectHandle` interface.
* @param handle - The object containing project identification properties.
* @returns The validated `ProjectHandle` object.
* @public
*/
function createProjectHandle(handle) {
	return handle;
}
/**
* Creates or validates a `DatasetHandle` object.
* Ensures the provided object conforms to the `DatasetHandle` interface.
* @param handle - The object containing dataset identification properties.
* @returns The validated `DatasetHandle` object.
* @public
*/
function createDatasetHandle(handle) {
	return handle;
}
/**
* Public API for configuring SDK logging
*
* @module loggingConfig
*/
/**
* Configure logging for the Sanity SDK
*
* This function allows you to control what logs are output by the SDK,
* making it easier to debug issues in development or production.
*
* @remarks
* **Zero-Config via Environment Variable (Recommended):**
*
* The SDK automatically reads the `DEBUG` environment variable, making it
* easy to enable logging without code changes:
*
* ```bash
* # Enable all SDK logging at debug level
* DEBUG=sanity:* npm start
*
* # Enable specific namespaces
* DEBUG=sanity:auth,sanity:document npm start
*
* # Enable trace level for all namespaces
* DEBUG=sanity:trace:* npm start
*
* # Enable internal/maintainer logs
* DEBUG=sanity:*:internal npm start
* ```
*
* This matches the pattern used by Sanity CLI and Studio, making it familiar
* and easy for support teams to help troubleshoot issues.
*
* **Programmatic Configuration (Advanced):**
*
* For more control (custom handlers, dynamic configuration), call this function
* explicitly. Programmatic configuration overrides environment variables.
*
* **For Application Developers:**
* Use `info`, `warn`, or `error` levels to see high-level SDK activity
* without being overwhelmed by internal details.
*
* **For SDK Maintainers:**
* Use `debug` or `trace` levels with `internal: true` to see detailed
* information about store operations, RxJS streams, and state transitions.
*
* **Instance Context:**
* Logs automatically include instance information (projectId, dataset, instanceId)
* when available, making it easier to debug multi-instance scenarios:
* ```
* [INFO] [auth] [project:abc] [dataset:production] User logged in
* ```
*
* **Available Namespaces:**
* - `sdk` - SDK initialization, configuration, and lifecycle
* - `auth` - Authentication and authorization (when instrumented in the future)
* - And more as logging is added to modules
*
* @example Zero-config via environment variable (recommended for debugging)
* ```bash
* # Just set DEBUG and run your app - no code changes needed!
* DEBUG=sanity:* npm start
* ```
*
* @example Programmatic configuration (application developer)
* ```ts
* import {configureLogging} from '@sanity/sdk'
*
* // Log warnings and errors for auth and document operations
* configureLogging({
*   level: 'warn',
*   namespaces: ['auth', 'document']
* })
* ```
*
* @example Programmatic configuration (SDK maintainer)
* ```ts
* import {configureLogging} from '@sanity/sdk'
*
* // Enable all logs including internal traces
* configureLogging({
*   level: 'trace',
*   namespaces: ['*'],
*   internal: true
* })
* ```
*
* @example Custom handler (for testing)
* ```ts
* import {configureLogging} from '@sanity/sdk'
*
* const logs: string[] = []
* configureLogging({
*   level: 'info',
*   namespaces: ['*'],
*   handler: {
*     error: (msg) => logs.push(msg),
*     warn: (msg) => logs.push(msg),
*     info: (msg) => logs.push(msg),
*     debug: (msg) => logs.push(msg),
*     trace: (msg) => logs.push(msg),
*   }
* })
* ```
*
* @public
*/
function configureLogging(config) {
	configureLogging$1(config);
	let configLevel = config.level || "warn", shouldLog = [
		"info",
		"debug",
		"trace"
	].includes(configLevel) || configLevel === "warn";
	shouldLog && config.handler?.info ? config.handler.info(`[${(/* @__PURE__ */ new Date()).toISOString()}] [INFO] [sdk] Logging configured`, {
		level: configLevel,
		namespaces: config.namespaces || [],
		internal: config.internal || !1,
		source: "programmatic"
	}) : shouldLog && console.info(`[${(/* @__PURE__ */ new Date()).toISOString()}] [INFO] [sdk] Logging configured`, {
		level: configLevel,
		namespaces: config.namespaces || [],
		internal: config.internal || !1,
		source: "programmatic"
	});
}
function resolveProjectId(instance, options) {
	let projectId = options?.projectId ?? instance.config.projectId;
	if (!projectId) throw Error("A projectId is required to use the datasets API.");
	return projectId;
}
/**
* Fetcher for a project's datasets (`GET /datasets`), on the shared fetcher
* cache.
*
* @internal
*/
const datasets = defineFetcher({
	name: "datasets",
	getKey: resolveProjectId,
	fetch: (instance) => (options) => getClientState(instance, {
		apiVersion: "v2025-02-19",
		projectId: resolveProjectId(instance, options),
		useProjectHostname: !0
	}).observable.pipe(switchMap((client) => client.observable.datasets.list()))
});
/**
* The id of the specific document a user is editing under a given perspective:
* the draft, the published document, or a release version.
*
* Distinct from {@link getEffectiveDocumentId}, which resolves the canonical key a
* document's state is stored under and so collapses a draft onto its published id.
* This one keeps them apart, because some things compare the exact id. Presence is
* the motivating case: the Studio's field indicators match the id its form is on,
* exactly, so an app reporting the published id shows up in the Studio at document
* level while never lighting up a single field.
*
* @remarks
* Resolved from the handle alone, which is right whenever a draft or version
* exists, so for any document actually being edited. The Studio additionally falls
* back to whichever document exists, so for a published document nobody has edited
* yet its form sits on the published id where this returns the draft id. Matching
* that would mean reading document state, which presence deliberately does not
* depend on.
*
* @internal
*/
function getEditingDocumentId(doc) {
	return doc.liveEdit ? getPublishedId(DocumentId(doc.documentId)) : isReleasePerspective(doc.perspective) ? getVersionId(DocumentId(doc.documentId), doc.perspective.releaseName) : doc.perspective === "published" ? getPublishedId(DocumentId(doc.documentId)) : getDraftId(DocumentId(doc.documentId));
}
function getEffectiveDocumentId(doc) {
	return doc.liveEdit ? doc.documentId : isReleasePerspective(doc.perspective) ? getVersionId(DocumentId(doc.documentId), doc.perspective.releaseName) : getPublishedId(DocumentId(doc.documentId));
}
const isSanityMutatePatch = (value) => !(typeof value != "object" || !value || !("type" in value) || typeof value.type != "string" || value.type !== "patch" || !("id" in value) || typeof value.id != "string" || !("patches" in value) || !Array.isArray(value.patches));
/**
* Creates a `CreateDocumentAction` object.
* @param doc - A handle identifying the document type, dataset, and project. An optional `documentId` can be provided.
* @param initialValue - Optional initial field values for the document. (System fields are omitted as they are set automatically.)
* @returns A `CreateDocumentAction` object ready for dispatch.
* @beta
*/
function createDocument(doc, initialValue) {
	let effectiveDocumentId;
	return typeof doc.documentId == "string" && (effectiveDocumentId = getEffectiveDocumentId({
		...doc,
		documentId: doc.documentId
	})), {
		type: "document.create",
		...doc,
		...effectiveDocumentId && { documentId: effectiveDocumentId },
		...initialValue && { initialValue }
	};
}
/**
* Creates a `DeleteDocumentAction` object.
* @param doc - A handle uniquely identifying the document to be deleted.
* @returns A `DeleteDocumentAction` object ready for dispatch.
* @beta
*/
function deleteDocument(doc) {
	let effectiveDocumentId = getEffectiveDocumentId(doc);
	return {
		type: "document.delete",
		...doc,
		documentId: effectiveDocumentId
	};
}
function convertSanityMutatePatch(sanityPatchMutation) {
	return SanityEncoder.encode(sanityPatchMutation).map((i) => {
		let copy = { ...i.patch };
		return "id" in copy && delete copy.id, copy;
	});
}
/**
* Creates an `EditDocumentAction` object with patches for modifying a document.
* This is the implementation signature and handles the different patch input types.
*
* @param doc - A handle uniquely identifying the document to be edited.
* @param patches - Patches in various formats (`PatchOperations`, `PatchOperations[]`, or `SanityMutatePatchMutation`).
* @param options - Options controlling how the patches are processed.
* @returns An `EditDocumentAction` object ready for dispatch.
*/
function editDocument(doc, patches, options) {
	let effectiveDocumentId = getEffectiveDocumentId(doc), preserveOperations = options?.preserveOperations && { preserveOperations: !0 };
	if (isSanityMutatePatch(patches)) {
		let converted = convertSanityMutatePatch(patches) ?? [];
		return {
			...doc,
			type: "document.edit",
			documentId: effectiveDocumentId,
			patches: converted,
			...preserveOperations
		};
	}
	return {
		...doc,
		type: "document.edit",
		documentId: effectiveDocumentId,
		...patches && { patches: Array.isArray(patches) ? patches : [patches] },
		...preserveOperations
	};
}
/**
* Creates a `PublishDocumentAction` object.
* @param doc - A handle uniquely identifying the document to be published.
* @returns A `PublishDocumentAction` object ready for dispatch.
* @beta
*/
function publishDocument(doc) {
	let effectiveDocumentId = getEffectiveDocumentId(doc);
	return {
		type: "document.publish",
		...doc,
		documentId: effectiveDocumentId
	};
}
/**
* Creates an `UnpublishDocumentAction` object.
* @param doc - A handle uniquely identifying the document to be unpublished.
* @returns An `UnpublishDocumentAction` object ready for dispatch.
* @beta
*/
function unpublishDocument(doc) {
	let effectiveDocumentId = getEffectiveDocumentId(doc);
	return {
		type: "document.unpublish",
		...doc,
		documentId: effectiveDocumentId
	};
}
/**
* Creates a `DiscardDocumentAction` object.
* @param doc - A handle uniquely identifying the document whose draft changes are to be discarded.
* @returns A `DiscardDocumentAction` object ready for dispatch.
* @beta
*/
function discardDocument(doc) {
	let effectiveDocumentId = getEffectiveDocumentId(doc);
	return {
		type: "document.discard",
		...doc,
		documentId: effectiveDocumentId
	};
}
/** @beta */
function createRelease(handle, metadata = { releaseType: "undecided" }) {
	return {
		type: "release.create",
		...handle,
		metadata
	};
}
/** @beta */
function editRelease(handle, patch) {
	return {
		type: "release.edit",
		...handle,
		patch
	};
}
/** @beta */
function publishRelease(handle) {
	return {
		type: "release.publish",
		...handle
	};
}
/** @beta */
function scheduleRelease(handle, publishAt) {
	return {
		type: "release.schedule",
		...handle,
		publishAt
	};
}
/** @beta */
function unscheduleRelease(handle) {
	return {
		type: "release.unschedule",
		...handle
	};
}
/** @beta */
function archiveRelease(handle) {
	return {
		type: "release.archive",
		...handle
	};
}
/** @beta */
function unarchiveRelease(handle) {
	return {
		type: "release.unarchive",
		...handle
	};
}
/** @beta */
function deleteRelease(handle) {
	return {
		type: "release.delete",
		...handle
	};
}
/**
* When a document has no more subscribers, its state is cleaned up and removed
* from the store. A delay used to prevent re-creating resources when the last
* subscriber is removed quickly before another one is added. This is helpful
* when used in a frontend where components may suspend or transition to
* different views quickly.
*/
const API_VERSION$4 = "v2025-05-06";
/**
* Returns the full release document ID for the given release name.
* e.g. `getReleaseDocumentId('my-release') === '_.releases.my-release'`
* @beta
*/
function getReleaseDocumentId(releaseId) {
	return `_.releases.${releaseId}`;
}
const RELEASE_ACTION_TYPES = /* @__PURE__ */ new Set([
	"release.create",
	"release.edit",
	"release.publish",
	"release.schedule",
	"release.unschedule",
	"release.archive",
	"release.unarchive",
	"release.delete"
]);
function isReleaseAction(action) {
	return RELEASE_ACTION_TYPES.has(action.type);
}
function generateArrayKey(length = 12) {
	let numBytes = Math.ceil(length / 2), bytes = crypto.getRandomValues(new Uint8Array(numBytes));
	return Array.from(bytes, (byte) => byte.toString(16).padStart(2, "0")).join("").slice(0, length);
}
function memoize(fn) {
	let cache = /* @__PURE__ */ new WeakMap();
	return ((input) => {
		if (!input || typeof input != "object") return fn(input);
		let cached = cache.get(input);
		if (cached) return cached;
		let result = fn(input);
		return cache.set(input, result), result;
	});
}
/**
* Recursively traverse a value. When an array is encountered, ensure that
* each object item has a _key property. Memoized such that sub-objects that
* have not changed aren't re-computed.
*/
const ensureArrayKeysDeep = memoize((input) => {
	if (!input || typeof input != "object") return input;
	if (Array.isArray(input)) return !input.length || typeof input[0] != "object" || input.every(isKeyedObject) ? input : input.map((item) => !item || typeof item != "object" ? item : isKeyedObject(item) ? ensureArrayKeysDeep(item) : {
		...ensureArrayKeysDeep(item),
		_key: generateArrayKey()
	});
	let entries = Object.entries(input).map(([key, value]) => [key, ensureArrayKeysDeep(value)]);
	return entries.every(([key, value]) => input[key] === value) ? input : Object.fromEntries(entries);
});
/**
* Whether every array-addressing segment (keyed `[_key=="…"]` or numeric
* index) in the given match path resolves to an existing array item.
*
* This mirrors Content Lake, which auto-creates missing *object* properties
* when setting but never creates *array items*:
*
* ```js
* // object properties are created, even nested ones:
* set({}, {'meta.author.name': 'Ada'})
* // -> {meta: {author: {name: 'Ada'}}}                    (applies)
*
* // array items are not:
* set({items: [{_key: 'a'}]}, {'items[_key=="b"].title': 'x'})
* // -> {items: [{_key: 'a'}]}                             (no-op)
* ```
*
* Without this filter, the second example fabricated a degenerate item
* (`{title: 'x'}`, no `_key`) that would never exist on the server, which
* is how concurrent Portable Text edits grew phantom span-less children.
*/
function resolvesArraySegments(input, path) {
	let current = input;
	for (let segment of path) {
		if (typeof segment == "string") {
			current = typeof current == "object" && current && !Array.isArray(current) ? current[segment] : void 0;
			continue;
		}
		if (!Array.isArray(current)) return !1;
		if (isKeySegment(segment)) {
			let index = getIndexForKey$1(current, segment._key);
			if (index === void 0) return !1;
			current = current[index];
		} else {
			let index = segment < 0 ? current.length + segment : segment;
			if (!(index in current)) return !1;
			current = current[index];
		}
	}
	return !0;
}
function set(input, pathExpressionValues) {
	let result = Object.entries(pathExpressionValues).flatMap(([pathExpression, replacementValue]) => Array.from(jsonMatch$1(input, pathExpression)).map((matchEntry) => ({
		...matchEntry,
		replacementValue
	}))).filter(({ path }) => resolvesArraySegments(input, path)).reduce((acc, { path, replacementValue }) => setDeep(acc, path, replacementValue), input);
	return ensureArrayKeysDeep(result);
}
function setIfMissing(input, pathExpressionValues) {
	let result = Object.entries(pathExpressionValues).flatMap(([pathExpression, replacementValue]) => Array.from(jsonMatch$1(input, pathExpression)).map((matchEntry) => ({
		...matchEntry,
		replacementValue
	}))).filter((matchEntry) => matchEntry.value === null || matchEntry.value === void 0).filter(({ path }) => resolvesArraySegments(input, path)).reduce((acc, { path, replacementValue }) => setDeep(acc, path, replacementValue), input);
	return ensureArrayKeysDeep(result);
}
function unset(input, pathExpressions) {
	let result = pathExpressions.flatMap((pathExpression) => Array.from(jsonMatch$1(input, pathExpression))).reverse().reduce((acc, { path }) => unsetDeep(acc, path), input);
	return ensureArrayKeysDeep(result);
}
function insert(input, { items, ...insertPatch }) {
	let operation, pathExpression;
	if ("before" in insertPatch ? (operation = "before", pathExpression = insertPatch.before) : "after" in insertPatch ? (operation = "after", pathExpression = insertPatch.after) : "replace" in insertPatch && (operation = "replace", pathExpression = insertPatch.replace), !operation || typeof pathExpression != "string" || !pathExpression.length) return input;
	let arrayPath = slicePath$1(pathExpression, 0, -1), positionPath = slicePath$1(pathExpression, -1), result = input;
	for (let { path, value } of jsonMatch$1(input, arrayPath)) {
		if (!Array.isArray(value)) continue;
		let arr = value;
		switch (operation) {
			case "replace": {
				let indexesToRemove = /* @__PURE__ */ new Set(), position = Infinity;
				for (let itemMatch of jsonMatch$1(arr, positionPath)) {
					if (itemMatch.path.length !== 1) continue;
					let [segment] = itemMatch.path;
					if (typeof segment == "string") continue;
					let index;
					typeof segment == "number" && (index = segment), typeof index == "number" && index < 0 && (index = arr.length + index), isKeySegment(segment) && (index = getIndexForKey$1(arr, segment._key)), typeof index == "number" && (index < 0 && (index = arr.length + index), indexesToRemove.add(index), index < position && (position = index));
				}
				if (position === Infinity) continue;
				arr = arr.map((item, index) => ({
					item,
					index
				})).filter(({ index }) => !indexesToRemove.has(index)).map(({ item }) => item), arr = [
					...arr.slice(0, position),
					...items,
					...arr.slice(position, arr.length)
				];
				break;
			}
			case "before": {
				let position = Infinity;
				for (let itemMatch of jsonMatch$1(arr, positionPath)) {
					if (itemMatch.path.length !== 1) continue;
					let [segment] = itemMatch.path;
					if (typeof segment == "string") continue;
					let index;
					typeof segment == "number" && (index = segment), typeof index == "number" && index < 0 && (index = arr.length + index), isKeySegment(segment) && (index = getIndexForKey$1(arr, segment._key)), typeof index == "number" && (index < 0 && (index = arr.length - index), index < position && (position = index));
				}
				if (position === Infinity) continue;
				arr = [
					...arr.slice(0, position),
					...items,
					...arr.slice(position, arr.length)
				];
				break;
			}
			case "after": {
				let position = -Infinity;
				for (let itemMatch of jsonMatch$1(arr, positionPath)) {
					if (itemMatch.path.length !== 1) continue;
					let [segment] = itemMatch.path;
					if (typeof segment == "string") continue;
					let index;
					typeof segment == "number" && (index = segment), typeof index == "number" && index < 0 && (index = arr.length + index), isKeySegment(segment) && (index = getIndexForKey$1(arr, segment._key)), typeof index == "number" && index > position && (position = index);
				}
				if (position === -Infinity) continue;
				arr = [
					...arr.slice(0, position + 1),
					...items,
					...arr.slice(position + 1, arr.length)
				];
				break;
			}
			default: continue;
		}
		result = setDeep(result, path, arr);
	}
	return ensureArrayKeysDeep(result);
}
function inc(input, pathExpressionValues) {
	let result = Object.entries(pathExpressionValues).flatMap(([pathExpression, valueToAdd]) => Array.from(jsonMatch$1(input, pathExpression)).map((matchEntry) => ({
		...matchEntry,
		valueToAdd
	}))).filter((matchEntry) => typeof matchEntry.value == "number").reduce((acc, { path, value, valueToAdd }) => setDeep(acc, path, value + valueToAdd), input);
	return ensureArrayKeysDeep(result);
}
function dec(input, pathExpressionValues) {
	let result = inc(input, Object.fromEntries(Object.entries(pathExpressionValues).filter(([, value]) => typeof value == "number").map(([key, value]) => [key, -value])));
	return ensureArrayKeysDeep(result);
}
function diffMatchPatch(input, pathExpressionValues) {
	let result = Object.entries(pathExpressionValues).flatMap(([pathExpression, dmp]) => Array.from(jsonMatch$1(input, pathExpression)).map((m) => ({
		...m,
		dmp
	}))).filter((i) => i.value !== void 0).map(({ path, value, dmp }) => {
		if (typeof value != "string") throw Error(`Can't diff-match-patch \`${JSON.stringify(value)}\` at path \`${stringifyPath$1(path)}\`, because it is not a string`);
		let [nextValue] = applyPatches(parsePatch(dmp), value, { allowExceedingIndices: !0 });
		return {
			path,
			value: nextValue
		};
	}).reduce((acc, { path, value }) => setDeep(acc, path, value), input);
	return ensureArrayKeysDeep(result);
}
function ifRevisionID(input, revisionId) {
	let inputRev = typeof input == "object" && input && "_rev" in input && typeof input._rev == "string" ? input._rev : void 0;
	if (typeof inputRev != "string") throw Error("Patch specified `ifRevisionID` but could not find document's revision ID.");
	if (revisionId !== inputRev) throw Error(`Patch's \`ifRevisionID\` \`${revisionId}\` does not match document's revision ID \`${inputRev}\``);
	return input;
}
function setDeep(input, path, value) {
	let [currentSegment, ...restOfPath] = path;
	if (currentSegment === void 0) return value;
	if (typeof input != "object" || !input) {
		if (typeof currentSegment == "string") return { [currentSegment]: setDeep(null, restOfPath, value) };
		let index;
		if (isKeySegment(currentSegment)) index = 0;
		else if (typeof currentSegment == "number" && currentSegment >= 0) index = currentSegment;
		else return input;
		return [...Array.from({ length: index }).fill(null), setDeep(null, restOfPath, value)];
	}
	if (Array.isArray(input)) {
		let index;
		return isKeySegment(currentSegment) ? index = getIndexForKey$1(input, currentSegment._key) ?? input.length : typeof currentSegment == "number" && (index = currentSegment < 0 ? input.length + currentSegment : currentSegment), index === void 0 ? input : index in input ? input.map((nestedInput, i) => i === index ? setDeep(nestedInput, restOfPath, value) : nestedInput) : [
			...input,
			...Array.from({ length: index - input.length }).fill(null),
			setDeep(null, restOfPath, value)
		];
	}
	return typeof currentSegment == "object" ? input : currentSegment in input ? Object.fromEntries(Object.entries(input).map(([key, nestedInput]) => key === currentSegment ? [key, setDeep(nestedInput, restOfPath, value)] : [key, nestedInput])) : {
		...input,
		[currentSegment]: setDeep(null, restOfPath, value)
	};
}
function unsetDeep(input, path) {
	let [currentSegment, ...restOfPath] = path;
	if (currentSegment === void 0 || typeof input != "object" || !input) return input;
	let _segment;
	if (isKeySegment(currentSegment) ? _segment = getIndexForKey$1(input, currentSegment._key) : (typeof currentSegment == "string" || typeof currentSegment == "number") && (_segment = currentSegment), _segment === void 0) return input;
	let segment = _segment;
	return typeof segment == "number" && Array.isArray(input) && (segment = segment < 0 ? input.length + segment : segment), segment in input ? restOfPath.length ? Array.isArray(input) ? input.map((nestedInput, index) => index === segment ? unsetDeep(nestedInput, restOfPath) : nestedInput) : Object.fromEntries(Object.entries(input).map(([key, value]) => key === segment ? [key, unsetDeep(value, restOfPath)] : [key, value])) : Array.isArray(input) ? input.filter((_nestedInput, index) => index !== segment) : Object.fromEntries(Object.entries(input).filter(([key]) => key !== segment.toString())) : input;
}
/**
* Exposes the patch operations used by {@link processMutations}.
*
* @remarks
* Property order determines execution order in {@link processMutations}:
* `ifRevisionID`, `set`, `setIfMissing`, `unset`, `inc`, `dec`, `insert`,
* then `diffMatchPatch`. Reordering the properties changes mutation results.
* Freezes the collection to prevent callers from replacing the functions
* used by the SDK's mutation evaluator.
*
* @see https://www.sanity.io/docs/http-mutations#5b4db1396e56
*
* @example
* ```ts
* const document = {items: [{_key: 'a', title: 'Before'}]}
* const updated = patchOperations.set<typeof document>(document, {
*   'items[_key=="a"].title': 'After',
* })
* ```
*
* @internal
*/
const patchOperations = Object.freeze({
	ifRevisionID,
	set,
	setIfMissing,
	unset,
	inc,
	dec,
	insert,
	diffMatchPatch
});
/**
* Implements ID generation:
*
* A create mutation creates a new document. It takes the literal document
* content as its argument. The rules for the new document's identifier are as
* follows:
*
* - If the `_id` attribute is missing, then a new, random, unique ID is
*   generated.
* - If the `_id` attribute is present but ends with `.`, then it is used as a
*   prefix for a new, random, unique ID.
* - If the _id attribute is present, it is used as-is.
*
* [- source](https://www.sanity.io/docs/http-mutations#c732f27330a4)
*/
function getId(id) {
	return !id || typeof id != "string" ? randomUuid() : id.endsWith(".") ? `${id}${randomUuid()}` : id;
}
function getDocumentIds(selection) {
	if ("id" in selection) {
		let ids = (Array.isArray(selection.id) ? selection.id : [selection.id]).filter((id) => typeof id == "string");
		return Array.from(new Set(ids));
	}
	if ("query" in selection) throw Error("'query' in mutations is not supported.");
	return [];
}
/**
* Applies mutations to an in-memory set of Sanity documents.
* Deleted documents remain in the returned set with a value of `null`.
*
* Include every document affected by `mutations` in `documents`, using `null`
* for documents that do not exist.
*
* Returns `documents` itself when `mutations` is empty. Otherwise, returns a
* shallow copy of that object. The result can reuse document objects and
* nested objects from `documents`. Mutating a reused object through the
* result also changes the input.
*
* Sets `_rev` to `transactionId` on created, replaced, and patched documents.
* Uses `timestamp` for timestamps unless the mutation preserves them.
* Ignores `merge` patches; `inc` and `dec` skip values that are not numbers.
* Generates document IDs when omitted or ending in `.`.
* Patch operations can generate missing array keys.
*
* Does not persist documents or run Content Lake's server validation.
*
* @throws If a selection uses a query, a `create` targets an existing document,
* or a `patch` targets a document that does not exist.
* @throws If a patch operation fails, including a revision check failure or
* `diffMatchPatch` applied to a value other than a string or `undefined`.
*
* @internal
*/
function processMutations({ documents, mutations, transactionId, timestamp }) {
	if (!mutations.length) return documents;
	let dataset = { ...documents }, now = timestamp || (/* @__PURE__ */ new Date()).toISOString();
	for (let mutation of mutations) {
		if ("create" in mutation) {
			let id = getId(mutation.create._id);
			if (dataset[id]) throw Error(`Cannot create document with \`_id\` \`${id}\` because another document with the same ID already exists.`);
			dataset[id] = {
				_createdAt: now,
				_updatedAt: now,
				...mutation.create,
				_rev: transactionId,
				_id: id
			};
			continue;
		}
		if ("createOrReplace" in mutation) {
			let id = getId(mutation.createOrReplace._id), prev = dataset[id];
			dataset[id] = {
				...mutation.createOrReplace,
				_createdAt: prev?._createdAt || typeof mutation.createOrReplace._createdAt == "string" && mutation.createOrReplace._createdAt || now,
				_updatedAt: prev ? now : typeof mutation.createOrReplace._updatedAt == "string" && mutation.createOrReplace._updatedAt || now,
				_rev: transactionId,
				_id: id
			};
			continue;
		}
		if ("createIfNotExists" in mutation) {
			let id = getId(mutation.createIfNotExists._id);
			if (dataset[id]) continue;
			dataset[id] = {
				_createdAt: now,
				_updatedAt: now,
				...mutation.createIfNotExists,
				_rev: transactionId,
				_id: id
			};
			continue;
		}
		if ("delete" in mutation) {
			for (let id of getDocumentIds(mutation.delete)) dataset[id] = null;
			continue;
		}
		if ("patch" in mutation) {
			let { patch } = mutation, patched = getDocumentIds(patch).map((id) => {
				if (!dataset[id]) throw Error(`Cannot patch document with ID \`${id}\` because it was not found.`);
				return Object.entries(patchOperations).reduce((acc, [type, operation]) => patch[type] ? operation(acc, patch[type]) : acc, dataset[id]);
			});
			for (let result of patched) dataset[result._id] = {
				...result,
				_rev: transactionId,
				_updatedAt: now
			};
			continue;
		}
	}
	return dataset;
}
var OutOfSyncError = class extends Error {
	constructor(message, state) {
		super(message), _defineProperty(
			this,
			/**
			* Attach state to the error for debugging/reporting
			*/
			"state",
			void 0
		), this.name = "OutOfSyncError", this.state = state;
	}
}, DeadlineExceededError = class extends OutOfSyncError {
	constructor(message, state) {
		super(message, state), this.name = "DeadlineExceededError";
	}
}, MaxBufferExceededError = class extends OutOfSyncError {
	constructor(message, state) {
		super(message, state), this.name = "MaxBufferExceededError";
	}
};
/**
* Orders the given mutation events into chains where each event's
* `previousRev` points at another event's `resultRev`. Events whose
* `previousRev` matches no other event start a new chain. The buffer may have
* multiple holes in it, so multiple chains can exist at once.
*/
function toOrderedChains(events) {
	return events.filter((event) => !events.some((other) => other.resultRev === event.previousRev)).map((start) => {
		let chain = [], current = start;
		for (; current;) {
			chain.push(current);
			let currentResultRev = current.resultRev;
			current = events.find((event) => event.previousRev === currentResultRev);
		}
		return chain;
	});
}
/**
* Discards the leading events of a chain up to and including the event whose
* `resultRev` equals the given revision. These events describe changes that
* are already reflected in the current base (e.g. events replayed by a
* resumed listener after the snapshot was refetched).
*/
function discardChainTo(chain, revision) {
	let revisionIndex = chain.findIndex((event) => event.resultRev === revision);
	return revisionIndex === -1 ? chain : chain.slice(revisionIndex + 1);
}
/**
* Takes an input observable of listener events that might arrive out of order, and emits them in sequence
* If we receive mutation events that doesn't line up in [previousRev, resultRev] pairs we'll put them in a buffer and
* check if we have an unbroken chain every time we receive a new event
*
* Buffered chains that lead up to the current base revision are discarded:
* they describe changes already reflected in the base (e.g. events replayed
* by a resumed listener after the snapshot was refetched)
*
* If the buffer grows beyond `maxBufferSize`, or if `resolveChainDeadline` milliseconds passes before the chain resolves
* an OutOfSyncError will be thrown on the stream
*
* @internal
*/
function sortListenerEvents(options) {
	let { resolveChainDeadline = 3e4, maxBufferSize = 20 } = options || {};
	return (input$) => input$.pipe(scan((state, event) => {
		if (event.type === "sync") return {
			base: { revision: event.document?._rev },
			buffer: [],
			emitEvents: [event]
		};
		if (event.type === "mutation") {
			if (!state.base) throw Error("Invalid state. Cannot process mutation event without a base sync event");
			let baseRevision = state.base.revision, chains = toOrderedChains(state.buffer.concat(event)).map((chain) => discardChainTo(chain, baseRevision)).filter((chain) => chain.length > 0), applicable = chains.find((chain) => chain[0].previousRev === baseRevision), buffer = chains.filter((chain) => chain !== applicable).flat();
			if (applicable) {
				let last = applicable[applicable.length - 1];
				return {
					base: { revision: last.transition === "disappear" ? void 0 : last.resultRev },
					buffer,
					emitEvents: applicable
				};
			}
			if (buffer.length >= maxBufferSize) throw new MaxBufferExceededError(`Too many unchainable mutation events (${buffer.length}) waiting to resolve.`, {
				base: { revision: baseRevision },
				buffer,
				emitEvents: []
			});
			return {
				base: { revision: baseRevision },
				buffer,
				emitEvents: []
			};
		}
		return {
			...state,
			emitEvents: [event]
		};
	}, {
		base: void 0,
		buffer: [],
		emitEvents: []
	}), switchMap((state) => state.buffer.length > 0 ? concat(of(state), timer(resolveChainDeadline).pipe(mergeMap$1(() => throwError(() => new DeadlineExceededError(`Did not resolve chain within a deadline of ${resolveChainDeadline}ms`, state))))) : of(state)), mergeMap$1((state) => of(...state.emitEvents)));
}
const listen = ({ state }, documentId) => {
	let { sharedListener, fetchDocument } = state.get();
	return sharedListener.events.pipe(concatMap((e) => e.type === "welcome" || e.type === "reset" ? fetchDocument(documentId).pipe(map((document) => ({
		type: "sync",
		document
	}))) : e.type === "mutation" && e.documentId === documentId ? of(e) : EMPTY), sortListenerEvents(), withLatestFrom(state.observable.pipe(map((s) => s.documentStates[documentId]), filter(Boolean), distinctUntilChanged())), map(([next, documentState]) => {
		if (next.type === "sync") return {
			type: "sync",
			documentId,
			document: next.document,
			revision: next.document?._rev,
			timestamp: next.document?._updatedAt ?? (/* @__PURE__ */ new Date()).toISOString()
		};
		let [document] = Object.values(processMutations({
			documents: { [documentId]: documentState.remote },
			mutations: next.mutations,
			transactionId: next.transactionId,
			timestamp: next.timestamp
		})), { previousRev, transactionId, timestamp } = next;
		return {
			type: "mutation",
			documentId,
			document: document ?? null,
			revision: transactionId,
			timestamp,
			mutations: next.mutations,
			...previousRev && { previousRev }
		};
	}));
};
function _checkPrivateRedeclaration(e, t) {
	if (t.has(e)) throw TypeError("Cannot initialize the same private elements twice on an object");
}
function _classPrivateMethodInitSpec(e, a) {
	_checkPrivateRedeclaration(e, a), a.add(e);
}
function _classPrivateFieldInitSpec(e, t, a) {
	_checkPrivateRedeclaration(e, t), t.set(e, a);
}
function _assertClassBrand(e, t, n) {
	if (typeof e == "function" ? e === t : e.has(t)) return arguments.length < 3 ? t : n;
	throw TypeError("Private element is not present on this object");
}
function _classPrivateFieldGet2(s, a) {
	return s.get(_assertClassBrand(s, a));
}
var _rootMap = /* @__PURE__ */ new WeakMap(), _idCache = /* @__PURE__ */ new WeakMap(), _MultiKeyWeakMap_brand = /* @__PURE__ */ new WeakSet(), MultiKeyWeakMap = class {
	constructor() {
		_classPrivateMethodInitSpec(this, _MultiKeyWeakMap_brand), _classPrivateFieldInitSpec(this, _rootMap, /* @__PURE__ */ new WeakMap()), _classPrivateFieldInitSpec(this, _idCache, /* @__PURE__ */ new WeakMap());
	}
	/**
	* Retrieves the value associated with the array of keys.
	* The keys are de-duplicated and sorted so that the order does not matter.
	*/
	get(keys) {
		let arrangedKeys = _assertClassBrand(_MultiKeyWeakMap_brand, this, _arrangeKeys).call(this, keys);
		return _assertClassBrand(_MultiKeyWeakMap_brand, this, _getDeep).call(this, arrangedKeys, _classPrivateFieldGet2(_rootMap, this));
	}
	/**
	* Associates the value with the given array of keys.
	*/
	set(keys, value) {
		let arrangedKeys = _assertClassBrand(_MultiKeyWeakMap_brand, this, _arrangeKeys).call(this, keys);
		_assertClassBrand(_MultiKeyWeakMap_brand, this, _setDeep).call(this, arrangedKeys, _classPrivateFieldGet2(_rootMap, this), value);
	}
};
/**
* Assigns a numeric ID to the key.
*/
function _assignId(key) {
	var _MultiKeyWeakMap$glob;
	let cachedId = _classPrivateFieldGet2(_idCache, this).get(key);
	if (cachedId !== void 0) return cachedId;
	let id = _globalIdCounter._;
	return _classPrivateFieldGet2(_idCache, this).set(key, id), _globalIdCounter._ = (_MultiKeyWeakMap$glob = _globalIdCounter._, _MultiKeyWeakMap$glob++, _MultiKeyWeakMap$glob), id;
}
/**
* Remove duplicate keys and arrange them in a consistent order
* by sorting according to their assigned IDs.
*/
function _arrangeKeys(keys) {
	let keyed = Array.from(new Set(keys)).map((key) => [_assertClassBrand(_MultiKeyWeakMap_brand, this, _assignId).call(this, key), key]);
	return keyed.sort((a, b) => a[0] - b[0]), keyed.map(([, key]) => key);
}
/**
* Recursively search the nested WeakMap structure for the value.
*/
function _getDeep(keys, map) {
	if (keys.length === 0) return;
	let [firstKey, ...restKeys] = keys, node = map.get(firstKey);
	if (node) return restKeys.length === 0 ? node.value : _assertClassBrand(_MultiKeyWeakMap_brand, this, _getDeep).call(this, restKeys, node.next);
}
/**
* Recursively create nodes along the key chain until the final key
* is reached, then assign the value.
*/
function _setDeep(keys, map, value) {
	if (keys.length === 0) return;
	let [firstKey, ...restKeys] = keys, node = map.get(firstKey);
	node || (node = {
		value: void 0,
		next: /* @__PURE__ */ new WeakMap()
	}, map.set(firstKey, node)), restKeys.length === 0 ? node.value = value : _assertClassBrand(_MultiKeyWeakMap_brand, this, _setDeep).call(this, restKeys, node.next, value);
}
var _globalIdCounter = { _: 0 };
function checkGrant(grantExpr, document, identity) {
	let value = evaluateSync(grantExpr, {
		params: { document },
		identity
	});
	return value.type === "boolean" && value.data;
}
/**
* Thrown when a precondition for an action failed.
*/
var ActionError = class extends Error {
	constructor(options) {
		super(options.message), _defineProperty(this, "documentId", void 0), _defineProperty(this, "transactionId", void 0), Object.assign(this, options);
	}
}, PermissionActionError = class extends ActionError {};
/**
* Creates a `processMutations` wrapper that surfaces application failures as
* `ActionError`s when the caller asked to preserve their patch operations.
* With preserved operations, patches can legitimately fail to apply (e.g.
* re-applied onto a diverged document during a rebase), so wrapping lets a
* rebase skip the transaction instead of failing the store. Without
* `preserveOperations`, errors are rethrown untouched.
*/
function createMutationApplier(options) {
	let { documentId, transactionId, timestamp, preserveOperations } = options;
	return (documents, mutations, documentSetName) => {
		try {
			return processMutations({
				documents,
				transactionId,
				mutations,
				timestamp
			});
		} catch (error) {
			throw preserveOperations ? new ActionError({
				documentId,
				transactionId,
				message: `Failed to apply patches to the ${documentSetName} document: ${error instanceof Error ? error.message : "Unknown error"}`
			}) : error;
		}
	};
}
/**
* Re-applies the operational intent of user-supplied `inc`/`dec` patches to a
* set of snapshot-diffed patches.
*
* The outgoing patches sent to Content Lake are re-derived by diffing the
* base document before and after the user's patches were applied. That diff
* collapses an `inc` into a `set` of the resulting number, which turns
* concurrent increments from different clients into last-write-wins. This
* helper finds diffed `set` operations that are exactly explained by the
* user's `inc`/`dec` operations and swaps them back, so the server applies
* the increment against whatever value is current at commit time.
*
* A `set` is only swapped when its value equals the base value plus the
* accumulated user deltas for that exact path; anything else (e.g. an `inc`
* combined with a `set` of the same field) keeps the diffed `set`.
*
* Content Lake fails the whole transaction when `inc`/`dec` targets a missing
* path (unlike `set`, which always succeeds). To avoid turning a concurrent
* field removal into a transaction revert, the emitted patch seeds the target
* with a `setIfMissing` of the base value; the server executes `setIfMissing`
* before `inc`/`dec` within a single patch, so a concurrently removed field
* degrades to the same result the diffed `set` would have produced. A path
* into a concurrently removed keyed array item can still fail the
* transaction (`setIfMissing` cannot create array items); the transaction is
* then reverted and reported, which is preferable to silently resurrecting
* the item.
*/
function preserveNumericOperations(baseBefore, userPatches, diffedPatches) {
	if (!baseBefore || !userPatches?.length) return diffedPatches;
	let targets = /* @__PURE__ */ new Map();
	for (let patch of userPatches) for (let [op, sign] of [["inc", 1], ["dec", -1]]) {
		let record = patch[op];
		if (record) {
			for (let [pathExpression, amount] of Object.entries(record)) if (typeof amount == "number") for (let match of jsonMatch$1(baseBefore, pathExpression)) {
				let path = stringifyPath$1(match.path), existing = targets.get(path);
				existing ? (existing.expectedValue += sign * amount, existing.delta += sign * amount) : typeof match.value == "number" && targets.set(path, {
					baseValue: match.value,
					expectedValue: match.value + sign * amount,
					delta: sign * amount
				});
			}
		}
	}
	if (!targets.size) return diffedPatches;
	let preserved = /* @__PURE__ */ new Map(), next = diffedPatches.flatMap((patch) => {
		if (!patch.set) return [patch];
		let keptSet = {};
		for (let [path, value] of Object.entries(patch.set)) {
			let target = targets.get(path);
			target && typeof value == "number" && value === target.expectedValue ? preserved.set(path, {
				delta: target.delta,
				baseValue: target.baseValue
			}) : keptSet[path] = value;
		}
		if (Object.keys(keptSet).length === Object.keys(patch.set).length) return [patch];
		let { set: _set, ...rest } = patch, withKept = Object.keys(keptSet).length ? {
			...rest,
			set: keptSet
		} : rest;
		return Object.keys(withKept).length ? [withKept] : [];
	});
	if (!preserved.size) return next;
	let setIfMissing = {}, inc = {}, dec = {};
	for (let [path, { delta, baseValue }] of preserved) setIfMissing[path] = baseValue, delta >= 0 ? inc[path] = delta : dec[path] = -delta;
	return [...next, {
		setIfMissing,
		...Object.keys(inc).length && { inc },
		...Object.keys(dec).length && { dec }
	}];
}
/**
* Shared logic for applying user-provided patches to a single document that
* is identified by an exact ID (no draft/published wrapping). Used by the
* liveEdit branch of `document.edit` and by `release.edit`.
*
* Returns the updated base + working sets, plus the diffed patches in both
* raw and mutation form so the caller can decide what to send to the server.
*/
function applySingleDocPatch({ base: initialBase, working: initialWorking, documentId, patches, transactionId, timestamp, grants, identity, notFoundMessage = "Cannot edit document because it does not exist.", permissionMessage = `You do not have permission to edit document "${documentId}".`, preserveOperations }) {
	let base = initialBase, working = initialWorking, userPatches = patches?.map((patch) => ({ patch: {
		id: documentId,
		...patch
	} }));
	if (!userPatches?.length) return {
		base,
		working,
		diffedPatches: [],
		workingMutations: []
	};
	if (!working[documentId] || !base[documentId]) throw new ActionError({
		documentId,
		transactionId,
		message: notFoundMessage
	});
	let applyMutations = createMutationApplier({
		documentId,
		transactionId,
		timestamp,
		preserveOperations
	}), baseBefore = base[documentId];
	base = applyMutations(base, userPatches, "base");
	let baseAfter = base[documentId], diffedPatches = preserveOperations ? patches : preserveNumericOperations(baseBefore, patches, diffValue(baseBefore, baseAfter)), workingBefore = working[documentId];
	if (!checkGrant(grants.update, workingBefore, identity)) throw new PermissionActionError({
		documentId,
		transactionId,
		message: permissionMessage
	});
	let workingMutations = diffedPatches.map((patch) => ({ patch: {
		id: documentId,
		...patch
	} }));
	return working = applyMutations(working, workingMutations, "working"), {
		base,
		working,
		diffedPatches,
		workingMutations
	};
}
function createGrantsLookup(datasetAcl) {
	let filtersByGrant = {
		create: /* @__PURE__ */ new Set(),
		history: /* @__PURE__ */ new Set(),
		read: /* @__PURE__ */ new Set(),
		update: /* @__PURE__ */ new Set()
	};
	for (let entry of datasetAcl) for (let grant of entry.permissions) {
		let set = filtersByGrant[grant];
		set && (set.add(entry.filter), filtersByGrant[grant] = set);
	}
	return Object.fromEntries(Object.entries(filtersByGrant).map(([grant, filters]) => {
		let combinedFilter = Array.from(filters).map((i) => `(${i})`).join("||");
		return combinedFilter ? [grant, parse(`$document {"_": ${combinedFilter}}._`)] : [grant, parse("false")];
	}));
}
const documentsCache = new MultiKeyWeakMap(), actionsCache = /* @__PURE__ */ new WeakMap(), nullReplacer = {}, documentsSelector = createSelector([({ state: { documentStates } }) => documentStates, (_context, { actions }) => actions], (documentStates, actions) => {
	let documentIds = new Set(actions.map((action) => {
		if (typeof action.documentId != "string") return [];
		if (action.liveEdit) return [action.documentId];
		let ids = [getPublishedId(DocumentId(action.documentId)), getDraftId(DocumentId(action.documentId))];
		return isReleasePerspective(action.perspective) && ids.push(getVersionId(DocumentId(action.documentId), action.perspective.releaseName)), ids;
	}).flat()), documents = {};
	for (let documentId of documentIds) {
		let local = documentStates[documentId]?.local;
		if (local === void 0) return;
		documents[documentId] = local;
	}
	let keys = Object.values(documents).map((doc) => doc === null ? nullReplacer : doc);
	return documentsCache.get(keys) || (documentsCache.set(keys, documents), documents);
}), memoizedActionsSelector = createSelector([documentsSelector, (_state, { actions }) => actions], (documents, actions) => {
	if (!documents) return;
	let nestedCache = actionsCache.get(documents);
	nestedCache || (nestedCache = /* @__PURE__ */ new Map(), actionsCache.set(documents, nestedCache));
	let normalizedActions = Array.isArray(actions) ? actions : [actions], actionsKey = JSON.stringify(normalizedActions);
	return nestedCache.get(actionsKey) || (nestedCache.set(actionsKey, normalizedActions), normalizedActions);
}), enNarrowConjunction = new Intl.ListFormat("en", {
	style: "narrow",
	type: "conjunction"
});
function calculatePermissions(...args) {
	return _calculatePermissions(...args);
}
const _calculatePermissions = createSelector([
	({ state: { grants } }) => grants,
	({ state: { identity } }) => identity,
	documentsSelector,
	memoizedActionsSelector
], (grants, identity, documents, actions) => {
	if (!documents || !grants || !actions) return;
	let timestamp = (/* @__PURE__ */ new Date()).toISOString(), reasons = [];
	try {
		processActions({
			actions,
			transactionId: randomId(16),
			working: documents,
			base: documents,
			timestamp,
			grants,
			identity
		});
	} catch (error) {
		if (error instanceof PermissionActionError) reasons.push({
			message: error.message,
			documentId: error.documentId,
			type: "access"
		});
		else if (error instanceof ActionError) reasons.push({
			message: error.message,
			documentId: error.documentId,
			type: "precondition"
		});
		else throw error;
	}
	for (let action of actions) if (action.type === "document.edit" && !action.patches?.length) {
		let docId = action.documentId, doc;
		doc = action.liveEdit ? documents[docId] : isReleasePerspective(action.perspective) ? documents[getVersionId(DocumentId(docId), action.perspective.releaseName)] : documents[getDraftId(DocumentId(docId))] ?? documents[getPublishedId(DocumentId(docId))], doc ? checkGrant(grants.update, doc, identity) || reasons.push({
			type: "access",
			message: `You are not allowed to edit the document with ID "${docId}".`,
			documentId: docId
		}) : isReleasePerspective(action.perspective) ? reasons.push({
			type: "precondition",
			message: `The version document with ID "${docId}" could not be found. Please create it or add it to the release first.`,
			documentId: docId
		}) : reasons.push({
			type: "precondition",
			message: `The document with ID "${docId}" could not be found. Please check that it exists before editing.`,
			documentId: docId
		});
	}
	let allowed = reasons.length === 0;
	if (allowed) return { allowed };
	let sortedReasons = reasons.map((reason, index) => ({
		...reason,
		index
	})).sort((a, b) => a.type === b.type ? a.message.localeCompare(b.message, "en-US") : a.type === "access" ? -1 : 1).map(({ index: _index, ...reason }) => reason);
	return {
		allowed,
		reasons: sortedReasons,
		message: enNarrowConjunction.format(sortedReasons.map((i) => i.message))
	};
});
function handleCreate(action, ctx) {
	let { transactionId, timestamp, grants, identity, outgoingActions, outgoingMutations } = ctx, { base, working } = ctx, documentId = getId(action.documentId);
	if (action.liveEdit) {
		if (working[documentId]) throw new ActionError({
			documentId,
			transactionId,
			message: "This document already exists."
		});
		let newDocBase = {
			_type: action.documentType,
			_id: documentId,
			...action.initialValue
		}, mutations = [{ create: {
			_type: action.documentType,
			_id: documentId,
			...action.initialValue
		} }];
		if (base = processMutations({
			documents: base,
			transactionId,
			mutations: [{ create: newDocBase }],
			timestamp
		}), working = processMutations({
			documents: working,
			transactionId,
			mutations,
			timestamp
		}), !checkGrant(grants.create, working[documentId], identity)) throw new PermissionActionError({
			documentId,
			transactionId,
			message: `You do not have permission to create document "${documentId}".`
		});
		return outgoingMutations.push(...mutations), {
			base,
			working
		};
	}
	let versionId = isReleasePerspective(action.perspective) ? getVersionId(DocumentId(documentId), action.perspective.releaseName) : void 0, draftId = getDraftId(DocumentId(documentId)), publishedId = getPublishedId(DocumentId(documentId));
	if (versionId ? working[versionId] : working[draftId]) {
		let errorDocType = versionId ? "release version" : "draft";
		throw new ActionError({
			documentId,
			transactionId,
			message: `A ${errorDocType} of this document already exists. Please use or discard the existing ${errorDocType} before creating a new one.`
		});
	}
	let newDocBase = {
		...base[draftId] ?? base[publishedId],
		_type: action.documentType,
		_id: versionId ?? draftId,
		...action.initialValue
	}, newDocWorking = {
		...working[draftId] ?? working[publishedId],
		_type: action.documentType,
		_id: versionId ?? draftId,
		...action.initialValue
	}, mutations = [{ create: newDocWorking }];
	if (base = processMutations({
		documents: base,
		transactionId,
		mutations: [{ create: newDocBase }],
		timestamp
	}), working = processMutations({
		documents: working,
		transactionId,
		mutations,
		timestamp
	}), versionId && !checkGrant(grants.create, working[versionId], identity)) throw new PermissionActionError({
		documentId,
		transactionId,
		message: `You do not have permission to create a release version for document "${documentId}".`
	});
	if (!versionId && !checkGrant(grants.create, working[draftId], identity)) throw new PermissionActionError({
		documentId,
		transactionId,
		message: `You do not have permission to create a draft for document "${documentId}".`
	});
	return outgoingMutations.push(...mutations), outgoingActions.push({
		actionType: "sanity.action.document.version.create",
		publishedId,
		attributes: newDocWorking
	}), {
		base,
		working
	};
}
function handleDelete(action, ctx) {
	let { transactionId, timestamp, grants, identity, outgoingActions, outgoingMutations } = ctx, { base, working } = ctx, documentId = action.documentId;
	if (isReleasePerspective(action.perspective)) throw new ActionError({
		documentId,
		transactionId,
		message: "Cannot delete a version document. You may want to use the \"unpublish\" or \"discard\" actions instead."
	});
	if (action.liveEdit) {
		if (!working[documentId]) throw new ActionError({
			documentId,
			transactionId,
			message: "The document you are trying to delete does not exist."
		});
		if (!checkGrant(grants.update, working[documentId], identity)) throw new PermissionActionError({
			documentId,
			transactionId,
			message: "You do not have permission to delete this document."
		});
		let mutations = [{ delete: { id: documentId } }];
		return base = processMutations({
			documents: base,
			transactionId,
			mutations,
			timestamp
		}), working = processMutations({
			documents: working,
			transactionId,
			mutations,
			timestamp
		}), outgoingMutations.push(...mutations), {
			base,
			working
		};
	}
	let draftId = getDraftId(DocumentId(documentId)), publishedId = getPublishedId(DocumentId(documentId));
	if (!working[publishedId]) throw new ActionError({
		documentId,
		transactionId,
		message: working[draftId] ? "Cannot delete a document without a published version." : "The document you are trying to delete does not exist."
	});
	let cantDeleteDraft = working[draftId] && !checkGrant(grants.update, working[draftId], identity), cantDeletePublished = working[publishedId] && !checkGrant(grants.update, working[publishedId], identity);
	if (cantDeleteDraft || cantDeletePublished) throw new PermissionActionError({
		documentId,
		transactionId,
		message: "You do not have permission to delete this document."
	});
	let mutations = [{ delete: { id: publishedId } }, { delete: { id: draftId } }], includeDrafts = working[draftId] ? [draftId] : void 0;
	return base = processMutations({
		documents: base,
		transactionId,
		mutations,
		timestamp
	}), working = processMutations({
		documents: working,
		transactionId,
		mutations,
		timestamp
	}), outgoingMutations.push(...mutations), outgoingActions.push({
		actionType: "sanity.action.document.delete",
		publishedId,
		...includeDrafts ? { includeDrafts } : {}
	}), {
		base,
		working
	};
}
function handleDiscard(action, ctx) {
	let { transactionId, timestamp, grants, identity, outgoingActions, outgoingMutations } = ctx, { base, working } = ctx, documentId = getId(action.documentId);
	if (action.liveEdit) throw new ActionError({
		documentId,
		transactionId,
		message: `Cannot discard changes for liveEdit document "${documentId}". LiveEdit documents do not support drafts.`
	});
	let versionId = isReleasePerspective(action.perspective) ? getVersionId(DocumentId(documentId), action.perspective.releaseName) : getDraftId(DocumentId(documentId)), mutations = [{ delete: { id: versionId } }];
	if (!working[versionId]) throw new ActionError({
		documentId,
		transactionId,
		message: `There is no draft or version available to discard for document "${documentId}".`
	});
	if (!checkGrant(grants.update, working[versionId], identity)) throw new PermissionActionError({
		documentId,
		transactionId,
		message: `You do not have permission to discard changes for document "${documentId}".`
	});
	return base = processMutations({
		documents: base,
		transactionId,
		mutations,
		timestamp
	}), working = processMutations({
		documents: working,
		transactionId,
		mutations,
		timestamp
	}), outgoingMutations.push(...mutations), outgoingActions.push({
		actionType: "sanity.action.document.version.discard",
		versionId
	}), {
		base,
		working
	};
}
function handleEdit(action, ctx) {
	let { transactionId, timestamp, grants, identity, outgoingActions, outgoingMutations } = ctx, { base, working } = ctx, documentId = getId(action.documentId);
	if (action.liveEdit) {
		let result = applySingleDocPatch({
			base,
			working,
			documentId,
			patches: action.patches,
			transactionId,
			timestamp,
			grants,
			identity,
			preserveOperations: action.preserveOperations
		});
		return outgoingMutations.push(...result.workingMutations), {
			base: result.base,
			working: result.working
		};
	}
	let versionId = isReleasePerspective(action.perspective) ? getVersionId(DocumentId(documentId), action.perspective.releaseName) : void 0, draftId = getDraftId(DocumentId(documentId)), publishedId = getPublishedId(DocumentId(documentId)), patchDocumentId = isReleasePerspective(action.perspective) ? versionId : draftId, userPatches = action.patches?.map((patch) => ({ patch: {
		id: patchDocumentId,
		...patch
	} }));
	if (!userPatches?.length) return {
		base,
		working
	};
	if (isReleasePerspective(action.perspective)) {
		if (!working[versionId] && !base[versionId]) throw new ActionError({
			documentId,
			transactionId,
			message: "This document does not exist in the release. Please create it or add it to the release first."
		});
	} else if (!working[draftId] && !working[publishedId] || !base[draftId] && !base[publishedId]) throw new ActionError({
		documentId,
		transactionId,
		message: "Cannot edit document because it does not exist in draft or published form."
	});
	let applyMutations = createMutationApplier({
		documentId,
		transactionId,
		timestamp,
		preserveOperations: action.preserveOperations
	}), baseMutations = [];
	!isReleasePerspective(action.perspective) && !base[draftId] && base[publishedId] && baseMutations.push({ create: {
		...base[publishedId],
		_id: draftId
	} });
	let baseBefore = base[patchDocumentId] ?? base[publishedId];
	userPatches && baseMutations.push(...userPatches), base = applyMutations(base, baseMutations, "base");
	let baseAfter = base[patchDocumentId], patches = action.preserveOperations ? action.patches ?? [] : preserveNumericOperations(baseBefore, action.patches, diffValue(baseBefore, baseAfter)), workingMutations = [];
	if (!isReleasePerspective(action.perspective) && !working[draftId] && working[publishedId]) {
		let newDraftFromPublished = {
			...working[publishedId],
			_id: draftId
		};
		if (!checkGrant(grants.create, newDraftFromPublished, identity)) throw new PermissionActionError({
			documentId,
			transactionId,
			message: "You do not have permission to create a draft for editing this document."
		});
		workingMutations.push({ create: newDraftFromPublished });
	}
	let workingBefore = working[patchDocumentId] ?? working[publishedId];
	if (!checkGrant(grants.update, workingBefore, identity)) throw new PermissionActionError({
		documentId,
		transactionId,
		message: `You do not have permission to edit document "${documentId}".`
	});
	return workingMutations.push(...patches.map((patch) => ({ patch: {
		id: patchDocumentId,
		...patch
	} }))), working = applyMutations(working, workingMutations, "working"), outgoingMutations.push(...workingMutations), outgoingActions.push(...patches.map((patch) => ({
		actionType: "sanity.action.document.edit",
		draftId: patchDocumentId,
		publishedId,
		patch
	}))), {
		base,
		working
	};
}
function handlePublish(action, ctx) {
	let { transactionId, timestamp, grants, identity, outgoingActions, outgoingMutations } = ctx, { base, working } = ctx, documentId = getId(action.documentId);
	if (action.liveEdit || isReleasePerspective(action.perspective)) throw new ActionError({
		documentId,
		transactionId,
		message: "Cannot publish this document. Publishing is not supported for liveEdit or version (release) documents."
	});
	let draftId = getDraftId(DocumentId(documentId)), publishedId = getPublishedId(DocumentId(documentId)), workingDraft = working[draftId], baseDraft = base[draftId];
	if (!workingDraft || !baseDraft) throw new ActionError({
		documentId,
		transactionId,
		message: `Cannot publish because no draft version was found for document "${documentId}".`
	});
	if (!isDeepEqual(workingDraft, baseDraft)) throw new ActionError({
		documentId,
		transactionId,
		message: "Publish aborted: The document has changed elsewhere. Please try again."
	});
	let newPublishedFromDraft = {
		...strengthenOnPublish(workingDraft),
		_id: publishedId
	}, mutations = [{ delete: { id: draftId } }, { createOrReplace: newPublishedFromDraft }];
	if (working[draftId] && !checkGrant(grants.update, working[draftId], identity)) throw new PermissionActionError({
		documentId,
		transactionId,
		message: `Publish failed: You do not have permission to update the draft for "${documentId}".`
	});
	if (working[publishedId] && !checkGrant(grants.update, newPublishedFromDraft, identity)) throw new PermissionActionError({
		documentId,
		transactionId,
		message: `Publish failed: You do not have permission to update the published version of "${documentId}".`
	});
	if (!working[publishedId] && !checkGrant(grants.create, newPublishedFromDraft, identity)) throw new PermissionActionError({
		documentId,
		transactionId,
		message: `Publish failed: You do not have permission to publish a new version of "${documentId}".`
	});
	return base = processMutations({
		documents: base,
		transactionId,
		mutations,
		timestamp
	}), working = processMutations({
		documents: working,
		transactionId,
		mutations,
		timestamp
	}), outgoingMutations.push(...mutations), outgoingActions.push({
		actionType: "sanity.action.document.publish",
		draftId,
		publishedId
	}), {
		base,
		working
	};
}
function strengthenOnPublish(draft) {
	let isStrengthenReference = (value) => "_strengthenOnPublish" in value;
	function strengthen(value) {
		if (typeof value != "object" || !value) return value;
		if (isStrengthenReference(value)) {
			let { _strengthenOnPublish, _weak, ...rest } = value;
			return {
				...rest,
				..._strengthenOnPublish.weak && { _weak: !0 }
			};
		}
		return Array.isArray(value) ? value.map(strengthen) : Object.fromEntries(Object.entries(value).map(([k, v]) => [k, strengthen(v)]));
	}
	return strengthen(draft);
}
function handleReleaseArchive(action, ctx) {
	let { base, working, grants, outgoingActions, transactionId, identity } = ctx, releaseDocumentId = getReleaseDocumentId(action.releaseId), existing = working[releaseDocumentId] ?? base[releaseDocumentId];
	if (!existing) throw new ActionError({
		documentId: releaseDocumentId,
		transactionId,
		message: `Cannot archive release "${action.releaseId}" because it does not exist.`
	});
	if (!checkGrant(grants.update, existing, identity)) throw new PermissionActionError({
		documentId: releaseDocumentId,
		transactionId,
		message: `You do not have permission to archive release "${action.releaseId}".`
	});
	return outgoingActions.push({
		actionType: "sanity.action.release.archive",
		releaseId: action.releaseId
	}), {
		base,
		working
	};
}
function handleReleaseUnarchive(action, ctx) {
	let { base, working, grants, outgoingActions, transactionId, identity } = ctx, releaseDocumentId = getReleaseDocumentId(action.releaseId), existing = working[releaseDocumentId] ?? base[releaseDocumentId];
	if (!existing) throw new ActionError({
		documentId: releaseDocumentId,
		transactionId,
		message: `Cannot unarchive release "${action.releaseId}" because it does not exist.`
	});
	if (!checkGrant(grants.update, existing, identity)) throw new PermissionActionError({
		documentId: releaseDocumentId,
		transactionId,
		message: `You do not have permission to unarchive release "${action.releaseId}".`
	});
	return outgoingActions.push({
		actionType: "sanity.action.release.unarchive",
		releaseId: action.releaseId
	}), {
		base,
		working
	};
}
function handleReleaseCreate(action, ctx) {
	let { transactionId, timestamp, grants, outgoingActions, outgoingMutations, identity } = ctx, { base, working } = ctx, releaseDocumentId = getReleaseDocumentId(action.releaseId);
	if (working[releaseDocumentId] || base[releaseDocumentId]) throw new ActionError({
		documentId: releaseDocumentId,
		transactionId,
		message: `A release with id "${action.releaseId}" already exists.`
	});
	let mutations = [{ create: {
		_id: releaseDocumentId,
		_type: "system.release",
		name: action.releaseId,
		state: "active",
		metadata: action.metadata
	} }];
	if (base = processMutations({
		documents: base,
		transactionId,
		mutations,
		timestamp
	}), working = processMutations({
		documents: working,
		transactionId,
		mutations,
		timestamp
	}), !checkGrant(grants.create, working[releaseDocumentId], identity)) throw new PermissionActionError({
		documentId: releaseDocumentId,
		transactionId,
		message: `You do not have permission to create release "${action.releaseId}".`
	});
	return outgoingMutations.push(...mutations), outgoingActions.push({
		actionType: "sanity.action.release.create",
		releaseId: action.releaseId,
		metadata: action.metadata
	}), {
		base,
		working
	};
}
const DELETABLE_STATES = /* @__PURE__ */ new Set(["archived", "published"]);
function handleReleaseDelete(action, ctx) {
	let { transactionId, timestamp, grants, outgoingActions, outgoingMutations, identity } = ctx, { base, working } = ctx, releaseDocumentId = getReleaseDocumentId(action.releaseId), existing = working[releaseDocumentId] ?? base[releaseDocumentId];
	if (!existing) throw new ActionError({
		documentId: releaseDocumentId,
		transactionId,
		message: `Cannot delete release "${action.releaseId}" because it does not exist.`
	});
	let state = existing.state;
	if (state && typeof state == "string" && !DELETABLE_STATES.has(state)) throw new ActionError({
		documentId: releaseDocumentId,
		transactionId,
		message: `Cannot delete release "${action.releaseId}" while it is "${state}". Archive it first.`
	});
	if (!checkGrant(grants.update, existing, identity)) throw new PermissionActionError({
		documentId: releaseDocumentId,
		transactionId,
		message: `You do not have permission to delete release "${action.releaseId}".`
	});
	let mutations = [{ delete: { id: releaseDocumentId } }];
	return base = processMutations({
		documents: base,
		transactionId,
		mutations,
		timestamp
	}), working = processMutations({
		documents: working,
		transactionId,
		mutations,
		timestamp
	}), outgoingMutations.push(...mutations), outgoingActions.push({
		actionType: "sanity.action.release.delete",
		releaseId: action.releaseId
	}), {
		base,
		working
	};
}
function handleReleaseEdit(action, ctx) {
	let { transactionId, timestamp, grants, outgoingActions, outgoingMutations, identity } = ctx, { base, working } = ctx, result = applySingleDocPatch({
		base,
		working,
		documentId: getReleaseDocumentId(action.releaseId),
		patches: [action.patch],
		transactionId,
		timestamp,
		grants,
		identity,
		notFoundMessage: `Cannot edit release "${action.releaseId}" because it does not exist.`,
		permissionMessage: `You do not have permission to edit release "${action.releaseId}".`
	});
	return outgoingMutations.push(...result.workingMutations), outgoingActions.push(...result.diffedPatches.map((patch) => ({
		actionType: "sanity.action.release.edit",
		releaseId: action.releaseId,
		patch
	}))), {
		base: result.base,
		working: result.working
	};
}
function handleReleasePublish(action, ctx) {
	let { base, working, grants, outgoingActions, transactionId, identity } = ctx, releaseDocumentId = getReleaseDocumentId(action.releaseId), existing = working[releaseDocumentId] ?? base[releaseDocumentId];
	if (!existing) throw new ActionError({
		documentId: releaseDocumentId,
		transactionId,
		message: `Cannot publish release "${action.releaseId}" because it does not exist.`
	});
	if (!checkGrant(grants.update, existing, identity)) throw new PermissionActionError({
		documentId: releaseDocumentId,
		transactionId,
		message: `You do not have permission to publish release "${action.releaseId}".`
	});
	return outgoingActions.push({
		actionType: "sanity.action.release.publish",
		releaseId: action.releaseId
	}), {
		base,
		working
	};
}
function handleReleaseSchedule(action, ctx) {
	let { base, working, grants, outgoingActions, transactionId, identity } = ctx, releaseDocumentId = getReleaseDocumentId(action.releaseId);
	if (Number.isNaN(Date.parse(action.publishAt))) throw new ActionError({
		documentId: releaseDocumentId,
		transactionId,
		message: `Cannot schedule release "${action.releaseId}": "publishAt" must be a valid ISO 8601 timestamp (received "${action.publishAt}").`
	});
	let existing = working[releaseDocumentId] ?? base[releaseDocumentId];
	if (!existing) throw new ActionError({
		documentId: releaseDocumentId,
		transactionId,
		message: `Cannot schedule release "${action.releaseId}" because it does not exist.`
	});
	if (!checkGrant(grants.update, existing, identity)) throw new PermissionActionError({
		documentId: releaseDocumentId,
		transactionId,
		message: `You do not have permission to schedule release "${action.releaseId}".`
	});
	return outgoingActions.push({
		actionType: "sanity.action.release.schedule",
		releaseId: action.releaseId,
		publishAt: action.publishAt
	}), {
		base,
		working
	};
}
function handleReleaseUnschedule(action, ctx) {
	let { base, working, grants, outgoingActions, transactionId, identity } = ctx, releaseDocumentId = getReleaseDocumentId(action.releaseId), existing = working[releaseDocumentId] ?? base[releaseDocumentId];
	if (!existing) throw new ActionError({
		documentId: releaseDocumentId,
		transactionId,
		message: `Cannot unschedule release "${action.releaseId}" because it does not exist.`
	});
	if (!checkGrant(grants.update, existing, identity)) throw new PermissionActionError({
		documentId: releaseDocumentId,
		transactionId,
		message: `You do not have permission to unschedule release "${action.releaseId}".`
	});
	return outgoingActions.push({
		actionType: "sanity.action.release.unschedule",
		releaseId: action.releaseId
	}), {
		base,
		working
	};
}
function handleUnpublish(action, ctx) {
	let { transactionId, timestamp, grants, identity, outgoingActions, outgoingMutations } = ctx, { base, working } = ctx, documentId = getId(action.documentId);
	if (action.liveEdit || isReleasePerspective(action.perspective)) throw new ActionError({
		documentId,
		transactionId,
		message: "Cannot unpublish this document. Unpublishing is not supported for liveEdit or version (release) documents."
	});
	let draftId = getDraftId(DocumentId(documentId)), publishedId = getPublishedId(DocumentId(documentId));
	if (!working[publishedId] && !base[publishedId]) throw new ActionError({
		documentId,
		transactionId,
		message: `Cannot unpublish because the document "${documentId}" is not currently published.`
	});
	let sourceDoc = working[publishedId] ?? base[publishedId], newDraftFromPublished = {
		...sourceDoc,
		_id: draftId
	}, mutations = [{ delete: { id: publishedId } }, { createIfNotExists: newDraftFromPublished }];
	if (!checkGrant(grants.update, sourceDoc, identity)) throw new PermissionActionError({
		documentId,
		transactionId,
		message: `You do not have permission to unpublish the document "${documentId}".`
	});
	if (!working[draftId] && !checkGrant(grants.create, newDraftFromPublished, identity)) throw new PermissionActionError({
		documentId,
		transactionId,
		message: `You do not have permission to create a draft from the published version of "${documentId}".`
	});
	return base = processMutations({
		documents: base,
		transactionId,
		mutations: [{ delete: { id: publishedId } }, { createIfNotExists: {
			...base[publishedId] ?? sourceDoc,
			_id: draftId
		} }],
		timestamp
	}), working = processMutations({
		documents: working,
		transactionId,
		mutations,
		timestamp
	}), outgoingMutations.push(...mutations), outgoingActions.push({
		actionType: "sanity.action.document.unpublish",
		draftId,
		publishedId
	}), {
		base,
		working
	};
}
/**
* Applies the given set of actions to the working set of documents and converts
* high-level actions into lower-level outgoing mutations/actions that respect
* the current state of the working documents.
*
* Supports a "base" and "working" set of documents to allow actions to be
* applied on top of a different working set of documents in a 3-way merge
*
* Actions are applied to the base set of documents first. The difference
* between the base before and after is used to create a patch. This patch is
* then applied to the working set of documents and is set as the outgoing patch
* sent to the server.
*/
function processActions({ actions, transactionId, working: initialWorking, base: initialBase, timestamp, grants, identity }) {
	let base = { ...initialBase }, working = { ...initialWorking }, outgoingActions = [], outgoingMutations = [], liveEditAction = actions.find((action) => !isReleaseAction(action) && action.liveEdit), otherAction = actions.find((action) => isReleaseAction(action) || !action.liveEdit);
	if (liveEditAction && otherAction) throw new ActionError({
		documentId: liveEditAction.documentId,
		transactionId,
		message: "Cannot combine liveEdit document actions with other actions in the same transaction. Submit them as separate transactions."
	});
	for (let action of actions) {
		let result = dispatch(action, {
			base,
			working,
			transactionId,
			timestamp,
			grants,
			identity,
			outgoingActions,
			outgoingMutations
		});
		base = result.base, working = result.working;
	}
	let previousRevs = Object.fromEntries(Object.entries(initialWorking).map(([id, doc]) => [id, doc?._rev]));
	return {
		working,
		outgoingActions,
		outgoingMutations,
		previous: initialWorking,
		previousRevs
	};
}
function dispatch(action, ctx) {
	switch (action.type) {
		case "document.create": return handleCreate(action, ctx);
		case "document.delete": return handleDelete(action, ctx);
		case "document.discard": return handleDiscard(action, ctx);
		case "document.edit": return handleEdit(action, ctx);
		case "document.publish": return handlePublish(action, ctx);
		case "document.unpublish": return handleUnpublish(action, ctx);
		case "release.create": return handleReleaseCreate(action, ctx);
		case "release.edit": return handleReleaseEdit(action, ctx);
		case "release.publish": return handleReleasePublish(action, ctx);
		case "release.schedule": return handleReleaseSchedule(action, ctx);
		case "release.unschedule": return handleReleaseUnschedule(action, ctx);
		case "release.archive": return handleReleaseArchive(action, ctx);
		case "release.unarchive": return handleReleaseUnarchive(action, ctx);
		case "release.delete": return handleReleaseDelete(action, ctx);
		default: throw Error(`Unknown action type: "${action.type}". Please contact support if this issue persists.`);
	}
}
const EMPTY_REVISIONS = {};
function queueTransaction(prev, transaction) {
	let { transactionId, actions } = transaction;
	return {
		...getDocumentIdsFromHandleLikes(actions).reduce((acc, id) => addSubscriptionIdToDocument(acc, id, transactionId), prev),
		queued: [...prev.queued, transaction]
	};
}
function removeQueuedTransaction(prev, transactionId) {
	let transaction = prev.queued.find((t) => t.transactionId === transactionId);
	return transaction ? {
		...getDocumentIdsFromHandleLikes(transaction.actions).reduce((acc, id) => removeSubscriptionIdFromDocument(acc, id, transactionId), prev),
		queued: prev.queued.filter((t) => transactionId !== t.transactionId)
	} : prev;
}
function applyFirstQueuedTransaction(prev) {
	let queued = prev.queued.at(0);
	if (!queued || !prev.grants) return prev;
	let ids = getDocumentIdsFromHandleLikes(queued.actions);
	if (ids.some((id) => prev.documentStates[id]?.local === void 0)) return prev;
	let working = ids.reduce((acc, id) => (acc[id] = prev.documentStates[id]?.local, acc), {}), timestamp = (/* @__PURE__ */ new Date()).toISOString(), result = processActions({
		...queued,
		working,
		base: working,
		timestamp,
		grants: prev.grants,
		identity: prev.identity
	}), applied = {
		...queued,
		...result,
		base: result.previous,
		timestamp
	};
	return {
		...prev,
		applied: [...prev.applied, applied],
		queued: prev.queued.filter((t) => t.transactionId !== queued.transactionId),
		documentStates: Object.entries(result.working).reduce((acc, [id, next]) => {
			let prevDoc = acc[id];
			return prevDoc && (acc[id] = {
				...prevDoc,
				local: next
			}), acc;
		}, { ...prev.documentStates })
	};
}
function batchAppliedTransactions([curr, ...rest]) {
	if (!curr) return;
	if (!curr.actions.length) return batchAppliedTransactions(rest);
	if (curr.actions.length > 1) return {
		...curr,
		disableBatching: !0,
		batchedTransactionIds: [curr.transactionId]
	};
	let [action] = curr.actions;
	if (action.type !== "document.edit" || curr.disableBatching) return {
		...curr,
		disableBatching: !0,
		batchedTransactionIds: [curr.transactionId]
	};
	let editAction = {
		...curr,
		actions: [action],
		disableBatching: !1,
		batchedTransactionIds: [curr.transactionId]
	};
	if (!rest.length) return editAction;
	let next = batchAppliedTransactions(rest);
	if (!next) return;
	if (next.disableBatching) return editAction;
	let nextFirst = next.actions[0], nextLiveEdit = nextFirst && "liveEdit" in nextFirst ? nextFirst.liveEdit : !1;
	return !!action.liveEdit == !!nextLiveEdit ? {
		disableBatching: !1,
		transactionId: next.transactionId,
		actions: [action, ...next.actions],
		outgoingActions: [...curr.outgoingActions, ...next.outgoingActions],
		batchedTransactionIds: [curr.transactionId, ...next.batchedTransactionIds],
		outgoingMutations: [...curr.outgoingMutations, ...next.outgoingMutations],
		working: {
			...curr.working,
			...next.working
		},
		previousRevs: {
			...next.previousRevs,
			...curr.previousRevs
		},
		previous: {
			...next.previous,
			...curr.previous
		},
		base: {
			...next.base,
			...curr.base
		},
		timestamp: curr.timestamp ?? next.timestamp
	} : editAction;
}
function transitionAppliedTransactionsToOutgoing(prev) {
	if (prev.outgoing) return prev;
	let transaction = batchAppliedTransactions(prev.applied);
	if (!transaction) return prev;
	let { transactionId, previousRevs, working, batchedTransactionIds: consumedTransactions } = transaction, timestamp = (/* @__PURE__ */ new Date()).toISOString();
	return {
		...prev,
		outgoing: transaction,
		applied: prev.applied.filter((i) => !consumedTransactions.includes(i.transactionId)),
		documentStates: Object.entries(previousRevs).reduce((acc, [documentId, previousRev]) => {
			if (working[documentId]?._rev === previousRev) return acc;
			let documentState = prev.documentStates[documentId];
			return documentState && (acc[documentId] = {
				...documentState,
				unverifiedRevisions: {
					...documentState.unverifiedRevisions,
					[transactionId]: {
						documentId,
						previousRev,
						transactionId,
						timestamp
					}
				},
				recentOwnTransactionIds: [...(documentState.recentOwnTransactionIds ?? []).slice(-49), transactionId]
			}), acc;
		}, { ...prev.documentStates })
	};
}
function cleanupOutgoingTransaction(prev) {
	let { outgoing } = prev;
	if (!outgoing) return prev;
	let next = prev, ids = getDocumentIdsFromHandleLikes(outgoing.actions);
	for (let transactionId of outgoing.batchedTransactionIds) for (let documentId of ids) next = removeSubscriptionIdFromDocument(next, documentId, transactionId);
	return {
		...next,
		outgoing: void 0,
		documentStates: evictOrphanedDocumentStates(next.documentStates)
	};
}
function revertOutgoingTransaction(prev) {
	if (!prev.grants) return prev;
	let base = prev;
	if (prev.outgoing) {
		let ids = getDocumentIdsFromHandleLikes(prev.outgoing.actions);
		for (let transactionId of prev.outgoing.batchedTransactionIds) for (let documentId of ids) base = removeSubscriptionIdFromDocument(base, documentId, transactionId);
	}
	let working = Object.fromEntries(Object.entries(base.documentStates).map(([documentId, documentState]) => [documentId, documentState?.remote])), nextApplied = [];
	for (let t of prev.applied) try {
		let next = processActions({
			...t,
			working,
			grants: prev.grants,
			identity: prev.identity
		});
		working = next.working, nextApplied.push({
			...t,
			...next
		});
	} catch (error) {
		if (error instanceof ActionError) continue;
		throw error;
	}
	return {
		...base,
		applied: nextApplied,
		outgoing: void 0,
		documentStates: evictOrphanedDocumentStates(Object.fromEntries(Object.entries(base.documentStates).filter((e) => !!e[1]).map(([documentId, documentState]) => {
			let { unverifiedRevisions = {} } = documentState, next = {
				...documentState,
				unverifiedRevisions: prev.outgoing && prev.outgoing.transactionId in unverifiedRevisions ? omitProperty(unverifiedRevisions, prev.outgoing.transactionId) : unverifiedRevisions
			};
			return !next.subscriptions.length && isAwaitingOwnEcho(next) ? [documentId, next] : [documentId, {
				...next,
				local: documentId in working ? working[documentId] : next.local
			}];
		})))
	};
}
/**
* Extracts the patch operations addressed to the given document from a set of
* raw listener mutations, stripping the mutation-level `id` addressing so the
* patches are rooted at the document. A transaction can carry mutations for
* other documents, so anything not explicitly id-addressed to this document
* (including query-addressed patches) is dropped.
*/
function extractPatchOperations(mutations, documentId) {
	return mutations ? mutations.flatMap((mutation) => {
		if (!("patch" in mutation) || !mutation.patch) return [];
		let { id, ...operations } = mutation.patch;
		return id === documentId ? [operations] : [];
	}) : [];
}
function applyRemoteDocument(prev, { document, documentId, previousRev, revision, timestamp, type, mutations }, events) {
	if (!prev.grants) return prev;
	let prevDocState = prev.documentStates[documentId];
	if (!prevDocState) return prev;
	let prevUnverifiedRevisions = prevDocState.unverifiedRevisions, revisionToVerify = revision ? prevUnverifiedRevisions?.[revision] : void 0, unverifiedRevisions = prevUnverifiedRevisions ?? EMPTY_REVISIONS;
	if (revision && revisionToVerify && (unverifiedRevisions = omitProperty(prevUnverifiedRevisions, revision)), type === "mutation" && revision) {
		let patches = extractPatchOperations(mutations, documentId);
		if (patches.length) {
			let isOwnTransaction = !!revisionToVerify || (prevDocState.recentOwnTransactionIds?.includes(revision) ?? !1);
			events.next({
				type: "remote-patches",
				documentId,
				transactionId: revision,
				previousRev,
				timestamp,
				patches,
				origin: isOwnTransaction ? "local" : "remote"
			});
		}
	}
	if (type === "sync" && (unverifiedRevisions = Object.fromEntries(Object.entries(unverifiedRevisions).filter(([, unverifiedRevision]) => unverifiedRevision ? new Date(timestamp).getTime() <= new Date(unverifiedRevision.timestamp).getTime() : !1))), revisionToVerify && revisionToVerify.previousRev === previousRev) {
		let modifiesDocument = (transaction) => transaction.working[documentId]?._rev !== transaction.previousRevs[documentId], local = !prev.applied.some(modifiesDocument) && (!prev.outgoing || prev.outgoing.transactionId === revision || !modifiesDocument(prev.outgoing)) && !isDeepEqual(prevDocState.local, document) ? document : prevDocState.local;
		return {
			...prev,
			documentStates: evictOrphanedDocumentStates({
				...prev.documentStates,
				[documentId]: {
					...prevDocState,
					remote: document,
					remoteRev: revision,
					local,
					unverifiedRevisions
				}
			})
		};
	}
	let working = {
		...prev.applied.at(0)?.previous,
		[documentId]: document
	}, nextApplied = [];
	for (let curr of prev.applied) try {
		let next = processActions({
			...curr,
			working,
			grants: prev.grants,
			identity: prev.identity
		});
		working = next.working, nextApplied.push({
			...curr,
			...next
		});
	} catch (error) {
		if (error instanceof ActionError) {
			events.next({
				type: "rebase-error",
				transactionId: error.transactionId,
				documentId: error.documentId,
				message: error.message,
				error
			});
			continue;
		}
		throw error;
	}
	let nextLocal = working[documentId], local = isDeepEqual(prevDocState.local, nextLocal) ? prevDocState.local : nextLocal;
	return {
		...prev,
		applied: nextApplied,
		documentStates: evictOrphanedDocumentStates({
			...prev.documentStates,
			[documentId]: {
				...prevDocState,
				remote: document,
				remoteRev: revision,
				local,
				unverifiedRevisions
			}
		})
	};
}
function addSubscriptionIdToDocument(prev, documentId, subscriptionId) {
	let prevDocState = prev.documentStates?.[documentId], prevSubscriptions = prevDocState?.subscriptions ?? [];
	return {
		...prev,
		documentStates: {
			...prev.documentStates,
			[documentId]: {
				...prevDocState,
				id: documentId,
				subscriptions: [...prevSubscriptions, subscriptionId]
			}
		}
	};
}
function removeSubscriptionIdFromDocument(prev, documentId, subscriptionId) {
	let prevDocState = prev.documentStates?.[documentId];
	if (!prevDocState) return prev;
	let subscriptions = prevDocState.subscriptions.filter((id) => id !== subscriptionId);
	return !subscriptions.length && !isAwaitingOwnEcho(prevDocState) ? {
		...prev,
		documentStates: omitProperty(prev.documentStates, documentId)
	} : {
		...prev,
		documentStates: {
			...prev.documentStates,
			[documentId]: {
				...prevDocState,
				subscriptions
			}
		}
	};
}
/**
* Whether this document still expects the listener echo of one of its own
* submitted transactions. That expectation is the only reason a document state
* with no subscribers is kept around.
*
* Revisions stop counting after `UNVERIFIED_REVISION_RETENTION_TIME` because an
* echo is not guaranteed to arrive at all: Content Lake records no transaction
* when every operation resolves to a no-op (e.g. a keyed path that no longer
* exists), while the client bumps `_rev` regardless and so tracks a revision
* for it. Without the expiry, such a document (and the listener keyed off its
* state) would be pinned for the lifetime of the store.
*
* `recentOwnTransactionIds` deliberately does not extend the wait: it is capped
* rather than drained, so honoring it would keep every edited document alive.
*/
function isAwaitingOwnEcho(documentState) {
	let expiresAfter = Date.now() - 3e4;
	return Object.values(documentState.unverifiedRevisions ?? {}).some((revision) => !!revision && new Date(revision.timestamp).getTime() > expiresAfter);
}
/**
* Removes every document state that has no subscribers left and no own
* transaction still awaiting its echo.
*
* This is a sweep rather than a decision made where the last subscriber leaves
* because `removeSubscriptionIdFromDocument` defers that eviction while an echo
* is in flight, and both halves of the condition can change without a
* subscriber going anywhere: the echo can verify, be dropped by a sync, be
* discarded by a revert, or simply never arrive. Every reducer that can settle
* an in-flight transaction sweeps, so the deferred decision is always re-made.
*
* Note this can evict the very document whose remote event is being processed,
* which synchronously unsubscribes that document's listener from inside its own
* emission (see `subscribeToSubscriptionsAndListenToDocuments`).
*/
function evictOrphanedDocumentStates(documentStates) {
	let next = documentStates;
	for (let [documentId, documentState] of Object.entries(documentStates)) documentState && (documentState.subscriptions.length || isAwaitingOwnEcho(documentState) || (next = omitProperty(next, documentId)));
	return next;
}
function manageSubscriberIds({ state }, handles) {
	let documentIds = getDocumentIdsFromHandleLikes(handles), subscriptionId = randomId(16);
	return state.set("addSubscribers", (prev) => documentIds.reduce((acc, id) => addSubscriptionIdToDocument(acc, id, subscriptionId), prev)), () => {
		setCleanupTimeout(() => {
			state.set("removeSubscribers", (prev) => documentIds.reduce((acc, id) => removeSubscriptionIdFromDocument(acc, id, subscriptionId), prev));
		}, 1e3);
	};
}
function getDocumentIdsFromHandleLikes(handles) {
	return handles.flatMap((handle) => {
		if ("type" in handle && isReleaseAction(handle)) return [getReleaseDocumentId(handle.releaseId)];
		let idsForDocument = [];
		return handle.documentId ? handle.liveEdit ? [handle.documentId] : (isReleasePerspective(handle.perspective) && idsForDocument.push(getVersionId(DocumentId(handle.documentId), handle.perspective.releaseName)), idsForDocument.push(getPublishedId(DocumentId(handle.documentId))), idsForDocument.push(getDraftId(DocumentId(handle.documentId))), idsForDocument) : [];
	});
}
const actionMap = {
	"document.create": "created",
	"document.delete": "deleted",
	"document.discard": "discarded",
	"document.edit": "edited",
	"document.publish": "published",
	"document.unpublish": "unpublished",
	"release.create": "created",
	"release.edit": "edited",
	"release.delete": "deleted"
};
function getDocumentEvents(outgoing) {
	let documentIdsByAction = outgoing.actions.reduce((acc, action) => {
		if (!(action.type in actionMap)) return acc;
		let documentId = isReleaseAction(action) ? getReleaseDocumentId(action.releaseId) : action.documentId;
		if (!documentId) return acc;
		let type = action.type, ids = acc[type] ?? /* @__PURE__ */ new Set();
		return ids.add(documentId), acc[type] = ids, acc;
	}, {});
	return Object.entries(documentIdsByAction).flatMap(([actionType, documentIds]) => Array.from(documentIds ?? []).map((documentId) => ({
		type: actionMap[actionType],
		documentId,
		outgoing
	})));
}
/**
* Drops mutation events that have already been observed, keyed by
* `transactionId#documentId`. With `enableResume`, the listener replays
* missed events after a reconnect on an at-least-once basis, so the same
* event can be delivered twice. A duplicate would never chain onto the
* current base revision and would clog the sequencing buffer until it
* overflows into an `OutOfSyncError`.
*
* Seen keys expire after `ttl` milliseconds and the cache is capped at
* `maxEntries`, evicting the oldest entries first.
*
* @internal
*/
function dedupeListenerEvents(options) {
	let { ttl = 12e4, maxEntries = 1e3 } = options ?? {};
	return (source) => defer(() => {
		let seen = /* @__PURE__ */ new Map();
		return source.pipe(filter((event) => {
			if (event.type !== "mutation") return !0;
			let key = `${event.transactionId}#${event.documentId}`, now = Date.now(), expiry = seen.get(key);
			if (expiry !== void 0 && expiry > now) return !1;
			seen.delete(key), seen.set(key, now + ttl);
			for (let [existingKey, existingExpiry] of seen) {
				if (seen.size <= maxEntries && existingExpiry > now) break;
				seen.delete(existingKey);
			}
			return !0;
		}));
	});
}
/**
* Buffers the mutation events of multi-document transactions (e.g. a publish
* touching both the draft and the published document) until every event of
* the transaction has arrived, then emits them contiguously. Without this,
* a rebase can run between the two halves of a transaction and observe the
* dataset in a state that never existed on the server (draft deleted, but
* published not yet updated).
*
* The backend's streaming listener pipeline delivers a transaction's events
* contiguously and in order, but the legacy pipeline can interleave
* concurrent transactions. So while a multi-document transaction is
* incomplete, mutation events from other transactions are held (in arrival
* order) rather than emitted, and released right after the transaction
* completes; released events are re-processed so a held multi-document
* transaction is grouped too. Per-document `previousRev` sequencing
* downstream tolerates the reordering this introduces.
*
* Escape hatches so a lost event degrades to ungrouped delivery instead of
* stalling the stream: everything is released in arrival order when a
* non-mutation event (e.g. reconnect/welcome) arrives, when more than
* `maxHeldEvents` are held, or after `flushDeadline` milliseconds.
* Single-event transactions pass through untouched when nothing is buffered.
*
* @internal
*/
function groupTransactionEvents(options) {
	let { flushDeadline = 3e4, maxHeldEvents = 100 } = options ?? {};
	return (source) => new Observable((subscriber) => {
		let open = null, held = [], closeOpen = () => {
			if (!open) return;
			clearTimeout(open.timer);
			let { events } = open;
			open = null;
			for (let event of events) subscriber.next(event);
		}, releaseHeld = () => {
			let queue = held;
			held = [];
			for (let event of queue) processEvent(event);
		}, releaseEverything = () => {
			closeOpen();
			let queue = held;
			held = [];
			for (let event of queue) subscriber.next(event);
		}, processEvent = (event) => {
			if (event.type !== "mutation") {
				releaseEverything(), subscriber.next(event);
				return;
			}
			let isMultiDocument = (event.transactionTotalEvents ?? 0) > 1;
			if (!open) {
				if (!isMultiDocument) {
					subscriber.next(event);
					return;
				}
				open = {
					transactionId: event.transactionId,
					events: [event],
					total: event.transactionTotalEvents,
					timer: setCleanupTimeout(() => {
						closeOpen(), releaseHeld();
					}, flushDeadline)
				};
				return;
			}
			if (event.transactionId === open.transactionId) {
				open.events.push(event), open.events.length >= open.total && (closeOpen(), releaseHeld());
				return;
			}
			held.push(event), held.length > maxHeldEvents && releaseEverything();
		}, subscription = source.subscribe({
			next: processEvent,
			error: (error) => {
				releaseEverything(), subscriber.error(error);
			},
			complete: () => {
				releaseEverything(), subscriber.complete();
			}
		});
		return () => {
			open && clearTimeout(open.timer), open = null, held = [], subscription.unsubscribe();
		};
	});
}
const API_VERSION$3 = "v2025-05-06";
function createSharedListener(instance, resource) {
	let dispose$ = new Subject(), events$ = getClientState(instance, {
		apiVersion: API_VERSION$3,
		resource
	}).observable.pipe(switchMap((client) => client.listen("*", {}, {
		events: [
			"mutation",
			"welcome",
			"welcomeback",
			"reconnect",
			"reset"
		],
		enableResume: !0,
		includeResult: !1,
		includeAllVersions: !0,
		tag: "document-listener"
	})), dedupeListenerEvents(), groupTransactionEvents(), takeUntil(dispose$), share()), [welcome$, mutation$] = partition(events$, (e) => e.type === "welcome");
	return {
		events: merge(welcome$.pipe(shareReplay(1)), mutation$),
		dispose: () => dispose$.next()
	};
}
function createFetchDocument(instance, resource) {
	return function(documentId) {
		return getClientState(instance, {
			apiVersion: API_VERSION$3,
			resource
		}).observable.pipe(switchMap((client) => createDocumentLoaderFromClient(client)(documentId)), map((result) => {
			if (!result.accessible) {
				if (result.reason === "existence") return null;
				throw Error(`Document with ID \`${documentId}\` is inaccessible due to permissions.`);
			}
			return result.document;
		}), first());
	};
}
const documentStore = defineStore({
	name: "Document",
	getInitialState: (instance, { resource }) => ({
		documentStates: {},
		queued: [],
		applied: [],
		sharedListener: createSharedListener(instance, resource),
		fetchDocument: createFetchDocument(instance, resource),
		events: new Subject()
	}),
	initialize(context) {
		let { sharedListener } = context.state.get(), subscriptions = [
			subscribeToQueuedAndApplyNextTransaction(context),
			subscribeToSubscriptionsAndListenToDocuments(context),
			subscribeToAppliedAndSubmitNextTransaction(context),
			subscribeToClientAndFetchDatasetAcl(context),
			subscribeToCurrentUserAndSetIdentity(context)
		];
		return () => {
			sharedListener.dispose(), subscriptions.forEach((subscription) => subscription.unsubscribe());
		};
	}
});
/** @beta */
function getDocumentState(...args) {
	return _getDocumentState(...args);
}
const _getDocumentState = bindActionByResource(documentStore, createStateSourceAction({
	selector: ({ state: { error, documentStates } }, options) => {
		let { documentId: docId, path, liveEdit, perspective } = options, documentId = DocumentId(docId);
		if (error) throw error;
		let document;
		if (liveEdit) document = documentStates[documentId]?.local;
		else {
			let version;
			if (isReleasePerspective(perspective) && (version = documentStates[getVersionId(documentId, perspective.releaseName)]?.local, version === void 0)) return;
			let draft = documentStates[getDraftId(documentId)]?.local, published = documentStates[getPublishedId(documentId)]?.local;
			if (draft === void 0 || published === void 0) return;
			document = version ?? draft ?? published;
		}
		if (!path) return document;
		let result = jsonMatch$1(document, path).next();
		if (result.done) return;
		let { value } = result.value;
		return value;
	},
	onSubscribe: (context, options) => manageSubscriberIds(context, [options])
}));
/** @beta */
function resolveDocument(...args) {
	return _resolveDocument(...args);
}
const _resolveDocument = bindActionByResource(documentStore, ({ instance }, docHandle) => firstValueFrom(getDocumentState(instance, {
	...docHandle,
	path: void 0
}).observable.pipe(filter((i) => i !== void 0)))), getDocumentSyncStatus = bindActionByResource(documentStore, createStateSourceAction({
	selector: ({ state: { error, documentStates: documents, outgoing, applied, queued } }, doc) => {
		let documentId = DocumentId(typeof doc == "string" ? doc : doc.documentId);
		if (error) throw error;
		if (doc.liveEdit) {
			if (documents[documentId] === void 0) return;
		} else {
			let version = isReleasePerspective(doc.perspective) ? documents[getVersionId(documentId, doc.perspective.releaseName)] : void 0;
			if (isReleasePerspective(doc.perspective) && version === void 0) return;
			let draft = documents[getDraftId(documentId)], published = documents[getPublishedId(documentId)];
			if (draft === void 0 || published === void 0) return;
		}
		return !queued.length && !applied.length && !outgoing;
	},
	onSubscribe: (context, doc) => manageSubscriberIds(context, [doc])
})), getPermissionsState = bindActionByResource(documentStore, createStateSourceAction({
	selector: calculatePermissions,
	onSubscribe: (context, { actions }) => {
		manageSubscriberIds(context, actions);
	}
})), resolvePermissions = bindActionByResource(documentStore, ({ instance }, options) => firstValueFrom(getPermissionsState(instance, options).observable.pipe(filter((i) => i !== void 0)))), subscribeDocumentEvents = bindActionByResource(documentStore, ({ state }, options) => {
	let { events } = state.get(), subscription = events.subscribe(options.eventHandler);
	return () => subscription.unsubscribe();
}), subscribeToQueuedAndApplyNextTransaction = ({ state }) => {
	let { events } = state.get();
	return state.observable.pipe(map(applyFirstQueuedTransaction), distinctUntilChanged(), tap((next) => state.set("applyFirstQueuedTransaction", next)), catchError((error, caught) => {
		if (error instanceof ActionError) return state.set("removeQueuedTransaction", (prev) => removeQueuedTransaction(prev, error.transactionId)), events.next({
			type: "error",
			message: error.message,
			documentId: error.documentId,
			transactionId: error.transactionId,
			error
		}), caught;
		throw error;
	})).subscribe({ error: (error) => state.set("setError", { error }) });
}, subscribeToAppliedAndSubmitNextTransaction = ({ state, instance, key: { resource } }) => {
	let { events } = state.get();
	return state.observable.pipe(throttle((s) => s.outgoing ? state.observable.pipe(first(({ outgoing }) => !outgoing)) : timer(1e3), {
		leading: !1,
		trailing: !0
	}), map(transitionAppliedTransactionsToOutgoing), distinctUntilChanged((a, b) => a.outgoing?.transactionId === b.outgoing?.transactionId), tap((next) => state.set("transitionAppliedTransactionsToOutgoing", next)), map((s) => s.outgoing), distinctUntilChanged(), withLatestFrom(getClientState(instance, {
		apiVersion: API_VERSION$4,
		resource
	}).observable), concatMap(([outgoing, client]) => {
		if (!outgoing) return EMPTY;
		let revertOnError = catchError((error) => {
			state.set("revertOutgoingTransaction", revertOutgoingTransaction);
			let message = error instanceof Error ? error.message : "Request failed";
			return events.next({
				type: "reverted",
				message,
				outgoing,
				error
			}), EMPTY;
		}), toResult = map((result) => ({
			result,
			outgoing
		}));
		return outgoing.actions.some((action) => !isReleaseAction(action) && action.liveEdit) ? client.observable.mutate(outgoing.outgoingMutations, {
			transactionId: outgoing.transactionId,
			visibility: "async",
			returnDocuments: !1,
			returnFirst: !1,
			tag: "document.mutate",
			skipCrossDatasetReferenceValidation: !0
		}).pipe(revertOnError, toResult) : client.observable.action(outgoing.outgoingActions, {
			transactionId: outgoing.transactionId,
			skipCrossDatasetReferenceValidation: !0,
			tag: "document.action"
		}).pipe(revertOnError, toResult);
	}), tap(({ outgoing, result }) => {
		state.set("cleanupOutgoingTransaction", cleanupOutgoingTransaction);
		for (let e of getDocumentEvents(outgoing)) events.next(e);
		events.next({
			type: "accepted",
			outgoing,
			result
		});
	})).subscribe({ error: (error) => state.set("setError", { error }) });
}, subscribeToSubscriptionsAndListenToDocuments = (context) => {
	let { state } = context, { events } = state.get();
	return state.observable.pipe(filter((s) => !!s.grants), map((s) => Object.keys(s.documentStates)), distinctUntilChanged((curr, next) => {
		if (curr.length !== next.length) return !1;
		let currSet = new Set(curr);
		return next.every((i) => currSet.has(i));
	}), startWith(/* @__PURE__ */ new Set()), pairwise(), switchMap((pair) => {
		let [curr, next] = pair.map((ids) => new Set(ids)), added = Array.from(next).filter((i) => !curr.has(i)), removed = Array.from(curr).filter((i) => !next.has(i)), changes = [...added.map((id) => ({
			id,
			add: !0
		})), ...removed.map((id) => ({
			id,
			add: !1
		}))].sort((a, b) => {
			let aIsDraft = a.id === getDraftId(DocumentId(a.id)), bIsDraft = b.id === getDraftId(DocumentId(b.id));
			return aIsDraft && bIsDraft ? a.id.localeCompare(b.id, "en-US") : aIsDraft ? -1 : bIsDraft ? 1 : a.id.localeCompare(b.id, "en-US");
		});
		return of(...changes);
	}), groupBy((i) => i.id), mergeMap((group) => group.pipe(switchMap((e) => e.add ? listen(context, e.id).pipe(retry({ delay: (error, retryCount) => {
		if (!(error instanceof OutOfSyncError)) return throwError(() => error);
		let backoff = Math.min(500 * 2 ** (retryCount - 1), 1e4);
		return timer(backoff);
	} }), tap((remote) => state.set("applyRemoteDocument", (prev) => applyRemoteDocument(prev, remote, events)))) : EMPTY)))).subscribe({ error: (error) => state.set("setError", { error }) });
}, subscribeToClientAndFetchDatasetAcl = ({ instance, state, key: { resource } }) => {
	let clientOptions = {
		apiVersion: API_VERSION$4,
		resource
	}, url;
	if (resource && isDatasetResource(resource)) url = `/projects/${resource.projectId}/datasets/${resource.dataset}/acl`;
	else if (resource && isMediaLibraryResource(resource)) url = `/media-libraries/${resource.mediaLibraryId}/acl`;
	else if (resource && isCanvasResource(resource)) url = `/canvases/${resource.canvasId}/acl`;
	else throw Error(`Received invalid resource: ${JSON.stringify(resource)}`);
	return getClientState(instance, clientOptions).observable.pipe(switchMap((client) => client.observable.request({
		url,
		tag: "acl.get"
	}).pipe(retry({ delay: (error, retryCount) => {
		let isTransientClientError = error instanceof ClientError && (error.statusCode === 408 || error.statusCode === 429);
		if (error instanceof ClientError && !isTransientClientError || error instanceof CorsOriginError) return throwError(() => error);
		let backoff = Math.min(500 * 2 ** (retryCount - 1), 1e4);
		return timer(backoff);
	} }))), tap((datasetAcl) => state.set("setGrants", { grants: createGrantsLookup(datasetAcl) }))).subscribe({ error: (error) => state.set("setError", { error }) });
}, subscribeToCurrentUserAndSetIdentity = ({ instance, state }) => getCurrentUserState(instance).observable.subscribe({
	next: (currentUser) => state.set("setIdentity", { identity: currentUser?.id }),
	error: () => state.set("setIdentity", { identity: void 0 })
}), MEDIA_LIBRARY_DRAFTED_TYPES = /* @__PURE__ */ new Set(["sanity.asset"]);
/**
* Different resources have different "default" editing models.
*
* Canvas uses a liveEdit model.
* Medial Library is mostly liveEdit except for `sanity.asset`, which retains the
* draft/published model. Neither resource supports release perspectives.
*/
function getEffectiveDocumentModel(resource, documentType) {
	return resource ? isCanvasResource(resource) ? {
		liveEdit: !0,
		supportsReleases: !1
	} : isMediaLibraryResource(resource) ? {
		liveEdit: !(documentType && MEDIA_LIBRARY_DRAFTED_TYPES.has(documentType)),
		supportsReleases: !1
	} : {
		liveEdit: void 0,
		supportsReleases: !0
	} : {
		liveEdit: void 0,
		supportsReleases: !0
	};
}
function describeResource(resource) {
	return resource && isCanvasResource(resource) ? "Canvas" : resource && isMediaLibraryResource(resource) ? "Media Library" : "this resource";
}
/**
* Rewrites edit actions to match the editing model of the bound resource.
*
* Canvas and Media Library resources do not support release perspectives, and
* Canvas (plus most Media Library types) does not have a draft/published
* model. When an edit action arrives with a release perspective for a resource
* that doesn't support it, the perspective is stripped and a single warning
* is logged. When the resource forces liveEdit, the action's `liveEdit` flag
* is set so the dispatcher takes the liveEdit branch.
*
* Only `document.edit` actions are normalized today — other action types
* (publish, unpublish, release actions, etc.) pass through unchanged.
*
* @internal
*/
function normalizeActionsForResource(actions, resource) {
	let stripped = [], normalized = actions.map((action) => {
		if (action.type !== "document.edit") return action;
		let { liveEdit: forcedLiveEdit, supportsReleases } = getEffectiveDocumentModel(resource, action.documentType), shouldRemovePerspective = isReleasePerspective(action.perspective) && !supportsReleases, shouldForceLiveEdit = forcedLiveEdit === !0 && !action.liveEdit;
		if (!shouldRemovePerspective && !shouldForceLiveEdit) return action;
		let corrected = { ...action };
		return shouldForceLiveEdit && (corrected.liveEdit = !0), shouldRemovePerspective && (corrected.perspective = void 0), corrected.documentId = getEffectiveDocumentId({
			...corrected,
			documentId: getPublishedId(DocumentId(corrected.documentId))
		}), shouldRemovePerspective && stripped.push({
			documentType: action.documentType,
			documentId: corrected.documentId
		}), corrected;
	});
	if (stripped.length > 0) {
		let docs = stripped.map((e) => `${e.documentType} (${e.documentId})`).join(", ");
		console.warn(`[sanity-sdk] ${describeResource(resource)} does not support release perspectives — falling back to the standard editing path for: ${docs}`);
	}
	return normalized;
}
/** @beta */
function applyDocumentActions(...args) {
	return boundApplyDocumentActions(...args);
}
const boundApplyDocumentActions = bindActionByResource(documentStore, _applyDocumentActions);
/** @internal */
async function _applyDocumentActions({ state }, { actions, resource, transactionId = randomUuid(), disableBatching }) {
	let { events } = state.get(), transaction = {
		transactionId,
		actions: normalizeActionsForResource(actions, resource),
		...disableBatching && { disableBatching }
	}, fatalError$ = state.observable.pipe(map((s) => s.error), first(Boolean), map((error) => ({
		type: "error",
		error
	}))), transactionError$ = events.pipe(filter((e) => e.type === "error"), first((e) => e.transactionId === transactionId)), appliedTransaction$ = state.observable.pipe(map((s) => s.applied), distinctUntilChanged(), map((applied) => applied.find((t) => t.transactionId === transactionId)), first(Boolean)), successfulTransaction$ = events.pipe(filter((e) => e.type === "accepted"), first((e) => e.outgoing.batchedTransactionIds.includes(transactionId))), rejectedTransaction$ = events.pipe(filter((e) => e.type === "reverted"), first((e) => e.outgoing.batchedTransactionIds.includes(transactionId))), appliedTransactionOrError = firstValueFrom(race([
		fatalError$,
		transactionError$,
		appliedTransaction$
	])), acceptedOrRejectedTransaction = firstValueFrom(race([
		successfulTransaction$,
		rejectedTransaction$,
		transactionError$
	]));
	state.set("queueTransaction", (prev) => queueTransaction(prev, transaction));
	let result = await appliedTransactionOrError;
	if ("type" in result && result.type === "error") throw result.error;
	let { working: documents, previous, previousRevs } = result, existingIds = new Set(Object.entries(previous).filter(([, value]) => !!value).map(([key]) => key)), resultingIds = new Set(Object.entries(documents).filter(([, value]) => !!value).map(([key]) => key)), allIds = /* @__PURE__ */ new Set([...existingIds, ...resultingIds]), updated = [], appeared = [], disappeared = [];
	for (let id of allIds) existingIds.has(id) && resultingIds.has(id) ? updated.push(id) : !existingIds.has(id) && resultingIds.has(id) ? appeared.push(id) : !resultingIds.has(id) && existingIds.has(id) && disappeared.push(id);
	async function submitted() {
		let raceResult = await acceptedOrRejectedTransaction;
		if (raceResult.type !== "accepted") throw raceResult.error;
		return raceResult.result;
	}
	return {
		transactionId,
		documents,
		previous,
		previousRevs,
		appeared,
		updated,
		disappeared,
		submitted
	};
}
function createFavoriteKey(context) {
	return `${context.documentId}:${context.documentType}:${context.resourceId}:${context.resourceType}${context.schemaName ? `:${context.schemaName}` : ""}`;
}
/**
* Fetcher for a document's favorite status, read from the dashboard over
* comlink, on the shared fetcher cache.
*
* @internal
*/
const favorites = defineFetcher({
	name: "favorites",
	getKey: (_instance, context) => createFavoriteKey(context),
	tags: (_data, context) => [{
		type: "favorite",
		id: createFavoriteKey(context)
	}],
	fetch: (instance) => (context) => {
		let nodeStateSource = getNodeState(instance, {
			name: SDK_NODE_NAME,
			connectTo: SDK_CHANNEL_NAME
		}), payload = { document: {
			id: context.documentId,
			type: context.documentType,
			resource: {
				id: context.resourceId,
				type: context.resourceType,
				schemaName: context.schemaName
			}
		} };
		return nodeStateSource.observable.pipe(first((nodeState) => !!nodeState), switchMap((nodeState) => {
			let node = nodeState.node;
			return from(node.fetch("dashboard/v1/events/favorite/query", payload)).pipe(map((response) => ({ isFavorited: response.isFavorited })), catchError((err) => (console.error("Favorites service connection error", err), of({ isFavorited: !1 }))));
		}));
	}
}), setFavorite = defineMutation({
	name: "setFavorite",
	mutationFn: (instance) => ({ isFavorited, ...context }) => {
		let nodeStateSource = getNodeState(instance, {
			name: SDK_NODE_NAME,
			connectTo: SDK_CHANNEL_NAME
		}), payload = {
			eventType: isFavorited ? "added" : "removed",
			document: {
				id: context.documentId,
				type: context.documentType,
				resource: {
					id: context.resourceId,
					type: context.resourceType,
					...context.schemaName ? { schemaName: context.schemaName } : {}
				}
			}
		};
		return nodeStateSource.observable.pipe(first((nodeState) => !!nodeState), switchMap((nodeState) => {
			let node = nodeState.node;
			return from(node.fetch("dashboard/v1/events/favorite/mutate", payload)).pipe(map((response) => {
				if (!response.success) throw Error("Failed to update favorite status");
				return { isFavorited };
			}));
		}));
	},
	invalidates: (_result, input) => [{
		type: "favorite",
		id: createFavoriteKey(input)
	}]
});
/** The API takes `include` as a comma-separated list; sorted so key and query agree */
function serializeInclude(include) {
	return include?.length ? [...include].sort().join(",") : void 0;
}
/**
* Fetcher for an organization's installations (`GET /installations`), on the
* shared fetcher cache.
*
* @see https://www.sanity.io/docs/http-reference/applications-api
* @internal
*/
const installations = defineFetcher({
	name: "installations",
	getKey: (_instance, options) => [
		options.organizationId,
		serializeInclude(options.include) ?? "",
		options.limit ?? "",
		options.cursor ?? ""
	].join(":"),
	fetch: (instance) => (options) => getClientState(instance, {
		apiVersion: "vX",
		scope: "global"
	}).observable.pipe(switchMap((client) => client.observable.request({
		url: "/installations",
		query: buildQuery({
			organizationId: options.organizationId,
			include: serializeInclude(options.include),
			limit: options.limit,
			cursor: options.cursor
		}),
		tag: "installations.list"
	}))),
	tags: (data) => [{
		type: "installation",
		id: "LIST"
	}, ...data.data.map((installation) => ({
		type: "installation",
		id: installation.id
	}))]
}), installation = defineFetcher({
	name: "installation",
	getKey: (_instance, installationId, options) => `${installationId}:${serializeInclude(options?.include) ?? ""}`,
	fetch: (instance) => (installationId, options) => getClientState(instance, {
		apiVersion: "vX",
		scope: "global"
	}).observable.pipe(switchMap((client) => client.observable.request({
		url: `/installations/${installationId}`,
		query: buildQuery({ include: serializeInclude(options?.include) }),
		tag: "installations.get"
	}))),
	tags: (data) => [{
		type: "installation",
		id: data.id
	}]
});
function resolveOrganizationId(options) {
	let organizationId = options?.organizationId;
	if (!organizationId) throw Error("An organizationId is required to use the organization API.");
	return organizationId;
}
function normalizeOrganizationOptions(options) {
	return {
		includeMembers: options?.includeMembers ?? !1,
		includeFeatures: options?.includeFeatures ?? !1
	};
}
/** @internal */
function getOrganizationCacheKey(_instance, options) {
	let organizationId = resolveOrganizationId(options), { includeMembers, includeFeatures } = normalizeOrganizationOptions(options);
	return `organization:${organizationId}${includeMembers ? ":members" : ""}${includeFeatures ? ":features" : ""}`;
}
/**
* Fetcher for a single organization (`GET /organizations/:id`), on the shared
* fetcher cache. No pre-seeding from the organizations list: it returns a
* subset of the detail fields, and a partial seed would serve as fresh for
* the whole stale window.
*
* @internal
*/
const organization = defineFetcher({
	name: "organization",
	getKey: getOrganizationCacheKey,
	fetch: (instance) => (options) => getClientState(instance, {
		apiVersion: "v2025-02-19",
		scope: "global"
	}).observable.pipe(switchMap((client) => client.observable.request({
		url: `/organizations/${resolveOrganizationId(options)}`,
		query: buildQuery(normalizeOrganizationOptions(options)),
		tag: "organizations.get"
	}))),
	tags: (data) => [{
		type: "organization",
		id: data.id
	}]
});
function normalizeOrganizationsOptions(options) {
	return {
		includeImplicitMemberships: options?.includeImplicitMemberships ?? !1,
		includeMembers: options?.includeMembers ?? !1,
		includeFeatures: options?.includeFeatures ?? !1
	};
}
/** @internal */
function getOrganizationsCacheKey(_instance, options) {
	let { includeMembers, includeFeatures, includeImplicitMemberships } = normalizeOrganizationsOptions(options);
	return `organizations${includeMembers ? ":members" : ""}${includeFeatures ? ":features" : ""}${includeImplicitMemberships ? ":implicit" : ""}`;
}
/**
* Fetcher for the current user's organizations (`GET /organizations`), on the
* shared fetcher cache.
*
* @internal
*/
const organizations = defineFetcher({
	name: "organizations",
	getKey: getOrganizationsCacheKey,
	fetch: (instance) => (options) => getClientState(instance, {
		apiVersion: "v2025-02-19",
		scope: "global"
	}).observable.pipe(switchMap((client) => client.observable.request({
		url: "/organizations",
		query: buildQuery(normalizeOrganizationsOptions(options)),
		tag: "organizations.list"
	}))),
	tags: (data) => [{
		type: "organization",
		id: "LIST"
	}, ...data.map((org) => ({
		type: "organization",
		id: org.id
	}))]
}), usersStore = defineStore({
	name: "UsersStore",
	getInitialState: () => ({ users: {} }),
	initialize: (context) => {
		let subscription = listenForLoadMoreAndFetch(context);
		return () => subscription.unsubscribe();
	}
}), errorHandler = (state) => (error) => state.set("setError", { error }), listenForLoadMoreAndFetch = ({ state, instance }) => state.observable.pipe(map((s) => new Set(Object.keys(s.users))), distinctUntilChanged((curr, next) => curr.size === next.size && Array.from(next).every((i) => curr.has(i))), startWith(/* @__PURE__ */ new Set()), pairwise(), mergeMap(([curr, next]) => {
	let added = Array.from(next).filter((i) => !curr.has(i)), removed = Array.from(curr).filter((i) => !next.has(i));
	return [...added.map((key) => ({
		key,
		added: !0
	})), ...removed.map((key) => ({
		key,
		added: !1
	}))];
}), groupBy((i) => i.key), mergeMap((group$) => group$.pipe(switchMap((e) => {
	if (!e.added) return EMPTY;
	let { userId, batchSize, ...options } = parseUsersKey(group$.key);
	if (userId) {
		if (userId.startsWith("p")) return getClient(instance, {
			apiVersion: PROJECT_API_VERSION,
			projectId: options.projectId,
			useProjectHostname: !0
		}).observable.request({
			method: "GET",
			url: `/users/${userId}`,
			tag: "users.get"
		}).pipe(map((user) => ({
			data: [{
				sanityUserId: user.sanityUserId,
				profile: {
					id: user.id,
					displayName: user.displayName,
					familyName: user.familyName ?? void 0,
					givenName: user.givenName ?? void 0,
					middleName: user.middleName ?? void 0,
					imageUrl: user.imageUrl ?? void 0,
					createdAt: user.createdAt,
					updatedAt: user.updatedAt,
					isCurrentUser: user.isCurrentUser,
					email: user.email,
					provider: user.provider
				},
				memberships: []
			}],
			totalCount: 1,
			nextCursor: null
		})), catchError((error) => (state.set("setUsersError", setUsersError(group$.key, error)), EMPTY)), tap((response) => state.set("setUsersData", setUsersData(group$.key, response))));
		let scope = userId.startsWith("g") ? "global" : void 0, client = getClient(instance, {
			scope,
			apiVersion: "vX"
		}), resourceType = options.resourceType || "project", resourceId = resourceType === "organization" ? options.organizationId : options.projectId;
		return resourceId ? client.observable.request({
			method: "GET",
			url: `access/${resourceType}/${resourceId}/users/${userId}`,
			tag: "users.get"
		}).pipe(map((response) => "sanityUserId" in response ? {
			data: [response],
			totalCount: 1,
			nextCursor: null
		} : response), catchError((error) => (state.set("setUsersError", setUsersError(group$.key, error)), EMPTY)), tap((response) => state.set("setUsersData", setUsersData(group$.key, response)))) : throwError(() => /* @__PURE__ */ Error("An organizationId or a projectId is required"));
	}
	let projectId = options.projectId, resourceType = options.resourceType ?? (options.organizationId ? "organization" : projectId ? "project" : "organization"), organizationId$ = options.organizationId ? of(options.organizationId) : getDashboardOrganizationId(instance).observable.pipe(filter((i) => typeof i == "string")), resource$ = resourceType === "project" ? projectId ? of({
		type: "project",
		id: projectId
	}) : throwError(() => /* @__PURE__ */ Error("Project ID required for this API.")) : organizationId$.pipe(map((id) => ({
		type: "organization",
		id
	}))), client$ = getClientState(instance, {
		scope: "global",
		apiVersion: "vX"
	}).observable, loadMore$ = state.observable.pipe(map((s) => s.users[group$.key]?.lastLoadMoreRequest), distinctUntilChanged()), cursor$ = state.observable.pipe(map((s) => s.users[group$.key]?.nextCursor), distinctUntilChanged(), filter((cursor) => cursor !== null));
	return combineLatest([
		resource$,
		client$,
		loadMore$
	]).pipe(withLatestFrom(cursor$), switchMap(([[resource, client], cursor]) => client.observable.request({
		method: "GET",
		url: `access/${resource.type}/${resource.id}/users`,
		tag: "users.list",
		query: cursor ? {
			nextCursor: cursor,
			limit: batchSize.toString()
		} : { limit: batchSize.toString() }
	})), catchError((error) => (state.set("setUsersError", setUsersError(group$.key, error)), EMPTY)), tap((response) => state.set("setUsersData", setUsersData(group$.key, response))));
})))).subscribe({ error: errorHandler(state) }), getUsersState = bindActionGlobally(usersStore, createStateSourceAction({
	selector: createSelector([
		({ instance, state }, options) => state.error ?? state.users[getUsersKey(instance, options)]?.error,
		({ instance, state }, options) => state.users[getUsersKey(instance, options)]?.users,
		({ instance, state }, options) => state.users[getUsersKey(instance, options)]?.totalCount,
		({ instance, state }, options) => state.users[getUsersKey(instance, options)]?.nextCursor
	], (error, data, totalCount, nextCursor) => {
		if (error) throw error;
		if (data !== void 0 && totalCount !== void 0 && nextCursor !== void 0) return {
			data,
			totalCount,
			hasMore: nextCursor !== null
		};
	}),
	onSubscribe: ({ instance, state }, options) => {
		let subscriptionId = randomId(16), key = getUsersKey(instance, options);
		return state.set("addSubscription", addSubscription(subscriptionId, key)), () => {
			setCleanupTimeout(() => state.set("removeSubscription", removeSubscription(subscriptionId, key)), USERS_STATE_CLEAR_DELAY);
		};
	}
})), resolveUsers = bindActionGlobally(usersStore, async ({ state, instance }, { signal, ...options }) => {
	let key = getUsersKey(instance, options), { getCurrent } = getUsersState(instance, options), aborted$ = signal ? new Observable((observer) => {
		let cleanup = () => {
			signal.removeEventListener("abort", listener);
		}, listener = () => {
			observer.error(new DOMException("The operation was aborted.", "AbortError")), observer.complete(), cleanup();
		};
		return signal.addEventListener("abort", listener), cleanup;
	}).pipe(catchError((error) => {
		throw error instanceof Error && error.name === "AbortError" && state.set("cancelRequest", cancelRequest(key)), error;
	})) : NEVER;
	state.set("initializeRequest", initializeRequest(key));
	let resolved$ = state.observable.pipe(map(getCurrent), first((i) => i !== void 0));
	return firstValueFrom(race([resolved$, aborted$]));
}), loadMoreUsers = bindActionGlobally(usersStore, async ({ state, instance }, options) => {
	let key = getUsersKey(instance, options), users = getUsersState(instance, options), usersState = users.getCurrent();
	if (!usersState) throw Error("Users not loaded for specified resource. Please call resolveUsers first.");
	if (!usersState.hasMore) throw Error("No more users available to load for this resource.");
	let promise = firstValueFrom(users.observable.pipe(filter((i) => i !== void 0), skip(1))), timestamp = (/* @__PURE__ */ new Date()).toISOString();
	return state.set("updateLastLoadMoreRequest", updateLastLoadMoreRequest(timestamp, key)), await promise;
}), getUserState = bindActionGlobally(usersStore, ({ instance }, { userId, ...options }) => getUsersState(instance, {
	userId,
	...options
}).observable.pipe(map((res) => res?.data[0]), distinctUntilChanged((a, b) => a?.profile.updatedAt === b?.profile.updatedAt))), resolveUser = bindActionGlobally(usersStore, async ({ instance }, { signal, ...options }) => (await resolveUsers(instance, {
	signal,
	...options
}))?.data[0]);
function getBifurClient(client, token$) {
	let { resource, dataset, url: baseUrl, requestTagPrefix = "sanity.sdk.presence" } = client.withConfig({ apiVersion: "2022-06-30" }).config(), resourcePath;
	if (resource?.type === "canvas") resourcePath = `canvases/${resource.id}`;
	else if (dataset) resourcePath = dataset;
	else throw Error("Unable to determine presence URL: no canvas resource or dataset configured");
	let urlWithTag = `${`${baseUrl}/socket/${resourcePath}`.replace(/^http/, "ws")}?tag=${requestTagPrefix}`;
	return fromUrl(urlWithTag, { token$ });
}
const handleIncomingMessage = (event) => {
	switch (event.type) {
		case "rollCall": return {
			type: "rollCall",
			userId: event.i,
			sessionId: event.session
		};
		case "state": {
			let { sessionId, locations } = event.m;
			return {
				type: "state",
				userId: event.i,
				sessionId,
				timestamp: (/* @__PURE__ */ new Date()).toISOString(),
				locations
			};
		}
		case "disconnect": return {
			type: "disconnect",
			userId: event.i,
			sessionId: event.m.session,
			timestamp: (/* @__PURE__ */ new Date()).toISOString()
		};
		default: throw Error(`Got unknown presence event: ${JSON.stringify(event)}`);
	}
}, createConnections = (bifur) => {
	let generation = 0;
	return defer(() => {
		let current = ++generation;
		return bifur.heartbeats.pipe(map$1(() => current));
	}).pipe(retry$1({
		delay: (_error, retryCount) => timer(Math.min(24e4, 2 ** retryCount * 100)),
		resetOnSuccess: !0
	}), distinctUntilChanged$1(), share$1());
}, createIncomingEvents = (bifur, connections$) => connections$.pipe(switchMap$1(() => bifur.listen("presence").pipe(catchError$1(() => EMPTY))), map$1(handleIncomingMessage), share$1()), createUnload = () => typeof window > "u" ? EMPTY : merge(fromEvent(window, "beforeunload"), fromEvent(window, "pagehide")).pipe(map$1(() => void 0)), createBifurTransport = (options) => {
	let { client, token$, sessionId } = options, bifur = getBifurClient(client, token$), connections$ = createConnections(bifur), incomingEvents$ = createIncomingEvents(bifur, connections$), dispatchMessage = (message) => {
		switch (message.type) {
			case "rollCall": return bifur.request("presence_rollcall", { session: sessionId });
			case "state": return bifur.request("presence_announce", { data: {
				locations: message.locations,
				sessionId
			} });
			case "disconnect": return bifur.request("presence_disconnect", { session: sessionId });
			default: return EMPTY;
		}
	};
	return [
		incomingEvents$,
		dispatchMessage,
		connections$,
		createUnload()
	];
};
/**
* Segments are not always strings, despite what `PresenceLocation.path` declares.
* Array items arrive as `{_key}` and Portable Text spans as a mix of keys and
* property names, because that is what the Studio sends.
*/
function isKeyedSegment(segment) {
	return typeof segment == "object" && !!segment && !Array.isArray(segment) && "_key" in segment;
}
/**
* Index tuples (`[from, to]`) only occur in slice selections, which presence never
* reports. Compared structurally rather than assumed absent.
*/
function isEqualTuple(a, b) {
	return a.length === b.length && a.every((value, index) => value === b[index]);
}
/** Compares two path segments, whatever shape they arrive in. */
function isEqualSegment(a, b) {
	return a === b ? !0 : isKeyedSegment(a) && isKeyedSegment(b) ? a._key === b._key : Array.isArray(a) && Array.isArray(b) ? isEqualTuple(a, b) : !1;
}
/**
* True when `candidate` is at or below `prefix` in the document tree.
*
* Equivalent to `startsWith` from the Studio's `@sanity/util/paths`, which is not
* an SDK dependency.
*
* @internal
*/
function startsWithPath(prefix, candidate) {
	return prefix.length > candidate.length ? !1 : prefix.every((segment, index) => isEqualSegment(segment, candidate[index]));
}
const logger = createLogger("presence"), PRESENCE_API_VERSION = "2026-03-30", SWEEP_INTERVAL = 15e3, getInitialState = () => ({
	locations: /* @__PURE__ */ new Map(),
	users: {}
}), sendSafely = (dispatch, message) => dispatch(message).pipe(catchError((error) => (logger.warn("Failed to send presence message", {
	messageType: message.type,
	error
}), EMPTY))), asTrigger = (source$, name) => source$.pipe(map(() => void 0), catchError((error) => (logger.error("Presence announce trigger failed", {
	trigger: name,
	error
}), EMPTY))), locationKey = ({ documentId, path, selection }) => JSON.stringify([
	documentId,
	path,
	selection ?? null
]);
/**
* Compares reported locations by value. Key order within a caller-supplied
* `selection` could in principle differ and read as a change, which costs one
* extra announcement and nothing else.
*/
function isEqualLocations(a, b) {
	return a === b ? !0 : !a || !b || a.length !== b.length ? !1 : a.every((location, index) => locationKey(location) === locationKey(b[index]));
}
/** @public */
const presenceStore = defineStore({
	name: "presence",
	getInitialState,
	initialize: (context) => {
		let { instance, state, key: { resource } } = context;
		if (isMediaLibraryResource(resource)) throw Error("Presence is not supported for media library resources.");
		let sessionId = randomId(16), client = isDatasetResource(resource) ? getClient(instance, {
			apiVersion: PRESENCE_API_VERSION,
			projectId: resource.projectId,
			dataset: resource.dataset,
			useProjectHostname: !0
		}) : getClient(instance, {
			apiVersion: PRESENCE_API_VERSION,
			resource
		}), token$ = getTokenState(instance).observable.pipe(distinctUntilChanged()), [incomingEvents$, dispatch, connections$, unload$] = createBifurTransport({
			client,
			token$,
			sessionId
		}), subscription = new Subscription();
		subscription.add(incomingEvents$.subscribe({
			next: (event) => {
				"sessionId" in event && event.sessionId === sessionId || (event.type === "state" ? state.set("presence/state", (prevState) => {
					let newLocations = new Map(prevState.locations);
					return newLocations.set(event.sessionId, {
						userId: event.userId,
						locations: event.locations,
						lastSeenAt: Date.now()
					}), {
						...prevState,
						locations: newLocations
					};
				}) : event.type === "disconnect" && state.set("presence/disconnect", (prevState) => {
					let newLocations = new Map(prevState.locations);
					return newLocations.delete(event.sessionId), {
						...prevState,
						locations: newLocations
					};
				}));
			},
			error: (error) => {
				logger.error("Presence event stream failed", { error });
			}
		})), subscription.add(connections$.subscribe(() => {
			state.set("presence/reset", (prevState) => ({
				...prevState,
				locations: /* @__PURE__ */ new Map()
			})), sendSafely(dispatch, { type: "rollCall" }).subscribe();
		})), subscription.add(connections$.pipe(switchMap(() => timer(SWEEP_INTERVAL, SWEEP_INTERVAL))).subscribe(() => {
			state.set("presence/expire", (prevState) => {
				let cutoff = Date.now() - 9e4, stale = [...prevState.locations].filter(([, s]) => s.lastSeenAt < cutoff);
				if (stale.length === 0) return prevState;
				let newLocations = new Map(prevState.locations);
				for (let [staleSessionId] of stale) newLocations.delete(staleSessionId);
				return {
					...prevState,
					locations: newLocations
				};
			});
		})), subscription.add(unload$.pipe(switchMap(() => sendSafely(dispatch, { type: "disconnect" }))).subscribe());
		let localLocations$ = state.observable.pipe(map((s) => s.localLocations), distinctUntilChanged(isEqualLocations)), rollCallRequests$ = incomingEvents$.pipe(filter((event) => event.type === "rollCall" && event.sessionId !== sessionId));
		subscription.add(merge(asTrigger(localLocations$, "locationChange"), asTrigger(rollCallRequests$, "rollCall"), asTrigger(connections$, "connection")).pipe(switchMap(() => timer(0, 3e4)), withLatestFrom(localLocations$), map(([, locations]) => locations), filter((locations) => locations !== void 0), auditTime(200), switchMap((locations) => sendSafely(dispatch, {
			type: "state",
			locations
		}))).subscribe());
		let userIds$ = state.observable.pipe(map((s) => Array.from(s.locations.values()).map((l) => l.userId).filter((id) => !!id)), distinctUntilChanged((a, b) => a.length === b.length && a.every((v, i) => v === b[i]))), organizationId$ = isCanvasResource(resource) ? state.observable.pipe(filter((s) => s.organizationId !== void 0 || s.organizationIdError !== void 0), first(), map((s) => s.organizationId)) : of(void 0);
		if (subscription.add(combineLatest([userIds$, organizationId$]).pipe(switchMap(([userIds, organizationId]) => {
			if (userIds.length === 0 || !isDatasetResource(resource) && !organizationId) return of([]);
			let userObservables = userIds.map((userId) => getUserState(instance, {
				userId,
				...isDatasetResource(resource) ? {
					resourceType: "project",
					projectId: resource.projectId
				} : {
					resourceType: "organization",
					organizationId
				}
			}).pipe(filter((v) => !!v)));
			return combineLatest(userObservables);
		})).subscribe((users) => {
			state.set("presence/users", (prevState) => ({
				...prevState,
				users: {
					...prevState.users,
					...users.reduce((acc, user) => (user && (acc[user.profile.id] = user), acc), {})
				}
			}));
		})), isCanvasResource(resource)) {
			let globalClient = getClient(instance, { apiVersion: PRESENCE_API_VERSION });
			subscription.add(globalClient.observable.request({
				url: `/canvases/${resource.canvasId}`,
				tag: "canvases.get"
			}).subscribe({
				next: ({ organizationId }) => {
					state.set("presence/organizationId", (prev) => ({
						...prev,
						organizationId
					}));
				},
				error: (organizationIdError) => {
					state.set("presence/organizationIdError", (prev) => ({
						...prev,
						organizationIdError
					}));
				}
			}));
		}
		return () => {
			sendSafely(dispatch, { type: "disconnect" }).subscribe(), subscription.unsubscribe();
		};
	}
}), selectLocations = (state) => state.locations, selectUsers = (state) => state.users, createUnresolvedUser = (userId) => ({
	sanityUserId: userId,
	profile: {
		id: userId,
		displayName: "Unknown user",
		email: "",
		provider: "",
		createdAt: ""
	},
	memberships: []
}), selectPresence = createSelector(selectLocations, selectUsers, (locations, users) => Array.from(locations.entries()).map(([sessionId, { userId, locations: locs }]) => ({
	user: users[userId] || createUnresolvedUser(userId),
	sessionId,
	locations: locs
}))), documentPresenceCache = /* @__PURE__ */ new WeakMap();
/** Flattens the session map down to one document. */
function selectDocumentPresence(state, options) {
	let cacheKey = JSON.stringify([
		getEditingDocumentId(options),
		options.path ?? null,
		options.excludeVersions ?? !1
	]), perState = documentPresenceCache.get(state);
	perState || (perState = /* @__PURE__ */ new Map(), documentPresenceCache.set(state, perState));
	let cached = perState.get(cacheKey);
	if (cached) return cached;
	let computed = computeDocumentPresence(state, options);
	return perState.set(cacheKey, computed), computed;
}
/**
* The id a query and a location are compared on. Exact when `excludeVersions` is
* set, otherwise normalized so a draft, its published version, and any release
* version count as one document.
*/
const scopeId = (documentId, excludeVersions) => excludeVersions ? documentId : getPublishedId(DocumentId(documentId)), pathOf = (location) => location.path;
function matchesQuery(location, target, { path, excludeVersions }) {
	return scopeId(location.documentId, excludeVersions) === target ? !path || startsWithPath(path, pathOf(location)) : !1;
}
function toDocumentPresence(users, sessionId, userId, location) {
	return {
		user: users[userId] || createUnresolvedUser(userId),
		sessionId,
		documentId: location.documentId,
		path: pathOf(location),
		lastActiveAt: location.lastActiveAt,
		...location.selection === void 0 ? {} : { selection: location.selection }
	};
}
function computeDocumentPresence(state, options) {
	let target = scopeId(getEditingDocumentId(options), options.excludeVersions);
	return Array.from(state.locations).flatMap(([sessionId, session]) => session.locations.filter((location) => matchesQuery(location, target, options)).map((location) => toDocumentPresence(state.users, sessionId, session.userId, location)));
}
const _getPresence = bindActionByResource(presenceStore, createStateSourceAction({ selector: (context) => selectPresence(context.state) }));
/** @beta */
function getPresence(instance, params) {
	return _getPresence(instance, params ?? {});
}
const _getDocumentPresence = bindActionByResource(presenceStore, createStateSourceAction({ selector: ({ state }, options) => selectDocumentPresence(state, options) }));
/**
* Presence within a single document, flattened to one entry per participant per
* location so it can be rendered directly against a field.
*
* Reading presence never announces anything. Use `reportPresence` to make the
* current user visible to others.
*
* @param instance - the Sanity instance
* @param params - the document to look at, its perspective, an optional `path` to
*   narrow to a field subtree, and `excludeVersions` to compare document ids exactly
*
* @beta
*/
function getDocumentPresence(instance, params) {
	return _getDocumentPresence(instance, params);
}
const _reportPresence = bindActionByResource(presenceStore, ({ state }, { locations }) => {
	let lastActiveAt = (/* @__PURE__ */ new Date()).toISOString(), wireLocations = locations.map((location) => ({
		type: "document",
		documentId: getEditingDocumentId(location),
		path: location.path ?? [],
		lastActiveAt,
		...location.selection === void 0 ? {} : { selection: location.selection }
	}));
	state.set("presence/report", (prevState) => ({
		...prevState,
		localLocations: wireLocations
	}));
});
/**
* Announces where the current user is, so other clients in the same project and
* dataset can show them. Reading presence never announces anything, so this is
* the only way an app becomes visible to others.
*
* Call it again whenever the user moves. Announcements are collapsed over a short
* window and then repeated every 30 seconds while idle, which is what tells peers
* the session is still alive.
*
* Which specific document each location resolves to depends on its perspective, and
* that matters for interoperability: other clients, the Studio included, compare
* exact document ids for field-level presence. Pass the perspective you are editing
* under rather than building a draft or version id yourself.
*
* @param instance - the Sanity instance
* @param params - the resource to announce on, plus the locations to report.
*   Pass one location with a `documentId` for document-level presence, add a
*   `path` for field-level presence, and pass an empty array to appear present
*   without being in any document.
*
* @beta
*/
function reportPresence(instance, params) {
	return _reportPresence(instance, params);
}
function hashString(str) {
	let hash = 0;
	for (let i = 0; i < str.length; i++) hash = (hash * 31 + str.charCodeAt(i)) % 2147483647;
	return Math.abs(hash).toString(16).padStart(8, "0");
}
const PROJECTION_TAG = "projection", STABLE_EMPTY_PROJECTION = {
	data: null,
	isPending: !1
};
function validateProjection(projection) {
	if (!projection.startsWith("{") || !projection.endsWith("}")) throw Error(`Invalid projection format: "${projection}". Projections must be enclosed in curly braces, e.g. "{title, 'author': author.name}"`);
	return projection;
}
function createProjectionQuery(documentIds, documentProjections) {
	let projections = Array.from(documentIds).flatMap((id) => {
		let projectionsForDoc = documentProjections[id];
		return projectionsForDoc ? Object.entries(projectionsForDoc).map(([projectionHash, projection]) => ({
			documentId: id,
			projection: validateProjection(projection),
			projectionHash
		})) : [];
	}).reduce((acc, { documentId, projection, projectionHash }) => {
		let obj = acc[projectionHash] ?? {
			documentIds: /* @__PURE__ */ new Set(),
			projection
		};
		return obj.documentIds.add(documentId), acc[projectionHash] = obj, acc;
	}, {});
	return {
		query: `[${Object.entries(projections).map(([projectionHash, { projection }]) => `...*[_id in $__ids_${projectionHash}]{_id,_type,_updatedAt,"__projectionHash":"${projectionHash}","result":{...${projection}}}`).join(",")}]`,
		params: Object.fromEntries(Object.entries(projections).map(([projectionHash, value]) => {
			let idsInProjection = Array.from(value.documentIds).flatMap((id) => DocumentId(id));
			return [`__ids_${projectionHash}`, Array.from(idsInProjection)];
		}))
	};
}
function processProjectionQuery({ ids, results, documentStatuses }) {
	let groupedResults = {};
	for (let result of results) {
		let originalId = getPublishedId(DocumentId(result._id)), hash = result.__projectionHash;
		ids.has(originalId) && (groupedResults[originalId] || (groupedResults[originalId] = {}), groupedResults[originalId][hash] || (groupedResults[originalId][hash] = void 0), groupedResults[originalId][hash] = result);
	}
	let finalValues = {};
	for (let originalId of ids) {
		finalValues[originalId] = {};
		let projectionsForDoc = groupedResults[originalId];
		if (projectionsForDoc) for (let hash in projectionsForDoc) {
			let projectionResultData = projectionsForDoc[hash]?.result;
			if (!projectionResultData) {
				finalValues[originalId][hash] = {
					data: null,
					isPending: !1
				};
				continue;
			}
			let statusFromStore = documentStatuses?.[originalId];
			finalValues[originalId][hash] = {
				data: {
					...projectionResultData,
					_status: statusFromStore
				},
				isPending: !1
			};
		}
	}
	return finalValues;
}
/**
* Builds an array of document IDs to query for status information using the "raw" perspective.
* Includes draft, published, and version IDs as needed.
*/
function buildStatusQueryIds(documentIds, perspective) {
	let ids = [], releaseName = isReleasePerspective(perspective) ? perspective.releaseName : null;
	for (let id of documentIds) {
		let publishedId = getPublishedId(DocumentId(id)), draftId = getDraftId(publishedId);
		ids.push(draftId, publishedId), releaseName && ids.push(getVersionId(publishedId, releaseName));
	}
	return ids;
}
/**
* Processes status query results into documentStatuses (same shape as _status returned to users).
*/
function processStatusQueryResults(results) {
	let documentStatuses = {};
	for (let result of results) {
		let id = DocumentId(result._id), updatedAt = result._updatedAt, publishedId = getPublishedId(id), statusData = documentStatuses[publishedId] ?? {};
		isDraftId(id) ? statusData.lastEditedDraftAt = updatedAt : isVersionId(id) ? statusData.lastEditedVersionAt = updatedAt : isPublishedId(id) && (statusData.lastEditedPublishedAt = updatedAt), documentStatuses[publishedId] = statusData;
	}
	return documentStatuses;
}
const isSetEqual = (a, b) => a.size === b.size && Array.from(a).every((i) => b.has(i)), subscribeToStateAndFetchBatches = ({ state, instance, key: { resource, perspective } }) => {
	let documentProjections$ = state.observable.pipe(map((s) => s.documentProjections), distinctUntilChanged(isDeepEqual)), activeDocumentIds$ = state.observable.pipe(map(({ subscriptions }) => new Set(Object.keys(subscriptions).map((id) => DocumentId(id)))), distinctUntilChanged(isSetEqual)), pendingUpdateSubscription = activeDocumentIds$.pipe(debounceTime(50), startWith(/* @__PURE__ */ new Set()), pairwise(), tap(([prevIds, currIds]) => {
		let newIds = [...currIds].filter((id) => !prevIds.has(id));
		newIds.length !== 0 && state.set("updatingPending", (prev) => {
			let nextValues = { ...prev.values };
			for (let id of newIds) {
				let projectionsForDoc = prev.documentProjections[id];
				if (!projectionsForDoc) continue;
				let updatedValuesForDoc = { ...prev.values[id] ?? {} };
				for (let hash in projectionsForDoc) updatedValuesForDoc[hash] = {
					data: updatedValuesForDoc[hash]?.data ?? null,
					isPending: !0
				};
				nextValues[id] = updatedValuesForDoc;
			}
			return { values: nextValues };
		});
	})).subscribe(), queryExecutionSubscription = combineLatest([activeDocumentIds$, documentProjections$]).pipe(debounceTime(50), distinctUntilChanged(isDeepEqual)).pipe(switchMap(([ids, documentProjections]) => {
		if (!ids.size) return EMPTY;
		let { query, params } = createProjectionQuery(ids, documentProjections), controller = new AbortController(), statusQueryIds = buildStatusQueryIds(ids, perspective), statusQuery = "*[_id in $statusIds]{_id, _updatedAt}", statusParams = { statusIds: statusQueryIds }, projectionQuery$ = new Observable((observer) => {
			let { getCurrent, observable } = getQueryState(instance, {
				query,
				params,
				tag: PROJECTION_TAG,
				perspective,
				resource
			}), subscription = defer(() => getCurrent() === void 0 ? from(resolveQuery(instance, {
				query,
				params,
				tag: PROJECTION_TAG,
				signal: controller.signal,
				perspective,
				resource
			})).pipe(switchMap(() => observable)) : observable).pipe(filter((result) => result !== void 0)).subscribe(observer);
			return () => {
				controller.signal.aborted || controller.abort(), subscription.unsubscribe();
			};
		}), statusQuery$ = new Observable((observer) => {
			let { getCurrent, observable } = getQueryState(instance, {
				query: statusQuery,
				params: statusParams,
				tag: PROJECTION_TAG,
				perspective: "raw",
				resource
			}), subscription = defer(() => getCurrent() === void 0 ? from(resolveQuery(instance, {
				query: statusQuery,
				params: statusParams,
				tag: PROJECTION_TAG,
				signal: controller.signal,
				perspective: "raw",
				resource
			})).pipe(switchMap(() => observable)) : observable).pipe(filter((result) => result !== void 0)).subscribe(observer);
			return () => {
				subscription.unsubscribe();
			};
		});
		return combineLatest([projectionQuery$, statusQuery$]).pipe(filter((pair) => pair[0] !== void 0 && pair[1] !== void 0), map(([projection, status]) => ({
			data: projection,
			ids,
			statusResults: status
		})));
	}), map(({ ids, data, statusResults }) => {
		let documentStatuses = processStatusQueryResults(statusResults);
		return state.set("updateStatuses", (prev) => ({ documentStatuses: {
			...prev.documentStatuses,
			...documentStatuses
		} })), processProjectionQuery({
			ids,
			results: data,
			documentStatuses: state.get().documentStatuses,
			perspective
		});
	})).subscribe({
		next: (processedValues) => {
			state.set("updateResult", (prev) => {
				let nextValues = { ...prev.values };
				for (let docId in processedValues) processedValues[docId] && (nextValues[docId] = {
					...prev.values[docId] ?? {},
					...processedValues[docId]
				});
				return { values: nextValues };
			});
		},
		error: (err) => {
			console.error("Error fetching projection batches:", err);
		}
	});
	return new Subscription(() => {
		pendingUpdateSubscription.unsubscribe(), queryExecutionSubscription.unsubscribe();
	});
}, projectionStore = defineStore({
	name: "Projection",
	getInitialState() {
		return {
			values: {},
			documentProjections: {},
			subscriptions: {},
			documentStatuses: {}
		};
	},
	initialize(context) {
		let batchSubscription = subscribeToStateAndFetchBatches(context);
		return () => batchSubscription.unsubscribe();
	}
});
/**
* @beta
*/
function getProjectionState(...args) {
	return _getProjectionState(...args);
}
/**
* @beta
*/
const _getProjectionState = bindActionByResourceAndPerspective(projectionStore, createStateSourceAction({
	selector: ({ state }, options) => {
		let documentId = getPublishedId(DocumentId(options.documentId)), projectionHash = hashString(options.projection);
		return state.values[documentId]?.[projectionHash] ?? STABLE_EMPTY_PROJECTION;
	},
	onSubscribe: ({ state }, options) => {
		let { projection, ...docHandle } = options, subscriptionId = randomId(16), documentId = getPublishedId(DocumentId(docHandle.documentId)), validProjection = validateProjection(projection), projectionHash = hashString(validProjection);
		return state.set("addSubscription", (prev) => ({
			documentProjections: {
				...prev.documentProjections,
				[documentId]: {
					...prev.documentProjections[documentId],
					[projectionHash]: validProjection
				}
			},
			subscriptions: {
				...prev.subscriptions,
				[documentId]: {
					...prev.subscriptions[documentId],
					[projectionHash]: {
						...prev.subscriptions[documentId]?.[projectionHash],
						[subscriptionId]: !0
					}
				}
			}
		})), () => {
			setCleanupTimeout(() => {
				state.set("removeSubscription", (prev) => {
					let documentSubscriptionsForHash = omitProperty(prev.subscriptions[documentId]?.[projectionHash], subscriptionId), hasSubscribersForProjection = !!Object.keys(documentSubscriptionsForHash).length, nextSubscriptions = { ...prev.subscriptions }, nextDocumentProjections = { ...prev.documentProjections }, nextValues = { ...prev.values };
					if (hasSubscribersForProjection) nextSubscriptions[documentId] && (nextSubscriptions[documentId][projectionHash] = documentSubscriptionsForHash);
					else {
						delete nextSubscriptions[documentId][projectionHash], delete nextDocumentProjections[documentId][projectionHash];
						let currentProjectionValue = prev.values[documentId]?.[projectionHash];
						currentProjectionValue && nextValues[documentId] && (nextValues[documentId][projectionHash] = {
							data: currentProjectionValue.data,
							isPending: !1
						});
					}
					return Object.values(nextSubscriptions[documentId] ?? {}).some((subs) => Object.keys(subs).length > 0) || (delete nextSubscriptions[documentId], delete nextDocumentProjections[documentId]), {
						subscriptions: nextSubscriptions,
						documentProjections: nextDocumentProjections,
						values: nextValues
					};
				});
			}, 1e3);
		};
	}
}));
/** @beta */
function resolveProjection(...args) {
	return _resolveProjection(...args);
}
/**
* @beta
*/
const _resolveProjection = bindActionByResourceAndPerspective(projectionStore, ({ instance }, options) => firstValueFrom(getProjectionState(instance, options).observable.pipe(filter((state) => !!state?.data))));
/**
* Preserves a projection string's literal type and returns the string unchanged.
* Use this import when migrating an app with existing experimental Typegen output.
*
* Current Typegen does not scan this helper. New or changed projections need an
* explicit result type until projection generation is supported.
*
* @example
* ```ts
* const preview = defineProjection('{title, "author": author->name}')
* ```
*
* @beta
*/
function defineProjection(projection) {
	return projection;
}
/**
* @public
* Extracts the project ID from a CorsOriginError message.
* @param error - The error to extract the project ID from.
* @returns The project ID or null if the error is not a CorsOriginError.
*/
function getCorsErrorProjectId(error) {
	if (!(error instanceof CorsOriginError)) return null;
	let projMatch = (error.message || "").match(/manage\/project\/([^/?#]+)/);
	return projMatch ? projMatch[1] : null;
}
/**
* Returns true when the given error looks like a dynamic-import or
* code-split chunk-loading failure.
*
* These errors typically surface when a user has a tab open against a
* previously-deployed version of an app and the JavaScript or CSS chunk
* filenames have since changed: a fresh deployment removes the hashed assets
* the open tab still references. Detecting them lets the SDK trigger an
* automatic reload so the user gets the new build without manual intervention.
*
* Recognized shapes (webpack ChunkLoadError, Vite "Failed to fetch
* dynamically imported module", Firefox "error loading dynamically imported
* module", Safari "Importing a module script failed", and Vite "Unable to
* preload CSS").
*
* @param error - The value to inspect. Anything that is not an Error
*   instance returns false.
* @returns True if the error matches a known import/chunk-load failure.
*
* @public
*/
function isImportError(error) {
	if (!(error instanceof Error)) return !1;
	if (error.name === "ChunkLoadError") return !0;
	let message = error.message || "";
	return /Loading chunk [\w-]+ failed/i.test(message) || /Failed to fetch dynamically imported module/i.test(message) || /error loading dynamically imported module/i.test(message) || /Import(?:ing)? a module script failed/i.test(message) || /Unable to preload CSS/i.test(message);
}
export { AuthStateType, CORE_SDK_VERSION, application, applications, applyDocumentActions, archiveRelease, checkPermissions, configureLogging, createComment, createDatasetHandle, createDocument, createDocumentHandle, createDocumentTypeHandle, createProjectHandle, createRelease, createSanityInstance, datasets, defineProjection, deleteApplication, deleteDocument, deleteRelease, discardDocument, editDocument, editRelease, favorites, getActiveReleasesState, getAllReleasesState, getAuthState, getClient, getClientState, getCommentThreadsState, getCommentsState, getCorsErrorProjectId, getCurrentUserState, getDashboardOrganizationId, getDocumentPresence, getDocumentState, getDocumentSyncStatus, getIndexForKey, getIsInDashboardState, getLoginUrlState, getOAuthTokensState, getPathDepth, getPermissionsState, getPerspectiveState, getPresence, getProjectionState, getQueryState, getReleaseDocumentId, getTokenState, getUserState, getUsersState, handleAuthCallback, handleOAuthCallback, installation, installations, isCanvasResource, isDatasetResource, isImportError, isMediaLibraryResource, joinPaths, jsonMatch, loadMoreUsers, logout, observeOrganizationVerificationState, organization, organizations, patchOperations, processMutations, project, projects, publishDocument, publishRelease, refreshOAuthTokens, removeComment, replyToComment, reportPresence, resolveCommentThreads, resolveComments, resolveDocument, resolvePermissions, resolveProjection, resolveQuery, resolveUser, resolveUsers, revokeOAuthTokens, scheduleRelease, setAuthToken, setCommentStatus, setFavorite, slicePath, startOAuthAuthorization, stringifyPath, subscribeDocumentEvents, unarchiveRelease, unpublishDocument, unscheduleRelease, updateApplication, updateComment };

//# sourceMappingURL=index.js.map