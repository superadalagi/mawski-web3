import { a as getLocalPackageVersion, i as getLocalPackageDir, n as resolveLocalPackageFrom, r as resolveLocalPackagePath, t as resolveLocalPackage } from "../resolveLocalPackage-Dr-TNTyk.js";
import { n as ReadPackageJsonOptions, r as readPackageJson, t as PackageJson } from "../readPackageJson-Cr_lbjVR.js";
/**
 * Runtime-detectable package managers (excludes 'manual' which is a UI-only choice).
 */
type DetectedPackageManager = 'bun' | 'npm' | 'pnpm' | 'yarn';
/**
 * Convenience wrapper that reads `process.env.npm_config_user_agent` and returns
 * the detected package manager.
 */
declare function getRunningPackageManager(): DetectedPackageManager | undefined;
/**
 * Extract the yarn major version from a `npm_config_user_agent` string.
 *
 * @param ua - User-agent string. Defaults to `process.env.npm_config_user_agent`.
 * @returns The major version number, or `undefined` if yarn isn't detected.
 */
declare function getYarnMajorVersion(ua?: string): number | undefined;
/**
 * Return the CLI invocation command for the detected (or provided) package manager.
 *
 * @param options - Optional `bin` (defaults to `'sanity'`) and `userAgent` override.
 * @returns A string like `"npx sanity"`, `"pnpm exec sanity"`, etc.
 */
declare function getBinCommand(options?: {
  bin?: string;
  userAgent?: string;
}): string;
export { type DetectedPackageManager, type PackageJson, type ReadPackageJsonOptions, getBinCommand, getLocalPackageDir, getLocalPackageVersion, getRunningPackageManager, getYarnMajorVersion, readPackageJson, resolveLocalPackage, resolveLocalPackageFrom, resolveLocalPackagePath };
//# sourceMappingURL=package-manager.d.ts.map