const requiredWhenUnattendedSymbol = Symbol.for('@sanity/cli-core/requiredWhenUnattended');
/** Marks an optional flag as required whenever the command is invoked in unattended mode. */ export function requiredWhenUnattended(flag) {
    Object.defineProperty(flag, requiredWhenUnattendedSymbol, {
        value: true
    });
    return flag;
}
export function resolveUnattendedFlagRequirements(flags, unattended) {
    if (!Object.values(flags).some((flag)=>flag[requiredWhenUnattendedSymbol])) {
        return flags;
    }
    return Object.fromEntries(Object.entries(flags).map(([name, flag])=>[
            name,
            flag[requiredWhenUnattendedSymbol] ? {
                ...flag,
                required: unattended
            } : flag
        ]));
}

//# sourceMappingURL=flags.js.map