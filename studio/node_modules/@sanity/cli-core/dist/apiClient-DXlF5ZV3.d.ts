import { ClientConfig, SanityClient } from "@sanity/client";
/**
 * @public
 */
interface GlobalCliClientOptions extends ClientConfig {
  /**
   * The API version to use for this client.
   */
  apiVersion: string;
  /**
   * Whether to require a user to be authenticated to use this client.
   * Default: `false`.
   * Throws an error if `true` and user is not authenticated.
   */
  requireUser?: boolean;
  /**
   * Whether to skip reading the stored CLI token. When `true`, the client will
   * have no token unless one is explicitly provided.
   * Default: `false`.
   */
  unauthenticated?: boolean;
}
/**
 * Create a "global" (unscoped) Sanity API client.
 *
 * @public
 *
 * @param options - The options to use for the client.
 * @returns Promise that resolves to a configured Sanity API client.
 */
declare function getGlobalCliClient({ requestHandler, requireUser, token: providedToken, unauthenticated, ...config }: GlobalCliClientOptions): Promise<SanityClient>;
/**
 * @public
 */
interface ProjectCliClientOptions extends ClientConfig {
  /**
   * The API version to use for this client.
   */
  apiVersion: string;
  /**
   * The project ID to use for this client.
   */
  projectId: string;
  /**
   * The dataset to use for this client.
   */
  dataset?: string;
  /**
   * Whether to require a user to be authenticated to use this client.
   * Default: `false`.
   * Throws an error if `true` and user is not authenticated.
   */
  requireUser?: boolean;
}
/**
 * Create a "project" (scoped) Sanity API client.
 *
 * @public
 *
 * @param options - The options to use for the client.
 * @returns Promise that resolves to a configured Sanity API client.
 */
declare function getProjectCliClient({ requestHandler, requireUser, token: providedToken, ...config }: ProjectCliClientOptions): Promise<SanityClient>;
export { getProjectCliClient as i, ProjectCliClientOptions as n, getGlobalCliClient as r, GlobalCliClientOptions as t };
//# sourceMappingURL=apiClient-DXlF5ZV3.d.ts.map