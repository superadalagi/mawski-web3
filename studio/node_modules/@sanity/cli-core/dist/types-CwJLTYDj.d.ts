import { Command } from "@oclif/core";
interface Output {
  error: Command['error'];
  log: Command['log'];
  warn: Command['warn'];
}
type RequireProps<T, K extends keyof T> = Omit<T, K> & Required<Pick<T, K>>;
type SanityOrgUser = {
  /**
   * Null when authenticating with an API token rather than a user account —
   * robot tokens resolve to a user whose `email` and `familyName` are null.
   * Use `getUserDisplayName()` for anything user-facing.
   */
  email: string | null;
  id: string;
  name: string;
  profileImage?: string;
  /** `sanity-token` is returned when authenticating with an API token. */
  provider: 'github' | 'google' | 'sanity' | 'sanity-token' | `saml-${string}`;
  tosAcceptedAt?: string;
};
export { RequireProps as n, SanityOrgUser as r, Output as t };
//# sourceMappingURL=types-CwJLTYDj.d.ts.map