import { $ as defaultIntentChecker, $t as MenuItemBuilder, A as StructureToolFeatures, At as UnserializedListItemChild, B as UserComponent, Bt as GenericListInput, C as RouterPaneSiblingContext, Ct as ListItemBuilder, D as StructureResolver, Dt as ListItemSerializeOptions, E as StrictVersionLayeringOptions, Et as ListItemInput, Ft as PartialDocumentList, G as ComponentBuilder, Gt as maybeSerializeMenuItemGroup, H as View, Ht as shallowIntentChecker, I as DefaultDocumentNodeContext, It as BaseGenericList, J as DEFAULT_INTENT_HANDLER, Jt as maybeSerializeInitialValueTemplateItem, K as ComponentInput, Kt as InitialValueTemplateItemBuilder, L as DefaultDocumentNodeResolver, Lt as BuildableGenericList, M as StructureToolPaneActionHandler, Mt as DocumentListBuilder, N as UnresolvedPaneNode, Nt as DocumentListInput, O as StructureResolverContext, Ot as PartialListItem, Pt as DocumentListOptions, Q as IntentParams, Qt as MenuItemActionType, R as StructureBuilder, Rt as GenericList, S as RouterPaneSibling, Sn as ItemChild, St as ListItem, T as SerializablePaneNode, Tt as ListItemDisplayOptions, U as BuildableComponent, Ut as MenuItemGroup, V as UserViewComponent, Vt as ListDisplayOptions, W as Component, Wt as MenuItemGroupBuilder, X as IntentChecker, Xt as KnownMenuItemParams, Y as Intent, Yt as menuItemsFromInitialValueTemplateItems, Z as IntentJsonParams, Zt as MenuItem, _ as PaneMenuItem, _n as ComponentView, _t as DocumentListItem, a as ParameterizedLinkProps, an as maybeSerializeMenuItem, at as DocumentNode, b as PaneNodeResolver, bn as ChildResolver, bt as PartialDocumentListItem, cn as PartialDocumentNode, ct as SerializeOptions, d as DocumentFieldMenuActionNode, dn as BaseView, dt as BuildableList, en as MenuItemParamsType, et as Builder, f as DocumentListPaneNode, fn as GenericViewBuilder, ft as List, g as PaneListItemDivider, gn as FormViewBuilder, gt as DocumentTypeListInput, h as PaneListItem, hn as FormView, ht as DocumentTypeListBuilder, i as PaneRouterContextValue, in as getOrderingMenuItemsForSchemaType, it as Divider, j as StructureToolOptions, jt as DocumentList, k as StructureToolContextValue, kt as UnserializedListItem, l as BaseResolvedPaneNode, ln as documentFromEditor, lt as SerializePath, m as ListPaneNode, mn as maybeSerializeView, mt as ListInput, n as ChildLinkProps, nn as SortMenuItem, nt as Collection, o as ReferenceChildLinkProps, on as DocumentBuilder, ot as EditorNode, p as DocumentPaneNode, pn as ViewBuilder, pt as ListBuilder, q as BaseIntentParams, qt as defaultInitialValueTemplateItems, r as EditReferenceOptions, rn as getOrderingMenuItem, rt as CollectionBuilder, sn as DocumentOptions, st as Serializable, t as BackLinkProps, tn as PartialMenuItem, tt as Child, u as CustomComponentPaneNode, un as documentFromEditorWithInitialValue, ut as StructureNode, v as PaneMenuItemGroup, vn as ComponentViewBuilder, vt as DocumentListItemBuilder, w as RouterPanes, wt as ListItemChild, x as RouterPaneGroup, xn as ChildResolverOptions, xt as isDocumentListItem, y as PaneNode, yn as ChildObservable, yt as DocumentListItemInput, z as StructureContext, zt as GenericListBuilder } from "./types-BiJeXZlE.js";
import { S as ConfirmDeleteDialogProps, _ as StructureLocaleResourceKeys, a as form, b as PaneLayout, c as StructureBuilderOptions, d as PaneContainer, f as UseDocumentTitle, g as DocumentInspectorHeader, h as DocumentPaneProviderProps, i as component, l as createStructureBuilder, m as useDocumentPane, n as StructureToolProvider, o as HELP_URL, p as useDocumentTitle, r as StructureToolProviderProps, s as SerializeError, t as useStructureTool, u as DocumentListPaneProps, v as PaneRouterContext, w as getTypeNamesFromFilter, x as ConfirmDeleteDialogContainer, y as usePaneRouter } from "./useStructureTool-C0IQbXd6.js";
import { ElementType, HTMLProps, ReactNode, RefAttributes } from "react";
import { BoxOverflow, CardProps, SelectableTone } from "@sanity/ui";
import { ActionComponent, DecorationMember, DocumentActionDialogProps, DocumentDefinition, PreviewConfig, SanityClient, SanityDocument } from "sanity";
type IncomingReferenceActionsContext = {
  /**
   * The document that is linked to the reference.
   */
  document: SanityDocument;
  getClient: (options: {
    apiVersion: string;
  }) => SanityClient;
};
type IncomingReferenceActionDescription = {
  label: string;
  icon?: React.ElementType;
  tone?: SelectableTone;
  disabled?: boolean;
  onHandle: (() => Promise<void>) | (() => void);
  dialog?: DocumentActionDialogProps | false | null;
};
/**
 * Incoming reference action component definition.
 * Allows users to define actions that will be shown in the IncomingReferences component as part
 * of each reference
 * example:
 * ```ts
 * export const LogReferenceAction: IncomingReferenceAction = ({document, getClient}) => {
 *   return {
 *     label: 'Log reference',
 *     icon: LogIcon,
 *     onHandle: () => console.log(document._id)
 *   }
 * }
 * ```
 *
 * @beta
 */
type IncomingReferenceAction = ActionComponent<IncomingReferenceActionsContext, IncomingReferenceActionDescription>;
interface CrossDatasetIncomingReference {
  type: string;
  title?: string;
  dataset: string;
  preview: PreviewConfig;
  studioUrl?: (document: {
    id: string;
    type?: string;
  }) => string | null;
}
interface IncomingReferenceType {
  type: string;
  dataset?: never;
  title?: string;
}
type IncomingReferencesFilterResolver = (context: {
  document: SanityDocument | undefined;
  getClient: (options: {
    apiVersion: string;
  }) => SanityClient;
}) => string | {
  filter: string;
  filterParams?: Record<string, string>;
} | Promise<{
  filter: string;
  filterParams?: Record<string, string>;
}>;
type IncomingReferencesOptions = {
  name: string;
  title?: string;
  description?: string;
  /**
   * The type of the incoming references.
   */
  types: (IncomingReferenceType | CrossDatasetIncomingReference)[];
  /**
   * The filter query to apply to the incoming references in addition to the type filter.
   * For example: filter all books that are from an specific editorial brand: `editorialBrand == "Random House"`
   * The `_type` filter is applied automatically.
   */
  filter?: string | IncomingReferencesFilterResolver;
  filterParams?: Record<string, string>;
  /**
   * Callback to link a document to a reference.
   *
   * This function is called when a user wants to link an existing document to the current document.
   * It receives two parameters:
   * - The document that will be linked (the target document)
   * - The reference object with all properties needed to establish the link
   *
   * The developer must implement this function to define where and how the reference
   * should be placed within the document structure. The function should return the
   * modified document with the reference properly added.
   *
   * For example:
   *  - We want to link a book to an author:
   * ```ts
   *  onLinkDocument: (document, reference) => {
   *   return {
   *     ...document,
   *     author: reference,
   *   }
   *  }
   * ```
   *
   *
   * @param document - The document to link to the reference.
   * @param reference - The reference to link to the document.
   * @returns The document to link to the reference. If false, the document will not be linked to the reference.
   */
  onLinkDocument?: (document: SanityDocument, reference: {
    _type: 'reference';
    _ref: string;
    _weak?: boolean;
    _strengthenOnPublish?: {
      type: string;
    };
  }) => SanityDocument | false;
  /**
   * Callback to define the actions that will be shown for the incoming reference document.
   *
   * For example:
   * ```ts
   * actions: ({document, client}) => {
   *   return [
   *     {label: 'Unlink document',
   *     icon: TrashIcon,
   *     tone: 'critical',
   *     onClick: async () => {
   *       await client.createOrReplace({...document, _id: getDraftId(document._id), author: undefined})
   *     }
   *   }
   *  ]
   * }
   * ```
   * @param context - The context of the linked document.
   * @returns The actions that will be shown for the linked document.
   */
  actions?: IncomingReferenceAction[];
  /**
   * Whether to allow creation of new references.
   * default: true
   *
   * If false, it will only be possible to link to existing documents, if the `onLinkDocument` callback is provided.
   * If true, it will be possible to create new references to any template of the type.
   * If an array is provided, it will only be possible to create new references that match the template id in the array provided.
   *
   * To define how a creation will be handled, you can use the `initialValue` option in the
   * document type that will be linked.
   *
   * For example:
   * I want to create a new book from an author:
   * In the author we will have the <IncomingReferenceInput /> with the creationAllowed set to true.
   *
   * Then in the book type, we will have the `initialValue` option to define the default values for the new book.
   * This initialValue callback will receive the `reference` object that needs to be linked to the book in the params.
   *
   * ```ts
   * // Book type
   * export default {
   *   type: 'document',
   *   name: 'book',
   *   title: 'Book',
   *   fields: [
   *     {
   *       name: 'author',
   *       type: 'reference',
   *       to: [{type: 'author'}],
   *     },
   *   ],
   *   initialValue: (params) => {
   *    return {
   *      author: params?.reference,
   *    }
   *  }
   * ```
   */
  creationAllowed?: boolean | string[];
};
/**
 * Helper function to define an incoming references decoration.
 *
 * example:
 * ```ts
 * defineType({
 *   name: 'author',
 *   type: 'document',
 *   renderMembers: (members) => {
 *    return [
 *      ...members,
 *      defineIncomingReferenceDecoration({
 *        name: 'incomingReferences',
 *        title: 'Incoming references',
 *        types: [{type: 'author'}],
 *      }),
 *    ]
 *   },
 * })
 * ```
 *
 * @beta
 */
export declare function defineIncomingReferenceDecoration(options: IncomingReferencesOptions): DecorationMember;
interface IncomingReferenceCreationParams {
  reference: {
    _type: 'reference';
    _ref: string;
    _weak?: boolean;
    _strengthenOnPublish?: {
      type: string;
    };
  };
  from: {
    fieldName: string;
    type: string;
  };
  __internal_isIncomingReferenceCreation: true;
  [key: string]: unknown;
}
/**
 * Helper function to check if the document is being created from an incoming reference.
 * It will be used in the initialValue callback to determine if the document is being created from an incoming reference.
 *
 * example:
 * ```ts
 * defineType({
 *   name: 'book',
 *   type: 'document',
 *   fields: [...],
 *   initialValue: (params) => {
 *     // If the document is being created from an incoming reference, return the reference
 *     // Otherwise, return undefined
 *     return {
 *       author: isIncomingReferenceCreation(params) ? params.reference : undefined,
 *     }
 *   },
 * })
 * ```
 *
 * @beta
 */
export declare function isIncomingReferenceCreation(initialValue: DocumentDefinition['initialValue']): initialValue is IncomingReferenceCreationParams;
interface PaneProps {
  children?: ReactNode;
  currentMinWidth?: number;
  currentMaxWidth?: number;
  flex?: number;
  id: string;
  minWidth?: number;
  maxWidth?: number;
  selected?: boolean;
}
/**
 * @hidden
 * @internal
 */
export declare function Pane(props: PaneProps & Omit<CardProps, 'as' | 'overflow'> & Omit<HTMLProps<HTMLDivElement>, 'as' | 'height' | 'hidden' | 'id' | 'style'> & RefAttributes<HTMLDivElement>): import("react").JSX.Element;
interface PaneContentProps {
  as?: ElementType | keyof React.JSX.IntrinsicElements;
  overflow?: BoxOverflow;
  padding?: number | number[];
}
/**
 * @hidden
 * @internal
 */
export declare function PaneContent(props: PaneContentProps & Omit<HTMLProps<HTMLDivElement>, 'as' | 'height' | 'ref'> & RefAttributes<HTMLDivElement>): import("react").JSX.Element;
/**
 * The locale namespace for the structure tool
 *
 * @public
 */
export declare const structureLocaleNamespace: 'structure';
/**
 * @internal
 */
export declare const DocumentPane: import("react").MemoExoticComponent<(props: DocumentPaneProviderProps) => import("react").JSX.Element>;
/**
 * @internal
 */
declare const DocumentPaneProviderWrapper: import("react").MemoExoticComponent<(props: DocumentPaneProviderProps) => import("react").JSX.Element>;
type DocumentPaneOptions = DocumentPaneNode['options'];
/**
 * @internal
 */
export declare function usePaneOptions(options: DocumentPaneOptions, params?: Record<string, string | undefined>): DocumentPaneOptions;
/**
 * Reserved sentinel field name. A sort order using this field signals that the
 * list should be ordered to match the incoming `$ids` param rather than sorted
 * server-side, so the surrounding pane preserves the caller-provided ordering.
 *
 * @internal
 */
export declare const ORDER_BY_IDS_PARAM_FIELD = "__orderByIdsParam";
/**
 * The structureTool is a studio plugin which adds the “structure tool” – a tool within
 * Sanity Studio in which content editors can drill down to specific documents to edit them.
 * You can configure your Studio's structure tool(s).
 *
 * @public
 * @param options - Options for the structure tool. See {@link StructureToolOptions}
 * @example Minimal example
 * ```ts
 * // sanity.config.ts
 * import { defineConfig } from 'sanity'
 * import { structureTool } from 'sanity/structure'
 *
 * export default defineConfig((
 *  // ...
 *  plugins: [
 *    structureTool() // use defaults
 *  ]
 * })
 * ```
 *
 * @example To customise your structure tool
 * ```ts
 * // sanity.config.ts
 * import { defineConfig } from 'sanity'
 * import { structureTool } from 'sanity/structure'
 * import { FaCar } from 'react-icons'
 * export default defineConfig((
 *	 // ...
 *   plugins: [
 *    structureTool({
 *      name: 'cars',
 *      title: 'Cars',
 *      icon: FaCar,
 *      structure: (S) => S.documentTypeList('car'),
 *      defaultDocumentNode: (S) =>
 *        S.document().views([
 *          S.view.form(),
 *          S.view.component(Preview).title('Preview')
 *        ])
 *    })
 *  ]
 * })
 * ```
 * */
export declare const structureTool: import("sanity").Plugin<void | StructureToolOptions>;
export { type BackLinkProps, type BaseGenericList, type BaseIntentParams, type BaseResolvedPaneNode, type BaseView, type BuildableComponent, type BuildableGenericList, type BuildableList, type Builder, type Child, type ChildLinkProps, type ChildObservable, type ChildResolver, type ChildResolverOptions, type Collection, type CollectionBuilder, type Component, ComponentBuilder, type ComponentInput, type ComponentView, ComponentViewBuilder, ConfirmDeleteDialogContainer as ConfirmDeleteDialog, type ConfirmDeleteDialogProps, type CustomComponentPaneNode, DEFAULT_INTENT_HANDLER, type DefaultDocumentNodeContext, type DefaultDocumentNodeResolver, type Divider, DocumentBuilder, type DocumentFieldMenuActionNode, DocumentInspectorHeader, type DocumentList, DocumentListBuilder, type DocumentListInput, type DocumentListItem, DocumentListItemBuilder, type DocumentListItemInput, type DocumentListOptions, PaneContainer as DocumentListPane, type DocumentListPaneNode, type DocumentListPaneProps, type DocumentNode, type DocumentOptions, type DocumentPaneNode, DocumentPaneProviderWrapper as DocumentPaneProvider, type DocumentPaneProviderProps, DocumentTypeListBuilder, type DocumentTypeListInput, type EditReferenceOptions, type EditorNode, type FormView, FormViewBuilder, type GenericList, GenericListBuilder, type GenericListInput, GenericViewBuilder, HELP_URL, type IncomingReferenceAction, type IncomingReferencesOptions, InitialValueTemplateItemBuilder, type Intent, type IntentChecker, type IntentJsonParams, type IntentParams, type ItemChild, type KnownMenuItemParams, type List, ListBuilder, type ListDisplayOptions, type ListInput, type ListItem, ListItemBuilder, type ListItemChild, type ListItemDisplayOptions, type ListItemInput, type ListItemSerializeOptions, type ListPaneNode, type MenuItem, type MenuItem as StructureToolMenuItem, type MenuItemActionType, MenuItemBuilder, type MenuItemGroup, MenuItemGroupBuilder, type MenuItemParamsType, PaneLayout, type PaneListItem, type PaneListItemDivider, type PaneMenuItem, type PaneMenuItemGroup, type PaneNode, type PaneNodeResolver, PaneRouterContext, type PaneRouterContextValue, type ParameterizedLinkProps, type PartialDocumentList, type PartialDocumentListItem, type PartialDocumentNode, type PartialListItem, type PartialMenuItem, type ReferenceChildLinkProps, type RouterPaneGroup, type RouterPaneSibling, type RouterPaneSiblingContext, type RouterPanes, type Serializable, type SerializablePaneNode, SerializeError, type SerializeOptions, type SerializePath, type SortMenuItem, type StrictVersionLayeringOptions, type StructureBuilder, type StructureBuilderOptions, type StructureContext, type StructureLocaleResourceKeys, type StructureNode, type StructureResolver, type StructureResolverContext, type StructureToolContextValue, type StructureToolFeatures, type StructureToolOptions, type StructureToolPaneActionHandler, StructureToolProvider, type StructureToolProviderProps, type UnresolvedPaneNode, type UnserializedListItem, type UnserializedListItemChild, type UseDocumentTitle, type UserComponent, type UserViewComponent, type View, type ViewBuilder, component, createStructureBuilder, defaultInitialValueTemplateItems, defaultIntentChecker, documentFromEditor, documentFromEditorWithInitialValue, form, getOrderingMenuItem, getOrderingMenuItemsForSchemaType, getTypeNamesFromFilter, isDocumentListItem, maybeSerializeInitialValueTemplateItem, maybeSerializeMenuItem, maybeSerializeMenuItemGroup, maybeSerializeView, menuItemsFromInitialValueTemplateItems, shallowIntentChecker, useDocumentPane, useDocumentTitle, usePaneRouter, useStructureTool };
//# sourceMappingURL=structure.d.ts.map