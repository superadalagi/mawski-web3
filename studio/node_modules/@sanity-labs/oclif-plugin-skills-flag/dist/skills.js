import { readFile } from "node:fs/promises";
import { join, posix, win32 } from "node:path";
const DEFAULT_DESCRIPTION = "Show agent-oriented guidance for this command";
const DEFAULT_SKILLS_FLAG_CONFIG = {
    aliases: ["skill"],
    directory: "skills",
    fallThroughOnMissingSkill: false,
    flag: "llms",
};
const SKILLS_FLAG_CONFIG_OPTIONS = new Set([
    "aliases",
    "directory",
    "fallThroughOnMissingSkill",
    "flag",
    "missingSkillMessage",
]);
export const skillsFlagConfigKey = "oclif-plugin-skills-flag";
function validateFlagName(value, kind) {
    if (typeof value !== "string" ||
        value.length === 0 ||
        value.startsWith("-") ||
        /[\s=\0]/u.test(value)) {
        throw new TypeError(`Invalid flag ${kind}: expected a name without leading dashes, whitespace, or "="`);
    }
    return value;
}
function validateSkillsDirectory(value) {
    if (typeof value !== "string" ||
        value.length === 0 ||
        value.includes("\0") ||
        posix.isAbsolute(value) ||
        win32.isAbsolute(value) ||
        value.split(/[\\/]/u).includes("..")) {
        throw new TypeError("Invalid skills directory: expected a relative path without parent traversal");
    }
    return value;
}
export function resolveSkillsFlagConfig(input = undefined) {
    if (input !== undefined &&
        (typeof input !== "object" || input === null || Array.isArray(input))) {
        throw new TypeError("Skills flag configuration must be an object");
    }
    const options = (input ?? {});
    for (const option of Object.keys(options)) {
        if (!SKILLS_FLAG_CONFIG_OPTIONS.has(option)) {
            throw new TypeError(`Unknown configuration option: ${option}`);
        }
    }
    const flag = validateFlagName(options.flag ?? DEFAULT_SKILLS_FLAG_CONFIG.flag, "name");
    const aliasesInput = options.aliases ?? [
        ...DEFAULT_SKILLS_FLAG_CONFIG.aliases,
    ];
    if (!Array.isArray(aliasesInput)) {
        throw new TypeError("Skills flag aliases must be an array");
    }
    const aliases = aliasesInput.map((alias) => validateFlagName(alias, "alias"));
    const names = [flag, ...aliases];
    if (new Set(names).size !== names.length) {
        throw new TypeError("Duplicate flag name in flag or aliases");
    }
    const missingSkillMessage = options.missingSkillMessage;
    if (missingSkillMessage !== undefined &&
        (typeof missingSkillMessage !== "string" ||
            missingSkillMessage.length === 0)) {
        throw new TypeError("Missing skill message must be a non-empty string");
    }
    const fallThroughOnMissingSkill = options.fallThroughOnMissingSkill ??
        DEFAULT_SKILLS_FLAG_CONFIG.fallThroughOnMissingSkill;
    if (typeof fallThroughOnMissingSkill !== "boolean") {
        throw new TypeError("Fall through on missing skill must be a boolean");
    }
    return {
        aliases,
        directory: validateSkillsDirectory(options.directory ?? DEFAULT_SKILLS_FLAG_CONFIG.directory),
        fallThroughOnMissingSkill,
        flag,
        ...(missingSkillMessage === undefined ? {} : { missingSkillMessage }),
    };
}
export function createSkillsFlagDefinition(config = {}, options = {}) {
    const resolved = resolveSkillsFlagConfig(config);
    const description = options.description ?? DEFAULT_DESCRIPTION;
    const hidden = options.hidden ?? true;
    if (typeof description !== "string") {
        throw new TypeError("Flag description must be a string");
    }
    if (typeof hidden !== "boolean") {
        throw new TypeError("Flag visibility must be a boolean");
    }
    return {
        definition: {
            aliases: resolved.aliases,
            description,
            hidden,
        },
        name: resolved.flag,
    };
}
export const llmsFlagConfig = createSkillsFlagDefinition().definition;
export function commandSkillFilename(commandId) {
    const filename = commandId.replaceAll(":", "-") || "index";
    if (filename === "." ||
        filename === ".." ||
        filename.includes("/") ||
        filename.includes("\\") ||
        filename.includes("\0")) {
        throw new TypeError(`Invalid oclif command ID: ${commandId}`);
    }
    return `${filename}.md`;
}
export function ensureFinalNewline(input) {
    return input.endsWith("\n") ? input : `${input}\n`;
}
export function hasSkillsFlag(argv, config = DEFAULT_SKILLS_FLAG_CONFIG) {
    const flags = new Set([
        `--${config.flag}`,
        ...config.aliases.map((alias) => alias.length === 1 ? `-${alias}` : `--${alias}`),
    ]);
    for (const argument of argv) {
        if (argument === "--")
            return false;
        if (flags.has(argument))
            return true;
    }
    return false;
}
export async function readCommandSkill(commandId, root, directory = DEFAULT_SKILLS_FLAG_CONFIG.directory) {
    const path = join(root, validateSkillsDirectory(directory), commandSkillFilename(commandId));
    try {
        return await readFile(path, "utf8");
    }
    catch (error) {
        if (error.code === "ENOENT")
            return;
        throw error;
    }
}
//# sourceMappingURL=skills.js.map